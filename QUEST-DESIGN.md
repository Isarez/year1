# ระบบเควสต์ NPC ในโหมด "บ้านของหนู" — เอกสารออกแบบ

> สถานะ: **เฟส 1-9 เสร็จครบแล้ว 🎉 (2026-08-07 → 2026-08-12)** · งานก่อนเฟส 5 (สัญญา Mount · หน้าเลือกทาง · โหมดมือในบ้าน · สะพาน HouseGames) เสร็จ 2026-08-11 — ⚠️ อ่านข้อ 40-43 ก่อนแตะของพวกนี้เสมอ (สิ่งที่เฟส 5-9 ทำต่างจากแผน) — ทุกอย่างอยู่บน branch `feature/house-owl` ยังไม่ขึ้น `main`
> 🆕 **มินิเกมเบาสมองในโลก 3D 14 เกม (คลังอยู่ข้อ 44 · รายการเต็มใน [`IDEA.md`](IDEA.md)) — กระจายเป็นเฟส 11 · 12 · 13**
> 🆕 **จังหวะเวลา & งบเวลาเควสต์ (ข้อ 45) — เป็นเฟส 10 (ระลอกโครงสร้าง) + ท้ายเฟส 11 (เปิดตาราง `qN`)** เป้า **20-60 นาที/วัน** — ⚠️ มีกติกาใหม่ที่ต้องล็อกอยู่ในข้อ 45.3
> 🆕 **ระบบฤดูกาล = เฟส 14 ท้ายสุด (ข้อ 46) · 💤 ยังไม่ตัดสินใจทำ** เป็นการประเมินขนาดงานเบื้องต้นเท่านั้น **ยังไม่ใช่สเปก ต้องกลับมาคุยกันก่อนลงมือทุกครั้ง**
> 📌 **ลำดับเฟส 10-14 จัดใหม่เมื่อ 2026-08-13 (ทับลำดับเดิมของ 2026-08-12) — ดูตารางแผนเฟสใหม่ที่หัวภาค 4**
> เริ่มร่าง 2026-08-06 · ปรับข้อเสนอ + ล็อกทุกข้อ 2026-08-07 · branch `feature/house-owl`
> เอกสารนี้คือที่เก็บการออกแบบก่อนลงมือ ไม่ใช่ changelog — เมื่อทำจริงแล้วให้ย้ายสรุปไป `LOG.md`/`CLAUDE.md` ตามปกติ
> 📄 **ฉบับอ่านง่าย (หน้าเว็บ): [`research/quest-design.html`](research/quest-design.html)** — เนื้อหาเดียวกับไฟล์นี้ จัดหน้าเป็นตาราง/ผังให้อ่านสบายตา ไฟล์เดียวจบเปิดด้วยเบราว์เซอร์ได้เลย **แก้ไฟล์ไหนต้องแก้อีกไฟล์ให้ตรงกันเสมอ** (ไฟล์ `.md` นี้คือฉบับหลักที่ session อื่นอ่าน)

---

# ⭐ สรุปบริบท — อ่านตรงนี้ก่อนเสมอ

> **ทุก session ที่จะแตะโหมด "บ้านของหนู" ต้องอ่านหัวข้อนี้ให้จบก่อนลงมือ** ส่วนที่เหลือของไฟล์คือรายละเอียดรายหัวข้อ เปิดอ่านเฉพาะเวลาทำเฟสนั้นๆ

## สรุประบบใน 5 บรรทัด

1. โหมดบ้านวันนี้เป็น **เมือง 3D ที่เดินเล่นได้ แต่ยังไม่มีอะไรให้ทำ** — ของทุกอย่างแจกฟรี เงิน 🪙 มีระบบแต่ไม่มีที่หา/ที่ใช้
2. แผนนี้เปลี่ยนเป็นวงจร **เล่นเควสต์ → ได้ดาว → แปลงเป็นเหรียญ → ซื้อของ/สัตว์/อาหาร → มีเหตุให้กลับมาเล่นอีก**
3. เควสต์มาจาก **3 แหล่งแยกโควตากัน**: NPC ในเมือง (57 ตัว) · กระดานเควสต์ 5 ชุด/วัน · พ่อ-แม่ในบ้าน 1 ชุด/วัน
4. เนื้อเควสต์สุ่มจาก **41 mechanic** (~3,500 โจทย์) โดย `quiz` ตอบคำถามพื้นฐาน **ต้องมีในทุก NPC ที่ทำเควสต์ได้เสมอ**
5. ทำเป็น **9 เฟส** แต่ละเฟสจบแล้วเล่นได้จริงไม่พัง ⇒ merge ทีละเฟสได้

## กติกาเหล็ก — ห้ามละเมิดทุกข้อ ไม่ว่าจะทำเฟสไหน

| # | กติกา |
|---|---|
| 1 | **ห้ามมี dead end** ทุกสถานการณ์ต้องมีทางไปต่อ (เงินไม่พอค่ารักษา → หมอให้เควสต์ทำแทน) |
| 2 | **ห้ามลงโทษเด็ก** ไม่หักเงิน ไม่หักดาว ไม่มีคำว่า "แพ้" ไม่มีตัวจับเวลากดดัน ไม่มีสัตว์เลี้ยงตาย |
| 3 | **ห้ามทำข้อมูลเก่าหาย** เด็กที่เล่นอยู่แล้วต้องได้ของเดิมครบทุกชิ้นหลังอัปเดต (migration มาก่อนโค้ดร้านเสมอ) |
| 4 | **ห้ามแตะหน้าหลัก** สมุดสติกเกอร์ / ปุ่มนกฮูก / `progress.stickers` อยู่ที่เดิม ระบบนี้อยู่ในโหมดบ้านล้วน |
| 5 | **เงินต้องผ่าน `window.OwlCoins` เท่านั้น** ห้ามเขียน `progress.coins` ตรงๆ |
| 6 | **ไม่มี build step / ไม่มี CDN / ไม่มี backend** ทุกอย่าง static + `localStorage` ตาม `CLAUDE.md` |
| 7 | **ก่อน merge เข้า `main` ต้องถามผู้ใช้ทุกครั้ง** ว่าจะเปิดโหมดบ้านให้เด็กเล่นจริง หรือคง `HOUSE_FEATURE_OFF` ไว้ |

## ข้อที่ผู้ใช้ตัดสินแล้ว — ล็อก ห้ามเปลี่ยนเองโดยไม่ถาม

| หัวข้อ | ที่ตกลง |
|---|---|
| ราคา / ค่าตอบแทน | **ราคาของ** ใช้ข้อ 17 · **ค่าตอบแทนเควสต์** ใช้ตารางที่จูนใหม่ในข้อ 6 (2026-08-08 ลดลง ~3 เท่ากันเงินเฟ้อ ทับตัวเลขเดิมในข้อ 19) **ห้ามปรับขึ้นกลับโดยไม่ถาม** |
| จำนวนสัตว์เลี้ยง | เลี้ยงได้ **1 ตัว** เหมือนเดิม · ซื้อตัวใหม่ทีหลังจ่ายครึ่งเดียว |
| ความหิว | ลดตาม **"วันที่เข้าเล่น"** (−35/วันเล่น) ไม่ใช่เวลาจริง |
| สัตว์ป่วย | ไม่ตาย · นอนในบ้านออกมาไม่ได้ · **หายได้ทางเดียวคือคุยหมอแล้วจ่ายเงิน** |
| โจทย์ระดับสูงกว่าชั้น | ยังมี แต่ต้องผ่าน**ประตูเช็คความพร้อม** (ข้อ 24) และเด็กกดตอบรับเอง |
| ชุดเฟอร์นิเจอร์เริ่มต้น | **12 ชิ้นตามข้อ 26** (ในบ้าน 9 + ของฉากนอกบ้าน 3) วางให้เรียบร้อยตั้งแต่เปิดครั้งแรก (ให้น้อยๆ ที่เหลือซื้อเอง) |
| พ่อ-แม่ในบ้าน | **มี default มาให้ทั้งหน้าตาและชื่อ** แต่เด็ก**แก้ชื่อและแต่งตัวใหม่เองได้ทั้งหมด** |
| กระดานเควสต์ | 1 จุด · **5 ชุด/วัน** · โควตาแยกจากเควสต์ NPC เด็ดขาด |
| เครื่องดนตรี | เป็น**เฟสสุดท้าย (เฟส 9)** |

## ตัวเลขสรุป (นับจากโค้ดจริง ไม่ใช่ประมาณ)

| | วันนี้ | เป้าหมาย |
|---|---|---|
| NPC ในเมือง | 69 ตัว (มีบท `quest:` แล้ว 58) | เท่าเดิม แต่ทำเควสต์ได้จริงทุกตัว |
| mechanic เล่นจริง | 12 (ภารกิจเดินสำรวจ ปิดอยู่) | **41** × 3 tier = 123 แม่แบบ ⇒ **~3,500 โจทย์** |
| เฟอร์นิเจอร์ | 116 ชิ้น / 10 หมวด (แจกฟรี) | **180 ชิ้น / 11 หมวด** (ต้องซื้อทั้งหมด) |
| ของแต่งตัว | 56 แบบ (แจกฟรี ปุ่มเป็นตัวเลข) | **106 แบบ** (ต้องซื้อ ปุ่มเป็นภาพ SVG) |
| สัตว์เลี้ยง | 12 ชนิด (เลือกฟรี) | 12 ชนิด (ซื้อ + ต้องเลี้ยงจริง) |
| เงิน 🪙 | มีระบบแต่ไม่มีที่หา/ที่ใช้ | ได้จากเควสต์ · ใช้กับทุกร้าน |

## ลำดับเฟส (ย่อ — เต็มอยู่ข้อ 32)

`1` เศรษฐกิจ+migration+ชุดเริ่มต้น+ร้าน 2 ร้าน → `2` quest engine+quiz+กระดาน+ประตูความพร้อม → `3` สัตว์เลี้ยงครบวงจร → `4` พ่อแม่+เควสต์ครอบครัว → `5` mechanic กลุ่ม C (รื้อ engine เดิม) → `6` แล็บ STEM + coding → `7` mechanic กลุ่ม B+D → `8` เฟอร์นิเจอร์/ของแต่งตัว/ไอคอน SVG → `9` เครื่องดนตรี → **`10` จังหวะเวลา (โครงสร้าง · ข้อ 45)** → **`11` มินิเกมกลุ่ม A + เปิด `qN` ใหม่ (ข้อ 44 + 45.4)** → **`12` มินิเกมกลุ่ม C (ข้อ 44)** → **`13` มินิเกมกลุ่ม B + D (ข้อ 44)** → **`14` ระบบฤดูกาล (💤 ยังไม่ตัดสินใจทำ · ข้อ 46)**

**เฟสปัจจุบัน: เฟส 1-9 เสร็จครบแล้ว → เฟสถัดไปคือ 10 (จังหวะเวลา ระลอกโครงสร้าง) — ⚠️ ลำดับเฟส 10-14 จัดใหม่เมื่อ 2026-08-13 ทับลำดับเดิม ดูตารางในภาค 4**

> ✅ **วงจรเศรษฐกิจปิดครบแล้วตั้งแต่เฟส 2** — เด็กหาเงินได้จากเควสต์ NPC 8 คน/วัน + กระดาน 5 ชุด/วัน แล้วเอาไปซื้อของในร้านได้จริง
> ⇒ ตั้งแต่นี้ merge ขึ้น production แล้ว**เปิดให้เด็กเล่นได้จริง** (ยังต้องถามผู้ใช้ก่อนทุกครั้งตามกติกาเหล็กข้อ 7)

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | บทบาท |
|---|---|
| `js/house.js` (~10,200 บรรทัด) | engine ทั้งหมด — `QUEST_POOL` 7230 · `QUEST_ENABLED` 7249 · `PET_TYPES` 8210 · `seedWorldDecor` 8944 · `buildCreatorRows` ~6936 |
| `js/house-map.js` | ผังเมือง · `NPC_DEFS`/`NPCS` (ธง `shop:` = พนักงานที่เปิดร้านให้) · ค่าคงที่ของแต่งตัว `H_*` · `PET_HOUSE_TILE` |
| `js/house-furniture.js` | คลังเฟอร์นิเจอร์ 116 ชิ้น |
| **`js/house-shop.js`** ✅ **มีแล้ว (เฟส 1)** | ตารางราคา · คลังสิทธิ์ `data.unlocked` · migration `econVer` · ชุดเริ่มต้น · หน้าร้าน — เรียกผ่าน `window.HouseShop` |
| **`js/house-quests.js`** ✅ **มีแล้ว (เฟส 2)** | กลไก `quiz`/`count` · สุ่มรายวัน seeded · สูตรรางวัล · กระดาน 5 ชุด · ประตูความพร้อม — เรียกผ่าน `window.HouseQuests` (หน้าจอ/ป้าย "!" อยู่ใน `js/house.js`) |
| `js/app-core.js` | `window.OwlCoins` (~600-628) · `window.APP_ASSET_VER` (cache-buster ของไฟล์ lazy) |
| **`js/house-pet-care.js`** ✅ **มีแล้ว (เฟส 3B)** | ความหิว · อาหาร · ป่วย · ค่ารักษา — เรียกผ่าน `window.HousePetCare` (แผงให้อาหาร/การ์ดคุณหมออยู่ใน `js/house.js`) |

## สิ่งที่มีอยู่แล้วในโค้ด อย่าเขียนซ้ำ

- `pickDailyQuests()` — seeded RNG (FNV-1a + LCG) **ใช้เป็นต้นแบบการสุ่มรายวันทุกที่** (รวมถึงสุ่มว่าวันนี้พ่อหรือแม่ให้เควสต์)
- `questEvent(kind,key)` + `quest.seen` — ระบบบันทึกความคืบหน้า 7 ชนิด (talk/vendor/zone/flower/critter/pet/sit) พร้อมกันนับซ้ำ
- กระดานภารกิจข้างน้ำพุ `#house-quest` / `#hq-list` / `#hq-claim` / `#hq-stars` — ✅ **เฟส 2 ผูกกับ 5 ชุด/วันแล้ว** (แถวกดเล่นได้เลย ไม่ต้องเดินหา NPC)
- หน้าจอเล่นเควสต์ `#house-qz` / `#hqz-who` / `#hqz-dots` / `#hqz-stage` — ✅ **เฟส 2** การ์ดลอยกลางจอ สลับ 4 สถานะในกล่องเดียว (ชวนรับงาน / ถามท้าทาย / โจทย์ / สรุปดาว) เนื้อในสร้างด้วย JS ทั้งหมด
- `window.HouseQuestUI` — จุดต่อสำหรับชุดเทสเท่านั้น (`talk`/`offer`/`board`/`run`/`marks`/`close`) โค้ดเกมจริงไม่เรียกใช้
- ป้ายยอดเหรียญ 🪙 ในโหมดบ้าน `#house-coins` — แสดงผลได้แล้ว
- **ร้านเครื่องดนตรี `shop-music` + พี่โน้ต `npc-musicshop` 🎸 สร้างไว้แล้วเมื่อ 2026-08-06** พร้อมบท quest "ช่วยพี่ทายเสียงเครื่องดนตรีหน่อยไหม" ⇒ เฟส 9 มีร้านรออยู่แล้ว ไม่ต้องสร้างใหม่
- `PET_HOUSE_TILE` + item `pet-house` (`action:'pethouse'`) — บ้านสัตว์มีโมเดลแล้ว แค่ต้องย้ายจุด seed ไปตอนซื้อ

---

## 0. โจทย์ตั้งต้นจากผู้ใช้

> "ใส่โจทย์ที่ทำต่างๆ ลงในเกมบ้านของหนูให้เป็น quest ตาม npc ร้านต่างๆ และให้เกมและ mechanic เกม
> เหมาะสมกับวัย ระดับชั้น และร้านแต่ละร้าน เมื่อทำเสร็จให้รางวัลเป็นเงินตอบแทนตามระดับดาวที่เหมาะสม
> ให้โจทย์เป็นการสุ่มทั้งหมดว่าเจอ quest แบบไหน mechanic หรือเกมแบบไหน ... ให้มี mechanic เกมใหม่ๆ ได้
> ออกแบบให้เหมาะสมกับโลกในเกม"

ข้อกำหนดที่ถอดออกมาได้ 6 ข้อ:
1. เควสต์ผูกกับ **NPC ตามร้าน** (ไม่ใช่กระดานกลางอย่างเดียว)
2. กลไกเกม **ต้องเข้ากับธีมของร้านนั้นๆ**
3. ความยาก **ปรับตามวัย/ระดับชั้นของเด็ก**
4. รางวัลเป็น **เงิน** ตาม **ระดับดาว**
5. **สุ่มทั้งหมด** — สุ่มว่าเจอเควสต์แบบไหน กลไกแบบไหน
6. เพิ่ม **กลไกใหม่** ได้ ต้องกลมกลืนกับโลก 3D เดิม

---

## 1. สำรวจของเดิมในโค้ด (ฐานที่ต่อยอดได้)

| สิ่งที่มีอยู่แล้ว | ตำแหน่ง | สถานะปัจจุบัน |
|---|---|---|
| `QUEST_POOL` (12 ภารกิจ) + `QUEST_BY_ID` + `QUEST_N = 3` | `js/house.js:7230-7246` | เขียนครบ **แต่ปิดอยู่** |
| `QUEST_ENABLED = false` | `js/house.js:7249` | **สวิตช์เปิดระบบ** |
| `questDayKey()` — คีย์ราย (เด็ก, วัน) | `js/house.js:~7251` | ใช้ได้ |
| `pickDailyQuests(day)` — seeded RNG (FNV-1a + LCG) | `js/house.js:~7256-7264` | ใช้ได้ **เป็นต้นแบบการสุ่ม** |
| `questEvent(kind, key)` — บันทึกความคืบหน้า + กันนับซ้ำด้วย `quest.seen` | `js/house.js:7287-7302` | ใช้ได้ |
| จุดที่เรียก `questEvent` แล้ว | `7080` flower · `7209` talk · `7210` vendor · `7329` zone · `7664` critter · `8457` pet · `9334/9437` sit | ครบ 7 ชนิด |
| `questZonesAt(x,z)` — โซน beach/farm/plaza2/school/pond/bridge | `js/house.js:~7310-7318` | ใช้ได้ |
| กระดานภารกิจข้างน้ำพุ `#house-quest` / `#hq-list` / `#hq-claim` / `#hq-stars` | `js/house.js:7330-7400` + `index.html` | UI ทำเสร็จแล้ว |
| `quest.stars` (ดาวสะสม) | เก็บใน house save key `quest` | มีแล้ว **แต่ยังไม่แปลงเป็นเหรียญ** |
| `window.OwlCoins = {get,set,add,spend}` + event `owlcoins` | `js/app-core.js:~600-628` | มีแล้ว **ยังไม่มีที่หา/ที่ใช้เงิน** |
| ป้ายยอดเหรียญ 🪙 ในโหมดบ้าน | `js/house.js:8780-8789` | แสดงผลได้แล้ว |
| ฟิลด์ `quest:` ใน NPC | `js/house-map.js` (หลาย NPC) | ตอนนี้เป็นแค่ **บทพูดบรรทัดพิเศษ** (`js/house.js:7194`) |
| `activeGradeId(child)` / `childAgeYears(child)` / `GRADES` | `js/app-core.js:48-102` | เรียกจาก `house.js` ได้ (global scope เดียวกัน) |
| `CATS` (คำถามควิซ 332 หมวด แยกตามชั้น) | `js/data-cats-*.js` | **ดึงมาใช้เป็นโจทย์โรงเรียนได้ฟรี** |
| `lotDoorTile(lot)` / `LOT_BY_ID` / `VILLAGE_LOTS` | `js/house-map.js` | ผูก NPC กับร้านได้แล้ว |
| `signPlane(icon,size)` + `userData.anim` + `spawnParticle()` | `js/house.js` | ใช้ทำป้าย "!" ลอยเหนือหัวได้เลย |

**สรุป: โครงกระดูกมีแล้ว ~70% ที่ขาดคือ "โจทย์จริง" + "เกมย่อย" + "ที่ใช้เงิน"**

### ของเดิมใน `QUEST_POOL` (12 ข้อ — เป็นภารกิจ "เดินสำรวจ" ล้วน)
```
talk3   💬 ทักทายชาวบ้าน 3 คน          (kind:'talk',  goal:3)
shop2   🛍️ ทักทายแม่ค้าหน้าร้าน 2 คน   (kind:'vendor', goal:2)
sit1    🪑 นั่งพัก 1 ครั้ง              (kind:'sit')
pet3    🐾 ลูบสัตว์ 3 ครั้ง             (kind:'pet')
flower4 🌸 เก็บดอกไม้ 4 ดอก            (kind:'flower')
critter2🐿️ ไล่จับสัตว์เล็ก 2 ตัว        (kind:'critter')
+ zone: beach / farm / plaza2 / school / pond / bridge (kind:'zone')
```
> ของชุดนี้ **เก็บไว้ทั้งหมด** — จะกลายเป็น "ภารกิจประจำวันของผู้ใหญ่บ้าน" (layer กลาง)
> ส่วนเควสต์ NPC ที่จะทำใหม่เป็นอีก layer ที่ซ้อนทับ ไม่ทับกัน

---

## 2. ภาพรวมระบบ — "ป้ายงานหน้าร้าน" (Shop Quest)

**แนวคิดหลัก:** ทุกร้าน/สถานที่มี NPC เจ้าของ 1 คน และ NPC นั้นมี "งานวันนี้" สุ่มมา 1 งาน
เด็กเดินไปคุย → รับงาน → เล่นเกมย่อย → ได้ดาว 1-3 ดวง → แปลงเป็นเหรียญนกฮูก 🪙 ทันที

```
เดินเข้าใกล้ร้าน
      │  NPC ที่มีงาน: ป้าย "!" สีทองลอยเหนือหัว เด้งขึ้นลง (+ เรืองแสงอ่อนตอนกลางคืน)
      ↓ แตะ NPC
┌─────────────────────────────────────────┐
│ 🎸 พี่โน้ต                                │
│ "ช่วยพี่ทายเสียงเครื่องดนตรีหน่อยไหม"        │
│      [ รับงาน ]      [ ไว้ก่อน ]          │
└─────────────────────────────────────────┘
      ↓
overlay เกมย่อย — การ์ดครีมมนๆ ลอยกลางจอ (ยังเห็นร้านข้างหลัง)
      ↓
┌─────────────────────────────────────────┐
│         ⭐ ⭐ ⭐   เก่งมาก!                │
│         + 20 🪙                          │
└─────────────────────────────────────────┘
  เหรียญบินเข้าป้ายยอดเงิน + confetti + playCongrats()
      ↓
NPC เปลี่ยนป้ายเป็น "✓" (พักงาน) → พรุ่งนี้สุ่มงานใหม่
```

**จุดขายที่หน้าหลักทำไม่ได้:** เควสต์บางชนิดต้อง **ออกไปเดินในโลกจริง** (ส่งของ ตามหา ซื้อของ)
ไม่ใช่แค่ตอบคำถามใน popup — อันนี้คือเหตุผลที่โหมดบ้านควรมีเควสต์เป็นของตัวเอง

---

## 3. เควสต์ 3 ตระกูล

### 🟦 A — "งานในร้าน" (in-shop mini-game)
- เล่นจบในที่เดียว 30-60 วินาที เหมาะกับสมาธิเด็กเล็ก
- overlay 2D ทับฉาก 3D (สไตล์เดียวกับกล่องเลือกเฟอร์นิเจอร์)
- **เป็นตระกูลหลัก** ~60% ของเควสต์ทั้งหมด

### 🟩 B — "งานเดินโลก" (world quest) ← *ของใหม่ ยังไม่มีในเกมเลย*
ต้องเดินไปที่อื่นในแผนที่ ใช้ `questEvent()` เดิมจับความคืบหน้าได้

| ชื่อ | รายละเอียด | ทักษะ |
|---|---|---|
| **ส่งของถึงมือ** | รับกล่องจาก NPC A → ไปส่ง NPC B โดยบอกแค่ **คำใบ้** ("คนใส่หมวกกันน็อก อยู่ใกล้บ่อน้ำใหญ่ทางทิศเหนือ") ป.1-2 มีลูกศรจางๆ ช่วย ป.5-6 ไม่มี | อ่าน, สังเกต, ทิศทาง |
| **รายการซื้อของ** | ป้าจ่ายตลาดโชว์รายการ 3-5 อย่าง 5 วินาทีแล้วซ่อน → เดินไปซื้อจากรถเข็นให้ครบตามจำนวน | working memory |
| **ตามหาแมวหาย** | ตำรวจให้ตามหาสัตว์/ของที่ซ่อนในแผนที่ มีมิเตอร์ "ร้อน/เย็น" ตามระยะ | สังเกต, ระยะทาง |
| **เก็บของให้ครบ** | เก็บเปลือกหอย 7 ชิ้นที่ชายหาด / ดอกไม้ 5 ดอก (ต่อยอด `questEvent('flower')` ที่มีอยู่) | นับ |
| **พาแขกไปส่ง** | NPC เดินตามเด็กไปยังจุดหมาย (escort) | นำทาง |

### 🟨 C — "งานสร้างสรรค์" (ไม่มีถูกผิด ได้ ⭐⭐⭐ เสมอ)
กันเด็กท้อ + ทำให้เงินมีปลายทางที่มีความหมาย
- ระบายสีป้ายร้านให้ NPC
- จัดตู้โชว์ / จัดโต๊ะ
- **แต่งตัวตามธีมที่ NPC ขอ** (ใช้ระบบแต่งตัวที่ทำเสร็จแล้ว)

**สัดส่วนที่แนะนำ: A 50% / B 30% / C 20%**

---

## 4. ผังร้าน → กลไกเกม

> ธีมกลไกต้องตรงกับร้าน ไม่งั้นเด็กจะรู้สึกว่าเป็นควิซสุ่มมาแปะ

| ร้าน / สถานที่ | lot id | NPC | กลไกที่เสนอ | ทักษะ |
|---|---|---|---|---|
| 🏪 ร้านสะดวกซื้อ | `shop-mart` | พี่นวล | **จ่ายเงินให้พอดี** (ลากเหรียญ/แบงก์ใส่ถาด), ทอนเงิน, นับของขึ้นชั้น | เงิน, บวกลบ |
| 🐾 ร้านสัตว์เลี้ยง | `shop-pet` | พี่ปุย | **จับคู่สัตว์-อาหาร-บ้าน**, นับขาสัตว์รวม, จำแนกสัตว์ | จับคู่, นับ |
| 🍜 ร้านอาหาร | `shop-food` | ป้าอิ่ม | **ทำตามสูตร** — ลากส่วนผสมตาม *ลำดับ* + *จำนวน* ที่ลูกค้าสั่ง | ลำดับ, ความจำ |
| 🍨 ไอศกรีม | `shop-food` | พี่ฟ้า | **เรียงสกู๊ปตามแพทเทิร์น**, แบ่งเค้กเป็นเศษส่วน | แพทเทิร์น, เศษส่วน |
| 🎸 ร้านเครื่องดนตรี | `shop-music` | พี่โน้ต | **เล่นตามทำนอง** (Simon says 5 คีย์ ยาวขึ้นเรื่อยๆ), ทายเสียงเครื่องดนตรี | ฟัง, ความจำ |
| 🏥 โรงพยาบาล | `hospital` | คุณหมอ / พยาบาล | **เรียงขั้นตอนดูแลตัวเอง**, จับคู่อาการ-วิธีแก้ | ลำดับ, สุขอนามัย |
| 🚓 สถานีตำรวจ | `police-station` | ตำรวจ | **ตามหาของ/คนหาย** (world quest), จำแนกลักษณะจากคำอธิบาย | สังเกต, ทิศทาง |
| 🏫 โรงเรียน | `school` | ครูอ้อย | **ดึงคำถามจริงจาก `CATS` ตามระดับชั้นเด็ก** 3 ข้อ | ตรงหลักสูตร |
| 🔬 ตึกแล็บ | (แล็บ ชุมชน 2) | ดร.แล็บ 1-3 | **เรียงขั้นตอนการทดลอง**, จม-ลอย, แยกหมวด | วิทยาศาสตร์ |
| 🛋️ ห้างเฟอร์นิเจอร์ | `mall-furniture` | พี่โซฟา | **จัดห้องตามแบบ** (ดูรูป 5 วิ แล้ววางตาม) | มิติสัมพันธ์ |
| 👗 ห้างแฟชั่น | `mall-fashion` | พี่ชุดสวย | **แต่งตัวตามโจทย์** ("ชุดไปทะเล สีฟ้า มีหมวก") | จำแนก, สี |
| 🍉 ตลาดรถเข็น | `MARKET_CARTS` | 12 แม่ค้า | **รายการซื้อของ** (world quest), ตวง/ชั่งให้ได้น้ำหนัก | ความจำ, ตาชั่ง |
| 🌾🐄 นา / โรงนา | `farm-house` `farm-barn` | ลุงชาวนา, คาวบอย | **นับผลผลิต/แบ่งเท่าๆ กัน**, รดน้ำตามตาราง | หาร, ปฏิทิน |
| 🎣 บ่อน้ำ | `pond-hut` | ลุงตกปลา | **ตกปลาตอบเลข** — ปลาแต่ละตัวมีเลข ตกตัวที่ผลลัพธ์ถูก | คิดเลขเร็ว |
| 🏨 โรงแรม | `hotel` | พนักงาน 2 คน | **พาแขกไปห้องตามเลข**, เรียงคิวเช็คอิน | เลขหลัก, เรียงลำดับ |
| 🪚 ช่างไม้ | `carpenter-hut` | ลุงช่างไม้, ป้ามะลิ | **ต่อรูปทรง / แทนแกรม** | รูปทรง |
| ⛺ ลานแคมป์ | (แคมป์กลางป่า) | หัวหน้าแคมป์ | **เรียงขั้นตอนก่อไฟ/กางเต็นท์** | ลำดับ |
| 🏖️ ชายหาด | (โซน beach) | พี่ชายหาด | **เก็บเปลือกหอยให้ครบ** (world quest), เรียงขนาด | นับ, เปรียบเทียบ |
| 🏛️📜 บ้านเทศมนตรี / ผู้ใหญ่บ้าน | `mayor-house` | ท่านเทศมนตรี, ผู้ใหญ่บ้าน | **กระดานรวม** — สรุปงานทั้งเมือง + โบนัสตอนครบ | (hub เดิม) |

รวม ~19 จุด × สุ่มกลไก × สุ่มพารามิเตอร์ = โจทย์ไม่ซ้ำหลายร้อยแบบ

---

## 5. ความยากตามระดับชั้น (ป.1 → ป.6)

**หลักการ: ไม่ทำเกมคนละชุดต่อชั้น** (บำรุงรักษาไม่ไหว) แต่ให้กลไกเดียวรับพารามิเตอร์ความยาก

```js
/* แนวคิด — ยังไม่เขียนจริง */
questDifficulty(gradeId) → {
  tier,       // 1..6 จาก GRADES index (activeGradeId(activeChild))
  numMax,     // ป.1: 1-10 · ป.2: 1-20 · ป.3: 1-100 · ป.4: 1-1000 · ป.5-6: ทศนิยม/เศษส่วน
  seqLen,     // ความยาวลำดับที่ต้องจำ: 3 → 7
  steps,      // จำนวนขั้นตอนของโจทย์: 1 → 3
  timeLimit,  // ป.1-2 = ไม่มีเลย · ป.3-4 = ผ่อนปรน · ป.5-6 = มี (แบบไม่กดดัน)
  hints       // ป.1-2 = ลูกศร/ไฮไลต์ช่วย · ป.5-6 = ไม่มี
}
```

### กติกาที่ยึด (ห้ามฝ่าฝืน)
- **ป.1-2** — ไม่มีเวลาจำกัด ไม่มีคำว่า "ผิด" พลาดแล้วลองใหม่ได้ไม่จำกัด (แค่ดาวลดลง)
- **ป.3-4** — มีเวลาแบบผ่อนปรน + โจทย์ 2 ขั้นตอน
- **ป.5-6** — โจทย์หลายขั้น มีการวางแผน เดินโลกไกลขึ้น
- **ร้านบางร้านล็อกตามชั้น** (เช่น ตึกแล็บ/โรงแรม เปิดที่ ป.3+) → โลกโตตามเด็ก ทำให้กลับมาเล่นอีก
- ระดับชั้นอ่านจาก `activeGradeId(activeChild)` **ไม่ใช่จากอายุตรงๆ** (เด็กเลือกชั้นเองได้ในหน้าหลัก)

---

## 6. ดาว → เหรียญ (สูตรรางวัล)

```
⭐     = ทำสำเร็จ (ไม่ว่าพยายามกี่ครั้ง)   ← ทุกคนได้เงินเสมอ กันเด็กท้อ
⭐⭐   = ผิดไม่เกิน 1-2 ครั้ง
⭐⭐⭐ = ผ่านรวดเดียว

เหรียญ = base(ชนิดเควสต์) × mulStar × mulTier   (× 1.5 ถ้าเป็นโจทย์ท้าทาย)

  base:     งานในร้าน (A) = 6 · งานเดินโลก (B) = 8 · งานสร้างสรรค์ (C) = 7 · กระดาน = 10
  mulStar:  ⭐ = 1.0 · ⭐⭐ = 1.5 · ⭐⭐⭐ = 2.0
  mulTier:  ป.1 = 1.0 → ป.6 = 1.4  (`1 + (tier-1)×0.08`)

โบนัสทำกระดานครบ 5 ชุดในวันเดียว: +35 🪙 · เพดานเหรียญต่อวัน 400 🪙
```

### ⚠️ ตัวเลขจริงหลังจูนกันเงินเฟ้อ (2026-08-08 — ตามคำสั่งผู้ใช้ ทับตัวเลขเดิมทั้งข้อ 6 และข้อ 19)

**เหตุผลที่ต้องจูน**: ชุดแรกที่ส่ง (base A=18 · กระดาน 28 · โบนัส 100) ให้เด็กวันละ ~600 🪙
ในขณะที่ของทั้งเกมรวมกันมีแค่ **~15,800 🪙** (เฟอร์นิเจอร์ 116 ชิ้น 11,005 + ของแต่งตัว 160 ชิ้น 4,780)
⇒ ซื้อครบทั้งเกมใน ~27 วัน และของแพงสุด 300 🪙 ใช้เวลาไม่ถึงครึ่งวัน
**เล่นแป๊บเดียวได้ของหลายชิ้น = เด็กเบื่อเร็ว ไม่ได้ฝึกเก็บออม**

| ระดับชั้น | เควสต์ NPC ⭐/⭐⭐/⭐⭐⭐ | กระดาน ⭐/⭐⭐/⭐⭐⭐ | ท้าทาย ⭐⭐⭐ | รวมต่อวัน (⭐/⭐⭐/⭐⭐⭐) |
|---|---|---|---|---|
| เตรียม ป.1 / ป.1 | 6 / 9 / 12 | 10 / 15 / 20 | 18 | 133 / 182 / 231 |
| ป.2 | 6 / 10 / 13 | 11 / 16 / 22 | 19 | 138 / 195 / 249 |
| ป.3 | 7 / 10 / 14 | 12 / 17 / 23 | 21 | 151 / 200 / 262 |
| ป.4 | 7 / 11 / 15 | 12 / 19 / 25 | 22 | 151 / 218 / 280 |
| ป.5 | 8 / 12 / 16 | 13 / 20 / 26 | 24 | 164 / 231 / 293 |
| ป.6 | 8 / 13 / 17 | 14 / 21 / 28 | 25 | 169 / 244 / 311 |

**จังหวะการออมที่ตั้งใจ** (คิดที่ ป.3 เล่นเรื่อยๆ ได้ ⭐⭐ = ~200 🪙/วัน):

| ของ | ราคา | ต้องทำกี่เควสต์ | ≈ กี่วัน |
|---|---|---|---|
| สีเสื้อ/กางเกง/รองเท้า | 15 🪙 | 2 | เล่นแป๊บเดียว |
| สีของแต่ง / เฟอร์จิ๋ว | 20-25 🪙 | 2-3 | เล่นแป๊บเดียว |
| ทรงผม/สีผม/สีตา · ลายเสื้อ | 30-40 🪙 | 3-4 | ครึ่งวัน |
| เฟอร์เล็ก · หมวก/แว่น | 60 🪙 | 6 | ครึ่งวัน |
| เป้/ของถือ | 80 🪙 | 8 | ครึ่งวัน |
| **เฟอร์กลาง (โซฟา/เตียง/ตู้เย็น)** | 140 🪙 | 14 | **~1 วัน** |
| **เฟอร์ใหญ่ (สระ/บ้านต้นไม้/ไฟประดับ)** | 300 🪙 | 30 | **~1.5 วัน** |
| สัตว์เลี้ยงเริ่มต้น (เฟส 3) | 250 🪙 | 25 | ~1.3 วัน |
| สัตว์พิเศษ (เฟส 3) | 800 🪙 | 80 | ~4 วัน |
| **สัตว์หายาก แพนด้า/ยูนิคอร์น (เฟส 3)** | 1,500 🪙 | 150 | **~1 สัปดาห์** |
| **ซื้อของครบทั้งเกม** | ~15,800 🪙 | — | **~80 วัน** (เดิม 27 วัน) |

หลักที่ยึด: **ของถูกสุดต้องได้ภายในวันแรก** (ไม่งั้นเด็กไม่เห็นผลลัพธ์ของการทำเควสต์เลย)
แต่ **ของชิ้นใหญ่ต้องข้ามวันเสมอ** ⇒ มีเหตุให้กลับมาเล่นพรุ่งนี้และรู้จักเก็บเงินรอ
⚠️ มีเทส (`tests/house-quests.spec.js`) คุมช่วงตัวเลขและรายได้ต่อวันสูงสุด (≤320 🪙) ไว้แล้ว
**ห้ามปรับรางวัลขึ้นกลับโดยไม่ถามผู้ใช้ก่อน** — เศรษฐกิจทั้งเกมผูกกับตัวเลขชุดนี้

---

## 7. การสุ่ม (ตามข้อกำหนด "สุ่มทั้งหมด")

ใช้แนวเดียวกับ `pickDailyQuests()` เดิม คือ **seeded RNG ไม่ใช่ `Math.random()` ตรงๆ**

```
seed = FNV1a(childId + '|' + questDayKey() + '|' + npcId)
```

| กติกา | เหตุผล |
|---|---|
| เปิดคุยกี่รอบก็ได้โจทย์เดิม | กันเด็ก reroll จนได้ข้อง่าย |
| แต่ละ NPC สุ่ม: **ชนิดเควสต์ → กลไก → ตัวเลข/ของในโจทย์ → บทพูด** | ครบทุกชั้นตามที่ขอ |
| ทำเสร็จแล้ว NPC **cooldown ถึงวันถัดไป** (ยังคุยเล่นได้ปกติ) | กันฟาร์มเงิน |
| เก็บ `recent[]` 5 กลไกล่าสุด แล้วหักออกจากสระสุ่ม | กันเจอ "จ่ายเงิน" 3 ร้านติด |
| สุ่มเฉพาะ **5-8 ร้านต่อวัน** ที่มีงาน ไม่ใช่ทุกร้าน | บังคับให้เดินสำรวจว่าวันนี้ใครมีป้าย "!" |
| กลไกที่ `minTier > tier ของเด็ก` ถูกคัดออกก่อนสุ่ม | กันโจทย์ยากเกินวัย |

---

## 8. ⚠️ จุดสำคัญที่สุด — "ที่ใช้เงิน" (money sink)

**ระบบเควสต์จะพังทันทีถ้าได้เงินมาแล้วไม่มีอะไรให้ซื้อ** เด็กจะเลิกเล่นภายใน 2 วัน
→ **ต้องทำร้านใช้เงินพร้อมกัน (หรือก่อน) ไม่ทำเควสต์อย่างเดียวเด็ดขาด**

| ร้าน | ขายอะไร | ทำไมเด็กอยากได้ | ต้นทุนงาน |
|---|---|---|---|
| 👗 ห้างแฟชั่น | ชุด/หมวก/แว่น/ของสะพาย/ของถือ เพิ่มเติม | **ระบบแต่งตัวทำเสร็จแล้ว ต่อได้เร็วสุด** | เล็ก ⭐ |
| 🛋️ ห้างเฟอร์นิเจอร์ | ปลดล็อกเฟอร์นิเจอร์ในคลัง (ตอนนี้ให้ฟรีหมด) | แต่งบ้านตัวเอง | เล็ก (มีคลัง 60+ ชิ้นแล้ว) |
| 🐾 ร้านสัตว์เลี้ยง | สัตว์เลี้ยงตัวใหม่ + ของเล่นสัตว์ | มีบ้านสัตว์เลี้ยงแล้ว | กลาง |
| 🎸 ร้านเครื่องดนตรี | ปลดเพลงใหม่ให้เล่นในบ้าน | ต่อจากระบบเปียโน/`MUSIC_TRACKS` เดิม | กลาง |
| 🍜🍨 ร้านอาหาร/ไอติม | ของกินให้ตัวละคร (เอฟเฟกต์น่ารัก ไม่มีผลต่อเกม) | คุ้มค่าเงินก้อนเล็ก ซื้อบ่อย | เล็ก |
| 📜 ผู้ใหญ่บ้าน | ป้ายชื่อบ้าน / ธง / **สีหลังคาบ้าน** | ของอวดชิ้นใหญ่ ราคาแพง | กลาง |

> ⚠️ ของที่ปลดล็อกแล้วต้องเก็บใน house save (`unlocked:[]`) เพื่อให้ export/import ตามไปด้วย

---

## 9. โครงข้อมูล + localStorage

### ไฟล์ใหม่ (ตามหลักการ "ไม่มี build step / lazy-load")
```
js/house-quests.js   ← คลังกลไก + ผังร้าน + ตัวสุ่ม (catalog ล้วน ไม่แตะ DOM/WebGL → เทสได้ง่ายเหมือน house-furniture.js)
```
โหลดต่อท้ายลำดับเดิมใน `js/games-ar.js`: `three → house-map → house-furniture → house-quests → house`

```js
HOUSE_QUESTS(kit) → {
  MECHANICS: [
    { id:'pay-exact', name:'จ่ายเงินให้พอดี', tags:['money'], minTier:1, fam:'A',
      gen(rng, diff){ /* คืนโจทย์ */ }, check(state, ans){ /* คืน {ok, stars} */ } },
    ...
  ],
  SHOP_QUESTS: {
    'shop-mart':  { npc:'npc-mart',      mech:['pay-exact','count-shelf'], base:5 },
    'shop-music': { npc:'npc-musicshop', mech:['melody-echo','sound-guess'], base:5 },
    ...
  },
  pickQuestFor(npcId, dayKey, childId, tier)
}
```

### บันทึกใน house save ก้อนเดิม (export/import ตามไปเอง)
```js
data.shopQuest = {
  d: '2026-8-6',                       // วันที่ (questDayKey())
  npc: {
    'npc-mart':      { mech:'pay-exact', st:'done',   stars:3 },
    'npc-food':      { mech:'recipe',    st:'active', prog:2 },
    'npc-musicshop': { mech:'melody-echo', st:'open' }
  },
  recent: ['pay-exact','recipe'],      // กันกลไกซ้ำ
  earnedToday: 85                      // เพดานเงินต่อวัน
}
data.unlocked = ['hat-crown','sofa-blue', ...]   // ของที่ซื้อแล้ว
```

**เหรียญไม่เก็บที่นี่** — ผ่าน `window.OwlCoins.add()/spend()` เท่านั้น
(ตามคอมเมนต์กำกับใน `js/app-core.js` ห้ามแตะ `progress.coins` ตรงๆ จากที่อื่น)

---

## 10. ดีไซน์ให้กลมกลืนกับโลก 3D เดิม

- **ป้าย "!" / "✓" ลอยเหนือหัว NPC** — ใช้ `signPlane()` + `userData.anim = {kind:'bob'}` (ระบบมีอยู่แล้ว)
  - ⚠️ ไอคอนใหม่ต้องเพิ่มเข้า `SIGN_ICONS` (`js/house.js:1478`) ให้ atlas texture มีช่อง
- **ร้านที่มีงานเรืองแสงอ่อนๆ ตอนกลางคืน** — เด็กมองหาเจอจากไกล
- **overlay เกมย่อย = การ์ดครีมมนๆ ลอยกลางจอ** สไตล์เดียวกับกล่องเลือกเฟอร์นิเจอร์ / `#house-quest` เดิม
  **ไม่ใช่หน้าเต็มจอ** — เด็กยังเห็นร้านข้างหลัง รู้สึกว่า "อยู่ในร้าน" จริง
- **เหรียญบินเข้าป้ายยอดเงิน** ตอนได้รางวัล (ต่อจาก `spawnParticle()` เดิม)
- **สมุดงานวันนี้ 📔** — ปุ่มใหม่ในเมนูเฟือง ลิสต์ว่าใครมีงานอยู่ตรงไหน กดแล้วชี้ทิศ
- ตัวอักษรใหญ่ ทรงมน สีสด ตามหลักการ "เหมาะกับเด็ก 5 ขวบ" ของโปรเจค
- อ่านโจทย์ออกเสียงไทยได้ (ใช้ `js/shared/speech.js`) — เด็ก ป.1 ยังอ่านไม่คล่อง

---

## 11. แผนทำเป็นเฟส

| เฟส | ได้อะไร | ขนาด |
|---|---|---|
| **1** | เปิด `QUEST_ENABLED` + ป้าย "!" + flow รับงาน/จบงาน + จ่ายเหรียญ + **กลไก 3 อัน** (จ่ายเงิน / จับคู่ / เรียงลำดับ) ที่ 3 ร้าน | กลาง |
| **2** | **ร้านใช้เงิน 2 ร้าน** (แฟชั่น + เฟอร์นิเจอร์) ← ต้องมาเร็ว ไม่งั้นเงินไร้ค่า | กลาง |
| **3** | เพิ่มกลไกอีก 6-8 อัน ครบทุกร้าน + ระบบความยากตามชั้น | ใหญ่ |
| **4** | **งานเดินโลก** (ส่งของ / รายการซื้อของ / ตามหา) + สมุดงาน + ชี้ทิศ | ใหญ่ |
| **5** | งานสร้างสรรค์, quest chain หลายวันของผู้ใหญ่บ้าน, เทศกาลตามฤดู | ต่อยอด |

---

## 12. ข้อควรระวัง / ความเสี่ยง

1. **ห้ามให้เควสต์กลายเป็น "การบ้าน"** — ถ้าเป็นเลขล้วนเด็กจะเลิกเข้าบ้าน ต้องคุมสัดส่วน A/B/C
2. **ห้ามลงโทษ** — ไม่มี "แพ้" ไม่เสียเงิน แย่สุดคือได้ ⭐ ดวงเดียว
3. **ขนาดไฟล์** — ชุด 3D ตอนนี้ ~1.2 MB แล้ว, `js/house.js` ~9,500 บรรทัด
   → กลไกเควสต์ต้องแยกไฟล์ + lazy-load **ห้ามยัดเพิ่มใน `house.js`**
4. **เควสต์เดินโลกต้องกันตัน** — ถ้า NPC เป้าหมายเดินไปมา (`roam`/`route`) ต้องมีปุ่มยกเลิกงาน/เปลี่ยนเป้าเสมอ
5. **เศรษฐกิจ** — เพดานเงินต่อวันต้องกำหนดตั้งแต่แรก
6. **`HOUSE_FEATURE_OFF`** — โหมดบ้านยังปิดบน `main` ระบบนี้จะไปกับรอบที่เปิดโหมดบ้านให้เด็กเล่นจริง
7. **ชุดเทส** — ควรเพิ่มเทส catalog แบบเดียวกับ `tests/house.spec.js` (id ไม่ซ้ำ, ทุกกลไก `gen()` ได้จริงทุก tier 1-6, ทุกร้านชี้ NPC ที่มีอยู่จริง)

---

## 13. คำถามที่ยังต้องตัดสินใจ (รอบแรก)

- [ ] เอาแผนเฟส 1→5 ตามนี้ไหม หรือสลับลำดับ
- [ ] "ร้านใช้เงิน" เริ่มจากร้านไหนก่อน (เสนอ **ห้างแฟชั่น** — ระบบแต่งตัวทำเสร็จแล้ว ต่อได้เร็วสุด)
- [ ] เควสต์ควรมี **จำกัดจำนวนต่อวัน** ไหม หรือปล่อยให้เด็กเล่นได้เรื่อยๆ (ตอนนี้เสนอ: จำกัดโดยธรรมชาติที่ 5-8 ร้าน/วัน)
- [ ] `QUEST_POOL` เดิม (ภารกิจ 3 อย่างของกระดานน้ำพุ) จะเก็บไว้เป็น layer แยกตามที่เสนอ หรือยุบรวมกับเควสต์ NPC
- [ ] จะให้ดึงคำถามจาก `CATS` มาใช้ในเควสต์โรงเรียนไหม (ประหยัดงานมาก แต่เด็กอาจรู้สึกว่าซ้ำกับหน้าหลัก)
- [ ] ราคาของในร้าน — จะจูนตัวเลขจริงตอนไหน (แนะนำ: ตอนทำเฟส 2 พร้อมกับร้านแรก)

---
---

# ภาค 2 — ข้อเสนอรอบขยาย (2026-08-07)

โจทย์ใหม่จากผู้ใช้ 6 ข้อ:
1. โจทย์ต้องเยอะกว่านี้มาก
2. แต่ละ NPC ต้องมี mechanic หลากหลาย (คิดใหม่ / รื้อของเก่ามาใช้ / ดึงจาก `IDEA.md`)
3. **เกมตอบคำถาม (quiz) เป็น mechanic พื้นฐานที่ต้องมีในทุก NPC ที่ทำเควสต์ได้**
4. **ปิดของฟรีทั้งหมด** — ทุกอย่างที่ custom ได้ (เฟอร์นิเจอร์ / ชุดแต่งตัว / สัตว์เลี้ยง) ต้องซื้อด้วยเงิน และไปอยู่ในร้านที่เหมาะสม
5. **สัตว์เลี้ยง**: ไม่มีสัตว์ = ไม่มีบ้านสัตว์ · เพิ่มอาหารหลายประเภท · ระบบความหิว · หิวจัด = ป่วย (ไม่ตาย) · ป่วยแล้วนอนในบ้าน ออกมาเดินไม่ได้ · ต้องคุยหมอที่โรงพยาบาลและจ่ายเงินรักษา
6. **กระดานเควสต์** เป็นจุดเดียว ทำได้ 5 เควสต์/วัน แยกโควตาจากเควสต์ NPC ปกติ

---

## 14. ตัวเลขจริงในโค้ดวันนี้ (ฐานคำนวณทั้งหมดในภาค 2)

นับจากไฟล์จริง ไม่ใช่ประมาณ:

| ของ | จำนวน | อยู่ที่ |
|---|---|---|
| เฟอร์นิเจอร์/ของตกแต่ง | **116 ชิ้น** (bath 9, bed 9, decor 13, decorout 23, garden 15, kitchen 9, play 12, seat 10, seatout 7, table 9) | `js/house-furniture.js` |
| ชนิดสัตว์เลี้ยง | **12 ชนิด** (แต่ละชนิดมี 2-4 สีขน) | `PET_TYPES` `js/house.js:8210` |
| NPC ทั้งหมด | **68 ตัว** | `NPC_DEFS` `js/house-map.js:777` |
| NPC ที่มีบทเควสต์แล้ว (`quest:`) | **57 ตัว** | เดียวกัน |
| QUEST_POOL ปัจจุบัน | **12 แบบ** | `js/house.js:7230` |
| แถวแต่งตัว | 11 แถว — ทรงผม 6 × สีผม 17, ตา 8 × สีตา 12, สีเสื้อ 26, ลายเสื้อ 10, สีกางเกง 28, สีรองเท้า 14, หมวก 9, แว่น 7, เป้ 7, ของถือ 9 (+ สีของแต่ง 12) | `js/house-map.js:19-80` |

**ของที่แจกฟรีตอน seed บ้านครั้งแรก** (`seedWorldDecor` `js/house.js:8944`): ต้นไม้ทั้งสนาม, รั้วรอบสนาม (มุม 4 + ท่อน), **บ้านสัตว์เลี้ยง 1 หลัง**, ทางเดินหน้าประตู 4 แผ่น — ทั้งหมดเป็น decor ที่เด็กย้าย/ลบได้

---

## 15. ระบบ mechanic: จาก 12 ภารกิจเดินสำรวจ → 23 mechanic เล่นจริง

หลักการ: **1 mechanic = 1 ฟังก์ชัน generator** ที่รับ `{tier, npc, seed}` แล้วคืนโจทย์ 1 ชุด ไม่ผูกกับ NPC ตัวใดตัวหนึ่ง ⇒ NPC ตัวไหนก็หยิบไปใช้ได้ และโจทย์ต่างกันทุกครั้งเพราะสุ่มพารามิเตอร์

### 15.1 กลุ่ม A — พื้นฐาน (ทุก NPC ต้องมี)

| id | ชื่อ | กติกา |
|---|---|---|
| `quiz` | **ตอบคำถาม** ⭐ | **บังคับมีในทุก NPC** — 3-5 ข้อ 4 ตัวเลือก ดึงคำถามจาก `CATS` ตามระดับชั้นของเด็ก + คลังคำถาม "ธีมร้าน" เฉพาะ NPC (เช่น NPC ร้านขนม → โจทย์บวกลบด้วยรูปขนม) |
| `count-things` | นับของ | นับของในฉาก 3D จริงตรงหน้า NPC แล้วเลือกตัวเลข |

`quiz` เป็นค่าเริ่มต้นเสมอ: ถ้า NPC ตัวไหนยังไม่ได้กำหนด mechanic เฉพาะ → ได้ `quiz` อัตโนมัติ ⇒ **NPC ทั้ง 57 ตัวมีเควสต์ได้ทันทีตั้งแต่วันแรก** ไม่ต้องรอทำ mechanic ครบ

### 15.2 กลุ่ม B — คิดใหม่สำหรับโหมดบ้านโดยเฉพาะ (10 แบบ)

| id | ชื่อ | กติกา | NPC ที่เหมาะ |
|---|---|---|---|
| `pay-exact` | จ่ายเงินให้พอดี | ของราคา N บาท เลือกเหรียญ/แบงก์ให้ครบพอดี | ทุกร้านค้า |
| `change-back` | ทอนเงิน | จ่ายไป X ของราคา Y ต้องทอนเท่าไร | ร้านสะดวกซื้อ, ห้าง |
| `recipe-seq` | ทำตามสูตร | ใส่วัตถุดิบตามลำดับ+จำนวนที่บอก (เห็นสูตร 3 วิ แล้วสูตรหาย) | ร้านอาหาร, ไอศกรีม, ตลาด |
| `shopping-list` | จำรายการซื้อของ | จำ 3-6 อย่าง แล้วไปหยิบจากชั้นให้ครบ | ร้านสะดวกซื้อ, ตลาด |
| `deliver` | ส่งของถึงมือ | รับของจาก NPC A เดินไปส่ง NPC B (ใช้แผนที่โลกจริง) | ทุกตัว — เชื่อม NPC เข้าหากัน |
| `escort` | พาไปส่ง | พาเด็ก/ผู้สูงอายุเดินไปสถานที่ตามที่บอก (NPC เดินตาม) | ตำรวจ, พยาบาล, ครู |
| `feed-pet` | ให้อาหารถูกชนิด | จับคู่สัตว์ ↔ อาหารที่มันกิน | ร้านสัตว์เลี้ยง, ฟาร์ม |
| `stock-shelf` | จัดชั้นวาง | เรียงของขึ้นชั้นตามหมวด/ขนาด/สี | ร้านสะดวกซื้อ, ห้าง |
| `fish-math` | ตกปลาคิดเลข | ตกปลาได้ตัวเลข ต้องตกให้ผลรวมเท่าโจทย์ | กระท่อมริมบ่อ |
| `traffic-lr` | ซ้าย-ขวาว่องไว | ไฟ/ป้ายบอกทิศ กดให้ทันเวลา (จาก `IDEA.md`) | ตำรวจจราจร |

### 15.3 กลุ่ม C — รื้อ engine เดิมในแอปมาใช้ (7 แบบ, งานน้อยที่สุด/คุ้มที่สุด)

engine พวกนี้เขียนไว้แล้วใน `js/games-*.js` — งานคือ **ห่อเป็น overlay ในโหมดบ้าน** ไม่ต้องเขียน logic ใหม่

| id | ชื่อ | ดึงจาก | NPC ที่เหมาะ |
|---|---|---|---|
| `match-pair` | จับคู่ความจำ | `games-think.js` | ห้างเฟอร์นิเจอร์, ร้านของเล่น |
| `color-mix` | ผสมสี | `games-art.js` | ช่างไม้ (ทาสี), ห้างแฟชั่น |
| `melody-echo` | เล่นตามทำนอง | `games-art.js` + `shared/piano.js` | ร้านเครื่องดนตรี |
| `sound-guess` | ทายเสียง | `games-art.js` | ร้านเครื่องดนตรี, ฟาร์ม (เสียงสัตว์) |
| `pattern-fill` | เติมแพทเทิร์น | หมวด `pattern` | โรงเรียน, ห้างแฟชั่น (ลายผ้า) |
| `weigh-balance` | ตาชั่ง/ตวง | `games-math.js` | ตลาด, ร้านขนม |
| `clock-set` | ตั้งนาฬิกา | `games-math.js` (นาฬิกาวิเศษ) | โรงเรียน, โรงแรม, สถานีรถ |

### 15.4 กลุ่ม D — ดึงจาก `IDEA.md` (4 แบบ — ทำในโหมดบ้านเลย ไม่ต้องเป็นหมวดแยก)

| id | ชื่อ | ปรับให้เข้าโหมดบ้านยังไง | NPC |
|---|---|---|---|
| `spot-diff` | จับผิดภาพ | ใช้ตู้โชว์/หน้าร้าน 2 ฝั่งเทียบกัน | ห้าง, พิพิธภัณฑ์ |
| `flash-count` | นับแว้บเดียว | ของบนรถเข็นโผล่ 2 วิ แล้วปิด | ตลาดรถเข็น |
| `dress-order` | แต่งตัวตามโจทย์ | **ใช้ระบบแต่งตัวจริงของเกม** — NPC สั่ง "หมวกฟาง + แว่นกลม + เป้หมี" | ห้างแฟชั่น |
| `find-hidden` | หาของที่หาย | ซ่อน object ในโลก 3D จริง เดินไปหา | ตำรวจ, เทศมนตรี |

> เมื่อทำ 4 อันนี้เสร็จ **ต้องลบไอเดียนั้นออกจาก `IDEA.md`** ตามกติกาโปรเจค — แต่ถ้าตั้งใจจะทำเป็นหมวดเกมแยกในหน้าหลักด้วยในอนาคต ให้เก็บไว้ (ต้องถามผู้ใช้ก่อนลบ)

**รวม 23 mechanic** (A 2 + B 10 + C 7 + D 4)

---

## 16. "โจทย์เยอะๆ" — คำนวณจำนวนจริง

โจทย์ 1 ชุด = `mechanic × tier × พารามิเตอร์สุ่ม × ธีม NPC`

- 23 mechanic × 3 tier (ป.เล็ก/กลาง/โต) = **69 แม่แบบ**
- แต่ละแม่แบบสุ่มพารามิเตอร์ได้เฉลี่ย ~30 หน้าตา (ตัวเลข/สี/ของ/จำนวน) = **~2,000 โจทย์ที่ไม่ซ้ำหน้า**
- `quiz` ดึงจาก `CATS` ที่มีอยู่แล้ว **332 หมวด** → คำถามหลักพันข้อฟรีๆ ไม่ต้องเขียนใหม่
- คูณธีม NPC 57 ตัว (ของในโจทย์เปลี่ยนตามร้าน) ⇒ เด็กแทบไม่เจอโจทย์เดิมซ้ำ

**กันซ้ำ**: เก็บ `recentQuests: [ 'npcId:mech:hash', … ]` 40 รายการล่าสุดใน save แล้วสุ่มเลี่ยง

---

## 17. เศรษฐกิจ — ปิดของฟรีทั้งหมด

### 17.1 ตารางราคา (เสนอ)

**เฟอร์นิเจอร์ 116 ชิ้น** แบ่ง 4 ระดับตามขนาด/ความอลัง:

| ระดับ | ราคา | ตัวอย่าง | ประมาณ |
|---|---|---|---|
| จิ๋ว | 25 🪙 | กระถาง, ตุ๊กตา, พรม, ป้าย | ~35 ชิ้น |
| เล็ก | 60 🪙 | เก้าอี้, โต๊ะเล็ก, ชั้นวาง | ~35 ชิ้น |
| กลาง | 140 🪙 | โซฟา, เตียง, ตู้เย็น, ชิงช้า | ~30 ชิ้น |
| ใหญ่/พิเศษ | 300 🪙 | สระ, บ้านต้นไม้, ม้าหมุน, ไฟประดับ | ~16 ชิ้น |

**ชุดแต่งตัว**:

| ของ | ราคา |
|---|---|
| สีเสื้อ/กางเกง/รองเท้า (ต่อสี) | 15 🪙 |
| ทรงผม (ต่อทรง) / สีผม / สีตา | 30 🪙 |
| ลายเสื้อ (ต่อลาย) | 40 🪙 |
| หมวก / แว่น (ต่อชิ้น) | 60 🪙 |
| เป้สะพาย / ของถือ (ต่อชิ้น) | 80 🪙 |
| **สีของหมวก/แว่น/เป้/ของถือ (ต่อสี)** | **20 🪙** — เปิดขายเพิ่ม 2026-08-08 ตามคำขอผู้ใช้ (เดิมแจกฟรีทั้งแถว) ถูกกว่าสีอื่นเพราะต้องซื้อตัวของก่อนถึงได้ใช้ |

**สัตว์เลี้ยง 12 ชนิด** — ราคาไล่ตามความ "หายาก" ให้เด็กมีอะไรให้ตั้งเป้า:

| กลุ่ม | ราคา | ชนิด |
|---|---|---|
| เริ่มต้น | 250 🪙 | 🐶 หมา, 🐱 แมว, 🐰 กระต่าย, 🐥 ลูกเจี๊ยบ |
| กลาง | 450 🪙 | 🐹 แฮมสเตอร์, 🐢 เต่า, 🐸 กบ, 🐷 หมู |
| พิเศษ | 800 🪙 | 🐑 แกะ, 🐧 เพนกวิน |
| หายาก | 1,500 🪙 | 🐼 แพนด้า, 🦄 ยูนิคอร์น |

สีขนเพิ่มเติมของสัตว์: สีแรกแถมฟรีตอนซื้อ, สีอื่น 100 🪙/สี

### 17.2 ของแถมฟรีตอนเริ่ม (สำคัญ — กันบ้านโล่งจนเล่นไม่สนุก) ✅ ทำแล้วเฟส 1

เด็กใหม่ยังต้องมีของพอเล่นวันแรก ให้ฟรี:
- **รั้ว + ทางเดินหน้าประตู + ต้นไม้ในสนาม + บ้านสัตว์** — ของฉากที่ `seedWorldDecor` วางอยู่แล้ว ไม่ใช่ของสะสม ให้ฟรีเหมือนเดิม (แต่ต้องนับเป็น "สิทธิ์" ด้วย ไม่งั้นลบทิ้งแล้วหยิบกลับมาวางไม่ได้)
- **เฟอร์นิเจอร์เริ่มต้นในบ้าน 9 ชิ้น** ตามข้อ 26 (**ไม่ใช่ 6 ชิ้นตามร่างแรก** — ข้อ 26 ที่ปรับ 2026-08-07 มาแทน) รวมของฉากนอกบ้าน 3 อย่างเป็น **12 ชิ้น**
- **ชุดแต่งตัวเริ่มต้น**: ทรงผม 2, **สีผม 3 (ดำ + น้ำตาล 2 เฉด)**, สีตา 1, สีเสื้อ 4, สีกางเกง 3, สีรองเท้า 2, **สีของเครื่องแต่งชิ้นละ 3 สี** (หมวก/แว่น/เป้/ของถือ = ตัวของต้องซื้อทั้งหมด ได้ฟรีแค่ตัวเลือก "ไม่ใส่")
  - **สีของเครื่องแต่ง แจก 3 สีแรกฟรีทุกชิ้น** (ซื้อหมวกมาแล้วต้องมีสีให้เลือกบ้าง ไม่ใช่ได้สีเดียว) ที่เหลือ 20 🪙/สี
  - ⚠ **index ที่แจกฟรีของแต่ละแถวต้องเรียงติดกันเสมอ** — ถ้าชิปที่ปลดล็อกกระจายสลับกับชิปที่ล็อก เด็กอ่านไม่ออกว่าอันไหนเลือกได้ (เดิมสีเสื้อเป็น `[0,1,2,5]` มีช่องล็อกคั่นกลาง แก้เป็น `[2,3,4,5]` เมื่อ 2026-08-07) มีเทสคุมข้อนี้ไว้แล้ว
  - ⚠ **เพิ่มแถวที่มีราคาใหม่เมื่อไหร่ ต้องปั๊ม `ECON_VER` ทุกครั้ง** (ตอนนี้ = 3) — `migrate()` จะได้รันซ้ำแล้วแจกสิทธิ์ของแถวใหม่ให้เด็กที่ใส่อยู่ก่อนแล้ว ไม่งั้นชุดที่ใส่อยู่กลายเป็นล็อกทันที = ผิดกติกาเหล็กข้อ 3
  - **เพศ · รูปทรงดวงตา · สีของหมวก/แว่น/เป้/ของถือ = ฟรีทั้งแถว ไม่วางขาย** (เป็นหน้าตาเด็กเอง / ต้องซื้อตัวของก่อนถึงจะเห็นสีอยู่แล้ว)
- **สัตว์เลี้ยง: ไม่แถม** — ต้องซื้อทุกตัว (ตามข้อ 5 ของผู้ใช้) → **ยังไม่ทำในเฟส 1** ดูหมายเหตุ 17.5
- เงินเริ่มต้น 0 🪙 แต่เควสต์แรกให้ 30-50 🪙 ⇒ เด็กซื้อของชิ้นแรกได้ในวันแรก (**เควสต์มาเฟส 2**)

### 17.3 ⚠️ Migration — เด็กที่เล่นอยู่แล้ว

**ห้ามให้ของที่เด็กวางไว้แล้วหายไปเด็ดขาด** ตอนอัปเกรด:

✅ **ทำแล้วเฟส 1** — `SHOP.migrate(d)` ใน `js/house-shop.js` ถูกเรียกจาก `loadHouseData()` ทุกครั้งที่อ่านข้อมูล (ทำงานจริงครั้งเดียวต่อเด็ก แล้วปั๊ม `econVer`)

```
ครั้งแรกที่โหลดด้วยเวอร์ชันใหม่ (data.econVer !== 2):
  data.unlocked += ทุก id ที่ปรากฏใน data.decor.out/in (นับว่าซื้อแล้ว)
  data.unlocked += 'fit:<แถว>:<index>' ของทุกค่าที่ปรากฏใน data.char (ทรงผม/สี/ลาย/หมวก… ที่ใส่อยู่)
  data.unlocked += ชุดเริ่มต้น 8 ชิ้น + ของฉากนอกบ้าน + ชุดแต่งตัวเริ่มต้น
  data.econVer = 2
```
**ยกไปเฟส 3 (ตอนเปิดร้านสัตว์เลี้ยงจริง)**: `ถ้ามี data.pet → owned.pets = [ชนิดนั้น] + ความอิ่มเต็ม` และ `ถ้าไม่มี data.pet → ลบ decor 'pet-house'`
> เหตุผล: เฟส 1 การรับเลี้ยงสัตว์ยังฟรีอยู่ ถ้าลบบ้านสัตว์ทิ้งตอนนี้ เด็กที่รับเลี้ยงในเฟส 1 จะไม่มีบ้านให้น้องนอน (ถอยหลัง) ⇒ ทำพร้อมกันทีเดียวตอนเฟส 3

### 17.4 ร้านไหนขายอะไร (ใช้ล็อตที่มีอยู่แล้วในแผนที่ ไม่ต้องสร้างตึกใหม่)

| ร้าน | ขาย |
|---|---|
| 🛒 ห้างเฟอร์นิเจอร์ (`mall-furniture`) | เฟอร์นิเจอร์ในบ้าน — bath, bed, kitchen, seat, table, decor |
| 🌷 ร้านต้นไม้/สวน (`garden` — ใช้ล็อตตลาด) | garden, decorout, seatout |
| 👗 ห้างแฟชั่น (`mall-fashion`) | ชุดแต่งตัวทั้งหมด |
| 🐾 ร้านสัตว์เลี้ยง (`shop-pet`) | สัตว์เลี้ยง + สีขน + **อาหารสัตว์** + ของเล่นสัตว์ |
| 🎮 ร้านของเล่น (`shop-mart` มุมของเล่น) | หมวด play |
| 🏥 โรงพยาบาล (`hospital`) | บริการรักษาสัตว์ป่วย (ไม่ใช่ของ) |

**การจัดหมวดในร้าน (ปรับ 2026-08-08)**: **"ตัวของ + สีของมัน อยู่แท็บเดียวกัน"** ไม่แยกสีเป็นหมวดต่างหาก
(ผม+สีผม · เสื้อ(ลาย)+สีเสื้อ · เครื่องหัว+สีเครื่องหัว · แว่น/เป้/ของถือ เช่นเดียวกัน)
⇒ ร้านแฟชั่นเหลือ **9 หมวด** (เคยลองแยกสีเป็นหมวดแล้วพุ่งเป็น 15 หมวด ยาวเกินจอจนต้องมี scrollbar เลื่อนหา
   และดันปุ่ม "ออกจากร้าน" หลุดนอกจอบนจอเตี้ย) · ทุกหมวดมี **ไอคอน SVG** (แฟชั่นใช้ `H_ROW_ICONS`
   จาก js/house-map.js · เฟอร์นิเจอร์ใช้ `FURN_CAT_ICONS` ใน js/house-shop.js)
**ห้ามให้แถบหมวดต้องเลื่อนหา** — ถ้าหมวดเยอะขึ้นอีกให้ไปยุบหมวดก่อน อย่าแก้ด้วยการปล่อยให้เลื่อน

**หน้าตาร้าน**: overlay แบบเดียวกับหน้าเลือกสัตว์เลี้ยง/หน้าแต่งตัวที่มีอยู่ (`.house-creator`) — การ์ดสินค้า grid มี emoji/พรีวิว 3D + ราคา + ปุ่ม "ซื้อ" ของที่ซื้อแล้วขึ้น ✓ ของที่เงินไม่พอขึ้นราคาสีเทา **ไม่ซ่อน** (ให้เด็กเห็นเป้าหมาย)

### 17.5 ⚠️ สิ่งที่เฟส 1 ทำต่างจากตารางข้างบน (ตั้งใจ — อ่านก่อนทำเฟส 2-3)

| หัวข้อ | ที่ทำจริงในเฟส 1 | ทำไม |
|---|---|---|
| ร้านที่เปิด | **ห้างเฟอร์นิเจอร์ + ห้างแฟชั่น เท่านั้น** | ตามแผนเฟส 1 |
| ห้างเฟอร์นิเจอร์ขายอะไร | **ทุกหมวดของเฟอร์นิเจอร์ รวมของนอกบ้าน** (สวน/เครื่องเล่น/ที่นั่งนอกบ้าน/ตกแต่งสวน) | ร้านต้นไม้กับร้านของเล่นยังไม่เปิดจนเฟส 2-3 ถ้าไม่ขายที่นี่ ของกลุ่มนั้นจะซื้อไม่ได้เลย = **ผิดกติกาเหล็กข้อ 1 (dead end)** ⇒ **เฟส 2-3 ค่อยย้ายหมวดไปร้านที่ถูกต้องตามข้อ 17.4** |
| สัตว์เลี้ยง / อาหาร / โรงพยาบาล | **ยังไม่แตะ** ยังรับเลี้ยงฟรีเหมือนเดิม | เป็นเฟส 3 ทั้งก้อน |
| `pet-house` | ยัง seed ให้ฟรีเหมือนเดิม | ดูเหตุผลข้อ 17.3 |
| แหล่งรายได้ | **ยังไม่มี** เงินเริ่ม 0 🪙 | เควสต์อยู่เฟส 2 ⇒ ห้าม merge ขึ้น production เดี่ยวๆ ถ้าจะเปิดให้เด็กเล่นจริง |

---

## 18. สัตว์เลี้ยง — วงจรชีวิตใหม่

### 18.1 ไม่มีสัตว์ = ไม่มีบ้านสัตว์

- ลบ `seed.push({id:'pet-house', …})` ออกจาก `seedWorldDecor` (`js/house.js:8967`)
- ย้ายไปสร้างตอน **ซื้อสัตว์ตัวแรกสำเร็จ**: วางบ้านที่ `PET_HOUSE_TILE` (ช่องเดิมที่จองไว้) พร้อม effect ปรากฏ + นกฮูกพูด "บ้านของ<ชื่อสัตว์>มาแล้ว!"
- ถ้าเด็กปล่อยสัตว์คืน (ปุ่ม `#house-pet-remove` เดิม) → บ้านสัตว์หายไปด้วย
- บ้านสัตว์ยัง**ย้ายที่ได้**ในโหมดตกแต่ง แต่**ลบเองไม่ได้** (ผูกกับการมีสัตว์)

### 18.2 อาหาร — คนละประเภทต่อชนิดสัตว์

ขายที่ร้านสัตว์เลี้ยง ซื้อเป็น "ถุง" เก็บใน `data.petFood = {meat:3, fish:1, …}`

| อาหาร | ราคา/ถุง (5 มื้อ) | สัตว์ที่กิน |
|---|---|---|
| 🍖 อาหารเนื้อ | 60 🪙 | 🐶 หมา |
| 🐟 อาหารปลา | 60 🪙 | 🐱 แมว, 🐧 เพนกวิน |
| 🥕 ผักสด | 50 🪙 | 🐰 กระต่าย, 🐢 เต่า |
| 🌾 เมล็ดพืช | 45 🪙 | 🐥 ลูกเจี๊ยบ, 🐹 แฮมสเตอร์ |
| 🌿 หญ้าแห้ง | 50 🪙 | 🐑 แกะ, 🐷 หมู |
| 🦗 แมลง | 55 🪙 | 🐸 กบ |
| 🎋 ไผ่ | 90 🪙 | 🐼 แพนด้า |
| 🌈 อาหารวิเศษ | 150 🪙 | 🦄 ยูนิคอร์น |

**นี่คือของเล่นสอนจริง**: เด็กต้องรู้ว่าสัตว์ตัวไหนกินอะไร ⇒ ให้อาหารผิดชนิด สัตว์ส่ายหัว 🙅 ไม่กิน ไม่เสียของ ไม่ลงโทษ แค่ลองใหม่ — และเป็นวัตถุดิบของ mechanic `feed-pet` โดยตรง

**วิธีให้อาหาร**: แตะชามหน้าบ้านสัตว์ (หรือแตะตัวสัตว์ → เมนู "ให้อาหาร") → เลือกถุงจากคลัง → สัตว์วิ่งมากิน animation กิน 2 วิ + หัวใจ + ความอิ่มเต็ม

### 18.3 ความหิว — ออกแบบให้ "ไม่ลงโทษเด็กที่หายไปนาน"

```
data.pet.fed = timestamp มื้อล่าสุด
data.pet.sick = false
```

- ความอิ่ม 0-100 · **ลดตาม "วันที่เด็กเข้าเล่น" ไม่ใช่เวลาจริงล้วน** — เข้าเล่นวันใหม่ = -35 (ไม่ว่าจะห่างกัน 2 วันหรือ 2 เดือน) ⇒ **หายไป 3 เดือนกลับมาสัตว์ก็แค่หิว 1 วัน ไม่ป่วยทันที**
- ในวันเดียวกันความอิ่มลดช้ามาก (-5 ต่อ 20 นาทีในเกม) เพื่อให้ยังมีอะไรทำระหว่างวัน
- แถบความอิ่มโชว์เหนือหัวสัตว์เมื่อ < 50 (หัวใจเต็ม/ครึ่ง/ว่าง แทนตัวเลข — เด็ก 5 ขวบอ่านเลขเปอร์เซ็นต์ไม่รู้เรื่อง)
- **ป่วยเมื่อ**: ความอิ่มเหลือ 0 แล้วเข้าเล่น**วันถัดไป**อีก 1 วันโดยไม่ให้อาหาร ⇒ ต้องละเลย 2 วันเล่นติดกันถึงจะป่วย และมีคำเตือนล่วงหน้า (สัตว์ส่งฟองข้อความ "หิวจัง 😢" + นกฮูกเตือนตอนเข้าเกม)

### 18.4 ป่วย → โรงพยาบาล

เมื่อ `sick = true`:
- สัตว์ **นอนในบ้านสัตว์ ออกมาไม่ได้** (ใช้กลไก `hPet.rest` ที่มีอยู่แล้ว แต่ล็อกไว้ ปลุกไม่ตื่น)
- แตะตัวสัตว์ → ฟองข้อความ "ไม่สบาย… 🤒 พาหนูไปหาหมอที" + ไอคอน 🏥 ชี้ทางบนเข็มทิศ
- เดินไปโรงพยาบาล คุย **คุณหมอใจดี 🩺 (`npc-doctor`)** หรือ **พี่พยาบาล 💉 (`npc-nurse`)** ที่มีอยู่แล้วในแผนที่
- หมอคิด **ค่ารักษา 120 🪙** → จ่ายแล้วเล่น mini-sequence สั้นๆ (ตรวจ → ฉีดยา → พันผ้า) ~5 วิ → สัตว์หายป่วย ความอิ่มเต็ม วิ่งกลับมาหาเด็ก
- **เงินไม่พอ**: หมอไม่ไล่กลับ — เสนอ "ทำงานช่วยหมอแทนค่ารักษา" = เควสต์ 1 ชุด ⇒ **เด็กไม่มีทางติดตัน** (สำคัญมาก ห้ามมี dead end ในเกมเด็ก)
- ป่วยแล้ว**ไม่หายเอง** ต้องคุยหมอเท่านั้น (ตามที่ผู้ใช้ระบุ)

---

## 19. กระดานเควสต์ — โควตาแยก 5 เควสต์/วัน

ปัจจุบัน `#house-quest` ข้างน้ำพุ ทำหน้าที่รวมเควสต์ NPC เสนอเปลี่ยนเป็น **แหล่งเควสต์อิสระ**:

| | กระดานเควสต์ 📋 | เควสต์ NPC 🧑 |
|---|---|---|
| โควตา | **5 ชุด/วัน** | ตามจำนวน NPC ที่ยังไม่ได้คุยวันนี้ |
| ใครออกโจทย์ | กระดาน (ไม่ต้องเดินหา NPC) | NPC ตัวนั้นๆ |
| mechanic | สุ่มจากทั้ง 23 แบบ | จาก pool เฉพาะของ NPC ตัวนั้น (+ `quiz` เสมอ) |
| รางวัล | **10-28 🪙** + ⭐ | **6-17 🪙** |
| โบนัส | ทำครบ 5 ชุด → **โบนัส 35 🪙 + ⭐ พิเศษ** | ไม่มี |

> ⚠️ ตัวเลขรางวัลในตารางนี้ **ถูกจูนใหม่ 2026-08-08 กันเงินเฟ้อ** (เดิมเสนอ 40-80 / 20-50 + โบนัส 100) — ที่มาและตารางเต็มอยู่ในข้อ 6

- นับวันด้วย local date string `YYYY-MM-DD` เก็บที่ `data.board = {date, done:[…], claimed:false}`
- กระดานโชว์ 5 ช่อง ช่องที่ทำแล้วติ๊ก ✓ — เด็กเห็นความคืบหน้าเป็นภาพ
- เที่ยงคืนรีเซ็ต · **ห้ามหักดาว/เงินถ้าวันก่อนทำไม่ครบ** (ไม่มีบทลงโทษใดๆ)
- state โควตา NPC แยกคนละ key ⇒ ทำกระดานครบ 5 แล้วยังเดินคุย NPC ต่อได้

---

## 20. โครงสร้างไฟล์ที่ต้องเพิ่ม

`js/house.js` วันนี้ **10,199 บรรทัด** — ถ้ายัดทั้งหมดลงไปอีกจะแตะ ~14,000 บรรทัด แก้ยากมาก เสนอแยก:

| ไฟล์ใหม่ | เนื้อหา | ประมาณ |
|---|---|---|
| `js/house-quests.js` | catalog 24 mechanic + generator + pool ต่อ NPC + logic กระดาน | ~1,800 บรรทัด |
| `js/house-shop.js` | ตารางราคา + inventory + UI ร้าน + migration | ~900 บรรทัด |
| `js/house-pet-care.js` | อาหาร/ความหิว/ป่วย/โรงพยาบาล | ~500 บรรทัด |

โหลดแบบ lazy ต่อจาก `house.js` ใน `js/games-ar.js` (three → map → furniture → **shop → quests → petcare** → house) และ **ต้องเพิ่ม `?v=` ตาม `window.APP_ASSET_VER`** เหมือนไฟล์ lazy อื่น

`js/house.js` แก้เฉพาะจุดเชื่อม: `seedWorldDecor` (ตัดบ้านสัตว์), pet picker (ผ่านร้าน), decorate palette (กรองเฉพาะของที่ซื้อแล้ว), NPC talk (เรียก quest engine)

---

## 21. แผนทำเป็นเฟส (แนะนำลำดับนี้)

| เฟส | เนื้อหา | ทำไมลำดับนี้ |
|---|---|---|
| **1** | เศรษฐกิจ + migration + ร้าน 2 ร้านแรก (เฟอร์นิเจอร์, แฟชั่น) | ถ้าไม่ปิดของฟรีก่อน เควสต์ก็ไม่มีความหมาย — เงินไม่มีที่ใช้ |
| **2** | quest engine + `quiz` + `count-things` ให้ NPC ครบ 57 ตัว + กระดาน 5/วัน | เด็กเริ่มหาเงินได้จริงทั้งเมืองทันที |
| **3** | ร้านสัตว์ + ซื้อสัตว์ + บ้านสัตว์ตามเงื่อนไข + อาหาร/ความหิว/ป่วย/หมอ | ก้อนใหญ่ที่สุด แต่ต้องรอเศรษฐกิจเสร็จก่อน |
| **4** | mechanic กลุ่ม C (รื้อ engine เดิม 7 แบบ) | คุ้มสุดต่อแรง — โจทย์เพิ่มเยอะโดยแทบไม่เขียน logic ใหม่ |
| **5** | mechanic กลุ่ม B ที่เหลือ (10 แบบ) | งานเขียนใหม่จริง ทยอยเพิ่มได้ |
| **6** | mechanic กลุ่ม D จาก `IDEA.md` (4 แบบ) | เพิ่มความหลากหลายรอบสุดท้าย |

แต่ละเฟสจบแล้วเล่นได้จริงไม่พัง ⇒ merge เข้า `main` ได้ทีละเฟส (ถ้าผู้ใช้ตัดสินใจเปิดโหมดบ้านให้เด็กเล่น)

---

## 22. ความเสี่ยง/จุดที่ต้องระวัง

1. **ของหายหลังอัปเดต** — migration ข้อ 17.3 ต้องมาก่อนโค้ดร้าน ไม่งั้นเด็กที่ตกแต่งบ้านไว้แล้วเสียของ ⇒ เขียนเทส Playwright ยิง save เก่าเข้าไปแล้วเช็คว่าของครบ
2. ~~**เงินฝืดเกิน**~~ → **ของจริงกลับเป็นเงินเฟ้อ** (แก้แล้ว 2026-08-08) — ตอนร่างกังวลว่าเควสต์ให้เงินน้อยไป แต่พอทำจริงแล้วโควตา 13 เควสต์/วันคูณกับรางวัล 20-50 กลายเป็น ~600 🪙/วัน = ซื้อของครบทั้งเกมใน 27 วัน ⇒ ลดรางวัลลง ~3 เท่าตามคำสั่งผู้ใช้ (ดูข้อ 6) · **บทเรียน: ต้องคิดรายได้ "ต่อวัน" เทียบกับ "มูลค่าของทั้งเกม" เสมอ ไม่ใช่ดูรางวัลต่อเควสต์อย่างเดียว**
3. **สัตว์ป่วยแล้วเด็กงง** — ต้องมีสัญญาณชัด 3 ชั้น: ฟองข้อความสัตว์ + นกฮูกเตือนตอนเข้าเกม + ไอคอน 🏥 บนเข็มทิศ
4. **ขนาดไฟล์โหลด** — ชุด 3D วันนี้ ~1.2 MB เพิ่มอีก ~3 ไฟล์ ⇒ ประเมิน +150 KB ยังรับได้ แต่ต้องอัปม่านโหลด `#house-loading` ให้ % ตรง
5. **เทสเดิมต้องไม่แดง** — `tests/house.spec.js` ยิง `dispatchEvent('click')` เข้าปุ่มบ้าน ต้องรันทุกเฟส
6. **สิ่งที่ต้องถามก่อน merge เข้า `main` เสมอ** — จะเปิดโหมดบ้านให้เด็กเล่นจริงหรือคง `HOUSE_FEATURE_OFF` ไว้ (กฎใน `CLAUDE.md`)

---

## 23. ข้อสรุปที่ผู้ใช้ตัดสินแล้ว (2026-08-07)

- [x] **ราคา/ค่าตอบแทน** — ✅ **ราคาของใช้ตามข้อ 17 ทั้งหมด** (เฟอร์นิเจอร์ 25/60/140/300, ชุดแต่งตัว 15-80, สัตว์ 250-1,500) · ⚠️ **ค่าตอบแทนเควสต์จูนใหม่ 2026-08-08 กันเงินเฟ้อ**: NPC **6-17** 🪙 · กระดาน **10-28** 🪙 + โบนัส **35** ⇒ ขยันทั้งวันได้ ~200-310 🪙 (ตารางเต็มในข้อ 6)
- [x] **จำนวนสัตว์เลี้ยง** — ✅ **เลี้ยงได้ 1 ตัวเหมือนเดิม** ไม่ต้องรื้อโค้ด `hPet` ให้รองรับหลายตัว · เปลี่ยนตัวได้ (ปล่อยตัวเดิมคืนร้านแล้วซื้อตัวใหม่) · **บ้านสัตว์จึงมีหลังเดียวเสมอ** · ชนิดที่เคยซื้อแล้วจำไว้ใน `owned.pets` ⇒ ซื้อซ้ำภายหลังไม่ต้องจ่ายเต็มราคา (เสนอ: จ่ายครึ่งเดียว) กันเด็กเสียดายจนไม่กล้าลองสัตว์ตัวอื่น
- [x] **ความหิว** — ✅ **ลดตาม "วันที่เข้าเล่น" ไม่ใช่เวลาจริง** ตามข้อ 18.3 (เข้าเล่นวันใหม่ = -35 ไม่ว่าห่างกัน 2 วันหรือ 2 เดือน) ⇒ หายไปนานกลับมาสัตว์แค่หิว ไม่ป่วยทันที ไม่ลงโทษเด็กที่ไม่ได้เล่นทุกวัน
- [ ] **ของแถมฟรีตอนเริ่ม** (ข้อ 17.2) — ยังไม่สรุป: เฟอร์นิเจอร์ 6 ชิ้น + ทรงผม 2 + สีเสื้อ 4 + สีกางเกง 3 + สีรองเท้า 2 พอไหม
- [ ] **เริ่มเฟส 1** — ผู้ใช้ขอปรับข้อเสนอก่อน รอสรุปว่าปรับตรงไหน

---

# ภาค 3 — ปรับข้อเสนอตามคำสั่ง (2026-08-07)

> ทั้ง 9 ข้อนี้ปรับทับภาค 2 ที่จุดที่ระบุไว้ ส่วนที่ไม่ได้พูดถึงคงเดิมทุกอย่าง

## 24. ประตูเช็คความพร้อม ก่อนเจอโจทย์ระดับสูงกว่า (แก้ข้อ 7)

คงกติกา "โจทย์สูงกว่าระดับชั้น 1 ระดับ" ไว้ แต่ไม่ยัดตั้งแต่วันแรก ต้องผ่าน 3 เงื่อนไขก่อน

| เงื่อนไข | เกณฑ์ |
|---|---|
| 1. ปริมาณ | ทำเควสต์ระดับตัวเองสำเร็จครบ **12 ชุด** (นับแยกตามชั้น เลื่อนชั้นแล้วเริ่มใหม่) |
| 2. คุณภาพ | ความแม่น 10 ชุดล่าสุด **≥ 70%** (วัดจากดาวที่ได้ ไม่ใช่จำนวนครั้งที่ผิด) |
| 3. ความสมัครใจ | NPC ถาม "อยากลองข้อยากขึ้นไหม" ปุ่ม **ลองเลย / ยังไม่พร้อม** — ห้ามโผล่มาเงียบๆ |

- ผ่านแล้วผสมแค่ **20%** (80% ยังเป็นระดับตัวเอง) โจทย์ท้าทายได้เหรียญ ×1.5 + ดาวพิเศษ
- พลาดโจทย์ท้าทาย **3 ชุดติด** ⇒ ปิดชั่วคราวกลับไปเก็บระดับตัวเอง **ไม่หักเงิน ไม่หักดาว ไม่มีคำว่าแพ้** แล้วค่อยถามใหม่
- เก็บใน save: `chal.done[grade]` · `chal.recent[]` · `chal.on` · `chal.miss`

## 25. สมุดสติกเกอร์คงเดิมทุกอย่าง

`#sticker-tally-btn` / `#sticker-book` / `progress.stickers` **ไม่แตะเลย** ระบบใหม่อยู่ในโหมดบ้านล้วน
เควสต์ในบ้านให้ "เหรียญ" อย่างเดียว **ไม่แจกสติกเกอร์หมวดเรียน** (สติกเกอร์คือหลักฐานว่าเรียนจบหมวดจริง ถ้าแจกจากการเดินเล่นความหมายจะเจือจาง)

*ข้อเสนอเสริม (ยังไม่ทำถ้าไม่สั่ง)*: เพิ่มแถบใหม่ในสมุดเล่มเดิมชื่อ "สติกเกอร์บ้านของหนู" เก็บเหรียญตราจากบ้านแยกแถบ ของเดิมไม่ขยับ

## 26. บ้านมาพร้อมชุดเฟอร์นิเจอร์มาตรฐาน (ตอบข้อค้าง 17.2)

ไม่ใช่แค่ปลดล็อกให้ซื้อ แต่**วางไว้ในบ้านเรียบร้อยตั้งแต่แรก** — เปิดบ้านครั้งแรกต้องเจอบ้านที่อยู่ได้จริง

| โซน | ของ | จำนวน |
|---|---|---|
| ห้องนอน | เตียงเดี่ยว · ตู้เสื้อผ้า | 2 |
| ห้องนั่งเล่น | โซฟา · โต๊ะกลาง · ชั้นหนังสือ | 3 |
| ครัว | โต๊ะกินข้าว · เก้าอี้ · **เตา** | 3 |
| ห้องน้ำ | โถส้วม | 1 |
| นอกบ้าน | รั้ว · ทางเดิน · ต้นไม้ | 3 |

> ✅ **ปรับตามคำสั่ง 2026-08-07 (รอบแรก)**: ห้องนอนเหลือแค่เตียง+ตู้ (ตัดโคมไฟ+พรม) · ห้องน้ำเหลือโถส้วมอย่างเดียว (ตัดอ่างล้างหน้า ฝักบัว) · นอกบ้านตัดไฟสนาม
> ✅ **ปรับรอบสอง (2026-08-07)**: **เพิ่มเตาในครัวเป็นของมาตรฐาน** (ครัวไม่มีเตาแล้วดูไม่เหมือนครัว) · **เตียงเด็ก → "เตียงเดี่ยว"** ทรงเตียงปกติแต่แคบ 1 ช่อง (id ยังเป็น `crib` ตามเดิม ห้ามเปลี่ยน ผูกกับ save)
> **เจตนา: ให้เริ่มมาน้อยๆ พออยู่ได้** ตู้เย็น/อ่างอาบน้ำ/โคมไฟ/พรม ยังเป็นของที่ต้องเก็บเงินซื้อ ⇒ เด็กมีเป้าหมายตั้งแต่วันแรก

รวม **12 ชิ้น** (ในบ้าน 9 + ของฉากนอกบ้าน 3) ย้าย/หมุน/ลบได้ · ลบแล้วไม่ได้เงินคืน (ของแถม) แต่ยังมีสิทธิ์ หยิบกลับมาวางฟรีได้เสมอ · มีปุ่ม **"จัดบ้านกลับแบบเดิม"** · **บ้านของเด็กที่เล่นอยู่แล้วไม่ถูกจัดใหม่ทับ**

**ผังบ้าน (ย่อเป็น 14×14 เมื่อ 2026-08-07)** — เดิม 20×14 กว้างเกิน เดินข้ามห้องนาน ของดูโล่ง:
นั่งเล่น/นอน หด 4 คอลัมน์ · ครัว/น้ำ หด 2 คอลัมน์ ⇒ นั่งเล่น x0-7 · ครัว x9-13 (กำแพง x=8) · นอน x0-8 · น้ำ x10-13 (กำแพง x=9 — เลื่อนจาก x=10 ให้ห้องน้ำกว้างขึ้นเป็น 4 คอลัมน์ 2026-08-07) · กำแพงนอน z=7
ของชุดเริ่มต้นทุกชิ้น**พิงผนังจริง**เท่านั้น (บ้านทรง dollhouse มีผนังแค่ x=0, z=0 และผนังกั้นห้อง — ด้าน x/z สูงสุดเปิดโล่งให้กล้องมอง)
สีพื้นครัว/ห้องน้ำเปลี่ยนจากฟ้าสด/มินต์สด เป็น **เขียวเซจหม่น / ฟ้าเทาหม่น** (ของเดิมสว่างจ้าตัดกับผนังครีม+พื้นไม้ จนดูไม่เหมือนบ้านหลังเดียวกัน)

## 27. เฟอร์นิเจอร์ 116 → 180 ชิ้น

| หมวด | วันนี้ | เพิ่ม | ตัวอย่าง |
|---|---|---|---|
| เตียง/ห้องนอน | 9 | +6 | เตียงสองชั้น เปลญวน ตู้ลิ้นชัก โต๊ะเครื่องแป้ง |
| ครัว | 9 | +8 | ไมโครเวฟ หม้อหุงข้าว เครื่องปั่น ชั้นเครื่องเทศ |
| ห้องน้ำ | 9 | +5 | อ่างเป็ดยาง ชั้นผ้าเช็ดตัว กระจกดอกไม้ |
| ที่นั่ง | 10 | +6 | บีนแบ็ก เก้าอี้โยก เก้าอี้ไข่แขวน |
| โต๊ะ | 9 | +6 | โต๊ะเรียน โต๊ะวาดรูป โต๊ะกาแฟกลม |
| ของแต่งในบ้าน | 13 | +10 | ตู้ปลา นาฬิกาแขวน โมบายดาว กระดานดำ กล่องของเล่น |
| สวน | 15 | +8 | เรือนกระจกจิ๋ว บ่อบัว กังหันลม รังนก |
| เครื่องเล่น | 12 | +5 | บ้านต้นไม้ แทรมโพลีน สระน้ำเป่าลม |
| ที่นั่งนอกบ้าน | 7 | +4 | เปลผ้าใบ ชุดปิกนิก ม้านั่งชิงช้า |
| ของแต่งนอกบ้าน | 23 | +6 | ตู้ไปรษณีย์ ป้ายชื่อบ้าน ไฟราวประดับ |
| **เครื่องดนตรี (หมวดใหม่)** | — | +11 | ดูข้อ 31 |

รวม **180 ชิ้น** · 10 หมวด → 11 หมวด · ทยอยเพิ่มตามเฟส

## 28. พ่อ แม่ ในบ้าน + เควสต์ครอบครัว

**ตัวละคร** — ใช้ `buildCharacter` เดิม ตัวสูงกว่าเด็ก ~1.25 เท่า อยู่เฉพาะ interior ไม่ออกไปในเมือง
ท่าประจำ: แม่ทำครัว/รดน้ำต้นไม้/นั่งโซฟา · พ่ออ่านหนังสือ/ซ่อมของ/ยืนระเบียง + ฟองข้อความสุ่ม

**✅ ตกลงแล้ว — มี default มาให้ แต่แก้เองได้ทั้งหมด**

- **ชื่อ default**: "คุณพ่อ" / "คุณแม่" · **หน้าตา default**: `H_DEFAULT_PARENT_DAD` / `H_DEFAULT_PARENT_MOM` (ค่าคงที่ใหม่ใน `js/house-map.js` คู่กับ `H_DEFAULT_CHAR` ที่มีอยู่)
- เด็กเปิดบ้านครั้งแรก **เจอพ่อแม่ยืนอยู่แล้วทันที ไม่ต้องตั้งค่าอะไรก่อน** (ไม่มีหน้าจอบังคับสร้างตัวละคร)
- อยากแก้เมื่อไหร่ก็ได้: แตะที่ตัวพ่อ/แม่ → เปิดหน้าเดียวกับแต่งตัวตัวเอง (`openCreator`) **แก้ได้ทั้งชื่อและทุกแถวของ `H_ROWS`**
- เก็บใน house save: `data.parents = {dad:{name, char}, mom:{name, char}}` — ไม่มีข้อมูลนี้ = ใช้ default (เด็กเก่าจึงไม่พัง)

**สุ่มรายวัน** — seeded RNG จาก `childId + วันที่` (แบบเดียวกับ `pickDailyQuests`) ⇒ วันนี้ได้พ่อก็พ่อทั้งวัน รีเฟรชไม่เปลี่ยน อีกคนพูดให้กำลังใจ/บอกใบ้

**เควสต์ 8 แบบ** (ค่าตอบแทน 50–90 🪙 สูงกว่า NPC ทั่วไป เพราะวันละชุดเดียว โควตาแยกจากกระดานและ NPC)

| id | ชื่อ | เล่นยังไง | ฝึก |
|---|---|---|---|
| `chore-tidy` | เก็บของเข้าที่ | ลากของที่วางผิดที่กลับหมวดของมัน | จัดหมวด |
| `shopping-list` | ไปซื้อของให้แม่ | จำรายการ 3–5 อย่าง ไปตลาด ซื้อครบ + เงินทอน | ความจำ+เงิน |
| `cook-help` | ช่วยทำอาหาร | เรียงขั้นตอน 4–5 ขั้นให้ถูกลำดับ | ลำดับเหตุการณ์ |
| `family-time` | กินข้าวพร้อมหน้า | ไปนั่งโต๊ะตอนพ่อแม่รออยู่ | เดินสำรวจ+เวลา |
| `wake-up` | ตื่นให้ตรงเวลา | หมุนเข็มนาฬิกาให้ตรง (รื้อ engine นาฬิกาวิเศษ) | เวลา |
| `laundry-sort` | แยกผ้าซัก | แยกตามสี/ประเภทลงตะกร้า | จัดหมวด+สี |
| `budget-help` | ใช้เงินให้พอ | ซื้อของให้ครบโดยไม่เกินงบ | บวกลบ+วางแผน |
| `pet-remind` | เตือนเรื่องสัตว์ | ให้อาหารตามชนิดที่บอก / พาไปหาหมอ | ความรับผิดชอบ |

**โทน**: เป็น "ขอความช่วยเหลือ" ไม่ใช่ "สั่งให้ทำ" ทำไม่ได้ไม่มีใครโกรธ ไม่มีบ่น ไม่มีดุ จบด้วยขอบคุณ+กอด

## 29. ของแต่งตัวเพิ่ม + ปุ่มเลือกเป็น SVG (แก้ `buildCreatorRows`)

**ปัญหา**: แถว `type:'num'` 7 แถวแสดงเป็นปุ่มตัวเลข `1 2 3 4…` — ขัดหลักการโปรเจคที่ว่าเด็ก 5 ขวบต้องเข้าใจด้วยสายตาโดยไม่ต้องอ่านออก

**แก้**: ทุกปุ่มในแถวรูปทรงวาดเป็น **SVG จำลองของชิ้นนั้นจริง** ผ่านฟังก์ชันใหม่ `outfitIcon(rowKey, i)` สไตล์แบนพาสเทลขอบมนเข้าชุดกับ `H_ROW_ICONS` ที่วาดไว้แล้ว · ปุ่ม "ไม่ใส่" ยังเป็น `✖` · **ชิปสีคงเดิมทั้งหมด**

| แถว | วันนี้ | ใหม่ | ที่เพิ่ม |
|---|---|---|---|
| ทรงผม `H_HAIR_N` | 6 | 12 | ผมเปีย มวยผม ผมหยิก ผมสั้นแสก หางม้าสูง บ๊อบ |
| ดวงตา `H_EYE_N` | 8 | 12 | ตาง่วง ตาหัวใจ ตากลมโต ตาปิดยิ้ม |
| ลายเสื้อ `H_PATTERN_N` | 10 | 20 | ลายดาว หัวใจ จุด ทางเฉียง สัตว์ ตัวเลข |
| เครื่องหัว `H_HAT_N` | 9 | 18 | หูสัตว์ มงกุฎ หมวกกันน็อก โบว์ ไหมพรม พวงมาลัย |
| แว่นตา `H_GLASS_N` | 7 | 12 | กันแดดหัวใจ ดำน้ำ นักบิน วิทยาศาสตร์ |
| ของสะพาย `H_BAG_N` | 7 | 14 | เป้ลายสัตว์ กระเป๋าคาดอก ถังน้ำ ปีกผีเสื้อ เป้จรวด |
| ของถือ `H_HOLD_N` | 9 | 18 | ไม้กายสิทธิ์ ลูกโป่ง ตาข่ายจับแมลง ไอศกรีม หนังสือ กล้อง |
| **รองเท้าแบบ `H_SHOE_N` (แถวใหม่)** | — | 8 | ผ้าใบ บูท แตะ รองเท้าเต้น บูทกันฝน |

56 → **106 แบบ** (+50 ยังไม่นับสี) · ต้องวาดไอคอน SVG ~106 ชิ้น ⇒ กันเป็นเฟสของตัวเอง

## 30. ตึกแล็บ = แหล่งโจทย์ STEM

ดร.ฟ้า 🔬 (`npc-lab1`) + `npc-lab2` + `npc-lab3` + นักเรียน `npc-stu*` วันนี้พูดได้อย่างเดียว ให้เป็นจุดเดียวในเมืองที่แจกโจทย์วิทย์/คณิตเชิงทดลอง (ดร.ฟ้าให้ยากสุด 60–100 🪙 · นักเรียนหน้าตึกให้เวอร์ชันง่าย)

| id | โจทย์ | สถานะ |
|---|---|---|
| `sink-float` | จม/ลอย — หย่อนของลงน้ำ ทายก่อนแล้วดูผลจริง | ใหม่ |
| `magnet-test` | แม่เหล็กดูดอะไรได้บ้าง | ใหม่ |
| `plant-grow` | เรียง เมล็ด→ต้นกล้า→ดอก→ผล | ใหม่ |
| `states-matter` | แข็ง/เหลว/แก๊ส จัด 3 ช่อง | ใหม่ |
| `animal-habitat` | สัตว์อยู่ถิ่นไหน ทะเล/ป่า/ทะเลทราย/ขั้วโลก | ใหม่ |
| `measure-lab` | ตวงน้ำตามขีด / วัดความยาว | ใหม่ |
| `mix-color-lab` | ผสมสีในหลอดทดลอง | รื้อ engine ผสมสี |
| `circuit-lab` | ต่อวงจรให้ไฟติด | รื้อ engine วงจรไฟฟ้า |
| `balance-lab` | ตาชั่งสมดุล | รื้อ engine ตาชั่ง |
| `shape-build` | ต่อรูปทรงตามแบบ | รื้อ engine แท็งแกรม |

### 30.1 เกม coding ในแล็บ (เพิ่ม 2026-08-07)

แล็บไม่ได้มีแค่วิทย์/คณิต — เป็นจุดเดียวในเมืองที่แจกโจทย์ **เขียนโปรแกรม** ด้วย
ของดีคือ **engine มีครบอยู่แล้ว** ใน `js/games-code.js`: `startCodeGame()` · `robotLevelsFor(cat)` · ชุดด่าน `ROBOT_LEVELS` 12 ชุดครอบ ป.1–ป.6 · รองรับ `codeLoop` (วนซ้ำ) และ `codeCond` (เงื่อนไข) อยู่แล้ว ⇒ **เลือกชุดด่านตามระดับชั้นเด็กได้ทันที แทบไม่ต้องเขียน logic ใหม่**

| id | โจทย์ | สถานะ |
|---|---|---|
| `robot-lab` | พาหุ่นยนต์เดินตามคำสั่งไปถึงเป้า (เลือกชุดด่านตามชั้นเด็ก) | รื้อ engine `mode:'code'` |
| `code-loop-lab` | ใช้คำสั่ง "วนซ้ำ" ให้โปรแกรมสั้นที่สุด | รื้อ engine + `codeLoop:true` |
| `code-if-lab` | "ถ้าเจอกำแพงให้เลี้ยว" — โปรแกรมมีเงื่อนไข | รื้อ engine + `codeCond:true` |
| `code-order` | เรียงบล็อกคำสั่งที่สลับอยู่ให้ถูกลำดับ **ก่อน**กดรัน | ใหม่ |
| `code-debug` | ในชุดคำสั่งมีบรรทัดผิดอยู่ 1 บรรทัด หาให้เจอแล้วแก้ | ใหม่ |

**แบ่งหน้าที่ NPC ในแล็บ** — ดร.ฟ้า `npc-lab1` 🔬 วิทยาศาสตร์ · `npc-lab2` วัด/ตวง/คณิต · `npc-lab3` **หุ่นยนต์กับ coding** · นักเรียนหน้าตึก `npc-stu*` เวอร์ชันง่ายสำหรับเด็กเล็ก

**รวมโจทย์ในแล็บ: 15 แบบ** (STEM 10 + coding 5)

**รวม mechanic ทั้งระบบ**: 23 (ชุดแรก) + 8 (ครอบครัว) + 6 (STEM เขียนใหม่) + 2 (coding เขียนใหม่) + 2 (ดนตรี) = **41 mechanic** × 3 tier = 123 แม่แบบ ⇒ **~3,500 โจทย์**
(อีก 7 แบบในแล็บใช้ engine เดิมที่รื้อในเฟส 5 จึงไม่นับซ้ำ)

## 31. เฟสสุดท้าย — เครื่องดนตรี

หมวดเฟอร์นิเจอร์ใหม่ 🎵 แตะแล้วมีเสียงจริง ใช้ Web Audio ชุดเดิมจาก `js/shared/piano.js` **ไม่ต้องโหลดไฟล์เสียงเพิ่มเลย**

| ระดับ | เครื่อง | แตะแล้ว | ราคา |
|---|---|---|---|
| 1 เสียงเดียว | ฉิ่ง แทมบูริน กรับ ระฆังลม | ดังทันที เด็กเล็กสุดเล่นได้ | 400–600 🪙 |
| 2 เล่นเป็นชุด | กล่องดนตรี ระนาด ขลุ่ย | ทำนองสั้น + ตัวโน้ตลอย | 700–900 🪙 |
| 3 เปิดหน้าเต็ม | เปียโน คีย์บอร์ด กีตาร์ อูคูเลเล่ | หน้าเล่นแบบ "เปียโนของหนู" | 1,000–1,500 🪙 |

**ร้านมีอยู่แล้ว** — `shop-music` "ร้านเครื่องดนตรี" 🎸 (ล็อต 7×4 แทน `home-6` เดิม) + พี่โน้ต `npc-musicshop` พร้อมบท quest "ช่วยพี่ทายเสียงเครื่องดนตรีหน่อยไหม" สร้างไว้แล้วเมื่อ 2026-08-06 ⇒ เฟส 9 แค่เติมสินค้ากับ mechanic ไม่ต้องสร้างร้านใหม่

11 ชิ้น เป็นของแพงที่สุดในเกมโดยตั้งใจ · เควสต์ผูก: `play-along` (ฟังทำนอง 4–6 โน้ตแล้วเล่นตาม) · `find-sound` (ฟังเสียงทายเครื่อง) — ต้องมีเครื่องในบ้านก่อน แต่ไม่ล็อกทางตันเพราะยังมีอีก 37 mechanic

## 32. แผนเฟสฉบับใหม่ — 9 เฟส (แทนข้อ 21) · +เฟส 10 เพิ่มทีหลัง (ข้อ 44)

| เฟส | เนื้อหา | ทำไมลำดับนี้ |
|---|---|---|
| ~~**1**~~ ✅ | ~~เศรษฐกิจ + migration + **ชุดเฟอร์นิเจอร์เริ่มต้น 12 ชิ้น** + ร้านเฟอร์นิเจอร์/แฟชั่น~~ **เสร็จ 2026-08-07** (ดูข้อ 17.5 ว่าทำต่างจากแผนตรงไหน) | ต้องปิดของฟรีก่อน และของเก่าของเด็กต้องไม่หาย |
| **2** | quest engine + `quiz` ครบ 57 NPC + กระดาน 5/วัน + **ประตูเช็คความพร้อม** | หาเงินได้ทั้งเมืองทันที ระบบความยากถูกตั้งแต่วันแรก |
| **3** | สัตว์เลี้ยง: ร้าน · บ้านตามเงื่อนไข · อาหาร · ความหิว · ป่วย · หมอ | ก้อนใหญ่สุด ต้องรอเศรษฐกิจ |
| **4** | **พ่อ แม่ + เควสต์ครอบครัว 8 แบบ** | ผูกกับสัตว์เลี้ยง (`pet-remind`) จึงมาหลังเฟส 3 |
| **5** | mechanic กลุ่ม C — รื้อ engine เดิม 7 แบบ | คุ้มสุดต่อแรง |
| **6** | **แล็บ STEM 10 แบบ + coding 5 แบบ** | 7 ใน 15 ใช้ engine ที่รื้อไว้แล้วในเฟส 5 (รวม `mode:'code'` ที่มีครบอยู่แล้ว) |
| **7** | mechanic กลุ่ม B ที่เหลือ (10) + D จาก IDEA.md (4) | งานเขียนใหม่ล้วน ไม่รีบ |
| **8** | **เฟอร์นิเจอร์ +64 · ของแต่งตัว +50 · ปุ่ม SVG ~106 ไอคอน** | งานวาดล้วน ไม่แตะ logic ทำคู่ขนานได้ |
| **9** | **เครื่องดนตรี 11 ชิ้น + เควสต์ดนตรี 2 แบบ** | ปิดท้ายตามสั่ง ต้องมีเศรษฐกิจถึงหลักพันก่อน |
| **10** 🆕 | **จังหวะเวลา — ระลอกโครงสร้าง** (ข้อ 45) | เฟสถัดไปทั้งหมดพึ่งสูตรทรงเดิน + `secPerQ` · แตะแค่ `house-quests.js` เสี่ยงต่ำสุด · เด็กได้ประโยชน์ทันทีโดยไม่มีใครถูกเพิ่มงาน |
| **11** 🆕 | **มินิเกมกลุ่ม A — เล่นในเมือง 3D 5 เกม** + เปิดตาราง `qN` ใหม่ตอนท้าย (ข้อ 44 + 45.4) | กลุ่มที่แปลกใหม่/คุ้มแรงที่สุด · A1 พิสูจน์สูตรทรงเดินก่อนลงทุนอีก 13 เกม · มีเกมใหม่แล้วค่อยเพิ่มจำนวนข้อ เด็กจะไม่จำเจ |
| **12** 🆕 | **มินิเกมกลุ่ม C — สัตว์เลี้ยง 3 เกม** (ข้อ 44) | ปิดช่องว่างใหญ่สุดที่เหลือ — สัตว์เลี้ยงตอนนี้มีแต่ "หิว/ป่วย" ไม่มีอะไรสนุกเลย |
| **13** 🆕 | **มินิเกมกลุ่ม B + D — 6 เกม** (ข้อ 44) | ต้นทุนต่ำสุด ใช้ทรงการ์ดที่มีอยู่แล้ว ทยอยเติมได้ไม่รีบ |
| **14** 💤 | **ระบบฤดูกาล** (ข้อ 46) | **ยังไม่ตัดสินใจทำ ต้องกลับมาคุยก่อน** · ก้อนใหญ่สุด 13-20 วัน และพึ่งเกม A2 ของเฟส 11 ให้มีความหมาย ⇒ ท้ายสุด |

⚠️ **ขอบเขตโตขึ้น**: 6 → 9 เฟส · mechanic 23 → 41 · เฟอร์นิเจอร์ 116 → 180 · ของแต่งตัว 56 → 106 + ไอคอน SVG วาดมือ ~106 ชิ้น ⇒ **เฟส 8 คือก้อนที่บวมที่สุด** ถ้าอยากได้ระบบเล่นได้เร็วแนะนำแบ่งย่อยเป็นชุดละ 20 ไอคอน

## 32.5 ⚠️ สิ่งที่เฟส 2 ทำต่างจากแผน (ตั้งใจ — อ่านก่อนทำเฟส 3+)

| หัวข้อ | แผนเดิม | ที่ทำจริง (2026-08-08) | เหตุผล |
|---|---|---|---|
| กลไกที่ส่ง | `quiz` + `count-things` | `quiz` + `count` (id สั้นลง) | ชื่อ id ใช้ในคีย์ save จึงเลือกสั้นไว้ก่อน |
| `count-things` | นับของ **ในฉาก 3D จริง** ตรงหน้า NPC | นับ **แถวอิโมจิตามธีมร้าน** ในการ์ด | นับของในฉาก 3D ต้องเขียนระบบชี้/นับ object ใหม่ทั้งชุด ไม่คุ้มในเฟสนี้ · แถวอิโมจิให้โจทย์ไม่ซ้ำได้เท่ากันและอ่านง่ายกว่าบนมือถือ (ธีมของยังผูกกับร้านจริงผ่าน `ITEM_SETS` + `NPC_THEMES`) |
| ตัวคูณรางวัล | `base` A=5 · B=10 · C=6 | `base` **A=6 · B=8 · C=7 · board=10** · โบนัส 35 · เพดาน 400/วัน | ชุดแรกที่ส่งใช้ A=18/board=28/โบนัส 100 เพื่อให้ตรงช่วง 20-50 ของข้อ 19 แต่พอคูณโควตา 13 เควสต์/วันแล้ว **เงินเฟ้อ** (600 🪙/วัน ซื้อของครบทั้งเกมใน 27 วัน) ⇒ ผู้ใช้สั่งลด 2026-08-08 · ตัวเลขชุดนี้ทับข้อ 19 (ตารางเต็มในข้อ 6) |
| `mulTier` | ป.1 = 1.0 → ป.6 = 1.5 | ป.1 = 1.0 → ป.6 = **1.4** (`1 + (tier-1)×0.08`) | ช่องว่างระหว่างชั้นแคบลง เด็กเล็กจะได้ไม่รู้สึกว่าหาเงินช้ากว่าพี่มาก |
| ประตูความพร้อม | ผสมโจทย์ท้าทาย 20% | **25%** | 20% ทำให้เด็กที่เพิ่งเปิดประตูแทบไม่เจอเลย เสียความรู้สึกว่า "ปลดล็อกแล้วได้อะไร" |
| กระดานเควสต์ | สุ่มจากทั้ง 23 กลไก | สุ่มจาก 2 กลไกที่มีในเฟสนี้ | เฟส 5-7 ค่อยเติม — เติมที่ตาราง `MECHS` ใน `js/house-quests.js` ที่เดียว `js/house.js` ไม่ต้องแก้ |
| ภารกิจเดินสำรวจเดิม | — | `QUEST_POOL` + `questEvent()` + `QUEST_ENABLED=false` **ยังอยู่ครบแต่ปิดไว้** | เฟส 7 (`deliver`/`escort`/งานเดินโลก) จะรื้อกลับมาใช้ ไม่ต้องเขียนระบบนับความคืบหน้าใหม่ |
| เพดานเหรียญ/วัน | ต้องมี | `DAY_CAP = 400` | ป.6 เล่นครบทุกงานแบบไม่พลาดเลย ~311 (+โจทย์ท้าทายอีกนิด) ⇒ ตั้ง 400 ให้เด็กขยันไม่ชนเพดาน แต่ยังกันฟาร์มได้ |

### 32.6 ⚠️ ชนิดโจทย์ในคลัง `CATS` ที่หน้าจอเควสต์ต้องรองรับให้ครบ

หน้าจอเควสต์ดึงคำถามจากคลังเดียวกับหน้าหลัก แต่ **หน้าหลักกับโหมดบ้านวาดคนละที่**
(`renderQuestion()` ใน `js/game-quiz.js` กับ `renderQuestStep()` ใน `js/house.js`)
⇒ เพิ่มชนิดโจทย์ใหม่ในคลังเมื่อไหร่ **ต้องแก้ทั้ง 2 ที่เสมอ** ไม่งั้นเด็กเจอการ์ดเปล่าในโหมดบ้าน

| ฟิลด์ | ใช้ที่ไหน | วาดยังไง |
|---|---|---|
| `q.img` | **หมวดเชาว์ `iq1`-`iq4` ทั้ง 60 ข้อ** (ระดับเตรียม ป.1) · `q.q` เป็นค่าว่าง รูปคือโจทย์ทั้งหมด | `<img class="hqz-img">` + ข้อความแทน "ดูรูปแล้วเลือกข้อที่ถูกต้อง" · ตัวเลือกเป็น ก/ข/ค **ห้ามสลับตำแหน่ง** เพราะอ้างถึงช่องในรูป |
| `q.pattern` | หมวดเติมแพทเทิร์น (30 ข้อ) | แถวการ์ด `.pattern-row`/`.pat-tile` + ช่อง `?` (ใช้ class ชุดเดียวกับหน้าหลัก) · ตัวเลือกเป็นอิโมจิต้องขยายฟอนต์ |
| `q.emoji` | โจทย์ทั่วไปส่วนใหญ่ | ตัวใหญ่กลางการ์ด + เติม VS16 ให้สัญลักษณ์อย่าง ✖ ➕ วาดเป็นอิโมจิสี ไม่ใช่ตัวอักษรดำ |
| (ไม่มีเลย) | โจทย์ข้อความล้วน | โชว์แค่ `q.q` |

🐞 **บั๊กจริงที่เจอ 2026-08-08**: รอบแรกส่งเฉพาะ `emoji` ⇒ เด็กระดับเตรียม ป.1 ที่เจอโจทย์หมวดเชาว์
เห็นการ์ดเปล่าๆ มีแต่ปุ่ม ก/ข/ค ตอบไม่ได้เลย · แก้แล้ว + มีเทสคุมไว้ใน `tests/house-quests.spec.js`

### 32.7 📚 หน้า "คลังคำถาม" — เครื่องมือเทสของโหมดบ้าน (เพิ่ม 2026-08-08)

โจทย์ในโหมดบ้าน**สุ่มทั้งหมด** (seeded รายวัน) ⇒ เล่นเองยังไงก็ไม่มีทางรู้ว่าคลังมีอะไรบ้าง
และข้อไหนวาดออกมาแล้วเพี้ยน (แบบบั๊กโจทย์ภาพในข้อ 32.6) จึงทำหน้ากางคลังเป็นตารางไว้เทส

| หัวข้อ | รายละเอียด |
|---|---|
| เข้ายังไง | ปุ่ม 📚 "คลังคำถาม" (`#house-qb-btn`) ในเมนูเฟือง `#house-ctrl-list` |
| แบ่งยังไง | **ระดับชั้น (7) × กลไก (2)** + แถบกรองหมวดสำหรับกลไก `quiz` |
| ตาราง `quiz` | ทุกข้อในคลัง `CATS` ของชั้นนั้น — หมวด · วิชา · **ชนิดโจทย์ (img/pattern/emoji/ข้อความ)** · โจทย์ · เฉลย |
| ตาราง `count` | 16 ธีมของ — ชุดอิโมจิ · **NPC ที่ใช้ธีมนั้นจริง** · ตัวอย่างโจทย์ที่สุ่มได้ |
| กดเล่น | ปุ่ม ▶ ท้ายแถว = เล่นข้อนั้นข้อเดียว · ปุ่มล่าง = สุ่มชุดเต็มเหมือนเควสต์จริง · ใช้เส้นทางวาด/ตอบ **ของจริงทุกบรรทัด** |
| ⚠️ ข้อห้าม | **เล่นในหน้านี้ห้ามแตะข้อมูลเด็ก** — ไม่ได้เหรียญ ไม่กินโควตา ไม่บวกดาว ไม่บันทึกสถิติประตูความพร้อม (`spec.test:true` → `finishTestQuest()`) |
| 🔒 สวิตช์ | `const QB_ENABLED` ที่ต้นไฟล์ `js/house-qbrowse.js` (`QB_FEATURE_OFF`) — **เปิดไว้บน branch เพื่อเทสตลอดทั้ง 9 เฟส · ตั้งเป็น `false` ทุกครั้งที่ merge ขึ้น `main`/deploy** (ผู้ใช้สั่งไว้ 2026-08-08 ไม่ต้องถามซ้ำ) ปิดที่เดียวจบ เทสจะ skip ตัวเองอัตโนมัติ |
| ไฟล์ | `js/house-qbrowse.js` (UI) + API อ่านอย่างเดียวใน `js/house-quests.js` + `window.HouseQuestUI.playTest()` ใน `js/house.js` |
| เทส | `tests/house-qbrowse.spec.js` (6 เทส × 2 จอ) — คุมทั้งตัวเลขในตาราง ชนิดโจทย์ครบ 4 แบบ และ "ไม่แตะ state" |

📌 **เพิ่ม mechanic ใหม่ในเฟส 5-7 แล้วต้องเติมแท็บที่ `MECH_TABS` ใน `js/house-qbrowse.js` ด้วย** (ที่เดียว)

**สิ่งที่เฟส 2 ทิ้งไว้ให้เฟสถัดไป**
- `count` ยังไม่มีตัวช่วย (`diff.hints`) สำหรับ ป.1-2 — ค่าถูกคำนวณไว้แล้วแต่ยังไม่มีใครใช้
- ยังไม่มี "สมุดงานวันนี้ 📔" ในเมนูเฟือง (ข้อ 10) — วันนี้ต้องเดินหาป้าย "!" เอง
- เควสต์ยังไม่ผูกกับพ่อแม่ (เฟส 4) และยังไม่มีเควสต์ตระกูล B/C

---

## 33. สถานะการตัดสินใจ — ปิดครบแล้ว

- [x] ราคา / ค่าตอบแทน — ใช้ตัวเลขข้อ 17 + 19
- [x] จำนวนสัตว์เลี้ยง — 1 ตัว ซื้อซ้ำจ่ายครึ่งเดียว
- [x] ความหิว — ลดตามวันที่เข้าเล่น (−35/วันเล่น)
- [x] ของแถมฟรีตอนเริ่ม — ชุดมาตรฐาน **12 ชิ้นตามข้อ 26** วางในบ้านให้เลย (ปรับ 2 รอบแล้ว 2026-08-07)
- [x] ชื่อ/หน้าตาพ่อแม่ — **มี default มาให้ แต่เด็กแก้ชื่อ+แต่งตัวใหม่เองได้ทั้งหมด** (ข้อ 28)
- [x] **เฟส 1 — เสร็จแล้ว 2026-08-07** (`js/house-shop.js` + จุดเชื่อมใน `js/house.js`/`js/house-map.js`/`index.html`/`css/style.css` + เทส `tests/house-shop.spec.js` 5 เทส)
- [x] **เฟส 2 — เสร็จแล้ว 2026-08-08** (`js/house-quests.js` + จุดต่อใน `js/house.js`/`index.html`/`css/style.css` + เทส `tests/house-quests.spec.js` 10 เทส × 2 จอ) — ดูข้อ 32.5 ว่าทำต่างจากแผนตรงไหน
- [x] **เฟส 3A — เสร็จแล้ว 2026-08-08** (ร้านสัตว์เลี้ยง + ซื้อสัตว์/สีขน + migration ของเก่า + บ้านสัตว์ตามเงื่อนไข · เทส `tests/house-pets.spec.js` 6 เทส × 2 จอ) — ดูข้อ 35 ว่าทำต่างจากแผนตรงไหน
- [x] **เฟส 3B — เสร็จแล้ว 2026-08-09**
- [x] **เฟส 4A — เสร็จแล้ว 2026-08-09** (พ่อ-แม่ในบ้าน + แก้ชื่อ/หน้าตา + เควสต์ครอบครัว 1 ชุด/วัน ใช้กลไก quiz/count เดิม) — `js/house-family.js` · เทส `tests/house-family.spec.js` 6 เทส × 2 จอ · ดูข้อ 38
- [ ] **เฟส 4B** (มินิเกมครอบครัว 8 แบบตามตารางข้อ 28 — เก็บของ/ซื้อของ/ทำอาหาร/นาฬิกา/แยกผ้า/งบประมาณ ฯลฯ) (อาหารสัตว์ · ความหิว · ป่วย · หมอ) — `js/house-pet-care.js` + แท็บอาหารในร้านสัตว์เลี้ยง + แผงให้อาหาร/การ์ดคุณหมอใน `js/house.js` · เทส `tests/house-pet-care.spec.js` 11 เทส × 2 จอ — ดูข้อ 37 ว่าทำต่างจากแผนตรงไหน

## 34. สิ่งที่เฟส 1 ทิ้งไว้ให้เฟสถัดไป (เช็คลิสต์)

- [x] **เฟส 2**: เควสต์จ่ายเหรียญผ่าน `window.OwlCoins.add()` ที่ `awardCoins()` ใน `js/house.js` **จุดเดียว** ✅
- [x] **เสร็จแล้ว 2026-08-08**: ย้ายหมวดของนอกบ้านจากห้างเฟอร์นิเจอร์ไปร้านต้นไม้/ร้านของเล่นตามข้อ 17.4 — สร้างล็อต `shop-garden` + `shop-toy` ใหม่ทั้งคู่ พร้อมตัวอาคาร/NPC/ทางเข้า (ดูข้อ 36)
- [x] **เฟส 3A**: migration ส่วนสัตว์เลี้ยง (ข้อ 17.3) + ย้ายจุด seed `pet-house` ไปตอนรับเลี้ยง ✅
- [ ] **เฟส 8**: ปุ่มเลือกชุดในร้านแฟชั่นยังเป็น "เลข + ชื่อไทย" อยู่ (`FIT_NAMES` ใน `js/house-shop.js`) — ตอนวาดไอคอน SVG 106 ชิ้นให้มาเปลี่ยนที่ `makeCard()` ด้วย

---

## 35. ⚠️ สิ่งที่เฟส 3A ทำต่างจากแผน (ตั้งใจ — อ่านก่อนทำ 3B)

| หัวข้อ | แผนเดิม | ที่ทำจริง (2026-08-08) | เหตุผล |
|---|---|---|---|
| ซื้อสัตว์ซ้ำ | "ซื้อซ้ำภายหลังจ่ายครึ่งเดียว" (ข้อ 23) | **เคยซื้อแล้ว = ของถาวร รับกลับมาเลี้ยงใหม่ฟรี** | สิทธิ์เก็บใน `data.unlocked` ถาวรเหมือนเฟอร์นิเจอร์/ชุดแต่งตัวอยู่แล้ว "ฟรี" จึงทั้งง่ายกว่าและตรงเจตนาเดิมกว่า (= กันเด็กเสียดายจนไม่กล้าลองสัตว์ตัวอื่น) · เลี้ยงได้ทีละ 1 ตัวเหมือนเดิม ไม่ใช่ช่องโหว่ |
| แจ้งเตือนเด็กใหม่ | เปิดหน้าเลือกสัตว์ให้อัตโนมัติหลังสร้างตัวละคร | เปลี่ยนเป็น **toast บอกทางไปร้านสัตว์เลี้ยง** | สัตว์ต้องซื้อแล้ว เด็กใหม่มี 0 เหรียญ ถ้าเด้งหน้าที่ล็อกทั้งหน้าให้ดู = เจอทางตันตั้งแต่นาทีแรก (ผิดกติกาเหล็กข้อ 1) |
| บ้านสัตว์ของเด็กเก่า | — | **ไม่แตะ** บ้านสัตว์ที่ seed ไว้แล้ว แม้เด็กคนนั้นจะยังไม่มีสัตว์ | กติกาเหล็กข้อ 3 มาก่อน — ลบของที่อยู่ในสนามเขาแล้วถือว่า "ทำข้อมูลเก่าหาย" · กฎ "ไม่มีสัตว์ = ไม่มีบ้าน" บังคับใช้กับ**เด็กใหม่และการปล่อยสัตว์คืนตั้งแต่นี้ไป**เท่านั้น |
| สีขนในร้าน | ขายทุกสีของทุกชนิด | โชว์**เฉพาะสีของสัตว์ที่มีแล้ว** (ซื้อสีของสัตว์ที่ยังไม่มีไม่ได้) | ซื้อไปก็ใช้ไม่ได้ + แท็บจะยาว 40 กว่าใบตั้งแต่ยังไม่มีสัตว์สักตัว |
| แท็บในร้านสัตว์ | — | 4 แท็บตามกลุ่มราคา (เริ่มต้น/กลาง/พิเศษ/หายาก) | ราคาเดียวกันอยู่ด้วยกัน เด็กเทียบง่าย และแท็บไม่ยาวจนต้องเลื่อนหา (กติกาข้อ 17.4) |
| บ้านสัตว์ในโหมดตกแต่ง | "ย้ายได้ ลบเองไม่ได้" | ทำตามแผน — กดลบตอนมีสัตว์อยู่จะขึ้น toast บอกว่าย้ายได้แต่ลบไม่ได้ | ลบทิ้งแล้วเพื่อนตัวน้อยไม่มีที่นอน และหยิบกลับมาวางเองไม่ได้ |

**สิ่งที่ 3A ทิ้งไว้ให้ 3B** — ✅ **ทำครบแล้ว 2026-08-09 (ดูข้อ 37)**
- ~~อาหารสัตว์ (`data.petFood`) · ความหิว · ป่วย · หมอ~~ ✅
- ~~แท็บ "อาหาร" ในร้านสัตว์เลี้ยง (ตารางข้อ 18.2)~~ ✅
- NPC `npc-pet` (พี่ปุย) ยังไม่มีบทพูดเกี่ยวกับร้าน — ยังค้าง (เอาไว้เฟส 4 ตอนแตะบทพูด NPC รอบใหญ่)

---

## 36. 🏙️ รื้อผังเมืองย่านห้าง + เปิดร้านต้นไม้/ร้านของเล่น (2026-08-08 · ตามคำสั่งผู้ใช้)

งานนี้ไม่ได้อยู่ในแผน 9 เฟส — ผู้ใช้สั่งระหว่างทำเฟส 3A เพื่อปิดงานค้างข้อ 17.4 (ของนอกบ้านต้องมีร้านของตัวเอง)

| ย้าย/เพิ่ม | จาก | เป็น |
|---|---|---|
| 🏬 แฟชั่นมอลล์ | x5-10 / z48-52 (ข้างตึกแล็บ) | **x0-4 / z44-52 · `face:'x'`** ต่อแถวเดียวกับเฟอร์นิเจอร์มอลล์ **หันหน้าทางเดียวกัน** ลานหน้าห้างเป็นคอลัมน์ x5 ยาวตลอด เว้น z53 คั่น 2 หลัง |
| 🌻 ทุ่งทานตะวันผืนใหญ่ | x0-4 / z43-52 | **เอาออก** (แฟชั่นมอลล์มาแทนที่) — ผืนเล็กอีก 3 ผืนทั่วเมืองยังอยู่ |
| 🪚 กระท่อมช่างไม้ | x1-4 / z36-38 | **x1-4 / z33-35** (ตะวันออก 4 → ถอย 2 → ถอยอีก 1 = สุทธิ −3) · `CARPENTER_PROPS`/`YARD`/`ROAM` เลื่อนตามทั้งชุดทุกครั้ง |
| 🌷 ร้านต้นไม้ (ใหม่) | — | **x22-25 / z47-50** ลานหญ้าหน้าโรงเรียน (ย้ายจาก x1-4/z37-40 เมื่อ 08-09) · `buildGardenShop()` = เรือนกระจกเพาะชำ (โครงเขียว ผนัง/หลังคากระจก ซุ้มไม้เลื้อย ชั้นวางกระถาง) · NPC **ป้าใบเตย** |
| 🎠 ร้านของเล่น (ใหม่) | — | **x6-10 / z39-43** (ย้ายจาก z48-52 เมื่อ 08-09) · `buildToyShop()` = ตึกหลังคาแบนสีฟ้า กันสาดสีรุ้ง ตัวต่อยักษ์+หมีบนดาดฟ้า ลูกโป่ง ม้าโยก · NPC **พี่ตุ๊กตา** |
| 🎈 ลานทางเดินใหม่ + โบโซ่ | — | **x5-10 / z44-53** ปูพื้นเป็นลานเดียวต่อกับลานหน้าห้าง 2 หลัง · โบโซ่ + รถเข็นลูกโป่งย้ายจาก (23,51) มาตั้ง **กลางลาน (8-9, 48)** · `roam` ปรับให้อยู่ในกรอบลานพอดี |

**หมวดสินค้าที่ย้าย** (`SHOPS[].groups` ใน `js/house-shop.js` — จุดเดียว):
`mall-furniture` เหลือของในบ้าน 6 หมวด · `shop-garden` ได้ `garden`/`seatout`/`decorout` · `shop-toy` ได้ `play`
⇒ **ของที่เด็กซื้อไปแล้วไม่หาย** (สิทธิ์เก็บถาวรใน `data.unlocked` ย้ายแค่ที่ขาย ไม่ต้อง migrate)

⚠️ **กติกาที่เทสคุมไว้** (`tests/house-shop.spec.js` 3 เทสใหม่): **ทุกหมวดต้องมีร้านขายเสมอ** ห้ามมีหมวดตกหล่น
ไม่มีร้านไหนขาย (= dead end ผิดกติกาเหล็กข้อ 1) · ล็อตห้ามทับกัน · ช่องหน้าประตูทุกล็อตต้องเดินได้จริง

📌 **บทเรียน**: ก่อนขยับล็อตทุกครั้งให้ **ดัมพ์กริดเดินได้จริงจาก engine** (`window.__houseDbg.grid()`)
มาดูเป็น ASCII ก่อน — คราวนี้เจอว่าคำสั่ง "ขยับ 5 ช่อง" จะไปทับลานแคมป์พอดี ถ้าเดาเอาคงพลาด

⚠️ **กับดัก 2 อย่างในไฟล์แผนที่ (เสียเวลาไปแล้วทั้งคู่)**
1. **`VILLAGE2_ROADS` ถูก `.map(s2Rect)` = บวก `EPAD2` (10) บนแกน z** ⇒ เขียนพิกัดถนน/ลานต้อง **ลบ 10 ก่อนเสมอ**
   (ลานจริง z43-53 → เขียน `z0:33, z1:43`) · แต่ `VILLAGE_LOTS` ท่อน `.concat([...])` ท้ายไฟล์ **เป็นพิกัดจริงตรงๆ ไม่ต้องลบ**
2. **ห้ามรันชุดเทสขณะกำลังแก้ไฟล์ในโปรเจค** — dev server เสิร์ฟไฟล์สด ไฟล์ที่ syntax พังชั่วขณะจะทำให้เทสแดง
   ยกแผง (เจอ 7 เทสแดงแบบนี้เมื่อ 2026-08-09 ทั้งที่โค้ดไม่ได้ผิด) รอให้แก้เสร็จก่อนแล้วค่อยรัน

### 36.1 ปรับละเอียดรอบ 2026-08-09 (คำสั่งผู้ใช้)

- ร้านของเล่น **เดินหน้า (+z) 1 ช่อง** → z39-43 · ลานทางเดินหดเหลือ **z44-53** ตามไปด้วย (ไม่งั้นล็อตทับพื้นถนน)
- กระท่อมช่างไม้ **ถอยหลัง (−z) 1 ช่อง** → z33-35 · ร้านต้นไม้ **ถอยหลัง 1 ช่อง** → z47-50
- **ย่อกรอบบริเวณบ้านเด็ก (`HOME_ZONE`)**: จริง x12-27/z26-45 → **x13-27 / z27-41**
  (ตัดแถว x12 · แถว z26 · ขอบท้าย z45 → z41) เขียนเป็น `{x0:sx(1), x1:RIVER_X[0]-1, z0:sz(1), z1:sz(15)}`
  - ⚠ ต้องเพิ่มตัวกรอง `inHomeZone` ที่ `TREES.filter(...)` ใน `seedWorldDecor()` ด้วย — ต้นไม้ seed
    บางต้นตกนอกกรอบใหม่ ถ้าไม่กรอง เด็กจะเจอต้นไม้ที่ย้าย/ลบไม่ได้เพราะอยู่นอกกรอบตกแต่ง
  - ✅ ตรวจแล้ว: ของที่ seed ให้เด็กใหม่ **46 ชิ้น อยู่ในกรอบครบทุกชิ้น** · สนามบ้าน/รั้ว/ทางเดิน/บ้านสัตว์ยังอยู่ในกรอบ
  - แถบที่ตัดออกกลายเป็นป่าปกติ (ต้นไม้ป่างอกทับได้ เพราะ `wildPlantable` กันเฉพาะในบริเวณบ้าน)

### 36.2 ⚡ เฟรมเรตตกที่ตลาด — วัดแล้วเจอต้นเหตุ (2026-08-09)

ผู้ใช้แจ้งว่าเฟรมเรตตกตรงตลาดรถเข็นหน้าโรงเรียน · เครื่องพัฒนา (Mac M5) วิ่ง 60fps เต็มทุกจุด
วัดปัญหาตรงๆ ไม่ได้ จึงวัด **ต้นทุนการวาดต่อเฟรม** แทน (เทียบข้ามเครื่องได้):

| จุด | draw calls | triangles |
|---|---|---|
| บ้านเด็ก | 112 | 365k |
| ลานทางเดินใหม่ | 265 | 355k |
| โรงเรียน | 219 | 474k |
| **ตลาด** | **511-551** | 296-358k |

⇒ **สามเหลี่ยมพอๆ กันทุกจุด แต่ตลาดมี draw call มากกว่า 4-5 เท่า** — บน iPad/แท็บเล็ต draw call
คือตัวที่กินแรงที่สุด ตรงกับอาการที่ผู้ใช้เจอเป๊ะ

**ต้นเหตุ**: `NPC 1 คน ≈ 27 draw call` — คอมเมนต์เดิมในโค้ด (`js/house.js` หัวข้อ "ชาวบ้าน (NPC)")
เขียนว่า "ทุกคน mergeDecorGroup → คนละ 1 draw call" **ซึ่งไม่จริงแล้ว** เพราะ `mergeDecorGroup()`
ยกเลิกการรวมเมื่อเจอ `userData` marker บนลูก (ข้อต่อแขน/ขา) ⇒ NPC ไม่เคยถูกรวมเลย
ตลาดมีแม่ค้ายืนติดกัน 12 คนในเฟรมเดียว = ~324 calls จากคนอย่างเดียว

**ที่ทำ (ตามที่ผู้ใช้อนุมัติไว้)**: ลดรถเข็นจาก **4 คัน/แถว → 3 คัน/แถว** (12 → 9 คัน)
พร้อมตัดแม่ค้าประจำคันออก 3 คน (`npc-mk-popcorn` / `snack` / `ice`) · คอลัมน์ใหม่ x = 10,13,16
(ห่างกัน 3 ช่องเท่ากันทุกคู่) · **ห้ามเพิ่มกลับเป็น 4 คัน/แถวโดยไม่ถามผู้ใช้ก่อน**

### 36.3 ✅ แก้ต้นเหตุจริงแล้ว — บั๊กค้างมานาน: NPC ไม่เคยถูก merge เลย (2026-08-09)

พอไล่ต่อว่าทำไม NPC ถึงเป็น ~27 draw call ทั้งที่โค้ดสั่ง merge ไว้แล้ว เจอว่า:

- `hairShell()` / `addHatHair()` **จดค่าชั่วคราว `_hairTop` / `_hairW` ไว้บน `head.userData`**
  ให้หน้าม้ากับหมวกอ้างอิงตอนประกอบร่าง แล้ว **ไม่ได้ล้างทิ้ง**
- `mergeDecorGroup()` มีกติกาว่า **เจอ `userData` บนลูกตัวไหนก็ยกเลิกการรวมทั้งก้อน** (กันไปโดนบานพับ/หลอดไฟ/จุดหมุน)
- ⇒ **NPC ทุกคนที่มีผม (คือเกือบทั้งเมือง) ไม่เคยถูก merge เลยสักคน** ทั้งที่คอมเมนต์ในโค้ดเขียนว่า
  "ทุกคน mergeDecorGroup → คนละ 1 draw call"

**วิธีแก้**: เพิ่ม `clearBuildScratch(core)` ใน `buildVillager()` ก่อนเรียก merge
(⚠️ ต้องล้างที่ `core` ไม่ใช่ `g` — ตรงจุดนั้น `core` ยังไม่ถูก add เข้า `g` traverse จะไปไม่ถึงหัว)

**ผลที่วัดได้ (median 60 เฟรม)**

| จุด | ก่อน | หลัง |
|---|---|---|
| ตลาดกลาง | 551 | **113** |
| ตลาดใต้ | 511 | **101** |
| โรงเรียน | 219 | **103** |
| บ้านเด็ก | 112 | 112 |

- แม่ค้ายืนประจำที่: **27-42 mesh/คน → 1 mesh/คน** · คนเดินได้: **→ 5 mesh/คน** (ตัว + แขน 2 + ขา 2 ตามที่ออกแบบไว้)
- `geometries` ทั้งฉาก: 1,279 → **372**
- ⇒ **เร็วขึ้นทั้งแผนที่ ไม่ใช่แค่ตลาด** และหน้าตาตัวละครไม่เปลี่ยนเลย (ตรวจด้วยภาพจริงแล้ว)

📌 **กติกาใหม่**: ถ้าเพิ่ม "ค่าชั่วคราวตอนสร้าง" ลง `userData` ของชิ้นส่วนตัวละครอีก **ต้องมาล้างที่
`clearBuildScratch()` ด้วยเสมอ** ไม่งั้นบั๊กนี้จะกลับมาเงียบๆ (ไม่มีอะไรพัง แค่ช้าลงเฉยๆ จับได้ยากมาก)

⚠️ **การลดรถเข็นเหลือ 3 คัน/แถวยังคงไว้ตามคำสั่งผู้ใช้** ถึงแม้ตอนนี้จูนจนไม่จำเป็นแล้วก็ตาม —
ถ้าจะเอา 4 คัน/แถวกลับ ต้องให้ผู้ใช้สั่งเอง

---

## 37. ⚠️ สิ่งที่เฟส 3B ทำต่างจากแผน (ตั้งใจ — อ่านก่อนทำเฟส 4)

ทำเสร็จ 2026-08-09 · ไฟล์ใหม่ `js/house-pet-care.js` (ตรรกะล้วน ไม่แตะ DOM/WebGL เหมือน `house-quests.js`)
เปิดออกมาที่ `window.HousePetCare` · หน้าจอ (แผงให้อาหาร/การ์ดคุณหมอ/แถบหัวใจ) อยู่ใน `js/house.js`

| หัวข้อ | แผนเดิม (ข้อ 18) | ที่ทำจริง | เหตุผล |
|---|---|---|---|
| จุดเข้า "ให้อาหาร" | แตะชามหน้าบ้านสัตว์ | **แถบสถานะ `#house-pet-bar` ใต้ชื่อเด็ก** — ชื่อสัตว์ · ไอคอนอาหารที่กิน · หลอดความอิ่ม · ปุ่มให้อาหาร (โผล่เฉพาะตอนมีสัตว์) | ชาม 3D ต้องผูกกับโมเดลบ้านสัตว์ที่เด็กลากย้ายเองได้ และเด็กต้องเดินไปให้ถึงก่อนถึงจะป้อนได้ · แถบบน HUD กดได้จากทุกที่ หาเจอแน่นอน **และบอกสถานะน้องตลอดเวลาโดยไม่ต้องเปิดอะไรเลย** (ผู้ใช้สั่งเพิ่มแถบนี้ 2026-08-09 · แผนข้อ 18.2 เองก็เปิดทางเลือก "แตะตัวสัตว์ → เมนูให้อาหาร" ไว้แล้ว) |
| แถบความอิ่ม | หัวใจเต็ม/ครึ่ง/ว่าง เหนือหัวสัตว์ | **ทำทั้งคู่**: หัวใจเหนือหัว (เมื่อ < 50) + **หลอดสีในแถบ HUD ตลอดเวลา** (เขียว → ส้ม < 50 → แดงอ่อน < 25 → เทาตอนป่วย) | หัวใจเหนือหัวเห็นเฉพาะตอนกล้องหันไปทางน้อง · หลอดในแถบเห็นตลอด เด็กเช็คได้ทันทีโดยไม่ต้องเดินไปหา |
| สีขนในร้านสัตว์ | — (เฟส 3A กางสีของสัตว์ทุกตัวที่มีเป็น section ต่อกัน) | **แตะเลือกสัตว์ตัวไหน สีของตัวนั้นถึงโผล่** — section เดียวเสมอ | มีสัตว์หลายตัวแล้วแท็บยาวขึ้นเรื่อยๆ เด็กต้องเลื่อนหาว่าสีไหนของใคร (ผู้ใช้สั่งแก้ 2026-08-09) |
| การ์ดอาหารในร้าน | — | **บอกเสมอว่าเป็นของสัตว์ตัวไหน** (บรรทัดรอง `.hs-sub` สัตว์ละบรรทัด + เหลือกี่มื้อ) | นี่คือตัวสอนหลักของเฟส 3B (เด็กต้องรู้ว่าสัตว์ตัวไหนกินอะไร) ถ้าไม่บอกก็เหลือแค่เดาสุ่ม (ผู้ใช้สั่งเพิ่ม 2026-08-09) |
| ความอิ่มลดในวันเดียวกัน | −5 ต่อ 20 นาทีในเกม | **ไม่ทำ** — ลดเฉพาะตอนขึ้นวันเล่นใหม่ (−35) | เล่นยาววันเดียวแล้วน้องป่วยได้ = ลงโทษเด็กที่ตั้งใจเล่น (ผิดกติกาเหล็กข้อ 2) · และทำให้ "ป่วย" มีต้นทางเดียวคือตัวนับวัน ตรวจสอบ/เทสได้แน่นอน |
| ตัวตัดสินว่าป่วย | อิ่ม 0 แล้วเข้าเล่นวันถัดไปอีกวัน | ตัวนับ `care.hungry` = **จำนวนวันเล่นติดกันที่อิ่ม 0** ครบ 2 เมื่อไหร่ป่วย | ผลลัพธ์เหมือนแผนเป๊ะ แต่ไม่ผูกกับเวลาจริงเลย ⇒ หายไป 3 เดือนกลับมาก็ลดแค่วันเดียว ไม่ป่วยทันที |
| อาหารถุงแรก | ไม่ได้ระบุ | **รับเลี้ยงปุ๊บได้อาหารถุงแรกของชนิดนั้นฟรีทันที** (และเด็กที่เลี้ยงอยู่ก่อนเฟส 3B ก็ได้ย้อนหลัง) | เด็กใหม่มี 0 เหรียญ ถ้าน้องหิวแล้วยังไม่มีเงินซื้ออาหาร = ทางตันตั้งแต่วันแรก (กติกาเหล็กข้อ 1) |
| `ECON_VER` | — | **ไม่ปั๊ม (ยังเป็น 4)** | อาหารเป็นของกินหมดไป เก็บใน `data.petFood` ไม่ใช่สิทธิ์ถาวรใน `data.unlocked` ⇒ ไม่มีของเก่าที่ต้องแจกย้อนหลังผ่าน `migrate()` · การเริ่มต้น `data.care` ทำเองใน `sync()` ของ pet-care |
| ฉากรักษา | mini-sequence ตรวจ → ฉีดยา → พันผ้า ~5 วิ | toast + พลุ + เสียงแฟนแฟร์ (`curedCelebrate`) | NPC ในฉากยังไม่มีระบบท่าทาง/animation รายตัว การทำ sequence จริงต้องรื้อ `buildVillager` ทั้งก้อน — เก็บไว้ทำพร้อมกันตอนเฟสที่แตะ NPC จริงๆ |
| เข็มทิศชี้ 🏥 | ไอคอนโรงพยาบาลบนเข็มทิศ | ฟองข้อความบอกทางแทน (แตะน้องตอนป่วยจะบอกทุกครั้ง) | เข็มทิศตอนนี้ชี้ทิศลม 4 ทิศ ไม่ใช่ระบบ waypoint · เพิ่ม waypoint เป็นงานคนละก้อน |

**ตัวเลขที่ล็อกไว้แล้ว (มีเทสคุมที่ `tests/house-pet-care.spec.js`)**
- อาหาร 8 ชนิด ครอบคลุมสัตว์ครบ 12 ตัว · ราคา 45-150 🪙/ถุง · **1 ถุง = 5 มื้อ · 1 มื้อ = อิ่ม +50**
- ลดวันละ 35 · โชว์หัวใจเมื่อ < 50 · ป่วยเมื่อหิวสนิท 2 วันเล่นติด · **ค่ารักษา 120 🪙**
- ⚠️ **ห้ามแก้ตัวเลขชุดนี้โดยไม่ถามผู้ใช้** เหมือนราคาสัตว์ในข้อ 17.1

**สิ่งที่ 3B ทิ้งไว้ให้เฟสถัดไป**
- บทพูดของพี่ปุย (`npc-pet`) เรื่องอาหาร/การดูแล · บทหมอ-พยาบาลตอนน้อง**ไม่**ป่วย ยังเป็นบททั่วไปเดิม
- ชามอาหาร 3D หน้าบ้านสัตว์ (ถ้าอยากได้ตามแผนเดิม) — ต้องผูกกับ decor group ของ `pet-house`
- เฟสถัดไปตามลำดับคือ **เฟส 4 (พ่อ-แม่ในบ้าน + เควสต์ครอบครัว — ข้อ 28)**

---

## 38. ⚠️ สิ่งที่เฟส 4A ทำต่างจากแผน (ตั้งใจ — อ่านก่อนทำ 4B)

ทำเสร็จ 2026-08-09 · ไฟล์ใหม่ `js/house-family.js` (ตรรกะล้วน ไม่แตะ DOM/WebGL) เปิดที่ `window.HouseFamily`
ตัวละคร 3D · ฟองคำพูด · การ์ดคุย/รับงาน อยู่ใน `js/house.js`

| หัวข้อ | แผนเดิม (ข้อ 28) | ที่ทำจริง | เหตุผล |
|---|---|---|---|
| ขอบเขตเฟส | ทำพ่อแม่ + เควสต์ครอบครัว 8 แบบรวดเดียว | **แบ่ง 4A/4B** — 4A = พ่อแม่ + เควสต์ครอบครัววันละชุดที่ใช้กลไก `quiz`/`count` เดิม | 8 แบบคือมินิเกมใหม่ทั้งหมด ใหญ่กว่าเฟส 3A+3B รวมกัน · แบ่งแล้ว 4A จบในตัว เล่นได้จริง merge ได้ (ผู้ใช้เลือกเอง 2026-08-09) |
| ค่าตอบแทน | 50-90 🪙 | **14-39 🪙** (`FAM_BASE.family = 14`) | ตัวเลข 50-90 เขียนไว้ก่อนจูนเศรษฐกิจลง ~3 เท่าเมื่อ 2026-08-08 · ยังสูงกว่า NPC (6-17) และกระดาน (10-28) ตามเจตนาแผนว่า "วันละชุดเดียวจึงให้มากกว่า" (ผู้ใช้เลือกเอง) |
| เพดานรายได้ต่อวัน | — | ขยับ **320 → 360** ในเทส (ยังต่ำกว่า `DAY_CAP` 400) | เพิ่มเควสต์ครอบครัวก็ต้องเพิ่มเพดาน ไม่งั้นเทสแดง · ของแพงสุดในร้าน 300 🪙 จึงยังต้องเก็บ ~1 วันเหมือนเดิม (ผู้ใช้อนุมัติ) |
| แตะพ่อแม่ | แตะ → เปิดหน้าแต่งตัวทันที | แตะ → **คุย/รับงานก่อน** · ปุ่ม "แต่งตัวให้ ✏️" อยู่ในการ์ดคุย | พอพ่อแม่มีเควสต์แล้ว การแตะต้องหมายถึง "คุย" เป็นหลัก ไม่งั้นเด็กกดหางานไม่เจอ |
| ตั้งชื่อ | — | เพิ่มแถวตั้งชื่อในหน้าแต่งตัว (`#house-creator-name-row`) **โผล่เฉพาะตอนแต่งตัวให้พ่อแม่** | ชื่อเด็กแก้ที่ป้ายชื่อบนหัวจออยู่แล้ว ไม่ต้องมีซ้ำ |
| ท่าประจำ | แม่ทำครัว/รดน้ำ · พ่ออ่านหนังสือ/ซ่อมของ | **ยังไม่ทำ** — ตอนนี้ยืนหายใจ + หันหน้าหาเด็กเมื่อเข้าใกล้ + ฟองคำพูดสุ่ม | ท่าเฉพาะต้องรื้อ rig ตัวละคร เก็บไปทำพร้อมมินิเกมใน 4B ที่ต้องแตะ rig อยู่แล้ว |

**ที่ล็อกไว้แล้ว (มีเทสคุมที่ `tests/house-family.spec.js`)**
- default ครบทั้งชื่อ (`คุณพ่อ`/`คุณแม่`) และหน้าตา (`H_DEFAULT_PARENT_DAD/MOM` ใน `js/house-map.js`)
- **ไม่มี `data.parents` = ใช้ default** ⇒ เด็กที่เล่นอยู่ก่อนเฟส 4 ไม่พังและไม่ต้อง migrate
- แก้ของพ่อต้องไม่ไปโดนของแม่ และ **ห้ามเขียนทับ `data.char` ของเด็กเอง**
- เควสต์ครอบครัว **วันละ 1 ชุด โควตาแยกจาก NPC/กระดานเด็ดขาด** · สุ่มคงที่ต่อวัน
- **ปฏิเสธงานได้เสมอ ไม่มีบ่น ไม่มีดุ** (กติกาเหล็กข้อ 2) — โทนเป็น "ขอความช่วยเหลือ" ทั้งหมด

**สิ่งที่ 4A ทิ้งไว้ให้ 4B**: มินิเกม 8 แบบตามตารางข้อ 28 · ท่าประจำของพ่อแม่ · เควสต์ที่ต้องเดินไปทำนอกบ้าน (`shopping-list`, `family-time`)

## 39. เฟส 4B — มินิเกมครอบครัว (กำลังทำ · เริ่ม 2026-08-10)

**เสร็จครบ 12 เกม 2026-08-10** (8 ตามแผน + 2 งานเดิน + 2 ที่ดึงคลังหน้าหลักมาใช้) — ทุกเกมโผล่เฉพาะเควสต์ครอบครัว วันละ 1 ชุด (`FAM_MECHS`)

| แผน (ข้อ 28) | ที่ทำจริง | หมายเหตุ |
|---|---|---|
| `chore-tidy` | 🧹 `tidy` ลากของลงถังตามหมวด | ถังเป็นหมวดของ ไม่ใช่ห้องในบ้าน |
| `laundry-sort` | 🧺 `laundry` แยกผ้าตามประเภท | |
| `cook-help` | 🍳 `cook` ลากขั้นตอนลงช่อง 1..N | 5 เมนู · ชั้นเล็ก 3 ขั้น ชั้นโต 5 ขั้น |
| `family-time` | 🍽️ `dinner` **เดินไปนั่งโต๊ะในบ้านจริง** + 🕰️ `routine` เรียงกิจวัตร | ผู้ใช้สั่งกลับ 2026-08-10 ให้ทำงานเดินตามแผนเดิม · งานเดินเป็นงานชิ้นเดียวจบ **ไม่เข้ากติกาขั้นต่ำ 5 ข้อ** (ผู้ใช้อนุมัติ) · ต้องมีโต๊ะ/เก้าอี้ในบ้านก่อนถึงจะแจก |
| `pet-remind` | 🐾 `petfeed` ลากอาหารไปให้สัตว์ที่กินของนั้น | อ่านตารางอาหารจริงจาก `js/house-pet-care.js` |
| `budget-help` | 💰 `budget` เลือกของให้ครบโดยไม่เกินงบ | มีคำตอบถูกได้หลายชุด |
| `shopping-list` | 🏪 `market` **จำรายการที่บ้าน → เดินไปตลาด → ซื้อของ** (+ 🛒 `shopping` ฉบับอยู่บ้าน) | ผู้ใช้สั่งกลับ 2026-08-10 ให้เดินไปตลาดจริง · ส่วน "เงินทอน" อยู่ในเกม `budget` |
| `wake-up` | ⏰ `clock` ดูหน้าปัดแล้วตอบว่ากี่โมง | ใช้คำบอกเวลาชุดเดียวกับเกมนาฬิกาวิเศษในหน้าหลัก |

**ท่าประจำของพ่อแม่ทำแล้ว** (`PARENT_POSE`) — ทำครัว/อ่านหนังสือ/ถูพื้น/เก็บที่นอน/จัดของ ท่าไม่ซ้ำกัน

| หัวข้อ | แผนเดิม (ข้อ 28) | ที่ทำจริง | เหตุผล |
|---|---|---|---|
| ท่าเล่น | ลากของลงถัง | **แตะของ → แตะถัง** | เด็ก 5 ขวบบนแท็บเล็ตลากพลาดง่าย · แตะคือท่าเดียวกับตอนตอบคำถาม ทั้งเควสต์จึงเป็นท่าเดียวกันหมด |
| ถังของ `chore-tidy` | ห้องในบ้าน | **หมวดของ** (ครัว/ห้องน้ำ/ของเล่น/ของใช้เรียน) | ห้องทำให้ของกำกวม (📖 ห้องนอนหรือห้องนั่งเล่น?) เด็กตอบตามเหตุผลตัวเองแล้วยังผิด = ลงโทษเด็กกลายๆ (กติกาเหล็กข้อ 2) |
| แยกผ้า | แยกตามสี/ประเภท | **ตามประเภท** (เสื้อ/กางเกง-กระโปรง/ของชิ้นเล็ก) | สีของอิโมจิเสื้อผ้าคุมไม่ได้ ต่างเครื่องต่างสี |
| จำนวนข้อ | — | **กระดาน = ครึ่งหนึ่งของ `diff.qN` (อย่างน้อย 3)** | 1 กระดานใช้เวลานานกว่าโจทย์ตอบคำถามหลายเท่า ถ้าใช้ `qN` เต็ม (5-10) เควสต์เดียวยาวเกินไป |
| ตรวจคำตอบ | — | **วางครบแล้วตรวจให้เอง** ไม่มีปุ่ม "ส่งคำตอบ" · ผิดเฉพาะชิ้นที่ผิดเด้งกลับถาด ชิ้นที่ถูกอยู่ในถังต่อไป | เด็กไม่ต้องหาว่ากดตรงไหน และไม่ต้องเริ่มกระดานใหม่ทั้งใบเพราะผิดชิ้นเดียว |

**โครงสร้างที่วางไว้ให้ 6 เกมที่เหลือ**
- `QUESTS.submit(run, payload)` — ส่งคำตอบของทุกกลไก (กลไกไหนมี `verify(it, payload)` จะใช้ตัวนั้น) · `answer(run, i)` เดิมยังใช้ได้กับโจทย์ 4 ตัวเลือก
- `it.kind` บนตัวโจทย์ = ตัวบอกหน้าจอว่าจะวาดแบบไหน (ไม่มี `kind` = โจทย์ตอบคำถามแบบเดิม) — `renderQuestStep()` ใน `js/house.js` แตกสาขาที่นี่
- `FAM_MECHS` = รายชื่อกลไกที่เควสต์ครอบครัวสุ่มได้ (NPC/กระดานแตะไม่ถึง)

**ทรงหน้าจอมี 3 แบบเท่านั้น** (เพิ่มเกมใหม่ให้เลือกทรงที่มีก่อนเสมอ)
1. `kind:'sort'` ถังหลายใบ — tidy · laundry · petfeed
2. `kind:'sort'` + `layout:'slots'` ช่องเรียงลำดับ ช่องละชิ้น — cook · routine
3. `kind:'sort'` + `basket:true` ตะกร้าใบเดียว + ปุ่มยืนยัน — budget · shopping

`clock` ไม่ใช่ทรงใหม่ — เป็นโจทย์ 4 ตัวเลือกธรรมดาที่มีรูปหน้าปัดประกอบ (`it.clock`)

**กติกาจำนวนโจทย์ (ผู้ใช้สั่ง 2026-08-10)**
- เกมที่ **เปิด popup ให้เด็กตอบคำถาม** → สุ่ม **5-10 ข้อต่อเควสต์** ไม่จำเป็นต้องเท่ากันทุกเควสต์ และ **ไล่ระดับความยากภายในรอบ** (ข้อแรกง่ายสุด ข้อท้ายเต็มระดับชั้น)
- เกม **"เดินไปทำ"** (`dinner` / `market`) → งานชิ้นเดียวจบได้ ไม่เข้ากติกานี้
- **จำนวนข้อไม่มีผลกับเงิน** — ค่าตอบแทนคิดต่อเควสต์เหมือนเดิมทุกประการ

**คลังโจทย์จากเกมหน้าหลักที่ดึงมาใช้ซ้ำ**: `ORDER_SETS` → 📚 `orderlearn` · `EF_CATEGORIES` → 🗂️ `sortcat` · `CATS` → `quiz` (ใช้มาตั้งแต่เฟส 2)

### 38.1 ปรับเพิ่มหลังลองเล่น 4A (ผู้ใช้สั่ง 2026-08-09)

- **พ่อแม่เดินไปมาในบ้าน + สุ่มทำกิจกรรมตามอุปกรณ์ที่มีจริง** — เดินด้วย `findPath(inGrid…)` แบบเดียวกับชาวบ้านในเมือง
  แต่ความเร็วช้ากว่า (`PARENT_SPEED = .62` เดินในบ้านไม่ต้องรีบ) · กิจกรรมมาจาก `data.decor.in` ที่เด็กวางเอง
  ⇒ **บ้านไม่มีของหมวดนั้น = ไม่มีกิจกรรมนั้น** (ไม่แกล้งทำลอยๆ) · เด็กเข้าใกล้ = หยุดคุยด้วย ไม่เดินหนี
- **แตะตัว = คุยเพื่อทำเควสต์เท่านั้น** — เอาการ์ด "แต่งตัวให้" ออกจากการแตะตัวทั้งหมด
  (เดิมแตะแล้วเด้งการ์ดมาบังจอทั้งที่เด็กแค่อยากคุย)
- **แต่งตัวย้ายไปปุ่มของตัวเอง** — ปุ่ม 👥 บนแถบบน โผล่เฉพาะตอนอยู่ในบ้าน · **ปุ่มเดียว** แล้วเลือกพ่อ/แม่
  จากแท็บในหน้าแต่งตัว · สลับแท็บบันทึกของคนเดิมให้อัตโนมัติ (ไม่งั้นที่แต่งค้างไว้หาย)
- **ปิดแถวเลือกเพศตอนแต่งตัวให้พ่อแม่** — เพศถูกกำหนดจากแท็บแล้ว ถ้าเปิดไว้เด็กจะเปลี่ยน "คุณแม่" เป็นเด็กชายได้
- เคอร์เซอร์ฟองคำพูด (hover) ครอบคลุมพ่อแม่ในบ้านด้วย ไม่ใช่เฉพาะชาวบ้านในเมือง
- **ป้ายงาน "!" / "✓" เหนือหัวพ่อแม่** — ใช้ของชุดเดียวกับชาวบ้านในเมือง ขึ้นเฉพาะคนที่วันนี้เป็นคนขอ
- แก้บั๊ก: ฟองคำพูดพ่อแม่/ป้ายชื่อเด็กทะลุมาทับหน้าแต่งตัว (เพิ่ม `clearFloatLabels()` + เช็ค `hMode`)
- แก้ทรงผมชายเบอร์ 2 — ท้ายทอยสั้นกว่าเบอร์ 1 อยู่ .10 หน่วย (ตั้ง `d` เองทับค่าปริยาย) เอาออกให้เท่ากัน

## 40. ⚠️ สิ่งที่เฟส 5 ทำต่างจากแผน (ตั้งใจ — อ่านก่อนทำเฟส 6)

**เสร็จ 2026-08-11** — แผนเดิมเขียนว่า "รื้อ engine เกมเดิมมาใช้" ซึ่งอ่านได้ว่าต้องแกะโค้ด engine มาเขียนใหม่ให้พอดีการ์ดเควสต์
**ของจริงไม่ได้แตะโค้ด engine เลยแม้แต่บรรทัดเดียว** — ใช้ "สัญญา Mount" (`js/owl-games.js`) ย้ายทั้งกล่อง view ไปแปะในการ์ดแทน

| หัวข้อ | แผนเดิม | ของจริง | เหตุผล |
|---|---|---|---|
| วิธีเอา engine มาใช้ | รื้อ/ก๊อปโค้ดเข้ามาในโหมดบ้าน | **ย้าย DOM ทั้งกล่อง** (`OwlGames.mount`) | id ลูกๆ ไม่ซ้ำในเอกสาร `$('mix-hint')` จึงยังหาเจอ ⇒ ไม่มีโค้ดซ้ำสองฝั่งให้ค้าง |
| จำนวนกลไกใหม่ | ~7 | **8** (mix · memory · balance · clockset · shadow · sortcat2 · orderg · melody) | เพิ่ม melody (เล่นตามทำนอง) เพราะ engine พร้อมอยู่แล้ว |
| ชื่อคีย์กลไก | ตามชื่อเกม | บางตัวไม่ตรงชื่อ engine (`clockset`→`clock` · `sortcat2`→`sort` · `orderg`→`order` · `melody`→`music`) | ชื่อสั้นๆ ถูกมินิเกมของเฟส 4B จองไปก่อนแล้ว |
| จำนวนข้อ/เควสต์ | 5-10 ข้อเหมือนกลไกอื่น | **1 เควสต์ = 1 รอบเกม (10 ด่านของ engine)** | engine เดิมเป็นเกมยาวอยู่แล้ว ตัดเป็นข้อๆ ไม่ได้ · ดาวคิดจาก "จำนวนที่พลาด" ที่ engine คืนมา |
| ใครสุ่มได้ | ไม่ได้ระบุ | **NPC + กระดานสุ่มได้** (quiz 45% · count 17% · ที่เหลือกลุ่ม engine) · เควสต์ครอบครัวยังเป็นมินิเกม 4B ล้วน | เด็กจะได้เจอเกมหลากหลายจากการเดินคุยชาวบ้าน ไม่ต้องรอเควสต์ครอบครัววันละชุด |

**ตะเข็บทั้งหมดมี 3 จุดเท่านั้น** — `finishP2Game()` ใน `js/games-math.js` (บรรทัดเดียว) · `renderEngineStep()`/`startEngineGame()`/`finishEngineRound()` ใน `js/house.js` · `HouseGames.ALLOW` ใน `js/house-games.js`
⇒ **เฟส 6 อยากได้เกมไหนเพิ่ม แค่เติมชื่อใน `ALLOW` แล้วเติม `engineMech()` ใน `MECHS` มันจะโผล่ในเควสต์เอง** (แต่ต้องลองเล่นจริงในการ์ดใบเล็กก่อนเสมอ — ไม่ใช่ทุกเกมที่พอดีกล่อง)

---

## 41. ⚠️ สิ่งที่เฟส 6 + 7 ทำต่างจากแผน (ตั้งใจ — อ่านก่อนทำเฟส 8)

**ทั้งสองเฟสเสร็จ 2026-08-12 ในรอบงานเดียวกัน** (ผู้ใช้สั่งให้ทำต่อเนื่องจนจบ) รวม **20 กลไกใหม่**
เก็บตกกลไก `sound-guess` ของเฟส 5 ที่ตกหล่นไปด้วย

### 41.1 ตารางสรุปสิ่งที่ต่างจากแผน

| หัวข้อ | แผนเดิม | ของจริง | เหตุผล |
|---|---|---|---|
| `escort` (พาคนเดินไปส่ง) | อยู่ในกลุ่ม B 10 แบบ | **ตัดทิ้ง** (ผู้ใช้ตัดสิน 2026-08-12) | ต้องเขียน NPC เดินตามเด็กทั้งชุด (ชนกำแพง/หลงทาง/ตกน้ำ) เสี่ยง dead end สูงสุดในกลุ่ม ได้ผลตอบแทนน้อยสุด |
| `sound-guess` | "ดึงจาก `games-art.js`" | **เขียนใหม่** แต่ใช้ Web Audio ชุดเดิม | หน้าหลัก**ไม่มีเกมทายเสียง**จริง มีแต่เกมเล่นตามทำนอง — แผนเขียนคลาดเคลื่อน |
| `dress-order` | NPC สั่ง "หมวกฟาง + แว่นกลม" แล้วเด็กใส่ของจริง | **แต่งหุ่นในการ์ด ไม่ผูกกับของที่ซื้อ** | เฟส 1 ปิดของฟรีไปแล้ว ⇒ เด็กที่ยังไม่ได้ซื้อหมวกจะทำเควสต์ไม่ได้เลย = dead end (ผู้ใช้ตัดสิน) |
| `traffic-lr` / `flash-count` | มีแถบเวลาหด กดให้ทันเวลา | **ตัดการจับเวลาตอบทิ้ง** | กติกาเหล็กข้อ 2 ห้ามกดดัน/ลงโทษเด็ก · `flash-count` ยังมี "โชว์แล้วซ่อน" เพราะนั่นคือตัวกลไกเอง ไม่ใช่การจับเวลาตอบ |
| `spot-diff` | 2 ฉากเต็มจอเทียบกัน จุดต่าง 3-5 จุด | **ย่อเป็น 2 แถว ต่างกัน 1 ชิ้น** | การ์ดเควสต์ต้องไม่บังโลก 3D (ข้อ 10) ฉากเต็มจอ 20 ชิ้นวางไม่ลง — อยากได้เวอร์ชันเต็มให้ทำเป็นหมวดในหน้าหลักแล้วยืมมาผ่านสัญญา Mount |
| `feed-pet` / `shopping-list` | กลไกใหม่ของกลุ่ม B | **ใช้ `petfeed`/`shopping` ของเฟส 4B ซ้ำ** แค่เปิดให้ NPC หยิบได้ | กติกาเดียวกันเป๊ะ เขียนใหม่ = โค้ดซ้ำ 2 ชุด · เปลี่ยนคนสั่งจาก "คุณแม่" เป็นชื่อ NPC + เปลี่ยนคลังของตามธีมร้านให้แล้ว |
| จำนวนคลังโจทย์ | ไม่ได้ระบุ | **≥ 40 รายการต่อกลไก** (ผู้ใช้สั่ง 2026-08-12) | ของจริงได้ 40-46 ทุกตัว มีเทสไล่นับที่ `tests/house-phase67.spec.js` |
| ใครแจกงาน STEM | "แล็บเป็นจุดเดียวในเมือง" | **ทำตามแผนเป๊ะ** — `LAB_MECHS` ผูกกับตัว NPC | ชาวบ้านทั่วเมืองสุ่มงานแล็บไม่ได้เลย (มีเทสคุม) |

### 41.2 โครงสร้างที่เพิ่มเข้ามา (ต้องรู้ก่อนทำเฟสถัดไป)

- **`LAB_MECHS` / `SPOT_MECHS` = "แทนที่พูลทั้งหมด"** — NPC กลุ่มนี้ได้เฉพาะงานในลิสต์ (+ `quiz` เสมอ)
  ใช้กับตึกแล็บ (ดร.ฟ้า 🔬 วิทย์ · ดร.ต้น 🧪 วัด-ตวง · พี่ผู้ช่วย 🥽 coding · นักเรียน 🎒 เวอร์ชันง่าย) และร้านดนตรี
- **`BONUS_MECHS` = "เพิ่มเข้าไปในพูลปกติ"** — ร้านค้ายังได้ `quiz`/`count`/เกมยืม engine ตามปกติ
  แค่มีโอกาสได้งานที่เข้ากับร้านตัวเองเพิ่มขึ้น ⚠️ **ห้ามเปลี่ยนเป็นแบบแทนที่** ไม่งั้นร้านสะดวกซื้อ
  จะไม่มีวันแจกโจทย์คณิตทั่วไปอีกเลย = โจทย์จำเจ
- ⚠️ **`rollBoard()` ต้องสุ่ม "คน" ก่อน "งาน" เสมอ** — งานแล็บผูกกับตัวคน ถ้าสลับลำดับกระดานจะแจกงานแล็บมั่ว
- ⚠️ **`pickCat()` ใน `js/house-games.js` ห้ามคืนหมวดที่ชั้นสูงกว่าเด็ก** (ของเดิม fallback เป็น `all`)
  วงจรไฟฟ้ามีหมวดเดียวคือ ป.6 · แท็งแกรมมี ป.5-6 ⇒ เด็ก ป.1 เคยได้โจทย์ ป.6 เข้าให้
  ตอนนี้คืน `null` แทน แล้ว `engineReady()` ถอยไป `quiz` ให้เอง · **หมวดหุ่นยนต์ที่ง่ายสุดคือของ ป.1
  ⇒ เด็กระดับเตรียม ป.1 จะไม่ถูกแจกงาน coding เลย** ถ้าอยากให้เล่นได้ต้องเพิ่มหมวดในคลัง `CATS` ก่อน
- **ทรงหน้าจอที่เพิ่ม 4 แบบ** (นอกจาก 3 ทรงเดิมของเฟส 4B): `beaker` (บีกเกอร์ SVG) · `code` (รายการคำสั่งมีเลขบรรทัด) · `spot` (2 แถวเทียบกัน) · `flash` (โชว์แล้วซ่อน) · `sound` (ปุ่มฟังซ้ำ)
  ⚠️ `flash` ต้องจำว่าโชว์ไปแล้วด้วย `qFlashDone` ไม่งั้นตอบผิดแล้ววาดใหม่ = ย้อนไปโชว์ของอีกรอบ = เฉลยฟรี
- **งานเดินเพิ่ม 2 ปลายทางใหม่**: `target:'npc'` (ส่งของถึงมือ — จบเมื่อแตะตัวคนนั้น) · `target:'zone'` (หาของที่หาย — จบเมื่อเดินถึงย่าน ใช้ `questZonesAt()` ตัวเดิม)
  ⚠️ ปลายทางของ `deliver` เลือกจาก `questableIds()` เท่านั้น (คนกลุ่มนี้ระบบเดินไปหาได้จริงทุกวัน) และคำใบ้ต้องบอก **ชื่อคน + สถานที่** เสมอ ไม่งั้นเด็กเดินหาทั้งเมืองไม่เจอ

### 41.3 บทเรียนจากการเขียนเทส (กันพลาดซ้ำ)

- ⚠️ **เทสที่ mock `npcDefs: []` จะทำให้ `deliver` ถอยไปใช้ `count` เงียบๆ** ⇒ เทสผ่านโดยไม่เคยรันโค้ดกลไกนั้นเลย
  ต้องส่งผัง NPC จริงเข้าไปด้วย (`__houseDbg.npcDefs()`)
- ⚠️ **เกมตะกร้าต้องเทสด้วยการไล่หา subset ที่ผ่านได้จริง** (brute force) ไม่ใช่วางของทุกชิ้นลงตะกร้า
  เพราะเกมกลุ่มนี้ไม่ได้ใช้ของทุกชิ้น — และนี่คือวิธีเดียวที่พิสูจน์ได้ว่า**ไม่มี dead end**
- ⚠️ **`kind:'engine'` กับ `kind:'walk'` ได้รับยกเว้นกติกา `MIN_Q` โดยตั้งใจ** อย่าเขียนเทสบังคับให้มี 5 ข้อ


---

## 42. ⚠️ สิ่งที่เฟส 8 ทำต่างจากแผน (ตั้งใจ — อ่านก่อนทำเฟส 9)

**เสร็จ 2026-08-12** — งานวาดล้วนตามที่แผนคาด ไม่แตะ logic เกมเลย

| หัวข้อ | แผนเดิม (ข้อ 27/29) | ของจริง | เหตุผล |
|---|---|---|---|
| เฟอร์นิเจอร์ | 116 → 180 (+64) | **180 ตรงเป๊ะ** | ทำครบทุกหมวดตามตาราง |
| ของแต่งตัว | 56 → 106 (+50) | **114 (+58)** | ทำเกินเพราะเพิ่มแถว "แบบรองเท้า" 8 แบบที่แผนนับแยกไว้ |
| เครื่องดนตรี +11 | อยู่ในตารางข้อ 27 | **ยังไม่ทำ** | เป็นของเฟส 9 (ข้อ 31) ไม่ใช่เฟส 8 |
| ไอคอนปุ่มเลือก | "~106 ชิ้น" | **109 ชิ้น** | ครบทุกแบบที่เป็นแถว `type:'num'` |

### 42.1 กติกาที่ต้องรู้ก่อนแตะของพวกนี้

- ⚠️ **index ของแบบเดิมห้ามขยับเด็ดขาด** — ค่าที่เด็กเลือกถูกเก็บเป็น index ตรงๆ ใน save
  การแทรกแบบใหม่กลางลิสต์ = ตัวละครของเด็กที่เล่นอยู่เปลี่ยนหน้าตาเองทั้งเมือง **เพิ่มได้แต่ต่อท้าย**
- ⚠️ **แบบรองเท้า index 0 ต้องเป็นทรงผ้าใบเดิมเป๊ะ** — เด็กก่อนเฟส 8 ไม่มีคีย์ `shoeStyle` ใน save
  จะตกมาที่ 0 เอง (มีเทสคุมว่าหน้าตาต้องไม่เปลี่ยน)
- ⚠️ **แถว `shoeStyle` (รูปทรง) คนละแถวกับ `shoes` (สี)** — อย่าสับสน ทั้งคู่อยู่ในร้านแฟชั่นติดกัน
  โดยแบบมาก่อนสีตามกติกาเดิมของหน้าแต่งตัว
- 💰 ราคา: เฟอร์นิเจอร์ใหม่ทั้ง 64 ชิ้นอยู่ใน `FURN_TIER` ครบ (มูลค่าคลังรวม **16,145 เหรียญ**
  จากเดิม ~11,000) · แบบรองเท้า `FIT_PRICE.shoeStyle = 40` เท่าลายเสื้อ
  ⚠️ **ชิ้นที่ไม่อยู่ในตารางจะได้ระดับ 2 (60 เหรียญ) อัตโนมัติ** ซึ่งอาจผิดขนาดของ — มีเทสไล่ตรวจว่าครบทุกชิ้น

### 42.2 บทเรียนจากการทำจริง (กันพลาดซ้ำ)

- ⚠️ **`js/house-map.js` ห่อด้วย IIFE** ⇒ ฟังก์ชันที่ประกาศในนั้น **ไม่เป็น global**
  ต้องส่งออกทาง object ที่ `HOUSE_MAP()` คืน แล้ว destructure ใน `js/house.js`
  (รอบแรกลืม ไอคอนเลยไม่ขึ้นสักอันโดยไม่มี error ให้จับ — `typeof outfitIcon` เป็น `undefined` เงียบๆ)
- ⚠️ **ไอคอนต้องต่างกันที่ "เงารวม" (silhouette) ไม่ใช่รายละเอียดเล็ก** — ไอคอนโชว์แค่ 32px
  รอบแรกวาดทรงผม 12 แบบด้วย path คล้ายกัน ผลคือดูเหมือนกันเป๊ะบนจอจริง เด็กแยกไม่ออกเลย
  **ต้องถ่าย screenshot ของจริงมาดูเสมอ อย่าเชื่อว่า path ต่างกันแล้วจะดูต่างกัน**
- ⚠️ **`build(g, col, k)` ของเฟอร์นิเจอร์รับ kit เป็นพารามิเตอร์ที่ 3** — เทสที่เรียกเองต้องส่ง kit
  ไปด้วย ไม่งั้นชิ้นที่ใช้ `k.THREE.Group` (11 ชิ้น) จะพังทั้งที่โค้ดถูก


---

## 43. ⚠️ สิ่งที่เฟส 9 ทำต่างจากแผน (เฟสสุดท้าย · เสร็จ 2026-08-12)

| หัวข้อ | แผนเดิม (ข้อ 31) | ของจริง | เหตุผล |
|---|---|---|---|
| จำนวนเครื่อง | 11 ชิ้น | **11 ชิ้นตรงเป๊ะ** | — |
| "เปียโน" ในรายการ | เข้าใจว่าเป็นของใหม่ | **ใช้ `piano` เดิมที่มีในคลังอยู่แล้ว** ย้ายจากหมวด `decor` มา `music` | สร้างใหม่ = มีเปียโน 2 ตัวในเกม · ย้ายหมวดไม่กระทบสิทธิ์ที่เด็กซื้อไว้ (ผูกกับ id) |
| ราคา | 400-1,500 🪙 | **500 / 900 / 1,500** ตามระดับ | ราคาในเกมเป็นขั้นบันได (TIER) ไม่ใช่เลขอิสระ ⇒ เพิ่ม TIER 5-7 แทนที่จะตั้งราคาต่อชิ้น |
| `find-sound` แยกเสียงเครื่อง | "ฟังเสียงทายเครื่อง" | เสียงต่างกันที่ **โน้ต/ทำนองที่ผูกกับชิ้นนั้น** ไม่ใช่ timbre | Web Audio ชุดนี้เปลี่ยน timbre ไม่ได้ · เด็กเล่นเครื่องเองที่บ้านได้ทุกเมื่อจึงจำเสียงได้จริง ไม่ใช่โจทย์เดา |
| ระฆังลม | อยู่ในรายการเฉยๆ | **`scope:'out'`** ⇒ ร้านต้องขายทั้ง 2 scope | เป็นเครื่องเดียวที่แขวนนอกบ้าน ถ้าร้านขายแต่ฝั่ง `in` จะไม่มีใครขาย = dead end |

### 43.1 กติกาที่ต้องรู้

- ⚠️ **เควสต์ดนตรีถูกกรองออกอัตโนมัติถ้าบ้านยังไม่มีเครื่องดนตรี** (`mechOk()` ใน `js/house-quests.js`)
  ต้องกรอง **ทั้งตอนสุ่มแจกงานและตอนสร้าง spec** เพราะเด็กอาจเก็บเครื่องทิ้งหลังรับงานไปแล้ว
  ⇒ พี่โน้ตจะแจก `quiz`/`count`/`soundguess`/`melody` แทน ไม่มีทางตัน (มีเทสคุมทั้ง 2 ทาง)
- ⚠️ **`hasInstrument()` ดูจาก "ของที่วางไว้จริง" ไม่ใช่ "ของที่ซื้อแล้ว"** — ซื้อแล้วยังไม่วางก็ยังเล่นไม่ได้
- ⚠️ **`playMusicSequence()` ต้องส่ง `noFlash = true` เสมอในโหมดบ้าน** (กับดักเดียวกับเกมทายเสียงของเฟส 5)
- ⚠️ **`submitQuestPayload()` ใน `js/house.js` คือทางส่งคำตอบกลาง** สำหรับกลไกที่ไม่ใช่ 4 ตัวเลือก
  และไม่ใช่ลากลงถัง — เพิ่มกลไกที่ส่งคำตอบเป็นก้อนเดียวให้เรียกตัวนี้ ไม่ต้องเขียนทางส่งใหม่
- 🐞 **กับดักที่เจอจริง**: `piano` เดิมมี `action:'bounce'` อยู่ท้าย object การเติม `action:'music'`
  ไว้ข้างหน้าไม่มีผลเลย (key ซ้ำใน object literal ตัวหลังชนะ) — เทสจับได้ ไม่งั้นเปียโนจะแตะแล้วเด้งเฉยๆ

---

# ภาค 4 — เฟส 10-14 (จัดลำดับใหม่ 2026-08-13 · ยังไม่เริ่มทำทั้งหมด)

> ⚠️ **ลำดับนี้ทับลำดับเดิมที่ตั้งไว้ 2026-08-12** (เดิม: 10 = มินิเกม · 11 = จังหวะเวลา · 12 = ฤดูกาล)
> **เลขข้อในเอกสาร (44/45/46) ไม่ขยับ** เพื่อไม่ให้ลิงก์เดิมพัง — ดูตารางแมปด้านล่าง

## แผนเฟสใหม่ — ทำไล่จากบนลงล่าง

| เฟส | เนื้อหา | รายละเอียดอยู่ | ทำไมลำดับนี้ |
|---|---|---|---|
| **10** ✅ | ⏱️ **จังหวะเวลา — ระลอกโครงสร้าง** *(เสร็จ 2026-08-13)* · `starsOf` พลาดฟรี 1 ข้อ · ตัวคูณระดับชั้น · แยกงานหลัก/รอง · วาง `secPerQ` + สูตรทรงเดิน · **ยังไม่เพิ่มจำนวนข้อ** | **ข้อ 45** (ยกเว้นตาราง `qN` ข้อ 45.4) | เฟสถัดไปทั้งหมดพึ่งสูตรทรงเดินกับ `secPerQ` · แตะแค่ `house-quests.js` ไม่แตะ 3D ⇒ เสี่ยงต่ำสุด · **เด็กได้ประโยชน์ทันทีโดยไม่มีใครถูกเพิ่มงาน** |
| **11** ✅ | 🌍 **มินิเกมกลุ่ม A — เล่นในเมือง 3D 5 เกม** *(เสร็จ 2026-08-13)* (ซ่อนแอบ · ของหล่นตามฤดู · ตกปลา · ช่างภาพ · แปลงผัก) **แล้วปิดท้ายด้วยการเปิดตาราง `qN` ใหม่** | **ข้อ 44** (กลุ่ม A) + **ข้อ 45.4** | กลุ่มที่แปลกใหม่ที่สุด/คุ้มแรงที่สุด · **A1 ซ่อนแอบเป็นตัวพิสูจน์สูตรทรงเดินของเฟส 10 ก่อนลงทุนอีก 13 เกม** · พอมีเกมเดินแล้วค่อยเพิ่มจำนวนข้อ เด็กจะไม่รู้สึกจำเจ |
| **12** ⬅️ | 🐾 **มินิเกมกลุ่ม C — สัตว์เลี้ยง 3 เกม** (อาบน้ำ · โยนบอล · สอนท่า) | **ข้อ 44** (กลุ่ม C) | ปิดช่องว่างที่ใหญ่ที่สุดที่เหลือ — ตอนนี้สัตว์เลี้ยงมีแต่ "หิว/ป่วย" ไม่มีอะไรสนุกเลย |
| **13** | 🃏 **มินิเกมกลุ่ม B + D — 6 เกม** (ของหายไปไหน · ตามลำดับ · ทายว่าใคร · ร้านขนม · ระบายสีตามเลข · วงดนตรี) | **ข้อ 44** (กลุ่ม B, D) | ต้นทุนต่ำสุด ใช้ทรงการ์ดที่มีอยู่แล้ว ⇒ ทยอยเติมได้ไม่รีบ |
| **14** 💤 | 🍂 **ระบบฤดูกาล** | **ข้อ 46** | ⚠️ **ยังไม่ตัดสินใจทำ ต้องกลับมาคุยก่อนลงมือ** · ก้อนใหญ่สุด (13-20 วัน) และพึ่งเกม A2 "ของหล่นตามฤดู" ของเฟส 11 ให้มีความหมาย ⇒ ต้องอยู่ท้ายสุด |

### ⚠️ ทำไมถึงต้องผ่า "จังหวะเวลา" เป็น 2 ระลอก (เฟส 10 กับท้ายเฟส 11)

การเพิ่มจำนวนข้อของเด็กเล็กจาก **5 → 9 ข้อ = งานเพิ่ม 80%**
ถ้าเปิดตอนที่ **ยังไม่มีเกมใหม่มาสลับให้หายจำเจ** เด็กจะเจอ *"โจทย์แบบเดิมเป๊ะ แต่เยอะขึ้นเกือบเท่าตัว"*
⇒ **ประสบการณ์แย่ลง ตรงข้ามกับเจตนาทั้งหมดของแผนนี้**

จึงแยกเป็น:
- **เฟส 10 (ระลอก 1)** — ของที่ "มีแต่ได้ ไม่มีใครถูกเพิ่มงาน": ดาวใจดีขึ้น · ป.6 ได้เงินเป็นธรรม · เด็กเล็กจบวันได้ที่ 6 ชุด · วางโครง `secPerQ`/สูตรทรงเดินที่ยังไม่มีผลกับเด็กเพราะยังไม่มีเกมเดิน
- **ท้ายเฟส 11 (ระลอก 2)** — เปิดตาราง `qN` ใหม่ (ข้อ 45.4) ตอนที่เกมกลุ่ม A พร้อมสลับให้แล้ว

## 44. คลังมินิเกม 14 เกม — ครอบคลุมเฟส 11 · 12 · 13 (เรียงลำดับ A → C → B → D)

> รายละเอียดเต็มของแต่ละเกม (กติกา · ของเดิมที่ใช้ซ้ำ · ข้อควรระวังรายตัว) อยู่ใน **[`IDEA.md`](IDEA.md) หัวข้อ "🏠 ไอเดียมินิเกมของโหมดบ้านของหนู"** — ข้อนี้เก็บแค่ภาพรวมกับเหตุผลเชิงออกแบบ
>
> 📌 **14 เกมนี้ถูกกระจายเป็น 3 เฟสตามลำดับใหม่ (2026-08-13)**: **เฟส 11** = กลุ่ม A (5 เกม · ลำดับ 1-5) · **เฟส 12** = กลุ่ม C (3 เกม · ลำดับ 6-8) · **เฟส 13** = กลุ่ม B + D (6 เกม · ลำดับ 9-14) — **ลำดับภายใน A → C → B → D ที่ผู้ใช้สั่งไว้ยังคงเดิมทุกประการ**
> ⚠️ ตามกติกาของ `IDEA.md` ไอเดียที่มีรายละเอียดครบแล้วต้อง **ถามผู้ใช้ + รอ "cf" ก่อนลงมือทำเสมอ**

### 44.1 ปัญหาที่เฟสนี้แก้

กลไกในโหมดบ้านวันนี้มี **49 ตัว** แต่**เกือบทั้งหมดคือ "การ์ดลอยที่มีโจทย์ให้ตอบ"** ต่างกันแค่หน้าตาการ์ด
⇒ เด็กรู้สึกว่า **ทุกอย่างในเมืองคือแบบฝึกหัด** เมืองสวยแต่ไม่มีอะไรให้ "เล่น" จริงๆ

เฟสนี้เติม 2 อย่างที่ยังไม่มีเลยสักตัว:

1. **เกมที่เล่นในโลก 3D จริง โดยไม่เปิดการ์ด** (กลุ่ม A + C2 + D2)
2. **เกมที่ไม่มีถูก/ผิด** เล่นเพลินๆ ได้รางวัลน้อยแต่ไม่มีอะไรเสีย (กลุ่ม C + D)

### 44.2 รายการ 14 เกม (ลำดับที่ผู้ใช้สั่งไว้ — ทำไล่จากบนลงล่าง)

| ลำดับ | กลุ่ม | เกม | ทรง | ของเดิมที่ยืมมา | ความเสี่ยง |
|---|---|---|---|---|---|
| 1 | **A** | 🙈 ซ่อนแอบกับเพื่อนบ้าน | โลก 3D | `NPC_DEFS` · `findPath` · ป้าย "!" · `zoneNameAt()` | ต่ำ — ใช้ของเดิมเกือบ 100% |
| 2 | **A** | 🍃 ของหล่นตามฤดู | โลก 3D | สุ่มรายวัน seeded · `mergeDecorGroup` · `handleTap()` | ต่ำ · ต้องคิด migration ของ state ใหม่ |
| 3 | **A** | 🎣 ตกปลาที่ท่าน้ำ | โลก 3D | ตู้ปลา (เฟอร์นิเจอร์เดิม) · กริดน้ำในผัง | กลาง — คลังปลา ~15 ชนิด + อนิเมชันทุ่น |
| 4 | **A** | 📷 ภารกิจช่างภาพ | โลก 3D | `renderer` · เฟอร์นิเจอร์หมวด `decor` | **สูง** — `preserveDrawingBuffer` กินเฟรมเรต ต้องวัดก่อน |
| 5 | **A** | 🌱 แปลงผักหน้าบ้าน | โลก 3D | กลไก "วันที่เข้าเล่น" ของ `house-pet-care.js` · ร้านต้นไม้ | กลาง — long-loop ต้องคิด migration ให้ดี |
| 6 | **C** | 🫧 อาบน้ำให้น้อง | แผงลาก | drag ชุดเดิม · `startFeedAnim` เป็นต้นแบบ | ต่ำ |
| 7 | **C** | 🐕 โยนบอลให้น้องคาบ | โลก 3D | `findPath` · `updatePet()` | กลาง — บอลต้องไม่ตกในที่เดินไม่ได้ |
| 8 | **C** | 🎪 สอนท่าให้น้อง | การ์ด + โลก 3D | ระบบ pose · แนวคิด `PARENT_POSE` | ต่ำ — รางวัลถาวรที่มองเห็นได้ |
| 9 | **B** | 🫥 ของหายไปไหน | การ์ด | ทรง `flash` + `qFlashDone` | ต่ำ |
| 10 | **B** | 🦉 นกฮูกสั่ง — ตามลำดับ | การ์ด | `playPianoNote` · สีประจำโน้ต | ต่ำ |
| 11 | **B** | 🕵️ ทายว่าใคร | การ์ด | `buildVillager()` ย้อมดำ | ต่ำ — แต่ต้องเช็คว่าเงาแยกออกจริง |
| 12 | **B** | 🍰 ร้านขนมตามใบสั่ง | การ์ด `slots` | ทรง `sort`+`layout:'slots'` ของ `cook`/`routine` | **ต่ำสุดในชุด** — เปลี่ยนแค่ธีมกับคลัง |
| 13 | **D** | 🎨 ระบายสีตามเลข (ฉบับบ้าน) | การ์ด | สัญญา Mount (`OwlGames`) ถ้าทำหน้าหลักด้วย | กลาง — กริด 12×12 ในการ์ดเล็ก |
| 14 | **D** | 🎺 วงดนตรีข้างถนน | โลก 3D | เครื่องดนตรีเฟส 9 · `playMusicSequence` · pose NPC | ต่ำ — ไม่มีเงื่อนไขแพ้/ชนะเลย |

### 44.3 กติกาที่ทุกเกมในเฟสนี้ต้องผ่าน

- **ห้ามมีตัวจับเวลากดดัน · ห้ามมีคำว่า "แพ้" · ห้ามหักเงิน/หักดาว** (กติกาเหล็กข้อ 2) — ทั้ง 14 เกมออกแบบมาแล้วให้ผ่านข้อนี้
- **ห้ามมี dead end** (ข้อ 1) — เกมที่ผูกกับของที่ต้องซื้อ (ตู้ปลา · เครื่องดนตรี · แปลงผัก) ต้องเล่นได้/ไม่ถูกแจกงาน เมื่อยังไม่มีของ
- **ห้ามแตะหน้าหลัก** (ข้อ 4) — ไม่มีหมวดใหม่ใน `CATS` ไม่มีสติกเกอร์ใหม่ ยกเว้น D1 ที่ถ้าทำทั้ง 2 ที่ให้เขียน engine ครั้งเดียวแล้วยืมผ่าน `OwlGames`
- **เงินผ่าน `awardCoins()` ใน `js/house.js` จุดเดียว** (ข้อ 5)
- **state ใหม่ทุกตัวต้องคิด migration + export/import ตั้งแต่แรก** (ข้อ 3) — เฟสนี้เพิ่ม state เยอะที่สุดนับจากเฟส 3 (ของสะสม · ปลา · รูปถ่าย · แปลงผัก · ท่าสัตว์)
- **โค้ดใหม่ไปอยู่ไฟล์ใหม่ ไม่ยัดใน `js/house.js`** (12,900 บรรทัดแล้ว) — คุยผ่านประตูเดิม `window.HouseQuestUI` ตามแบบ `js/house-games.js`

### 44.4 ข้อควรระวังระดับเฟส

- ⚠️ **เกมกลุ่ม A ทุกตัวต้องดัมพ์กริดเดินได้จริงก่อนวางจุด** (`window.__houseDbg.grid()`) ห้ามเดาพิกัด — กติกาเดียวกับตอนแก้ผังเมือง
- ⚠️ **ชาวบ้านเข้าเขตบ้านเด็กไม่ได้** (`inHomeZone`) ⇒ เกมที่ใช้ NPC ห้ามวางเป้าหมายในเขตนั้น
- ⚠️ **เกมในโลก 3D ต้องเช็คว่าไม่ชนกับ `handleTap()` เดิม** — ทุกสาขาใน `handleTap()` ต้อง `return` ไม่งั้นแตะครั้งเดียวแล้วตัวละครเดินตามไปด้วย
- ⚠️ **โควตารายวันต้องแยกจากเควสต์ 14 ชุดเดิม** ไม่งั้นเกมเบาสมองจะไปกินโควตาเควสต์ที่เด็กใช้หาเงิน
- ⚠️ **เกมที่จ่ายเหรียญต้องคิดผลกระทบเงินเฟ้อ** — รายได้วันนี้ ~206 🪙/วัน เทียบของทั้งเกม ~26,545 🪙 · แนะนำให้กลุ่ม A2/C/D **จ่ายเป็นของสะสม/ความสุข ไม่ใช่เหรียญ** เพื่อไม่ต้องจูนเศรษฐกิจใหม่ทั้งระบบ

---

## 45. เฟส 10 (+ ท้ายเฟส 11) — จังหวะเวลา & งบเวลาเควสต์ (อนุมัติ 2026-08-12 · ยังไม่เริ่มทำ)

> ⚠️ **ข้อนี้ถูกผ่าเป็น 2 ระลอกตามลำดับเฟสใหม่ (2026-08-13) — อ่านตรงนี้ก่อน**
>
> | ระลอก | ทำเมื่อไหร่ | เนื้อหาในข้อนี้ |
> |---|---|---|
> | **1** | **เฟส 10** (เฟสถัดไปที่จะทำ) | ทุกอย่างในข้อ 45 **ยกเว้นตาราง `qN` ในข้อ 45.4** — คือ `starsOf` พลาดฟรี 1 ข้อ (45.9) · ตัวคูณระดับชั้น (45.8) · แยกงานหลัก/รอง (45.6) · วาง `secPerQ` + สูตรทรงเดิน (45.4-45.5) |
> | **2** | **ท้ายเฟส 11** (หลังเกมกลุ่ม A เสร็จ) | **เปิดตาราง `qN` ใหม่ในข้อ 45.4** (การ์ด 9-12 · ลาก 6-8) |
>
> **เหตุผล**: เพิ่มข้อของเด็กเล็ก 5 → 9 = งานเพิ่ม 80% ถ้าเปิดตอนยังไม่มีเกมใหม่มาสลับ เด็กจะเจอ *"โจทย์แบบเดิมเป๊ะ แต่เยอะขึ้นเกือบเท่าตัว"* = ประสบการณ์แย่ลง ตรงข้ามกับเจตนาของแผน

> **เฟสนี้ไม่เพิ่มกลไกใหม่แม้แต่ตัวเดียว** — ปรับ "จังหวะการเล่น" ของระบบที่มีอยู่ให้เด็กทุกชั้นเล่นได้
> **20-60 นาที/วัน** และ **ได้ค่าตอบแทนรายวันเท่ากันทุกชั้น** · ทำคู่กับเฟส 10 ได้ (คนละส่วนกัน)

### 45.1 ตัวเลขฐาน (วัดจากโค้ดจริง ไม่ใช่ประมาณ)

| ค่า | ของจริง | ที่มา |
|---|---|---|
| ความเร็วเดิน | **3 ช่อง/วินาที** | `WALK_SPEED` · `js/house.js` |
| ขนาดแผนที่ | **68 × 68 ช่อง** | `OUT_W`/`OUT_D` · `js/house-map.js` |
| จำนวนข้อ/เควสต์ (เดิม) | `Math.min(10, 4+tier)` ⇒ เตรียม-ป.1 = 5 … ป.6 = 10 | `difficulty()` |
| ขั้นต่ำ | `MIN_Q = 5` (`kind:'walk'`/`kind:'engine'` ได้รับยกเว้นอยู่แล้ว) | `js/house-quests.js:49` |
| ชุด/วัน | NPC 8 + กระดาน 5 + ครอบครัว 1 = **14** | `NPC_PER_DAY`/`BOARD_N` |
| ตัวคูณระดับชั้นในสูตรเงิน (เดิม) | **1.0 ทุกชั้น** (ล็อกไว้ 2026-08-09) — ⚠️ **เฟสนี้เปลี่ยนเป็น 1.00-1.15 ดูข้อ 45.8** | `coinsFor()` |
| เพดานเงินจริง/วัน (เดิม) | **206 🪙 เท่ากันทุกชั้น** (NPC 80 + กระดาน 65 + ครอบครัว 16 + โบนัส 45) | คำนวณจาก `FAM_BASE`×`STAR_MUL` |

### 45.2 ปัญหา 2 ข้อที่เฟสนี้แก้

1. 🐣 **เควสต์ของเด็กเล็กสั้นเกินไป** — 5 ข้อ × 16 วิ = **80 วินาที/ชุด** เด็ก "ทำแป๊บเดียวเสร็จแล้วไม่มีอะไรทำต่อ"
2. ⚖️ **เงินเท่ากันแต่เวลาไม่เท่ากัน** — ป.6 ใช้เวลามากกว่า ป.1 เกือบเท่าตัวเพื่อเงินก้อนเดียวกัน
   ⇒ **ของจริงคือ ป.6 เสียเปรียบ ไม่ใช่ได้เปรียบ** (เหรียญ/นาที 4.6 เทียบ 8.6)

### 45.3 🔒 กติกาใหม่ที่ต้องล็อก — ห้ามละเมิด

> **จำนวนชุดที่จ่ายเงินต้องเท่ากันทุกระดับชั้น (14 ชุด/วัน) เสมอ**
> ⇒ **ปรับเวลาเล่นด้วย "จำนวนข้อต่อชุด" เท่านั้น ห้ามปรับด้วย "จำนวนชุดต่อวัน"**
> ถ้าให้ ป.6 ได้ 18 ชุดเพื่อยืดเวลา = ช่องว่างเงินจะโตเกินคุมทันที (ผู้ใช้สั่ง 2026-08-12)

> **ระบบต้องไม่ล็อกจำนวนงานตามระดับชั้นเด็ดขาด** — ป.1 ที่อยากเล่นครบ 14 ชุดต้องได้เต็มโควตาของตัวเอง
> ความต่างตามชั้นอยู่ที่ **คำแนะนำบนหน้าจอเท่านั้น** ไม่ใช่การกั้นสิทธิ์

> ⚖️ **นิยาม "ยุติธรรม" ของระบบนี้ = "🪙 ต่อนาทีเท่ากัน" ไม่ใช่ "🪙 ต่อวันเท่ากัน"**
> เงินต่อวันต่างกันได้ตามสัดส่วนเวลาที่ต้องลง (ข้อ 45.8) แต่ **เวลาที่ต้องลงเพื่อซื้อของครบทั้งเกมต้องเท่ากันทุกชั้น**

### 45.4 งบเวลาต่อ 1 เควสต์ = **150-240 วินาที (2.5-4 นาที)**

ต่ำกว่า 150 วิ = "แป๊บเดียวเสร็จ" (สิ่งที่ผู้ใช้ห้าม) · เกิน 240 วิ = เด็ก 5 ขวบเริ่มหลุดสมาธิ

**เวลาต่อข้อโดยประมาณ (`secPerQ`) — ให้แต่ละกลไกประกาศค่านี้ไว้ใน `MECHS`**

| ทรง | `secPerQ` | เดิม | **ใหม่** | เวลา/ชุด |
|---|---|---|---|---|
| การ์ด 4 ตัวเลือก | 16 วิ | 5-10 ตามชั้น | **9-12 ตามชั้น** | 144-192 วิ |
| ลากลงถัง/ช่อง/ตะกร้า | 30 วิ | 5-10 | **6-8 ตามชั้น** | 180-240 วิ |
| ทรงเดิน (เฟส 10) | 12 + d×0.6 วิ | — | **5-10 ตามระยะ** | 145-230 วิ |
| ยืม engine หน้าหลัก | — | 1 ชุด = 10 ด่าน | **คงเดิม** | ~200 วิ |

**ตารางจำนวนข้อใหม่ (ใช้เป็น lookup ตรงๆ ไม่ต้องเป็นสูตร)**

| tier | ระดับชั้น | การ์ด 4 ตัวเลือก | ลาก |
|---|---|---|---|
| 1 | เตรียม ป.1 / ป.1 | **9** | **6** |
| 2 | ป.2 | **10** | **6** |
| 3 | ป.3 | **10** | **7** |
| 4 | ป.4 | **11** | **7** |
| 5 | ป.5 | **12** | **8** |
| 6 | ป.6 | **12** | **8** |

⚠️ **ทับมติเดิม 2026-08-09 ที่ตั้งไว้ 5-10 ข้อ** (ผู้ใช้อนุมัติ 2026-08-12) — เป็นตัวแก้ปัญหา "แป๊บเดียวเสร็จ" โดยตรง
⚠️ **`MIN_Q = 5` ยังอยู่เหมือนเดิม** ตัวเลขใหม่ทั้งหมดสูงกว่าอยู่แล้ว ไม่มีอะไรต้องผ่อน

### 45.5 สูตรจำนวนข้อของทรงเดิน (เฟส 10)

```
n = clamp(5, 10, round(180 / (12 + d × 0.6)))
```

- `d` = ระยะเดินต่อ 1 ข้อ หน่วยเป็นช่อง — **ต้องวัดด้วย `findPath()` จริง ห้ามใช้ระยะเส้นตรง**
- `12` = วินาทีที่ใช้คิด/ตอบ · `0.6` = วินาทีต่อช่องของจริง (3 ช่อง/วิ ตามโค้ด × 1.8 เผื่อเด็กเดินอ้อม/แตะซ้ำ)
- สุ่ม ±1 ด้วย seed ของเควสต์นั้นได้ แต่ยังต้อง clamp ใน 5-10

| ระยะเดิน/ข้อ | ได้กี่ข้อ | เวลารวม |
|---|---|---|
| 8 ช่อง (ในลานเดียวกัน) | **10 ข้อ** | ~168 วิ |
| 15 ช่อง (ข้ามลาน) | **9 ข้อ** | ~189 วิ |
| 25 ช่อง (ข้ามย่าน) | **7 ข้อ** | ~189 วิ |
| 40 ช่อง (คนละมุมเมือง) | **5 ข้อ** | ~180 วิ |

⚠️ **ห้ามใช้ระยะเส้นตรงเด็ดขาด** — แผนที่นี้มีแม่น้ำกับสะพาน จุดที่ห่างกัน 10 ช่องแบบเส้นตรง
อาจต้องเดินอ้อมสะพาน 40 ช่อง ⇒ จะแจก 10 ข้อให้เควสต์ที่เดินไกลที่สุด ตรงข้ามกับที่ออกแบบไว้เป๊ะ

### 45.6 งานหลัก / งานรอง

| | ชุด | เท่ากันทุกชั้นไหม | เวลา | เงิน (ป.1 → ป.6) |
|---|---|---|---|---|
| 🎯 **งานหลัก** = กระดาน 5 + ครอบครัว 1 | **6 ชุด** | ✅ เท่ากัน | ~19-22 นาที | **126 → 145 🪙 (61%)** |
| ⭐ **งานรอง** = NPC 8 คนในเมือง | **8 ชุด** | ✅ เปิดครบทุกชั้น | ~25-29 นาที | 80 → 92 🪙 (39%) |
| | **14 ชุด** | | **44-51 นาที** | **206 → 237 🪙** |

> สัดส่วน 61/39 คงที่ทุกชั้น เพราะตัวคูณข้อ 45.8 คูณทั้งก้อนเท่ากัน

- **ใช้โควตาที่มีอยู่แล้ว ไม่ต้องเขียนระบบใหม่** — กระดาน 5 + ครอบครัว 1 มีโควตาตายตัวอยู่แล้วทั้งคู่
- เด็กเล็กเล่นงานหลักจบใน ~20 นาที แล้ว**จบวันอย่างมีเกียรติ** ("วันนี้ครบแล้ว!") · เด็กโตเล่นต่อได้ถึง ~50 นาที
- **แถบสรุปเควสต์ (`#house-quest-bar` / `#house-qsum`) ต้องแยก 2 กลุ่มให้ชัด** — ตัวนับ ❗ ของ "วันนี้ครบแล้วไหม"
  ให้นับจาก**งานหลัก 6 ชุดเท่านั้น** ส่วนงานรองแสดงเป็นกลุ่มแยกที่ทำเพิ่มได้ ไม่ใช่ค้างเป็นหนี้
- **คำแนะนำบนหน้าจอต่างกันตามชั้น** (ไม่ใช่การกั้นสิทธิ์): เตรียม-ป.1 เชียร์ที่ 6 ชุด · ป.2-3 ที่ ~10 · ป.4-6 ที่ 14

### 45.7 ผลลัพธ์ก่อน-หลัง

> ตัวเลขรวมคิดรวมเควสต์ยืม engine (~38% ของพูล · ~225 วิ/ชุด) และ overhead ต่อชุด (~25 วิ) แล้ว

| | ก่อน | หลัง (รวมตัวคูณข้อ 45.8) |
|---|---|---|
| เวลาเล่นเต็ม ป.1 | ~35 นาที | **~44 นาที** (งานหลัก ~19) |
| เวลาเล่นเต็ม ป.6 | ~47 นาที | **~51 นาที** (งานหลัก ~22) |
| เหรียญ/นาที ป.1 : ป.6 | 5.9 : 4.4 | **4.64 : 4.62** (เท่ากัน ±1%) |
| เงินเต็ม/วัน | 206 ทุกชั้น | 206 (ป.1) → **237 (ป.6)** |
| **เวลาที่ต้องลงเพื่อซื้อของครบทั้งเกม** | ป.1 95 ชม. · ป.6 **110 ชม.** ❌ | **≈ 95-96 ชม. เท่ากันทุกชั้น** ✅ |
| ชุดที่สั้นที่สุด | 80 วิ ❌ | **144 วิ** ✅ |

### 45.8 ✨ ตัวคูณระดับชั้นในสูตรเงิน — คืนกลับมาแบบละมุน (อนุมัติ 2026-08-12)

#### ปัญหา

หลังเฟส 11 ป.6 ทำ **12 ข้อ/ชุด** ส่วน ป.1 ทำ **9 ข้อ/ชุด** แต่ได้เงินเท่ากัน
⇒ 🪙/นาที ต่างกัน **15%** ⇒ **ป.6 ต้องลงเวลามากกว่า 15 ชั่วโมงเพื่อซื้อของชุดเดียวกัน**

| ชั้น | ข้อ/ชุด | เวลา/วัน | เงิน/วัน (ก่อนแก้) | 🪙/นาที |
|---|---|---|---|---|
| เตรียม-ป.1 | 9 | 44.4 นาที | 206 | **4.64** |
| ป.2 · ป.3 | 10 | 46.7 | 206 | 4.41 |
| ป.4 | 11 | 49.0 | 206 | 4.20 |
| ป.5 · ป.6 | 12 | 51.3 | 206 | **4.02** |

#### ⚠️ ทำไมไม่ขัดกับมติ 2026-08-09 ที่เขียนล็อกว่า "ห้ามใส่ตัวคูณกลับ"

คอมเมนต์ที่ล็อกไว้ใน `coinsFor()` ให้เหตุผลว่า *"ป.6 ได้เงินมากกว่า ป.1 **ทั้งที่ทำงานเท่ากัน**"*
ตอนนั้นถูกต้องเพราะทุกชั้นทำ **5 ข้อ/ชุดเท่ากัน** — **แต่เฟส 11 ทำให้จำนวนข้อต่างกันแล้ว**
⇒ ใส่ตัวคูณกลับตอนนี้ **ไม่ได้ขัดเจตนาเดิม แต่เป็นการรักษาเจตนาเดิม**:
"ทำงานเท่ากันได้เท่ากัน" → **"ทำงานมากกว่าได้มากกว่าตามสัดส่วนเวลาที่วัดจริง"**

#### ตัวคูณที่ใช้

| tier | ระดับชั้น | **ตัวคูณ** | เงิน/วัน | 🪙/นาที | วันที่ซื้อของครบ | ชั่วโมงรวม |
|---|---|---|---|---|---|---|
| 1 | เตรียม ป.1 / ป.1 | **1.00** | 206 | 4.64 | 129 วัน | 95.5 ชม. |
| 2-3 | ป.2 · ป.3 | **1.05** | 216 | 4.63 | 123 วัน | 95.7 ชม. |
| 4 | ป.4 | **1.10** | 227 | 4.63 | 117 วัน | 95.6 ชม. |
| 5-6 | ป.5 · ป.6 | **1.15** | 237 | 4.62 | 112 วัน | 95.8 ชม. |

- **ตัวคูณ = อัตราส่วนเวลาเล่นจริง** (44.4 : 46.7 : 49.0 : 51.3 นาที) ปัดเป็นขั้นละ 0.05
- ทุกชั้นได้ **🪙/นาที เท่ากัน ±1%** และ **ลงเวลาเท่ากัน ≈95-96 ชั่วโมงเพื่อซื้อของครบทั้งเกม (26,545 🪙)**
- **เงินเฟ้อสูงสุด +15%** (ไม่ใช่ +40% แบบระบบเก่าที่ถูกเอาออก) · `DAY_CAP = 400` ยังคุมอยู่สบาย

#### 🔒 ราวกันตก — ล็อกคู่กับตัวคูณ ห้ามละเมิด

1. **ตัวคูณห้ามเกิน 1.20 เด็ดขาด**
2. **ต้องถอดจากอัตราส่วนเวลาที่วัดจริงเสมอ** — แก้ตาราง `qN` ในข้อ 45.4 เมื่อไหร่ **ต้องคำนวณตัวคูณใหม่ให้ตรงทุกครั้ง** ห้ามตั้งเลขสวยๆ เอาเอง
3. **ตัวคูณผูกกับ `tier` เท่านั้น ห้ามผูกกับจำนวนข้อจริงของเควสต์นั้น** — ไม่งั้นกลไกที่มีข้อเยอะ (การ์ด 12 ข้อ) จะจ่ายมากกว่ากลไกที่มีข้อน้อย (ลาก 8 ข้อ) ในชั้นเดียวกัน ⇒ เด็กจะเลือกเล่นแต่กลไกที่คุ้ม
4. **นิยาม "ยุติธรรม" ของระบบนี้เปลี่ยนแล้ว** — จาก *"เงินต่อวันเท่ากัน"* เป็น **"เงินต่อนาทีเท่ากัน + เวลาที่ต้องลงเพื่อซื้อของครบเท่ากัน"**

### 45.9 อีก 2 ข้อที่ผู้ใช้อนุมัติพร้อมกัน (2026-08-12)

**① `starsOf()` — "พลาดได้ 1 ข้อฟรีเสมอ ไม่ว่าเควสต์จะกี่ข้อ"**
ของเดิมคิดเป็นสัดส่วน ~2/3 ของจำนวนข้อ ⇒ เควสต์เดิน 5 ข้อ พลาด 2 ข้อร่วงเหลือ 1 ดาว
ขณะที่เควสต์ 12 ข้อพลาด 2 ยังได้ 3 ดาว — **เควสต์สั้นถูกลงโทษหนักกว่าโดยไม่ตั้งใจ** (เฉียดกติกาเหล็กข้อ 2)

**② `DAY_CAP = 400` — แก้คอมเมนต์ ไม่แตะตัวเลข**
เพดานจริงคือ 206 🪙 ⇒ `DAY_CAP` ไม่เคยทำงานเลย และคอมเมนต์ในโค้ดที่เขียนว่า "ป.6 เล่นครบ ~340"
**ค้างมาจากยุคก่อนตัดตัวคูณระดับชั้นทิ้ง (2026-08-09)** ⇒ แก้ข้อความให้ตรง แต่คงเลข 400 ไว้เป็นกันชนอนาคต

### 45.10 จุดที่ต้องแก้ในโค้ด

| ไฟล์ | แก้อะไร |
|---|---|
| `js/house-quests.js` | `difficulty().qN` → ตาราง 45.4 · `sortRounds()` → 6-8 · `starsOf()` → พลาดฟรี 1 ข้อ · เพิ่ม `secPerQ` ใน `MECHS` ทุกตัว · สูตรทรงเดิน 45.5 · **`coinsFor()` คืนชีวิตให้พารามิเตอร์ `tier` + ตาราง `TIER_MUL` ข้อ 45.8 + เขียนคอมเมนต์ที่ล็อกไว้ใหม่** · แก้คอมเมนต์ `DAY_CAP` · `daySummary()` คืนธง "หลัก/รอง" |
| `js/house.js` | `#house-quest-bar` / `#house-qsum` แยกกลุ่มหลัก-รอง · ตัวนับ ❗ นับเฉพาะงานหลัก · ข้อความเชียร์ตามชั้น |
| `tests/house-phase11.spec.js` (ใหม่) | **🪙/นาที ของทุกชั้นต้องต่างกันไม่เกิน 5%** · **เงิน/วันต่างกันไม่เกิน 15% และ ป.6 ต้องไม่น้อยกว่า ป.1** · ทุกเควสต์อยู่ในงบ 150-240 วิ · จำนวนชุดจ่ายเงินเท่ากันทุกชั้น · ทรงเดินได้ 5-10 ข้อและวัดด้วย `findPath` · เควสต์สั้นพลาด 1 ข้อยังได้ 3 ดาว |
| ⚠️ **เทสเดิมที่ต้องแก้** | เทสที่บังคับว่า **"ทุกชั้นต้องได้เหรียญเท่ากันเป๊ะ"** (คุมมติ 2026-08-09) จะแดงทันที ⇒ เปลี่ยนเป็นเกณฑ์ 2 ข้อข้างบนแทน **ห้ามลบเทสทิ้งเฉยๆ** |

### 45.11 ข้อควรระวัง

- ⚠️ **`coinsFor()` รู้จักได้แค่ `tier` เท่านั้น ห้ามรู้จักจำนวนข้อจริงของเควสต์เด็ดขาด** — ผูกกับจำนวนข้อเมื่อไหร่ กลไกที่มีข้อเยอะจะจ่ายมากกว่ากลไกที่มีข้อน้อยในชั้นเดียวกัน เด็กจะเลือกเล่นแต่ตัวที่คุ้ม (ราวกันตกข้อ 3 ของ 45.8)
- ⚠️ **เพิ่มข้อของเด็กเล็ก 5 → 9 = งานเพิ่ม 80%** ⇒ ต้องลองเล่นจริงกับเด็กก่อนว่าไม่ล้าเกิน ถ้าล้าให้ลดที่ตาราง 45.4 **ห้ามไปลดจำนวนชุด**
- ⚠️ **`secPerQ` เป็นค่าประมาณ ไม่ใช่ตัวจับเวลา** — ห้ามเอาไปโชว์บนหน้าจอหรือใช้ตัดจบเควสต์ (กติกาเหล็กข้อ 2 ห้ามมีตัวจับเวลากดดัน) ใช้เฉพาะตอนคำนวณจำนวนข้อเท่านั้น
- ⚠️ **`starsOf` ที่ใจดีขึ้นจะทำให้เงินเฉลี่ยต่อวันขยับขึ้นเล็กน้อย** (ดาวสูงขึ้น) — ต้องวัดจริงว่ายังไม่เกินเพดานของชั้นนั้นมาก (206-237 ตามข้อ 45.8) ถ้าเกินให้ปรับเกณฑ์ดาว **ห้ามลดค่าตอบแทนโดยไม่ถามผู้ใช้**
- ⚠️ **ตัวคูณระดับชั้นกับ `CHAL_MUL = 1.5` คูณทับกันได้** — ป.6 ทำโจทย์ท้าทาย 3 ดาว = `8 × 1.6 × 1.5 × 1.15` ⇒ ต้องเช็คว่าวันที่ท้าทายเยอะยังไม่ชน `DAY_CAP = 400`
- ⚠️ **`STAR_BONUS`/`BOARD_BONUS` เป็นเลขคงที่ ไม่ผ่าน `coinsFor()`** — ถ้าไม่คูณตัวคูณให้ด้วย ช่องว่าง 🪙/นาที จะปิดได้ไม่สนิท (เหลือ ~5%) ⇒ **ต้องคูณโบนัสด้วย** ตัวเลขในตาราง 45.8 คิดแบบคูณครบทั้งก้อนแล้ว

---

## 46. เฟส 14 (ท้ายสุด) — ระบบฤดูกาล (ตั้งไว้ 2026-08-13 · ยังไม่ตัดสินใจทำ)

> 📌 **เป็นเฟสท้ายสุด (เฟส 14) ตามคำสั่งผู้ใช้ 2026-08-13** — ต้องรอเฟส 10-13 เสร็จก่อน โดยเฉพาะ **เกม A2 "ของหล่นตามฤดู" ของเฟส 11** ที่ระบบฤดูจะทำให้มีความหมายขึ้น
> ⚠️ **ข้อนี้เป็นการประเมินขนาดงานเบื้องต้นเท่านั้น ยังไม่ใช่สเปก** — ผู้ใช้สั่งให้เก็บไว้ก่อน
> **ก่อนลงมือต้องกลับมาคุยกันใหม่ทุกครั้ง** (ยังมีคำถามค้าง 3 ข้อในหัวข้อ 46.5 ที่ยังไม่ได้ตัดสิน)

### 46.1 ไอเดีย

แต่ละฤดูปรับ **แสง · สีธรรมชาติ · ของในโลก · UI** ให้ต่างกัน เพื่อให้เมืองไม่หน้าเดิมตลอดไป
และให้เด็กมีเหตุผลกลับมาดูว่า "วันนี้เมืองเปลี่ยนไปยังไง"

### 46.2 ✅ สถาปัตยกรรมที่มีอยู่แล้วและช่วยได้มาก (วัดจากโค้ดจริง 2026-08-13)

1. **`matCache` — วัสดุทั้งเมืองไหลผ่านประตูเดียว** (`toonMat()`/`softMat()` ใน `js/house.js`)
   มี `new THREE.Mesh*Material` ตรงๆ แค่ **17 จุด** ที่เหลือผ่าน helper หมด
   ⇒ วัสดุเป็น **object เดียวกันที่แชร์ทั้งฉาก** สั่ง `mat.color.set()` แล้วกระจายทันที
   **รวมถึง mesh ที่ผ่าน `mergeDecorGroup` ไปแล้ว โดยไม่ต้อง rebuild เมือง**
2. **ระบบไล่แสงแบบนุ่มมีอยู่แล้ว** — `updateLights()` + `lightLerp` (smoothstep 2 วินาที) ที่ใช้ทำ day/night
   ⇒ ฤดูเพิ่มเป็นมิติที่ 2 ใน `lightTargets()` ได้เลย ไม่ต้องเขียนระบบใหม่
3. **CSS มี pattern ธีมแล้ว** — 113 rules ของ `night-mode` + ท้องฟ้า 2 เลเยอร์ crossfade

### 46.3 ❌ กับดักใหญ่ที่กำหนดขนาดงานจริง — **hex ไม่ใช่ semantic**

| ไฟล์ | บรรทัด | สีฮาร์ดโค้ด (`0x......`) |
|---|---|---|
| `js/house.js` | 11,669 | 1,248 |
| `js/house-furniture.js` | 1,863 | 673 |
| `js/house-models.js` | 984 | 371 |
| `js/house-map.js` | 1,626 | 312 |
| `js/house-avatar.js` | 1,077 | 105 |
| **รวม** | | **2,709 จุด · 513 สีไม่ซ้ำ · ไม่มี palette กลางเลย** |

เขียวหญ้า `0x8fd06c` ปรากฏ **16 จุด** แต่โค้ดไม่รู้ว่าจุดไหนคือหญ้า จุดไหนคือเสื้อเด็ก/หมวก/ถังขยะ
⇒ **remap สีที่ `toonMat()` ตรงๆ = เสื้อเขียวของเด็กจะกลายเป็นสีส้มตอนใบไม้ร่วงไปด้วย**

> 🎯 **งานหลักจริงๆ ของเฟสนี้ไม่ใช่ "วาดฤดู" แต่คือ "ติดป้ายความหมายให้สีก่อน"**
> เปลี่ยน `toonMat(0x66bb6a)` เป็น `toonMat(NAT.foliage)` **เฉพาะจุดที่เป็นของธรรมชาติจริง**

### 46.4 ประเมินขนาดงาน — 5 ชั้น

| ชั้น | เนื้องาน | แรง | ความเสี่ยง |
|---|---|---|---|
| **1. แสง + ท้องฟ้า + หมอก** | 4 ฤดู × 2 (วัน/คืน) = 8 ชุดค่าแสง · ใช้ `lightLerp` เดิมทั้งดุ้น | **~1 วัน** | ต่ำมาก |
| **2. สีธรรมชาติ** (ใบไม้/หญ้า/น้ำ/ดิน) | ทำ semantic layer + ไล่คัด 2,709 จุดว่าอันไหนเป็นของธรรมชาติ (~30-60 สี) | **3-5 วัน** | **สูงสุด** — แก้ผิดจุด = ของเด็กเปลี่ยนสีเอง |
| **3. object เปลี่ยนตามฤดู** | หิมะบนหลังคา · ใบไม้ร่วงลอย · ดอกไม้บาน · ของประดับตามเทศกาล | **5-8 วัน** | กระทบ draw call + `mergeDecorGroup` |
| **4. UI/CSS** | 4 ฤดู × 2 ธีม = **8 combo** · rules น่าจะโตจาก 113 → ~350 | **2-3 วัน** | กับดัก `@keyframes` override opacity ที่เคยเจอแล้ว |
| **5. ระบบ + ข้อมูล + เทส** | ฤดูมาจากไหน · `localStorage` + migration + export/import · **ชุดเทสโตขึ้น 4 เท่า** | **2-3 วัน** | กลาง |
| | | **รวม 13-20 วัน** | |

### 46.5 ⚠️ 3 คำถามที่ยังไม่ตัดสิน — ต้องถามผู้ใช้ก่อนลงมือ

1. **ฤดูมาจากอะไร** — วันที่จริงของเครื่อง (⚠️ ไทยมี 3 ฤดู ไม่ใช่ 4 · และ**เด็กจะไม่เห็นฤดูอื่นเลยเป็นเดือนๆ**) · "วันที่เข้าเล่น" แบบเดียวกับความหิวสัตว์ · หรือให้เด็กเลือกเอง
2. **draw call** — ใบไม้ร่วง/หิมะแบบ particle กินเฟรมเรตแน่ (บทเรียนเดิม: **เฟรมเรตในโหมดนี้ = จำนวน draw call ไม่ใช่จำนวนสามเหลี่ยม**)
3. **เปลี่ยนฤดูกลางเกม** — ถ้าต้อง rebuild ฉากจะค้าง 1-2 วินาที ⇒ ควรเปลี่ยนตอนเข้าโหมดบ้านเท่านั้นหรือเปล่า

### 46.6 🎯 ทางลด scope ที่คุ้มที่สุด (ถ้าตัดสินใจทำ)

- **ทำแค่ชั้น 1 + 2 ได้ความรู้สึก ~80% ด้วยแรง ~25% (4-6 วัน)** — โลกเปลี่ยนสีและแสงตามฤดู
  ซึ่งเป็นสิ่งที่เด็กสังเกตเห็นก่อนอย่างอื่นทั้งหมด
- ชั้น 3 ค่อยเลือกเฉพาะ **3-4 ชิ้นที่เห็นชัดที่สุด** (หิมะบนหลังคา · ใบไม้ร่วง · ดอกไม้บาน)
  **ไม่ต้องแตะเฟอร์นิเจอร์ 180 ชิ้น**

### 46.7 🔗 จุดที่เชื่อมกับแผนที่มีอยู่แล้ว

**เฟส 10 เกม A2 "ของหล่นตามฤดู"** (เก็บใบไม้/เปลือกหอย/ดาวตก · อยู่ใน `IDEA.md` แล้ว)
ตอนนี้เป็นแค่ธีมชื่อเฉยๆ — **ถ้ามีระบบฤดูจริง เกมนั้นจะมีความหมายขึ้นทันที**
⇒ ถ้าจะทำเฟส 12 แนะนำให้ทำคู่กับ A2 ไม่ใช่แยกกันคนละเฟส

---

## 47. ⚠️ สิ่งที่เฟส 10 + 11 ทำจริงต่างจากแผน (ตั้งใจ — อ่านก่อนทำเฟส 12)

> เสร็จทั้งคู่ 2026-08-13 · ทำต่อกันในรอบเดียวแล้ว push พร้อมกัน

### 47.1 เฟส 10 — ทำครบตามข้อ 45 ทุกข้อ ไม่มีอะไรตัดออก

| หัวข้อ | ทำจริง |
|---|---|
| `starsOf()` พลาดฟรี 1 ข้อ | `run.wrong <= 1` → 3 ดาว · เกณฑ์ 2 ดาวยังเป็นสัดส่วน ~2/3 เหมือนเดิม |
| `TIER_MUL` | `[_,1.00,1.05,1.05,1.10,1.15,1.15]` + `tierMul()` หนีบค่านอกช่วง · คูณ `STAR_BONUS`/`BOARD_BONUS` ด้วย |
| `secPerQ` | ติดป้าย `shape` ให้ทุกกลไกอัตโนมัติ (`card` 16 · `drag` 30 · `engine` 225 · `walk` คำนวณ) |
| สูตรทรงเดิน | `walkQCount(d, jitter)` + `secWalk(d)` — clamp 5-10 เสมอ |
| งานหลัก/รอง | `daySummary()` คืนธง `main` + `mainLeft`/`sideLeft` · UI แยก 3 กลุ่ม · ตัวนับ ❗ นับเฉพาะงานหลัก |

**ต่างจากแผน 2 จุด**

1. **`goalSets()` เพิ่มมาใหม่** (แผนไม่ได้ระบุชื่อฟังก์ชัน) — คืนจำนวนชุดที่ "เชียร์" ตามชั้น
   (เตรียม-ป.1 = 6 · ป.2-3 = 10 · ป.4-6 = 14) **เป็นคำแนะนำบนหน้าจอเท่านั้น ไม่ได้กั้นสิทธิ์ใคร**
2. **เกณฑ์เทส "ต่างกันไม่เกิน 15%" ต้องเทียบค่าที่ปัดเศษแล้ว ไม่ใช่อัตราส่วนดิบ** — เควสต์ NPC 1 ดาว
   ได้ 6 🪙 พอคูณ 1.15 แล้วปัดขึ้นเป็น 7 คิดเป็น 16.7% ทั้งที่ตัวคูณจริง 15% ⇒ อัตราส่วนที่แท้จริง
   ต้องวัดที่ระดับ **เงินรวมต่อวัน** (ซึ่งเทส 10B คุมไว้แล้ว)

### 47.2 เฟส 11 — 5 เกมกลุ่ม A อยู่ใน `js/house-play.js` (ไฟล์ใหม่)

**ทางเข้าเดียว = ปุ่ม 🎈 "เล่น" (`#house-play-btn`) + แผง `#house-playpanel`**
⚠ **ตั้งใจให้เป็นปุ่มเดียว ไม่ใช่ 5 ทางเข้ากระจาย** — HUD ในโหมดบ้านต้องแน่นเสมอ (กติกา 2026-08-09)
ปุ่มอยู่แถวเดียวกับแถบสรุปเควสต์ สูงเท่ากันเป๊ะ และ **โผล่เฉพาะฉากนอกบ้าน**

**ประตูใหม่ `window.HouseWorld`** — `house.js` ห่อด้วย IIFE ⇒ ไฟล์ใหม่แตะภายในไม่ได้
เปิดเฉพาะที่จำเป็น: กริด/พิกัด/`pathLen` (findPath จริง) · `spawn`/`despawn` · `kit()` ชิ้นส่วน 3D ·
`load`/`save` · `award` · `toast` · `refreshHud` · `inHomeZone`/`lotAt`/`zoneName`
⚠ **ห้ามเปิด `worldGroup`/`scene` ดิบๆ ออกไป** ไม่งั้นการแยกไฟล์ไร้ความหมาย

| เกม | ทำจริง | ต่างจากแผน |
|---|---|---|
| 🙈 ซ่อนแอบ | 3-5 คน (จำนวนมาจาก `walkQCount` ของเฟส 10) · ชิป "ร้อน/เย็น" 4 ระดับบน HUD · 1 ชุด/วัน · จ่ายเท่าเควสต์ NPC 3 ดาว | แผนบอก 3 คนตายตัว — ใช้สูตรทรงเดินแทนเพื่อคุมเวลาเล่นตามงบ |
| 🍃 ของหล่นตามฤดู | 8 ชิ้น/วัน seeded · ธีมหมุนตามเดือน (ใบไม้/เปลือกหอย/ดาวตก/กลีบดอกไม้) · **ไม่จ่ายเหรียญ** · เก็บครบ 3/7/12 วัน = ปลดของแต่งบ้านฟรี | ใช้ของที่มีอยู่ในคลัง (`birdhouse`/`fountain`/`gnome`) ผ่าน `HouseShop.grantFree()` ใหม่ **ไม่ได้วาดของใหม่** |
| 🎣 ตกปลา | ปลา 15 ชนิด 3 ระดับความหายาก + รองเท้าบูตเก่า · ต้องยืนติดน้ำ (ดูจากกริดจริง) · ดึงไม่ทัน = ปลาหนี ไม่หักอะไร | **ยังไม่ได้ทำ "ปลาว่ายในตู้ปลา"** — เก็บลงสมุดปลาอย่างเดียวก่อน (ตู้ปลาไม่ใช่เงื่อนไข ⇒ ไม่มี dead end) |
| 📷 ช่างภาพ | ใบสั่ง 4 แบบ/วัน · เก็บได้ 6 รูป ย่อกว้าง 160px jpeg คุณภาพ .72 | ⭐ **ไม่ต้องเปิด `preserveDrawingBuffer` เลย** — ใช้ `window.__housePaint()` สั่งวาด 1 เฟรมแล้ว `toDataURL()` **ในจังหวะเดียวกัน** ก่อนบัฟเฟอร์ถูกเคลียร์ ⇒ ได้ภาพครบโดยไม่แลกเฟรมเรต (ความเสี่ยงที่แผนกลัวไว้หายไปทั้งก้อน) |
| 🌱 แปลงผัก | 4 แปลง · ผัก 4 ชนิด · โต 3 ขั้นตาม "วันที่เข้าเล่น" · ลืมรดน้ำ = ไม่โตวันนั้น **ไม่ตาย** · เก็บขายได้ 2-3 🪙/ต้น | **เมล็ดยังไม่ต้องซื้อ** (แผนบอกให้ซื้อที่ร้านต้นไม้) — เด็กใหม่มี 0 เหรียญ ถ้าบังคับซื้อคือ dead end ตั้งแต่วันแรก |

**ระลอก 2 — เปิดตาราง `qN` ใหม่แล้ว** (`Q_CARD` 9-12 · `Q_DRAG` 6-8 ตามชั้น)
`buildRun()` เลือกตารางจาก **ทรงของกลไก** ไม่ใช่สุ่ม 5-10 แบบเดิมอีกต่อไป
⚠ **มติ 2026-08-10 ที่ว่า "จำนวนข้อสุ่มได้ ไม่ต้องเท่ากัน" ถูกทับแล้ว** — การสุ่มทำให้เควสต์เดียวกัน
บางวัน 80 วิ บางวัน 160 วิ คุมงบเวลาไม่ได้เลย

### 47.3 🐞 กับดักที่เจอจริงตอนทำ (อ่านก่อนทำเฟส 12)

- ⚠️ **`seekStart()` ต้องกัน "เริ่มซ้ำ" ตอนที่ยังเล่นค้างอยู่ด้วย ไม่ใช่เช็คแค่ `done`** — กดปุ่มซ้ำ
  จะสุ่มจุดแอบใหม่ทั้งชุด คนที่เด็กอุตส่าห์เดินไปเจอมาแล้วหายเกลี้ยง (เทสจับได้)
- ⚠️ **เทสที่รอ "จังหวะในเกม" ต้องเผื่อเวลาเยอะกว่าที่คิดมาก** — engine clamp `dt` ที่ 50ms/เฟรม
  ⇒ ตอนเฟรมตก เวลาในเกมเดินช้ากว่านาฬิกาจริงหลายเท่า (รอปลากิน 1.2-3.4 วิในเกม กินจริง 20+ วิ)
  กับดักเดียวกับเทสอนิเมชันป้อนอาหารของเฟส 3B ที่ต้องใช้ timeout 25 วิ
- ⚠️ **ของกลุ่ม A ต้องแขวนกับ `worldGroup` ผ่าน `spawn()` เท่านั้น** และถูกเก็บกวาดอัตโนมัติที่
  `clearPlayObjs()` ตอนออกจากบ้าน/สลับฉาก — ถ้าไป `scene.add()` เองจะค้างข้ามรอบการเล่น
- ⚠️ **tag `userData.hPick` ต้องถูกเช็คเป็นอันดับแรกใน `handleTap()`** — ของกลุ่ม A เป็นชิ้นเล็ก
  วางบนพื้น ถ้าไปอยู่ท้ายแถวจะถูกของฉากที่ใหญ่กว่าบังจนแตะไม่โดน
- 🧪 เทสอยู่ที่ `tests/house-phase11.spec.js` (11 เทส × 2 จอ) + `tests/house-phase10.spec.js` (8 × 2)

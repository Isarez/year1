# ระบบเควสต์ NPC ในโหมด "บ้านของหนู" — เอกสารออกแบบ

> สถานะ: **เฟส 1 ทำเสร็จแล้ว (2026-08-07)** — เศรษฐกิจ/ร้านค้า/migration/ชุดเริ่มต้น อยู่บน branch แล้ว · เฟสถัดไป = **เฟส 2 (quest engine)**
> เริ่มร่าง 2026-08-06 · ปรับข้อเสนอ + ล็อกทุกข้อ 2026-08-07 · branch `feature/house-owl`
> เอกสารนี้คือที่เก็บการออกแบบก่อนลงมือ ไม่ใช่ changelog — เมื่อทำจริงแล้วให้ย้ายสรุปไป `LOG.md`/`CLAUDE.md` ตามปกติ
> 📄 **ฉบับอ่านง่าย (หน้าเว็บ): [`research/quest-design.html`](research/quest-design.html)** — เนื้อหาเดียวกับไฟล์นี้ จัดหน้าเป็นตาราง/ผังให้อ่านสบายตา ไฟล์เดียวจบเปิดด้วยเบราว์เซอร์ได้เลย **แก้ไฟล์ไหนต้องแก้อีกไฟล์ให้ตรงกันเสมอ** (ไฟล์ `.md` นี้คือฉบับหลักที่ session อื่นอ่าน)

---

# ⭐ สรุปบริบท — อ่านตรงนี้ก่อนเสมอ

> **ทุก session ที่จะแตะโหมด "บ้านของหนู" ต้องอ่านหัวข้อนี้ให้จบก่อนลงมือ** ส่วนที่เหลือของไฟล์คือรายละเอียดรายหัวข้อ เปิดอ่านเฉพาะเวลาทำเฟสนั้นๆ

## สรุประบบใน 5 บรรทัด

1. โหมดบ้านวันนี้เป็น **เมือง 3D ที่เดินเล่นได้ แต่ยังไม่มีอะไรให้ทำ** — ของทุกอย่างแจกฟรี เงิน 🪙 มีระบบแต่ไม่มีที่หา/ที่ใช้
2. แผนนี้เปลี่ยนเป็นวงจร **เล่นเควสต์ → ได้ดาว → แปลงเป็นเหรียญ → ซื้อของ/สัตว์/อาหาร → มีเหตุให้กลับมาเล่นอีก**
3. เควสต์มาจาก **3 แหล่งแยกโควตากัน**: NPC ในเมือง (57 ตัว) · กระดานเควสต์ 5 ชุด/วัน · พ่อ-แม่ในบ้าน 1 ชุด/วัน
4. เนื้อเควสต์สุ่มจาก **41 mechanic** (~3,500 โจทย์) โดย `quiz` ตอบคำถามพื้นฐาน **ต้องมีในทุก NPC ที่ทำเควสต์ได้เสมอ**
5. ทำเป็น **9 เฟส** แต่ละเฟสจบแล้วเล่นได้จริงไม่พัง ⇒ merge ทีละเฟสได้

## กติกาเหล็ก — ห้ามละเมิดทุกข้อ ไม่ว่าจะทำเฟสไหน

| # | กติกา |
|---|---|
| 1 | **ห้ามมี dead end** ทุกสถานการณ์ต้องมีทางไปต่อ (เงินไม่พอค่ารักษา → หมอให้เควสต์ทำแทน) |
| 2 | **ห้ามลงโทษเด็ก** ไม่หักเงิน ไม่หักดาว ไม่มีคำว่า "แพ้" ไม่มีตัวจับเวลากดดัน ไม่มีสัตว์เลี้ยงตาย |
| 3 | **ห้ามทำข้อมูลเก่าหาย** เด็กที่เล่นอยู่แล้วต้องได้ของเดิมครบทุกชิ้นหลังอัปเดต (migration มาก่อนโค้ดร้านเสมอ) |
| 4 | **ห้ามแตะหน้าหลัก** สมุดสติกเกอร์ / ปุ่มนกฮูก / `progress.stickers` อยู่ที่เดิม ระบบนี้อยู่ในโหมดบ้านล้วน |
| 5 | **เงินต้องผ่าน `window.OwlCoins` เท่านั้น** ห้ามเขียน `progress.coins` ตรงๆ |
| 6 | **ไม่มี build step / ไม่มี CDN / ไม่มี backend** ทุกอย่าง static + `localStorage` ตาม `CLAUDE.md` |
| 7 | **ก่อน merge เข้า `main` ต้องถามผู้ใช้ทุกครั้ง** ว่าจะเปิดโหมดบ้านให้เด็กเล่นจริง หรือคง `HOUSE_FEATURE_OFF` ไว้ |

## ข้อที่ผู้ใช้ตัดสินแล้ว — ล็อก ห้ามเปลี่ยนเองโดยไม่ถาม

| หัวข้อ | ที่ตกลง |
|---|---|
| ราคา / ค่าตอบแทน | ใช้ตัวเลขในข้อ 17 + 19 ทั้งหมด |
| จำนวนสัตว์เลี้ยง | เลี้ยงได้ **1 ตัว** เหมือนเดิม · ซื้อตัวใหม่ทีหลังจ่ายครึ่งเดียว |
| ความหิว | ลดตาม **"วันที่เข้าเล่น"** (−35/วันเล่น) ไม่ใช่เวลาจริง |
| สัตว์ป่วย | ไม่ตาย · นอนในบ้านออกมาไม่ได้ · **หายได้ทางเดียวคือคุยหมอแล้วจ่ายเงิน** |
| โจทย์ระดับสูงกว่าชั้น | ยังมี แต่ต้องผ่าน**ประตูเช็คความพร้อม** (ข้อ 24) และเด็กกดตอบรับเอง |
| ชุดเฟอร์นิเจอร์เริ่มต้น | **11 ชิ้นตามข้อ 26** วางในบ้านให้เรียบร้อยตั้งแต่เปิดครั้งแรก (ให้น้อยๆ ที่เหลือซื้อเอง) |
| พ่อ-แม่ในบ้าน | **มี default มาให้ทั้งหน้าตาและชื่อ** แต่เด็ก**แก้ชื่อและแต่งตัวใหม่เองได้ทั้งหมด** |
| กระดานเควสต์ | 1 จุด · **5 ชุด/วัน** · โควตาแยกจากเควสต์ NPC เด็ดขาด |
| เครื่องดนตรี | เป็น**เฟสสุดท้าย (เฟส 9)** |

## ตัวเลขสรุป (นับจากโค้ดจริง ไม่ใช่ประมาณ)

| | วันนี้ | เป้าหมาย |
|---|---|---|
| NPC ในเมือง | 69 ตัว (มีบท `quest:` แล้ว 58) | เท่าเดิม แต่ทำเควสต์ได้จริงทุกตัว |
| mechanic เล่นจริง | 12 (ภารกิจเดินสำรวจ ปิดอยู่) | **41** × 3 tier = 123 แม่แบบ ⇒ **~3,500 โจทย์** |
| เฟอร์นิเจอร์ | 116 ชิ้น / 10 หมวด (แจกฟรี) | **180 ชิ้น / 11 หมวด** (ต้องซื้อทั้งหมด) |
| ของแต่งตัว | 56 แบบ (แจกฟรี ปุ่มเป็นตัวเลข) | **106 แบบ** (ต้องซื้อ ปุ่มเป็นภาพ SVG) |
| สัตว์เลี้ยง | 12 ชนิด (เลือกฟรี) | 12 ชนิด (ซื้อ + ต้องเลี้ยงจริง) |
| เงิน 🪙 | มีระบบแต่ไม่มีที่หา/ที่ใช้ | ได้จากเควสต์ · ใช้กับทุกร้าน |

## ลำดับเฟส (ย่อ — เต็มอยู่ข้อ 32)

`1` เศรษฐกิจ+migration+ชุดเริ่มต้น+ร้าน 2 ร้าน → `2` quest engine+quiz+กระดาน+ประตูความพร้อม → `3` สัตว์เลี้ยงครบวงจร → `4` พ่อแม่+เควสต์ครอบครัว → `5` mechanic กลุ่ม C (รื้อ engine เดิม) → `6` แล็บ STEM + coding → `7` mechanic กลุ่ม B+D → `8` เฟอร์นิเจอร์/ของแต่งตัว/ไอคอน SVG → `9` เครื่องดนตรี

**เฟสปัจจุบัน: เฟส 1 เสร็จแล้ว → เฟสถัดไปคือเฟส 2 (quest engine + `quiz` ครบ 57 NPC + กระดาน 5/วัน + ประตูเช็คความพร้อม)**

> ⚠️ **เฟส 1 อย่างเดียวยังไม่มีที่หาเงิน** — ของทุกอย่างถูกล็อกให้ต้องซื้อแล้ว แต่แหล่งรายได้ (เควสต์) มาในเฟส 2
> ⇒ **ห้าม merge เฟส 1 ขึ้น production เดี่ยวๆ ถ้าจะเปิดโหมดบ้านให้เด็กเล่นจริง** ต้องรอเฟส 2 คู่กันเสมอ
> (ถ้ายังคง `HOUSE_FEATURE_OFF` ไว้เหมือนเดิม merge ได้ตามปกติ ไม่กระทบเด็ก)

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | บทบาท |
|---|---|
| `js/house.js` (~10,200 บรรทัด) | engine ทั้งหมด — `QUEST_POOL` 7230 · `QUEST_ENABLED` 7249 · `PET_TYPES` 8210 · `seedWorldDecor` 8944 · `buildCreatorRows` ~6936 |
| `js/house-map.js` | ผังเมือง · `NPC_DEFS`/`NPCS` (ธง `shop:` = พนักงานที่เปิดร้านให้) · ค่าคงที่ของแต่งตัว `H_*` · `PET_HOUSE_TILE` |
| `js/house-furniture.js` | คลังเฟอร์นิเจอร์ 116 ชิ้น |
| **`js/house-shop.js`** ✅ **มีแล้ว (เฟส 1)** | ตารางราคา · คลังสิทธิ์ `data.unlocked` · migration `econVer` · ชุดเริ่มต้น · หน้าร้าน — เรียกผ่าน `window.HouseShop` |
| `js/app-core.js` | `window.OwlCoins` (~600-628) · `window.APP_ASSET_VER` (cache-buster ของไฟล์ lazy) |
| **ไฟล์ใหม่ที่ยังต้องสร้าง** | `js/house-quests.js` (เฟส 2) · `js/house-pet-care.js` (เฟส 3) — ดูข้อ 20 |

## สิ่งที่มีอยู่แล้วในโค้ด อย่าเขียนซ้ำ

- `pickDailyQuests()` — seeded RNG (FNV-1a + LCG) **ใช้เป็นต้นแบบการสุ่มรายวันทุกที่** (รวมถึงสุ่มว่าวันนี้พ่อหรือแม่ให้เควสต์)
- `questEvent(kind,key)` + `quest.seen` — ระบบบันทึกความคืบหน้า 7 ชนิด (talk/vendor/zone/flower/critter/pet/sit) พร้อมกันนับซ้ำ
- กระดานภารกิจข้างน้ำพุ `#house-quest` / `#hq-list` / `#hq-claim` / `#hq-stars` — **UI ทำเสร็จแล้ว** แค่ยังไม่ผูกกับ 5 ชุด/วัน
- ป้ายยอดเหรียญ 🪙 ในโหมดบ้าน `#house-coins` — แสดงผลได้แล้ว
- **ร้านเครื่องดนตรี `shop-music` + พี่โน้ต `npc-musicshop` 🎸 สร้างไว้แล้วเมื่อ 2026-08-06** พร้อมบท quest "ช่วยพี่ทายเสียงเครื่องดนตรีหน่อยไหม" ⇒ เฟส 9 มีร้านรออยู่แล้ว ไม่ต้องสร้างใหม่
- `PET_HOUSE_TILE` + item `pet-house` (`action:'pethouse'`) — บ้านสัตว์มีโมเดลแล้ว แค่ต้องย้ายจุด seed ไปตอนซื้อ

---

## 0. โจทย์ตั้งต้นจากผู้ใช้

> "ใส่โจทย์ที่ทำต่างๆ ลงในเกมบ้านของหนูให้เป็น quest ตาม npc ร้านต่างๆ และให้เกมและ mechanic เกม
> เหมาะสมกับวัย ระดับชั้น และร้านแต่ละร้าน เมื่อทำเสร็จให้รางวัลเป็นเงินตอบแทนตามระดับดาวที่เหมาะสม
> ให้โจทย์เป็นการสุ่มทั้งหมดว่าเจอ quest แบบไหน mechanic หรือเกมแบบไหน ... ให้มี mechanic เกมใหม่ๆ ได้
> ออกแบบให้เหมาะสมกับโลกในเกม"

ข้อกำหนดที่ถอดออกมาได้ 6 ข้อ:
1. เควสต์ผูกกับ **NPC ตามร้าน** (ไม่ใช่กระดานกลางอย่างเดียว)
2. กลไกเกม **ต้องเข้ากับธีมของร้านนั้นๆ**
3. ความยาก **ปรับตามวัย/ระดับชั้นของเด็ก**
4. รางวัลเป็น **เงิน** ตาม **ระดับดาว**
5. **สุ่มทั้งหมด** — สุ่มว่าเจอเควสต์แบบไหน กลไกแบบไหน
6. เพิ่ม **กลไกใหม่** ได้ ต้องกลมกลืนกับโลก 3D เดิม

---

## 1. สำรวจของเดิมในโค้ด (ฐานที่ต่อยอดได้)

| สิ่งที่มีอยู่แล้ว | ตำแหน่ง | สถานะปัจจุบัน |
|---|---|---|
| `QUEST_POOL` (12 ภารกิจ) + `QUEST_BY_ID` + `QUEST_N = 3` | `js/house.js:7230-7246` | เขียนครบ **แต่ปิดอยู่** |
| `QUEST_ENABLED = false` | `js/house.js:7249` | **สวิตช์เปิดระบบ** |
| `questDayKey()` — คีย์ราย (เด็ก, วัน) | `js/house.js:~7251` | ใช้ได้ |
| `pickDailyQuests(day)` — seeded RNG (FNV-1a + LCG) | `js/house.js:~7256-7264` | ใช้ได้ **เป็นต้นแบบการสุ่ม** |
| `questEvent(kind, key)` — บันทึกความคืบหน้า + กันนับซ้ำด้วย `quest.seen` | `js/house.js:7287-7302` | ใช้ได้ |
| จุดที่เรียก `questEvent` แล้ว | `7080` flower · `7209` talk · `7210` vendor · `7329` zone · `7664` critter · `8457` pet · `9334/9437` sit | ครบ 7 ชนิด |
| `questZonesAt(x,z)` — โซน beach/farm/plaza2/school/pond/bridge | `js/house.js:~7310-7318` | ใช้ได้ |
| กระดานภารกิจข้างน้ำพุ `#house-quest` / `#hq-list` / `#hq-claim` / `#hq-stars` | `js/house.js:7330-7400` + `index.html` | UI ทำเสร็จแล้ว |
| `quest.stars` (ดาวสะสม) | เก็บใน house save key `quest` | มีแล้ว **แต่ยังไม่แปลงเป็นเหรียญ** |
| `window.OwlCoins = {get,set,add,spend}` + event `owlcoins` | `js/app-core.js:~600-628` | มีแล้ว **ยังไม่มีที่หา/ที่ใช้เงิน** |
| ป้ายยอดเหรียญ 🪙 ในโหมดบ้าน | `js/house.js:8780-8789` | แสดงผลได้แล้ว |
| ฟิลด์ `quest:` ใน NPC | `js/house-map.js` (หลาย NPC) | ตอนนี้เป็นแค่ **บทพูดบรรทัดพิเศษ** (`js/house.js:7194`) |
| `activeGradeId(child)` / `childAgeYears(child)` / `GRADES` | `js/app-core.js:48-102` | เรียกจาก `house.js` ได้ (global scope เดียวกัน) |
| `CATS` (คำถามควิซ 332 หมวด แยกตามชั้น) | `js/data-cats-*.js` | **ดึงมาใช้เป็นโจทย์โรงเรียนได้ฟรี** |
| `lotDoorTile(lot)` / `LOT_BY_ID` / `VILLAGE_LOTS` | `js/house-map.js` | ผูก NPC กับร้านได้แล้ว |
| `signPlane(icon,size)` + `userData.anim` + `spawnParticle()` | `js/house.js` | ใช้ทำป้าย "!" ลอยเหนือหัวได้เลย |

**สรุป: โครงกระดูกมีแล้ว ~70% ที่ขาดคือ "โจทย์จริง" + "เกมย่อย" + "ที่ใช้เงิน"**

### ของเดิมใน `QUEST_POOL` (12 ข้อ — เป็นภารกิจ "เดินสำรวจ" ล้วน)
```
talk3   💬 ทักทายชาวบ้าน 3 คน          (kind:'talk',  goal:3)
shop2   🛍️ ทักทายแม่ค้าหน้าร้าน 2 คน   (kind:'vendor', goal:2)
sit1    🪑 นั่งพัก 1 ครั้ง              (kind:'sit')
pet3    🐾 ลูบสัตว์ 3 ครั้ง             (kind:'pet')
flower4 🌸 เก็บดอกไม้ 4 ดอก            (kind:'flower')
critter2🐿️ ไล่จับสัตว์เล็ก 2 ตัว        (kind:'critter')
+ zone: beach / farm / plaza2 / school / pond / bridge (kind:'zone')
```
> ของชุดนี้ **เก็บไว้ทั้งหมด** — จะกลายเป็น "ภารกิจประจำวันของผู้ใหญ่บ้าน" (layer กลาง)
> ส่วนเควสต์ NPC ที่จะทำใหม่เป็นอีก layer ที่ซ้อนทับ ไม่ทับกัน

---

## 2. ภาพรวมระบบ — "ป้ายงานหน้าร้าน" (Shop Quest)

**แนวคิดหลัก:** ทุกร้าน/สถานที่มี NPC เจ้าของ 1 คน และ NPC นั้นมี "งานวันนี้" สุ่มมา 1 งาน
เด็กเดินไปคุย → รับงาน → เล่นเกมย่อย → ได้ดาว 1-3 ดวง → แปลงเป็นเหรียญนกฮูก 🪙 ทันที

```
เดินเข้าใกล้ร้าน
      │  NPC ที่มีงาน: ป้าย "!" สีทองลอยเหนือหัว เด้งขึ้นลง (+ เรืองแสงอ่อนตอนกลางคืน)
      ↓ แตะ NPC
┌─────────────────────────────────────────┐
│ 🎸 พี่โน้ต                                │
│ "ช่วยพี่ทายเสียงเครื่องดนตรีหน่อยไหม"        │
│      [ รับงาน ]      [ ไว้ก่อน ]          │
└─────────────────────────────────────────┘
      ↓
overlay เกมย่อย — การ์ดครีมมนๆ ลอยกลางจอ (ยังเห็นร้านข้างหลัง)
      ↓
┌─────────────────────────────────────────┐
│         ⭐ ⭐ ⭐   เก่งมาก!                │
│         + 20 🪙                          │
└─────────────────────────────────────────┘
  เหรียญบินเข้าป้ายยอดเงิน + confetti + playCongrats()
      ↓
NPC เปลี่ยนป้ายเป็น "✓" (พักงาน) → พรุ่งนี้สุ่มงานใหม่
```

**จุดขายที่หน้าหลักทำไม่ได้:** เควสต์บางชนิดต้อง **ออกไปเดินในโลกจริง** (ส่งของ ตามหา ซื้อของ)
ไม่ใช่แค่ตอบคำถามใน popup — อันนี้คือเหตุผลที่โหมดบ้านควรมีเควสต์เป็นของตัวเอง

---

## 3. เควสต์ 3 ตระกูล

### 🟦 A — "งานในร้าน" (in-shop mini-game)
- เล่นจบในที่เดียว 30-60 วินาที เหมาะกับสมาธิเด็กเล็ก
- overlay 2D ทับฉาก 3D (สไตล์เดียวกับกล่องเลือกเฟอร์นิเจอร์)
- **เป็นตระกูลหลัก** ~60% ของเควสต์ทั้งหมด

### 🟩 B — "งานเดินโลก" (world quest) ← *ของใหม่ ยังไม่มีในเกมเลย*
ต้องเดินไปที่อื่นในแผนที่ ใช้ `questEvent()` เดิมจับความคืบหน้าได้

| ชื่อ | รายละเอียด | ทักษะ |
|---|---|---|
| **ส่งของถึงมือ** | รับกล่องจาก NPC A → ไปส่ง NPC B โดยบอกแค่ **คำใบ้** ("คนใส่หมวกกันน็อก อยู่ใกล้บ่อน้ำใหญ่ทางทิศเหนือ") ป.1-2 มีลูกศรจางๆ ช่วย ป.5-6 ไม่มี | อ่าน, สังเกต, ทิศทาง |
| **รายการซื้อของ** | ป้าจ่ายตลาดโชว์รายการ 3-5 อย่าง 5 วินาทีแล้วซ่อน → เดินไปซื้อจากรถเข็นให้ครบตามจำนวน | working memory |
| **ตามหาแมวหาย** | ตำรวจให้ตามหาสัตว์/ของที่ซ่อนในแผนที่ มีมิเตอร์ "ร้อน/เย็น" ตามระยะ | สังเกต, ระยะทาง |
| **เก็บของให้ครบ** | เก็บเปลือกหอย 7 ชิ้นที่ชายหาด / ดอกไม้ 5 ดอก (ต่อยอด `questEvent('flower')` ที่มีอยู่) | นับ |
| **พาแขกไปส่ง** | NPC เดินตามเด็กไปยังจุดหมาย (escort) | นำทาง |

### 🟨 C — "งานสร้างสรรค์" (ไม่มีถูกผิด ได้ ⭐⭐⭐ เสมอ)
กันเด็กท้อ + ทำให้เงินมีปลายทางที่มีความหมาย
- ระบายสีป้ายร้านให้ NPC
- จัดตู้โชว์ / จัดโต๊ะ
- **แต่งตัวตามธีมที่ NPC ขอ** (ใช้ระบบแต่งตัวที่ทำเสร็จแล้ว)

**สัดส่วนที่แนะนำ: A 50% / B 30% / C 20%**

---

## 4. ผังร้าน → กลไกเกม

> ธีมกลไกต้องตรงกับร้าน ไม่งั้นเด็กจะรู้สึกว่าเป็นควิซสุ่มมาแปะ

| ร้าน / สถานที่ | lot id | NPC | กลไกที่เสนอ | ทักษะ |
|---|---|---|---|---|
| 🏪 ร้านสะดวกซื้อ | `shop-mart` | พี่นวล | **จ่ายเงินให้พอดี** (ลากเหรียญ/แบงก์ใส่ถาด), ทอนเงิน, นับของขึ้นชั้น | เงิน, บวกลบ |
| 🐾 ร้านสัตว์เลี้ยง | `shop-pet` | พี่ปุย | **จับคู่สัตว์-อาหาร-บ้าน**, นับขาสัตว์รวม, จำแนกสัตว์ | จับคู่, นับ |
| 🍜 ร้านอาหาร | `shop-food` | ป้าอิ่ม | **ทำตามสูตร** — ลากส่วนผสมตาม *ลำดับ* + *จำนวน* ที่ลูกค้าสั่ง | ลำดับ, ความจำ |
| 🍨 ไอศกรีม | `shop-food` | พี่ฟ้า | **เรียงสกู๊ปตามแพทเทิร์น**, แบ่งเค้กเป็นเศษส่วน | แพทเทิร์น, เศษส่วน |
| 🎸 ร้านเครื่องดนตรี | `shop-music` | พี่โน้ต | **เล่นตามทำนอง** (Simon says 5 คีย์ ยาวขึ้นเรื่อยๆ), ทายเสียงเครื่องดนตรี | ฟัง, ความจำ |
| 🏥 โรงพยาบาล | `hospital` | คุณหมอ / พยาบาล | **เรียงขั้นตอนดูแลตัวเอง**, จับคู่อาการ-วิธีแก้ | ลำดับ, สุขอนามัย |
| 🚓 สถานีตำรวจ | `police-station` | ตำรวจ | **ตามหาของ/คนหาย** (world quest), จำแนกลักษณะจากคำอธิบาย | สังเกต, ทิศทาง |
| 🏫 โรงเรียน | `school` | ครูอ้อย | **ดึงคำถามจริงจาก `CATS` ตามระดับชั้นเด็ก** 3 ข้อ | ตรงหลักสูตร |
| 🔬 ตึกแล็บ | (แล็บ ชุมชน 2) | ดร.แล็บ 1-3 | **เรียงขั้นตอนการทดลอง**, จม-ลอย, แยกหมวด | วิทยาศาสตร์ |
| 🛋️ ห้างเฟอร์นิเจอร์ | `mall-furniture` | พี่โซฟา | **จัดห้องตามแบบ** (ดูรูป 5 วิ แล้ววางตาม) | มิติสัมพันธ์ |
| 👗 ห้างแฟชั่น | `mall-fashion` | พี่ชุดสวย | **แต่งตัวตามโจทย์** ("ชุดไปทะเล สีฟ้า มีหมวก") | จำแนก, สี |
| 🍉 ตลาดรถเข็น | `MARKET_CARTS` | 12 แม่ค้า | **รายการซื้อของ** (world quest), ตวง/ชั่งให้ได้น้ำหนัก | ความจำ, ตาชั่ง |
| 🌾🐄 นา / โรงนา | `farm-house` `farm-barn` | ลุงชาวนา, คาวบอย | **นับผลผลิต/แบ่งเท่าๆ กัน**, รดน้ำตามตาราง | หาร, ปฏิทิน |
| 🎣 บ่อน้ำ | `pond-hut` | ลุงตกปลา | **ตกปลาตอบเลข** — ปลาแต่ละตัวมีเลข ตกตัวที่ผลลัพธ์ถูก | คิดเลขเร็ว |
| 🏨 โรงแรม | `hotel` | พนักงาน 2 คน | **พาแขกไปห้องตามเลข**, เรียงคิวเช็คอิน | เลขหลัก, เรียงลำดับ |
| 🪚 ช่างไม้ | `carpenter-hut` | ลุงช่างไม้, ป้ามะลิ | **ต่อรูปทรง / แทนแกรม** | รูปทรง |
| ⛺ ลานแคมป์ | (แคมป์กลางป่า) | หัวหน้าแคมป์ | **เรียงขั้นตอนก่อไฟ/กางเต็นท์** | ลำดับ |
| 🏖️ ชายหาด | (โซน beach) | พี่ชายหาด | **เก็บเปลือกหอยให้ครบ** (world quest), เรียงขนาด | นับ, เปรียบเทียบ |
| 🏛️📜 บ้านเทศมนตรี / ผู้ใหญ่บ้าน | `mayor-house` | ท่านเทศมนตรี, ผู้ใหญ่บ้าน | **กระดานรวม** — สรุปงานทั้งเมือง + โบนัสตอนครบ | (hub เดิม) |

รวม ~19 จุด × สุ่มกลไก × สุ่มพารามิเตอร์ = โจทย์ไม่ซ้ำหลายร้อยแบบ

---

## 5. ความยากตามระดับชั้น (ป.1 → ป.6)

**หลักการ: ไม่ทำเกมคนละชุดต่อชั้น** (บำรุงรักษาไม่ไหว) แต่ให้กลไกเดียวรับพารามิเตอร์ความยาก

```js
/* แนวคิด — ยังไม่เขียนจริง */
questDifficulty(gradeId) → {
  tier,       // 1..6 จาก GRADES index (activeGradeId(activeChild))
  numMax,     // ป.1: 1-10 · ป.2: 1-20 · ป.3: 1-100 · ป.4: 1-1000 · ป.5-6: ทศนิยม/เศษส่วน
  seqLen,     // ความยาวลำดับที่ต้องจำ: 3 → 7
  steps,      // จำนวนขั้นตอนของโจทย์: 1 → 3
  timeLimit,  // ป.1-2 = ไม่มีเลย · ป.3-4 = ผ่อนปรน · ป.5-6 = มี (แบบไม่กดดัน)
  hints       // ป.1-2 = ลูกศร/ไฮไลต์ช่วย · ป.5-6 = ไม่มี
}
```

### กติกาที่ยึด (ห้ามฝ่าฝืน)
- **ป.1-2** — ไม่มีเวลาจำกัด ไม่มีคำว่า "ผิด" พลาดแล้วลองใหม่ได้ไม่จำกัด (แค่ดาวลดลง)
- **ป.3-4** — มีเวลาแบบผ่อนปรน + โจทย์ 2 ขั้นตอน
- **ป.5-6** — โจทย์หลายขั้น มีการวางแผน เดินโลกไกลขึ้น
- **ร้านบางร้านล็อกตามชั้น** (เช่น ตึกแล็บ/โรงแรม เปิดที่ ป.3+) → โลกโตตามเด็ก ทำให้กลับมาเล่นอีก
- ระดับชั้นอ่านจาก `activeGradeId(activeChild)` **ไม่ใช่จากอายุตรงๆ** (เด็กเลือกชั้นเองได้ในหน้าหลัก)

---

## 6. ดาว → เหรียญ (สูตรรางวัล)

```
⭐     = ทำสำเร็จ (ไม่ว่าพยายามกี่ครั้ง)   ← ทุกคนได้เงินเสมอ กันเด็กท้อ
⭐⭐   = ผิดไม่เกิน 1-2 ครั้ง
⭐⭐⭐ = ผ่านรวดเดียว

เหรียญ = base(ชนิดเควสต์) × mulStar × mulTier

  base:     งานในร้าน (A) = 5
            งานเดินโลก (B) = 10
            งานสร้างสรรค์ (C) = 6
  mulStar:  ⭐ = 1.0 · ⭐⭐ = 1.5 · ⭐⭐⭐ = 2.0
  mulTier:  ป.1 = 1.0 → ป.6 = 1.5 (ยากขึ้นควรได้มากขึ้น)

โบนัสทำครบทุกร้านที่มีงานในวันเดียว: +50 🪙 รับที่กระดานผู้ใหญ่บ้าน (ต่อยอดปุ่ม #hq-claim เดิม)
```

### เป้าหมายการจูนเศรษฐกิจ
- เล่นวันละ ~15 นาที → ได้ ~60-100 🪙
- ของถูกสุดในร้าน ~50 🪙 (ได้ของใหม่เกือบทุกวัน — feedback loop สั้น)
- ของแพงสุด ~800 🪙 (เป้าหมายระยะยาว ~2 สัปดาห์)
- **เพดานเงินต่อวัน** ต้องมีตั้งแต่แรก (เก็บที่ `shopQuest.earnedToday`) ไม่งั้นจูนราคาทีหลังไม่ได้

---

## 7. การสุ่ม (ตามข้อกำหนด "สุ่มทั้งหมด")

ใช้แนวเดียวกับ `pickDailyQuests()` เดิม คือ **seeded RNG ไม่ใช่ `Math.random()` ตรงๆ**

```
seed = FNV1a(childId + '|' + questDayKey() + '|' + npcId)
```

| กติกา | เหตุผล |
|---|---|
| เปิดคุยกี่รอบก็ได้โจทย์เดิม | กันเด็ก reroll จนได้ข้อง่าย |
| แต่ละ NPC สุ่ม: **ชนิดเควสต์ → กลไก → ตัวเลข/ของในโจทย์ → บทพูด** | ครบทุกชั้นตามที่ขอ |
| ทำเสร็จแล้ว NPC **cooldown ถึงวันถัดไป** (ยังคุยเล่นได้ปกติ) | กันฟาร์มเงิน |
| เก็บ `recent[]` 5 กลไกล่าสุด แล้วหักออกจากสระสุ่ม | กันเจอ "จ่ายเงิน" 3 ร้านติด |
| สุ่มเฉพาะ **5-8 ร้านต่อวัน** ที่มีงาน ไม่ใช่ทุกร้าน | บังคับให้เดินสำรวจว่าวันนี้ใครมีป้าย "!" |
| กลไกที่ `minTier > tier ของเด็ก` ถูกคัดออกก่อนสุ่ม | กันโจทย์ยากเกินวัย |

---

## 8. ⚠️ จุดสำคัญที่สุด — "ที่ใช้เงิน" (money sink)

**ระบบเควสต์จะพังทันทีถ้าได้เงินมาแล้วไม่มีอะไรให้ซื้อ** เด็กจะเลิกเล่นภายใน 2 วัน
→ **ต้องทำร้านใช้เงินพร้อมกัน (หรือก่อน) ไม่ทำเควสต์อย่างเดียวเด็ดขาด**

| ร้าน | ขายอะไร | ทำไมเด็กอยากได้ | ต้นทุนงาน |
|---|---|---|---|
| 👗 ห้างแฟชั่น | ชุด/หมวก/แว่น/ของสะพาย/ของถือ เพิ่มเติม | **ระบบแต่งตัวทำเสร็จแล้ว ต่อได้เร็วสุด** | เล็ก ⭐ |
| 🛋️ ห้างเฟอร์นิเจอร์ | ปลดล็อกเฟอร์นิเจอร์ในคลัง (ตอนนี้ให้ฟรีหมด) | แต่งบ้านตัวเอง | เล็ก (มีคลัง 60+ ชิ้นแล้ว) |
| 🐾 ร้านสัตว์เลี้ยง | สัตว์เลี้ยงตัวใหม่ + ของเล่นสัตว์ | มีบ้านสัตว์เลี้ยงแล้ว | กลาง |
| 🎸 ร้านเครื่องดนตรี | ปลดเพลงใหม่ให้เล่นในบ้าน | ต่อจากระบบเปียโน/`MUSIC_TRACKS` เดิม | กลาง |
| 🍜🍨 ร้านอาหาร/ไอติม | ของกินให้ตัวละคร (เอฟเฟกต์น่ารัก ไม่มีผลต่อเกม) | คุ้มค่าเงินก้อนเล็ก ซื้อบ่อย | เล็ก |
| 📜 ผู้ใหญ่บ้าน | ป้ายชื่อบ้าน / ธง / **สีหลังคาบ้าน** | ของอวดชิ้นใหญ่ ราคาแพง | กลาง |

> ⚠️ ของที่ปลดล็อกแล้วต้องเก็บใน house save (`unlocked:[]`) เพื่อให้ export/import ตามไปด้วย

---

## 9. โครงข้อมูล + localStorage

### ไฟล์ใหม่ (ตามหลักการ "ไม่มี build step / lazy-load")
```
js/house-quests.js   ← คลังกลไก + ผังร้าน + ตัวสุ่ม (catalog ล้วน ไม่แตะ DOM/WebGL → เทสได้ง่ายเหมือน house-furniture.js)
```
โหลดต่อท้ายลำดับเดิมใน `js/games-ar.js`: `three → house-map → house-furniture → house-quests → house`

```js
HOUSE_QUESTS(kit) → {
  MECHANICS: [
    { id:'pay-exact', name:'จ่ายเงินให้พอดี', tags:['money'], minTier:1, fam:'A',
      gen(rng, diff){ /* คืนโจทย์ */ }, check(state, ans){ /* คืน {ok, stars} */ } },
    ...
  ],
  SHOP_QUESTS: {
    'shop-mart':  { npc:'npc-mart',      mech:['pay-exact','count-shelf'], base:5 },
    'shop-music': { npc:'npc-musicshop', mech:['melody-echo','sound-guess'], base:5 },
    ...
  },
  pickQuestFor(npcId, dayKey, childId, tier)
}
```

### บันทึกใน house save ก้อนเดิม (export/import ตามไปเอง)
```js
data.shopQuest = {
  d: '2026-8-6',                       // วันที่ (questDayKey())
  npc: {
    'npc-mart':      { mech:'pay-exact', st:'done',   stars:3 },
    'npc-food':      { mech:'recipe',    st:'active', prog:2 },
    'npc-musicshop': { mech:'melody-echo', st:'open' }
  },
  recent: ['pay-exact','recipe'],      // กันกลไกซ้ำ
  earnedToday: 85                      // เพดานเงินต่อวัน
}
data.unlocked = ['hat-crown','sofa-blue', ...]   // ของที่ซื้อแล้ว
```

**เหรียญไม่เก็บที่นี่** — ผ่าน `window.OwlCoins.add()/spend()` เท่านั้น
(ตามคอมเมนต์กำกับใน `js/app-core.js` ห้ามแตะ `progress.coins` ตรงๆ จากที่อื่น)

---

## 10. ดีไซน์ให้กลมกลืนกับโลก 3D เดิม

- **ป้าย "!" / "✓" ลอยเหนือหัว NPC** — ใช้ `signPlane()` + `userData.anim = {kind:'bob'}` (ระบบมีอยู่แล้ว)
  - ⚠️ ไอคอนใหม่ต้องเพิ่มเข้า `SIGN_ICONS` (`js/house.js:1478`) ให้ atlas texture มีช่อง
- **ร้านที่มีงานเรืองแสงอ่อนๆ ตอนกลางคืน** — เด็กมองหาเจอจากไกล
- **overlay เกมย่อย = การ์ดครีมมนๆ ลอยกลางจอ** สไตล์เดียวกับกล่องเลือกเฟอร์นิเจอร์ / `#house-quest` เดิม
  **ไม่ใช่หน้าเต็มจอ** — เด็กยังเห็นร้านข้างหลัง รู้สึกว่า "อยู่ในร้าน" จริง
- **เหรียญบินเข้าป้ายยอดเงิน** ตอนได้รางวัล (ต่อจาก `spawnParticle()` เดิม)
- **สมุดงานวันนี้ 📔** — ปุ่มใหม่ในเมนูเฟือง ลิสต์ว่าใครมีงานอยู่ตรงไหน กดแล้วชี้ทิศ
- ตัวอักษรใหญ่ ทรงมน สีสด ตามหลักการ "เหมาะกับเด็ก 5 ขวบ" ของโปรเจค
- อ่านโจทย์ออกเสียงไทยได้ (ใช้ `js/shared/speech.js`) — เด็ก ป.1 ยังอ่านไม่คล่อง

---

## 11. แผนทำเป็นเฟส

| เฟส | ได้อะไร | ขนาด |
|---|---|---|
| **1** | เปิด `QUEST_ENABLED` + ป้าย "!" + flow รับงาน/จบงาน + จ่ายเหรียญ + **กลไก 3 อัน** (จ่ายเงิน / จับคู่ / เรียงลำดับ) ที่ 3 ร้าน | กลาง |
| **2** | **ร้านใช้เงิน 2 ร้าน** (แฟชั่น + เฟอร์นิเจอร์) ← ต้องมาเร็ว ไม่งั้นเงินไร้ค่า | กลาง |
| **3** | เพิ่มกลไกอีก 6-8 อัน ครบทุกร้าน + ระบบความยากตามชั้น | ใหญ่ |
| **4** | **งานเดินโลก** (ส่งของ / รายการซื้อของ / ตามหา) + สมุดงาน + ชี้ทิศ | ใหญ่ |
| **5** | งานสร้างสรรค์, quest chain หลายวันของผู้ใหญ่บ้าน, เทศกาลตามฤดู | ต่อยอด |

---

## 12. ข้อควรระวัง / ความเสี่ยง

1. **ห้ามให้เควสต์กลายเป็น "การบ้าน"** — ถ้าเป็นเลขล้วนเด็กจะเลิกเข้าบ้าน ต้องคุมสัดส่วน A/B/C
2. **ห้ามลงโทษ** — ไม่มี "แพ้" ไม่เสียเงิน แย่สุดคือได้ ⭐ ดวงเดียว
3. **ขนาดไฟล์** — ชุด 3D ตอนนี้ ~1.2 MB แล้ว, `js/house.js` ~9,500 บรรทัด
   → กลไกเควสต์ต้องแยกไฟล์ + lazy-load **ห้ามยัดเพิ่มใน `house.js`**
4. **เควสต์เดินโลกต้องกันตัน** — ถ้า NPC เป้าหมายเดินไปมา (`roam`/`route`) ต้องมีปุ่มยกเลิกงาน/เปลี่ยนเป้าเสมอ
5. **เศรษฐกิจ** — เพดานเงินต่อวันต้องกำหนดตั้งแต่แรก
6. **`HOUSE_FEATURE_OFF`** — โหมดบ้านยังปิดบน `main` ระบบนี้จะไปกับรอบที่เปิดโหมดบ้านให้เด็กเล่นจริง
7. **ชุดเทส** — ควรเพิ่มเทส catalog แบบเดียวกับ `tests/house.spec.js` (id ไม่ซ้ำ, ทุกกลไก `gen()` ได้จริงทุก tier 1-6, ทุกร้านชี้ NPC ที่มีอยู่จริง)

---

## 13. คำถามที่ยังต้องตัดสินใจ (รอบแรก)

- [ ] เอาแผนเฟส 1→5 ตามนี้ไหม หรือสลับลำดับ
- [ ] "ร้านใช้เงิน" เริ่มจากร้านไหนก่อน (เสนอ **ห้างแฟชั่น** — ระบบแต่งตัวทำเสร็จแล้ว ต่อได้เร็วสุด)
- [ ] เควสต์ควรมี **จำกัดจำนวนต่อวัน** ไหม หรือปล่อยให้เด็กเล่นได้เรื่อยๆ (ตอนนี้เสนอ: จำกัดโดยธรรมชาติที่ 5-8 ร้าน/วัน)
- [ ] `QUEST_POOL` เดิม (ภารกิจ 3 อย่างของกระดานน้ำพุ) จะเก็บไว้เป็น layer แยกตามที่เสนอ หรือยุบรวมกับเควสต์ NPC
- [ ] จะให้ดึงคำถามจาก `CATS` มาใช้ในเควสต์โรงเรียนไหม (ประหยัดงานมาก แต่เด็กอาจรู้สึกว่าซ้ำกับหน้าหลัก)
- [ ] ราคาของในร้าน — จะจูนตัวเลขจริงตอนไหน (แนะนำ: ตอนทำเฟส 2 พร้อมกับร้านแรก)

---
---

# ภาค 2 — ข้อเสนอรอบขยาย (2026-08-07)

โจทย์ใหม่จากผู้ใช้ 6 ข้อ:
1. โจทย์ต้องเยอะกว่านี้มาก
2. แต่ละ NPC ต้องมี mechanic หลากหลาย (คิดใหม่ / รื้อของเก่ามาใช้ / ดึงจาก `IDEA.md`)
3. **เกมตอบคำถาม (quiz) เป็น mechanic พื้นฐานที่ต้องมีในทุก NPC ที่ทำเควสต์ได้**
4. **ปิดของฟรีทั้งหมด** — ทุกอย่างที่ custom ได้ (เฟอร์นิเจอร์ / ชุดแต่งตัว / สัตว์เลี้ยง) ต้องซื้อด้วยเงิน และไปอยู่ในร้านที่เหมาะสม
5. **สัตว์เลี้ยง**: ไม่มีสัตว์ = ไม่มีบ้านสัตว์ · เพิ่มอาหารหลายประเภท · ระบบความหิว · หิวจัด = ป่วย (ไม่ตาย) · ป่วยแล้วนอนในบ้าน ออกมาเดินไม่ได้ · ต้องคุยหมอที่โรงพยาบาลและจ่ายเงินรักษา
6. **กระดานเควสต์** เป็นจุดเดียว ทำได้ 5 เควสต์/วัน แยกโควตาจากเควสต์ NPC ปกติ

---

## 14. ตัวเลขจริงในโค้ดวันนี้ (ฐานคำนวณทั้งหมดในภาค 2)

นับจากไฟล์จริง ไม่ใช่ประมาณ:

| ของ | จำนวน | อยู่ที่ |
|---|---|---|
| เฟอร์นิเจอร์/ของตกแต่ง | **116 ชิ้น** (bath 9, bed 9, decor 13, decorout 23, garden 15, kitchen 9, play 12, seat 10, seatout 7, table 9) | `js/house-furniture.js` |
| ชนิดสัตว์เลี้ยง | **12 ชนิด** (แต่ละชนิดมี 2-4 สีขน) | `PET_TYPES` `js/house.js:8210` |
| NPC ทั้งหมด | **68 ตัว** | `NPC_DEFS` `js/house-map.js:777` |
| NPC ที่มีบทเควสต์แล้ว (`quest:`) | **57 ตัว** | เดียวกัน |
| QUEST_POOL ปัจจุบัน | **12 แบบ** | `js/house.js:7230` |
| แถวแต่งตัว | 11 แถว — ทรงผม 6 × สีผม 17, ตา 8 × สีตา 12, สีเสื้อ 26, ลายเสื้อ 10, สีกางเกง 28, สีรองเท้า 14, หมวก 9, แว่น 7, เป้ 7, ของถือ 9 (+ สีของแต่ง 12) | `js/house-map.js:19-80` |

**ของที่แจกฟรีตอน seed บ้านครั้งแรก** (`seedWorldDecor` `js/house.js:8944`): ต้นไม้ทั้งสนาม, รั้วรอบสนาม (มุม 4 + ท่อน), **บ้านสัตว์เลี้ยง 1 หลัง**, ทางเดินหน้าประตู 4 แผ่น — ทั้งหมดเป็น decor ที่เด็กย้าย/ลบได้

---

## 15. ระบบ mechanic: จาก 12 ภารกิจเดินสำรวจ → 23 mechanic เล่นจริง

หลักการ: **1 mechanic = 1 ฟังก์ชัน generator** ที่รับ `{tier, npc, seed}` แล้วคืนโจทย์ 1 ชุด ไม่ผูกกับ NPC ตัวใดตัวหนึ่ง ⇒ NPC ตัวไหนก็หยิบไปใช้ได้ และโจทย์ต่างกันทุกครั้งเพราะสุ่มพารามิเตอร์

### 15.1 กลุ่ม A — พื้นฐาน (ทุก NPC ต้องมี)

| id | ชื่อ | กติกา |
|---|---|---|
| `quiz` | **ตอบคำถาม** ⭐ | **บังคับมีในทุก NPC** — 3-5 ข้อ 4 ตัวเลือก ดึงคำถามจาก `CATS` ตามระดับชั้นของเด็ก + คลังคำถาม "ธีมร้าน" เฉพาะ NPC (เช่น NPC ร้านขนม → โจทย์บวกลบด้วยรูปขนม) |
| `count-things` | นับของ | นับของในฉาก 3D จริงตรงหน้า NPC แล้วเลือกตัวเลข |

`quiz` เป็นค่าเริ่มต้นเสมอ: ถ้า NPC ตัวไหนยังไม่ได้กำหนด mechanic เฉพาะ → ได้ `quiz` อัตโนมัติ ⇒ **NPC ทั้ง 57 ตัวมีเควสต์ได้ทันทีตั้งแต่วันแรก** ไม่ต้องรอทำ mechanic ครบ

### 15.2 กลุ่ม B — คิดใหม่สำหรับโหมดบ้านโดยเฉพาะ (10 แบบ)

| id | ชื่อ | กติกา | NPC ที่เหมาะ |
|---|---|---|---|
| `pay-exact` | จ่ายเงินให้พอดี | ของราคา N บาท เลือกเหรียญ/แบงก์ให้ครบพอดี | ทุกร้านค้า |
| `change-back` | ทอนเงิน | จ่ายไป X ของราคา Y ต้องทอนเท่าไร | ร้านสะดวกซื้อ, ห้าง |
| `recipe-seq` | ทำตามสูตร | ใส่วัตถุดิบตามลำดับ+จำนวนที่บอก (เห็นสูตร 3 วิ แล้วสูตรหาย) | ร้านอาหาร, ไอศกรีม, ตลาด |
| `shopping-list` | จำรายการซื้อของ | จำ 3-6 อย่าง แล้วไปหยิบจากชั้นให้ครบ | ร้านสะดวกซื้อ, ตลาด |
| `deliver` | ส่งของถึงมือ | รับของจาก NPC A เดินไปส่ง NPC B (ใช้แผนที่โลกจริง) | ทุกตัว — เชื่อม NPC เข้าหากัน |
| `escort` | พาไปส่ง | พาเด็ก/ผู้สูงอายุเดินไปสถานที่ตามที่บอก (NPC เดินตาม) | ตำรวจ, พยาบาล, ครู |
| `feed-pet` | ให้อาหารถูกชนิด | จับคู่สัตว์ ↔ อาหารที่มันกิน | ร้านสัตว์เลี้ยง, ฟาร์ม |
| `stock-shelf` | จัดชั้นวาง | เรียงของขึ้นชั้นตามหมวด/ขนาด/สี | ร้านสะดวกซื้อ, ห้าง |
| `fish-math` | ตกปลาคิดเลข | ตกปลาได้ตัวเลข ต้องตกให้ผลรวมเท่าโจทย์ | กระท่อมริมบ่อ |
| `traffic-lr` | ซ้าย-ขวาว่องไว | ไฟ/ป้ายบอกทิศ กดให้ทันเวลา (จาก `IDEA.md`) | ตำรวจจราจร |

### 15.3 กลุ่ม C — รื้อ engine เดิมในแอปมาใช้ (7 แบบ, งานน้อยที่สุด/คุ้มที่สุด)

engine พวกนี้เขียนไว้แล้วใน `js/games-*.js` — งานคือ **ห่อเป็น overlay ในโหมดบ้าน** ไม่ต้องเขียน logic ใหม่

| id | ชื่อ | ดึงจาก | NPC ที่เหมาะ |
|---|---|---|---|
| `match-pair` | จับคู่ความจำ | `games-think.js` | ห้างเฟอร์นิเจอร์, ร้านของเล่น |
| `color-mix` | ผสมสี | `games-art.js` | ช่างไม้ (ทาสี), ห้างแฟชั่น |
| `melody-echo` | เล่นตามทำนอง | `games-art.js` + `shared/piano.js` | ร้านเครื่องดนตรี |
| `sound-guess` | ทายเสียง | `games-art.js` | ร้านเครื่องดนตรี, ฟาร์ม (เสียงสัตว์) |
| `pattern-fill` | เติมแพทเทิร์น | หมวด `pattern` | โรงเรียน, ห้างแฟชั่น (ลายผ้า) |
| `weigh-balance` | ตาชั่ง/ตวง | `games-math.js` | ตลาด, ร้านขนม |
| `clock-set` | ตั้งนาฬิกา | `games-math.js` (นาฬิกาวิเศษ) | โรงเรียน, โรงแรม, สถานีรถ |

### 15.4 กลุ่ม D — ดึงจาก `IDEA.md` (4 แบบ — ทำในโหมดบ้านเลย ไม่ต้องเป็นหมวดแยก)

| id | ชื่อ | ปรับให้เข้าโหมดบ้านยังไง | NPC |
|---|---|---|---|
| `spot-diff` | จับผิดภาพ | ใช้ตู้โชว์/หน้าร้าน 2 ฝั่งเทียบกัน | ห้าง, พิพิธภัณฑ์ |
| `flash-count` | นับแว้บเดียว | ของบนรถเข็นโผล่ 2 วิ แล้วปิด | ตลาดรถเข็น |
| `dress-order` | แต่งตัวตามโจทย์ | **ใช้ระบบแต่งตัวจริงของเกม** — NPC สั่ง "หมวกฟาง + แว่นกลม + เป้หมี" | ห้างแฟชั่น |
| `find-hidden` | หาของที่หาย | ซ่อน object ในโลก 3D จริง เดินไปหา | ตำรวจ, เทศมนตรี |

> เมื่อทำ 4 อันนี้เสร็จ **ต้องลบไอเดียนั้นออกจาก `IDEA.md`** ตามกติกาโปรเจค — แต่ถ้าตั้งใจจะทำเป็นหมวดเกมแยกในหน้าหลักด้วยในอนาคต ให้เก็บไว้ (ต้องถามผู้ใช้ก่อนลบ)

**รวม 23 mechanic** (A 2 + B 10 + C 7 + D 4)

---

## 16. "โจทย์เยอะๆ" — คำนวณจำนวนจริง

โจทย์ 1 ชุด = `mechanic × tier × พารามิเตอร์สุ่ม × ธีม NPC`

- 23 mechanic × 3 tier (ป.เล็ก/กลาง/โต) = **69 แม่แบบ**
- แต่ละแม่แบบสุ่มพารามิเตอร์ได้เฉลี่ย ~30 หน้าตา (ตัวเลข/สี/ของ/จำนวน) = **~2,000 โจทย์ที่ไม่ซ้ำหน้า**
- `quiz` ดึงจาก `CATS` ที่มีอยู่แล้ว **332 หมวด** → คำถามหลักพันข้อฟรีๆ ไม่ต้องเขียนใหม่
- คูณธีม NPC 57 ตัว (ของในโจทย์เปลี่ยนตามร้าน) ⇒ เด็กแทบไม่เจอโจทย์เดิมซ้ำ

**กันซ้ำ**: เก็บ `recentQuests: [ 'npcId:mech:hash', … ]` 40 รายการล่าสุดใน save แล้วสุ่มเลี่ยง

---

## 17. เศรษฐกิจ — ปิดของฟรีทั้งหมด

### 17.1 ตารางราคา (เสนอ)

**เฟอร์นิเจอร์ 116 ชิ้น** แบ่ง 4 ระดับตามขนาด/ความอลัง:

| ระดับ | ราคา | ตัวอย่าง | ประมาณ |
|---|---|---|---|
| จิ๋ว | 25 🪙 | กระถาง, ตุ๊กตา, พรม, ป้าย | ~35 ชิ้น |
| เล็ก | 60 🪙 | เก้าอี้, โต๊ะเล็ก, ชั้นวาง | ~35 ชิ้น |
| กลาง | 140 🪙 | โซฟา, เตียง, ตู้เย็น, ชิงช้า | ~30 ชิ้น |
| ใหญ่/พิเศษ | 300 🪙 | สระ, บ้านต้นไม้, ม้าหมุน, ไฟประดับ | ~16 ชิ้น |

**ชุดแต่งตัว**:

| ของ | ราคา |
|---|---|
| สีเสื้อ/กางเกง/รองเท้า (ต่อสี) | 15 🪙 |
| ทรงผม (ต่อทรง) / สีผม / สีตา | 30 🪙 |
| ลายเสื้อ (ต่อลาย) | 40 🪙 |
| หมวก / แว่น (ต่อชิ้น) | 60 🪙 |
| เป้สะพาย / ของถือ (ต่อชิ้น) | 80 🪙 |

**สัตว์เลี้ยง 12 ชนิด** — ราคาไล่ตามความ "หายาก" ให้เด็กมีอะไรให้ตั้งเป้า:

| กลุ่ม | ราคา | ชนิด |
|---|---|---|
| เริ่มต้น | 250 🪙 | 🐶 หมา, 🐱 แมว, 🐰 กระต่าย, 🐥 ลูกเจี๊ยบ |
| กลาง | 450 🪙 | 🐹 แฮมสเตอร์, 🐢 เต่า, 🐸 กบ, 🐷 หมู |
| พิเศษ | 800 🪙 | 🐑 แกะ, 🐧 เพนกวิน |
| หายาก | 1,500 🪙 | 🐼 แพนด้า, 🦄 ยูนิคอร์น |

สีขนเพิ่มเติมของสัตว์: สีแรกแถมฟรีตอนซื้อ, สีอื่น 100 🪙/สี

### 17.2 ของแถมฟรีตอนเริ่ม (สำคัญ — กันบ้านโล่งจนเล่นไม่สนุก) ✅ ทำแล้วเฟส 1

เด็กใหม่ยังต้องมีของพอเล่นวันแรก ให้ฟรี:
- **รั้ว + ทางเดินหน้าประตู + ต้นไม้ในสนาม + บ้านสัตว์** — ของฉากที่ `seedWorldDecor` วางอยู่แล้ว ไม่ใช่ของสะสม ให้ฟรีเหมือนเดิม (แต่ต้องนับเป็น "สิทธิ์" ด้วย ไม่งั้นลบทิ้งแล้วหยิบกลับมาวางไม่ได้)
- **เฟอร์นิเจอร์เริ่มต้นในบ้าน 8 ชิ้น** ตามข้อ 26 (**ไม่ใช่ 6 ชิ้นตามร่างแรก** — ข้อ 26 ที่ปรับ 2026-08-07 มาแทน) รวมของฉากนอกบ้าน 3 อย่างเป็น **11 ชิ้น** ตามที่ผู้ใช้ล็อกไว้
- **ชุดแต่งตัวเริ่มต้น**: ทรงผม 2, สีผม 1, สีตา 1, สีเสื้อ 4, สีกางเกง 3, สีรองเท้า 2 (หมวก/แว่น/เป้/ของถือ = ต้องซื้อทั้งหมด ได้ฟรีแค่ตัวเลือก "ไม่ใส่")
  - **เพศ · รูปทรงดวงตา · สีของหมวก/แว่น/เป้/ของถือ = ฟรีทั้งแถว ไม่วางขาย** (เป็นหน้าตาเด็กเอง / ต้องซื้อตัวของก่อนถึงจะเห็นสีอยู่แล้ว)
- **สัตว์เลี้ยง: ไม่แถม** — ต้องซื้อทุกตัว (ตามข้อ 5 ของผู้ใช้) → **ยังไม่ทำในเฟส 1** ดูหมายเหตุ 17.5
- เงินเริ่มต้น 0 🪙 แต่เควสต์แรกให้ 30-50 🪙 ⇒ เด็กซื้อของชิ้นแรกได้ในวันแรก (**เควสต์มาเฟส 2**)

### 17.3 ⚠️ Migration — เด็กที่เล่นอยู่แล้ว

**ห้ามให้ของที่เด็กวางไว้แล้วหายไปเด็ดขาด** ตอนอัปเกรด:

✅ **ทำแล้วเฟส 1** — `SHOP.migrate(d)` ใน `js/house-shop.js` ถูกเรียกจาก `loadHouseData()` ทุกครั้งที่อ่านข้อมูล (ทำงานจริงครั้งเดียวต่อเด็ก แล้วปั๊ม `econVer`)

```
ครั้งแรกที่โหลดด้วยเวอร์ชันใหม่ (data.econVer !== 2):
  data.unlocked += ทุก id ที่ปรากฏใน data.decor.out/in (นับว่าซื้อแล้ว)
  data.unlocked += 'fit:<แถว>:<index>' ของทุกค่าที่ปรากฏใน data.char (ทรงผม/สี/ลาย/หมวก… ที่ใส่อยู่)
  data.unlocked += ชุดเริ่มต้น 8 ชิ้น + ของฉากนอกบ้าน + ชุดแต่งตัวเริ่มต้น
  data.econVer = 2
```
**ยกไปเฟส 3 (ตอนเปิดร้านสัตว์เลี้ยงจริง)**: `ถ้ามี data.pet → owned.pets = [ชนิดนั้น] + ความอิ่มเต็ม` และ `ถ้าไม่มี data.pet → ลบ decor 'pet-house'`
> เหตุผล: เฟส 1 การรับเลี้ยงสัตว์ยังฟรีอยู่ ถ้าลบบ้านสัตว์ทิ้งตอนนี้ เด็กที่รับเลี้ยงในเฟส 1 จะไม่มีบ้านให้น้องนอน (ถอยหลัง) ⇒ ทำพร้อมกันทีเดียวตอนเฟส 3

### 17.4 ร้านไหนขายอะไร (ใช้ล็อตที่มีอยู่แล้วในแผนที่ ไม่ต้องสร้างตึกใหม่)

| ร้าน | ขาย |
|---|---|
| 🛒 ห้างเฟอร์นิเจอร์ (`mall-furniture`) | เฟอร์นิเจอร์ในบ้าน — bath, bed, kitchen, seat, table, decor |
| 🌷 ร้านต้นไม้/สวน (`garden` — ใช้ล็อตตลาด) | garden, decorout, seatout |
| 👗 ห้างแฟชั่น (`mall-fashion`) | ชุดแต่งตัวทั้งหมด |
| 🐾 ร้านสัตว์เลี้ยง (`shop-pet`) | สัตว์เลี้ยง + สีขน + **อาหารสัตว์** + ของเล่นสัตว์ |
| 🎮 ร้านของเล่น (`shop-mart` มุมของเล่น) | หมวด play |
| 🏥 โรงพยาบาล (`hospital`) | บริการรักษาสัตว์ป่วย (ไม่ใช่ของ) |

**หน้าตาร้าน**: overlay แบบเดียวกับหน้าเลือกสัตว์เลี้ยง/หน้าแต่งตัวที่มีอยู่ (`.house-creator`) — การ์ดสินค้า grid มี emoji/พรีวิว 3D + ราคา + ปุ่ม "ซื้อ" ของที่ซื้อแล้วขึ้น ✓ ของที่เงินไม่พอขึ้นราคาสีเทา **ไม่ซ่อน** (ให้เด็กเห็นเป้าหมาย)

### 17.5 ⚠️ สิ่งที่เฟส 1 ทำต่างจากตารางข้างบน (ตั้งใจ — อ่านก่อนทำเฟส 2-3)

| หัวข้อ | ที่ทำจริงในเฟส 1 | ทำไม |
|---|---|---|
| ร้านที่เปิด | **ห้างเฟอร์นิเจอร์ + ห้างแฟชั่น เท่านั้น** | ตามแผนเฟส 1 |
| ห้างเฟอร์นิเจอร์ขายอะไร | **ทุกหมวดของเฟอร์นิเจอร์ รวมของนอกบ้าน** (สวน/เครื่องเล่น/ที่นั่งนอกบ้าน/ตกแต่งสวน) | ร้านต้นไม้กับร้านของเล่นยังไม่เปิดจนเฟส 2-3 ถ้าไม่ขายที่นี่ ของกลุ่มนั้นจะซื้อไม่ได้เลย = **ผิดกติกาเหล็กข้อ 1 (dead end)** ⇒ **เฟส 2-3 ค่อยย้ายหมวดไปร้านที่ถูกต้องตามข้อ 17.4** |
| สัตว์เลี้ยง / อาหาร / โรงพยาบาล | **ยังไม่แตะ** ยังรับเลี้ยงฟรีเหมือนเดิม | เป็นเฟส 3 ทั้งก้อน |
| `pet-house` | ยัง seed ให้ฟรีเหมือนเดิม | ดูเหตุผลข้อ 17.3 |
| แหล่งรายได้ | **ยังไม่มี** เงินเริ่ม 0 🪙 | เควสต์อยู่เฟส 2 ⇒ ห้าม merge ขึ้น production เดี่ยวๆ ถ้าจะเปิดให้เด็กเล่นจริง |

---

## 18. สัตว์เลี้ยง — วงจรชีวิตใหม่

### 18.1 ไม่มีสัตว์ = ไม่มีบ้านสัตว์

- ลบ `seed.push({id:'pet-house', …})` ออกจาก `seedWorldDecor` (`js/house.js:8967`)
- ย้ายไปสร้างตอน **ซื้อสัตว์ตัวแรกสำเร็จ**: วางบ้านที่ `PET_HOUSE_TILE` (ช่องเดิมที่จองไว้) พร้อม effect ปรากฏ + นกฮูกพูด "บ้านของ<ชื่อสัตว์>มาแล้ว!"
- ถ้าเด็กปล่อยสัตว์คืน (ปุ่ม `#house-pet-remove` เดิม) → บ้านสัตว์หายไปด้วย
- บ้านสัตว์ยัง**ย้ายที่ได้**ในโหมดตกแต่ง แต่**ลบเองไม่ได้** (ผูกกับการมีสัตว์)

### 18.2 อาหาร — คนละประเภทต่อชนิดสัตว์

ขายที่ร้านสัตว์เลี้ยง ซื้อเป็น "ถุง" เก็บใน `data.petFood = {meat:3, fish:1, …}`

| อาหาร | ราคา/ถุง (5 มื้อ) | สัตว์ที่กิน |
|---|---|---|
| 🍖 อาหารเนื้อ | 60 🪙 | 🐶 หมา |
| 🐟 อาหารปลา | 60 🪙 | 🐱 แมว, 🐧 เพนกวิน |
| 🥕 ผักสด | 50 🪙 | 🐰 กระต่าย, 🐢 เต่า |
| 🌾 เมล็ดพืช | 45 🪙 | 🐥 ลูกเจี๊ยบ, 🐹 แฮมสเตอร์ |
| 🌿 หญ้าแห้ง | 50 🪙 | 🐑 แกะ, 🐷 หมู |
| 🦗 แมลง | 55 🪙 | 🐸 กบ |
| 🎋 ไผ่ | 90 🪙 | 🐼 แพนด้า |
| 🌈 อาหารวิเศษ | 150 🪙 | 🦄 ยูนิคอร์น |

**นี่คือของเล่นสอนจริง**: เด็กต้องรู้ว่าสัตว์ตัวไหนกินอะไร ⇒ ให้อาหารผิดชนิด สัตว์ส่ายหัว 🙅 ไม่กิน ไม่เสียของ ไม่ลงโทษ แค่ลองใหม่ — และเป็นวัตถุดิบของ mechanic `feed-pet` โดยตรง

**วิธีให้อาหาร**: แตะชามหน้าบ้านสัตว์ (หรือแตะตัวสัตว์ → เมนู "ให้อาหาร") → เลือกถุงจากคลัง → สัตว์วิ่งมากิน animation กิน 2 วิ + หัวใจ + ความอิ่มเต็ม

### 18.3 ความหิว — ออกแบบให้ "ไม่ลงโทษเด็กที่หายไปนาน"

```
data.pet.fed = timestamp มื้อล่าสุด
data.pet.sick = false
```

- ความอิ่ม 0-100 · **ลดตาม "วันที่เด็กเข้าเล่น" ไม่ใช่เวลาจริงล้วน** — เข้าเล่นวันใหม่ = -35 (ไม่ว่าจะห่างกัน 2 วันหรือ 2 เดือน) ⇒ **หายไป 3 เดือนกลับมาสัตว์ก็แค่หิว 1 วัน ไม่ป่วยทันที**
- ในวันเดียวกันความอิ่มลดช้ามาก (-5 ต่อ 20 นาทีในเกม) เพื่อให้ยังมีอะไรทำระหว่างวัน
- แถบความอิ่มโชว์เหนือหัวสัตว์เมื่อ < 50 (หัวใจเต็ม/ครึ่ง/ว่าง แทนตัวเลข — เด็ก 5 ขวบอ่านเลขเปอร์เซ็นต์ไม่รู้เรื่อง)
- **ป่วยเมื่อ**: ความอิ่มเหลือ 0 แล้วเข้าเล่น**วันถัดไป**อีก 1 วันโดยไม่ให้อาหาร ⇒ ต้องละเลย 2 วันเล่นติดกันถึงจะป่วย และมีคำเตือนล่วงหน้า (สัตว์ส่งฟองข้อความ "หิวจัง 😢" + นกฮูกเตือนตอนเข้าเกม)

### 18.4 ป่วย → โรงพยาบาล

เมื่อ `sick = true`:
- สัตว์ **นอนในบ้านสัตว์ ออกมาไม่ได้** (ใช้กลไก `hPet.rest` ที่มีอยู่แล้ว แต่ล็อกไว้ ปลุกไม่ตื่น)
- แตะตัวสัตว์ → ฟองข้อความ "ไม่สบาย… 🤒 พาหนูไปหาหมอที" + ไอคอน 🏥 ชี้ทางบนเข็มทิศ
- เดินไปโรงพยาบาล คุย **คุณหมอใจดี 🩺 (`npc-doctor`)** หรือ **พี่พยาบาล 💉 (`npc-nurse`)** ที่มีอยู่แล้วในแผนที่
- หมอคิด **ค่ารักษา 120 🪙** → จ่ายแล้วเล่น mini-sequence สั้นๆ (ตรวจ → ฉีดยา → พันผ้า) ~5 วิ → สัตว์หายป่วย ความอิ่มเต็ม วิ่งกลับมาหาเด็ก
- **เงินไม่พอ**: หมอไม่ไล่กลับ — เสนอ "ทำงานช่วยหมอแทนค่ารักษา" = เควสต์ 1 ชุด ⇒ **เด็กไม่มีทางติดตัน** (สำคัญมาก ห้ามมี dead end ในเกมเด็ก)
- ป่วยแล้ว**ไม่หายเอง** ต้องคุยหมอเท่านั้น (ตามที่ผู้ใช้ระบุ)

---

## 19. กระดานเควสต์ — โควตาแยก 5 เควสต์/วัน

ปัจจุบัน `#house-quest` ข้างน้ำพุ ทำหน้าที่รวมเควสต์ NPC เสนอเปลี่ยนเป็น **แหล่งเควสต์อิสระ**:

| | กระดานเควสต์ 📋 | เควสต์ NPC 🧑 |
|---|---|---|
| โควตา | **5 ชุด/วัน** | ตามจำนวน NPC ที่ยังไม่ได้คุยวันนี้ |
| ใครออกโจทย์ | กระดาน (ไม่ต้องเดินหา NPC) | NPC ตัวนั้นๆ |
| mechanic | สุ่มจากทั้ง 23 แบบ | จาก pool เฉพาะของ NPC ตัวนั้น (+ `quiz` เสมอ) |
| รางวัล | 40-80 🪙 + ⭐ | 20-50 🪙 |
| โบนัส | ทำครบ 5 ชุด → **โบนัส 100 🪙 + ⭐ พิเศษ** | ไม่มี |

- นับวันด้วย local date string `YYYY-MM-DD` เก็บที่ `data.board = {date, done:[…], claimed:false}`
- กระดานโชว์ 5 ช่อง ช่องที่ทำแล้วติ๊ก ✓ — เด็กเห็นความคืบหน้าเป็นภาพ
- เที่ยงคืนรีเซ็ต · **ห้ามหักดาว/เงินถ้าวันก่อนทำไม่ครบ** (ไม่มีบทลงโทษใดๆ)
- state โควตา NPC แยกคนละ key ⇒ ทำกระดานครบ 5 แล้วยังเดินคุย NPC ต่อได้

---

## 20. โครงสร้างไฟล์ที่ต้องเพิ่ม

`js/house.js` วันนี้ **10,199 บรรทัด** — ถ้ายัดทั้งหมดลงไปอีกจะแตะ ~14,000 บรรทัด แก้ยากมาก เสนอแยก:

| ไฟล์ใหม่ | เนื้อหา | ประมาณ |
|---|---|---|
| `js/house-quests.js` | catalog 24 mechanic + generator + pool ต่อ NPC + logic กระดาน | ~1,800 บรรทัด |
| `js/house-shop.js` | ตารางราคา + inventory + UI ร้าน + migration | ~900 บรรทัด |
| `js/house-pet-care.js` | อาหาร/ความหิว/ป่วย/โรงพยาบาล | ~500 บรรทัด |

โหลดแบบ lazy ต่อจาก `house.js` ใน `js/games-ar.js` (three → map → furniture → **shop → quests → petcare** → house) และ **ต้องเพิ่ม `?v=` ตาม `window.APP_ASSET_VER`** เหมือนไฟล์ lazy อื่น

`js/house.js` แก้เฉพาะจุดเชื่อม: `seedWorldDecor` (ตัดบ้านสัตว์), pet picker (ผ่านร้าน), decorate palette (กรองเฉพาะของที่ซื้อแล้ว), NPC talk (เรียก quest engine)

---

## 21. แผนทำเป็นเฟส (แนะนำลำดับนี้)

| เฟส | เนื้อหา | ทำไมลำดับนี้ |
|---|---|---|
| **1** | เศรษฐกิจ + migration + ร้าน 2 ร้านแรก (เฟอร์นิเจอร์, แฟชั่น) | ถ้าไม่ปิดของฟรีก่อน เควสต์ก็ไม่มีความหมาย — เงินไม่มีที่ใช้ |
| **2** | quest engine + `quiz` + `count-things` ให้ NPC ครบ 57 ตัว + กระดาน 5/วัน | เด็กเริ่มหาเงินได้จริงทั้งเมืองทันที |
| **3** | ร้านสัตว์ + ซื้อสัตว์ + บ้านสัตว์ตามเงื่อนไข + อาหาร/ความหิว/ป่วย/หมอ | ก้อนใหญ่ที่สุด แต่ต้องรอเศรษฐกิจเสร็จก่อน |
| **4** | mechanic กลุ่ม C (รื้อ engine เดิม 7 แบบ) | คุ้มสุดต่อแรง — โจทย์เพิ่มเยอะโดยแทบไม่เขียน logic ใหม่ |
| **5** | mechanic กลุ่ม B ที่เหลือ (10 แบบ) | งานเขียนใหม่จริง ทยอยเพิ่มได้ |
| **6** | mechanic กลุ่ม D จาก `IDEA.md` (4 แบบ) | เพิ่มความหลากหลายรอบสุดท้าย |

แต่ละเฟสจบแล้วเล่นได้จริงไม่พัง ⇒ merge เข้า `main` ได้ทีละเฟส (ถ้าผู้ใช้ตัดสินใจเปิดโหมดบ้านให้เด็กเล่น)

---

## 22. ความเสี่ยง/จุดที่ต้องระวัง

1. **ของหายหลังอัปเดต** — migration ข้อ 17.3 ต้องมาก่อนโค้ดร้าน ไม่งั้นเด็กที่ตกแต่งบ้านไว้แล้วเสียของ ⇒ เขียนเทส Playwright ยิง save เก่าเข้าไปแล้วเช็คว่าของครบ
2. **เงินฝืดเกิน** — 116 เฟอร์นิเจอร์ + สัตว์ 1,500 🪙 ถ้าเควสต์ให้ 30 🪙 เด็กต้องทำ 50 เควสต์กว่าจะได้แพนด้า ⇒ ต้องจูนจริงตอนเฟส 1-2 (เสนอ: เควสต์ NPC 20-50, กระดาน 40-80 + โบนัส 100 ⇒ วันละ ~400-500 🪙 ถ้าขยัน)
3. **สัตว์ป่วยแล้วเด็กงง** — ต้องมีสัญญาณชัด 3 ชั้น: ฟองข้อความสัตว์ + นกฮูกเตือนตอนเข้าเกม + ไอคอน 🏥 บนเข็มทิศ
4. **ขนาดไฟล์โหลด** — ชุด 3D วันนี้ ~1.2 MB เพิ่มอีก ~3 ไฟล์ ⇒ ประเมิน +150 KB ยังรับได้ แต่ต้องอัปม่านโหลด `#house-loading` ให้ % ตรง
5. **เทสเดิมต้องไม่แดง** — `tests/house.spec.js` ยิง `dispatchEvent('click')` เข้าปุ่มบ้าน ต้องรันทุกเฟส
6. **สิ่งที่ต้องถามก่อน merge เข้า `main` เสมอ** — จะเปิดโหมดบ้านให้เด็กเล่นจริงหรือคง `HOUSE_FEATURE_OFF` ไว้ (กฎใน `CLAUDE.md`)

---

## 23. ข้อสรุปที่ผู้ใช้ตัดสินแล้ว (2026-08-07)

- [x] **ราคา/ค่าตอบแทน** — ✅ **ใช้ตัวเลขตามข้อ 17 และ 19 ทั้งหมด** (เฟอร์นิเจอร์ 25/60/140/300, ชุดแต่งตัว 15-80, สัตว์ 250-1,500, เควสต์ NPC 20-50, กระดาน 40-80 + โบนัส 100) ⇒ ขยันทั้งวันได้ ~400-500 🪙
- [x] **จำนวนสัตว์เลี้ยง** — ✅ **เลี้ยงได้ 1 ตัวเหมือนเดิม** ไม่ต้องรื้อโค้ด `hPet` ให้รองรับหลายตัว · เปลี่ยนตัวได้ (ปล่อยตัวเดิมคืนร้านแล้วซื้อตัวใหม่) · **บ้านสัตว์จึงมีหลังเดียวเสมอ** · ชนิดที่เคยซื้อแล้วจำไว้ใน `owned.pets` ⇒ ซื้อซ้ำภายหลังไม่ต้องจ่ายเต็มราคา (เสนอ: จ่ายครึ่งเดียว) กันเด็กเสียดายจนไม่กล้าลองสัตว์ตัวอื่น
- [x] **ความหิว** — ✅ **ลดตาม "วันที่เข้าเล่น" ไม่ใช่เวลาจริง** ตามข้อ 18.3 (เข้าเล่นวันใหม่ = -35 ไม่ว่าห่างกัน 2 วันหรือ 2 เดือน) ⇒ หายไปนานกลับมาสัตว์แค่หิว ไม่ป่วยทันที ไม่ลงโทษเด็กที่ไม่ได้เล่นทุกวัน
- [ ] **ของแถมฟรีตอนเริ่ม** (ข้อ 17.2) — ยังไม่สรุป: เฟอร์นิเจอร์ 6 ชิ้น + ทรงผม 2 + สีเสื้อ 4 + สีกางเกง 3 + สีรองเท้า 2 พอไหม
- [ ] **เริ่มเฟส 1** — ผู้ใช้ขอปรับข้อเสนอก่อน รอสรุปว่าปรับตรงไหน

---

# ภาค 3 — ปรับข้อเสนอตามคำสั่ง (2026-08-07)

> ทั้ง 9 ข้อนี้ปรับทับภาค 2 ที่จุดที่ระบุไว้ ส่วนที่ไม่ได้พูดถึงคงเดิมทุกอย่าง

## 24. ประตูเช็คความพร้อม ก่อนเจอโจทย์ระดับสูงกว่า (แก้ข้อ 7)

คงกติกา "โจทย์สูงกว่าระดับชั้น 1 ระดับ" ไว้ แต่ไม่ยัดตั้งแต่วันแรก ต้องผ่าน 3 เงื่อนไขก่อน

| เงื่อนไข | เกณฑ์ |
|---|---|
| 1. ปริมาณ | ทำเควสต์ระดับตัวเองสำเร็จครบ **12 ชุด** (นับแยกตามชั้น เลื่อนชั้นแล้วเริ่มใหม่) |
| 2. คุณภาพ | ความแม่น 10 ชุดล่าสุด **≥ 70%** (วัดจากดาวที่ได้ ไม่ใช่จำนวนครั้งที่ผิด) |
| 3. ความสมัครใจ | NPC ถาม "อยากลองข้อยากขึ้นไหม" ปุ่ม **ลองเลย / ยังไม่พร้อม** — ห้ามโผล่มาเงียบๆ |

- ผ่านแล้วผสมแค่ **20%** (80% ยังเป็นระดับตัวเอง) โจทย์ท้าทายได้เหรียญ ×1.5 + ดาวพิเศษ
- พลาดโจทย์ท้าทาย **3 ชุดติด** ⇒ ปิดชั่วคราวกลับไปเก็บระดับตัวเอง **ไม่หักเงิน ไม่หักดาว ไม่มีคำว่าแพ้** แล้วค่อยถามใหม่
- เก็บใน save: `chal.done[grade]` · `chal.recent[]` · `chal.on` · `chal.miss`

## 25. สมุดสติกเกอร์คงเดิมทุกอย่าง

`#sticker-tally-btn` / `#sticker-book` / `progress.stickers` **ไม่แตะเลย** ระบบใหม่อยู่ในโหมดบ้านล้วน
เควสต์ในบ้านให้ "เหรียญ" อย่างเดียว **ไม่แจกสติกเกอร์หมวดเรียน** (สติกเกอร์คือหลักฐานว่าเรียนจบหมวดจริง ถ้าแจกจากการเดินเล่นความหมายจะเจือจาง)

*ข้อเสนอเสริม (ยังไม่ทำถ้าไม่สั่ง)*: เพิ่มแถบใหม่ในสมุดเล่มเดิมชื่อ "สติกเกอร์บ้านของหนู" เก็บเหรียญตราจากบ้านแยกแถบ ของเดิมไม่ขยับ

## 26. บ้านมาพร้อมชุดเฟอร์นิเจอร์มาตรฐาน (ตอบข้อค้าง 17.2)

ไม่ใช่แค่ปลดล็อกให้ซื้อ แต่**วางไว้ในบ้านเรียบร้อยตั้งแต่แรก** — เปิดบ้านครั้งแรกต้องเจอบ้านที่อยู่ได้จริง

| โซน | ของ | จำนวน |
|---|---|---|
| ห้องนอน | เตียงเด็ก · ตู้เสื้อผ้า | 2 |
| ห้องนั่งเล่น | โซฟา · โต๊ะกลาง · ชั้นวางของ | 3 |
| ครัว | โต๊ะกินข้าว · เก้าอี้ | 2 |
| ห้องน้ำ | โถส้วม | 1 |
| นอกบ้าน | รั้ว · ทางเดิน · ต้นไม้ 1 ต้น | 3 |

> ✅ **ปรับตามคำสั่ง 2026-08-07**: ห้องนอนเหลือแค่เตียง+ตู้ (ตัดโคมไฟ+พรม) · ครัวเหลือโต๊ะกินข้าว+เก้าอี้ (ตัดเตา ตู้เย็น อ่างล้างจาน) · ห้องน้ำเหลือโถส้วมอย่างเดียว (ตัดอ่างล้างหน้า ฝักบัว) · นอกบ้านตัดไฟสนาม ต้นไม้เหลือ 1 ต้น
> **เจตนา: ให้เริ่มมาน้อยๆ พออยู่ได้** เตา/ตู้เย็น/อ่างอาบน้ำ/โคมไฟ/พรม กลายเป็นของที่ต้องเก็บเงินซื้อ ⇒ เด็กมีเป้าหมายตั้งแต่วันแรก

รวม **11 ชิ้น** ย้าย/หมุน/ลบได้ · ลบแล้วไม่ได้เงินคืน (ของแถม) แต่ยังมีสิทธิ์ หยิบกลับมาวางฟรีได้เสมอ · มีปุ่ม **"จัดบ้านกลับแบบเดิม"** · **บ้านของเด็กที่เล่นอยู่แล้วไม่ถูกจัดใหม่ทับ**

## 27. เฟอร์นิเจอร์ 116 → 180 ชิ้น

| หมวด | วันนี้ | เพิ่ม | ตัวอย่าง |
|---|---|---|---|
| เตียง/ห้องนอน | 9 | +6 | เตียงสองชั้น เปลญวน ตู้ลิ้นชัก โต๊ะเครื่องแป้ง |
| ครัว | 9 | +8 | ไมโครเวฟ หม้อหุงข้าว เครื่องปั่น ชั้นเครื่องเทศ |
| ห้องน้ำ | 9 | +5 | อ่างเป็ดยาง ชั้นผ้าเช็ดตัว กระจกดอกไม้ |
| ที่นั่ง | 10 | +6 | บีนแบ็ก เก้าอี้โยก เก้าอี้ไข่แขวน |
| โต๊ะ | 9 | +6 | โต๊ะเรียน โต๊ะวาดรูป โต๊ะกาแฟกลม |
| ของแต่งในบ้าน | 13 | +10 | ตู้ปลา นาฬิกาแขวน โมบายดาว กระดานดำ กล่องของเล่น |
| สวน | 15 | +8 | เรือนกระจกจิ๋ว บ่อบัว กังหันลม รังนก |
| เครื่องเล่น | 12 | +5 | บ้านต้นไม้ แทรมโพลีน สระน้ำเป่าลม |
| ที่นั่งนอกบ้าน | 7 | +4 | เปลผ้าใบ ชุดปิกนิก ม้านั่งชิงช้า |
| ของแต่งนอกบ้าน | 23 | +6 | ตู้ไปรษณีย์ ป้ายชื่อบ้าน ไฟราวประดับ |
| **เครื่องดนตรี (หมวดใหม่)** | — | +11 | ดูข้อ 31 |

รวม **180 ชิ้น** · 10 หมวด → 11 หมวด · ทยอยเพิ่มตามเฟส

## 28. พ่อ แม่ ในบ้าน + เควสต์ครอบครัว

**ตัวละคร** — ใช้ `buildCharacter` เดิม ตัวสูงกว่าเด็ก ~1.25 เท่า อยู่เฉพาะ interior ไม่ออกไปในเมือง
ท่าประจำ: แม่ทำครัว/รดน้ำต้นไม้/นั่งโซฟา · พ่ออ่านหนังสือ/ซ่อมของ/ยืนระเบียง + ฟองข้อความสุ่ม

**✅ ตกลงแล้ว — มี default มาให้ แต่แก้เองได้ทั้งหมด**

- **ชื่อ default**: "คุณพ่อ" / "คุณแม่" · **หน้าตา default**: `H_DEFAULT_PARENT_DAD` / `H_DEFAULT_PARENT_MOM` (ค่าคงที่ใหม่ใน `js/house-map.js` คู่กับ `H_DEFAULT_CHAR` ที่มีอยู่)
- เด็กเปิดบ้านครั้งแรก **เจอพ่อแม่ยืนอยู่แล้วทันที ไม่ต้องตั้งค่าอะไรก่อน** (ไม่มีหน้าจอบังคับสร้างตัวละคร)
- อยากแก้เมื่อไหร่ก็ได้: แตะที่ตัวพ่อ/แม่ → เปิดหน้าเดียวกับแต่งตัวตัวเอง (`openCreator`) **แก้ได้ทั้งชื่อและทุกแถวของ `H_ROWS`**
- เก็บใน house save: `data.parents = {dad:{name, char}, mom:{name, char}}` — ไม่มีข้อมูลนี้ = ใช้ default (เด็กเก่าจึงไม่พัง)

**สุ่มรายวัน** — seeded RNG จาก `childId + วันที่` (แบบเดียวกับ `pickDailyQuests`) ⇒ วันนี้ได้พ่อก็พ่อทั้งวัน รีเฟรชไม่เปลี่ยน อีกคนพูดให้กำลังใจ/บอกใบ้

**เควสต์ 8 แบบ** (ค่าตอบแทน 50–90 🪙 สูงกว่า NPC ทั่วไป เพราะวันละชุดเดียว โควตาแยกจากกระดานและ NPC)

| id | ชื่อ | เล่นยังไง | ฝึก |
|---|---|---|---|
| `chore-tidy` | เก็บของเข้าที่ | ลากของที่วางผิดที่กลับหมวดของมัน | จัดหมวด |
| `shopping-list` | ไปซื้อของให้แม่ | จำรายการ 3–5 อย่าง ไปตลาด ซื้อครบ + เงินทอน | ความจำ+เงิน |
| `cook-help` | ช่วยทำอาหาร | เรียงขั้นตอน 4–5 ขั้นให้ถูกลำดับ | ลำดับเหตุการณ์ |
| `family-time` | กินข้าวพร้อมหน้า | ไปนั่งโต๊ะตอนพ่อแม่รออยู่ | เดินสำรวจ+เวลา |
| `wake-up` | ตื่นให้ตรงเวลา | หมุนเข็มนาฬิกาให้ตรง (รื้อ engine นาฬิกาวิเศษ) | เวลา |
| `laundry-sort` | แยกผ้าซัก | แยกตามสี/ประเภทลงตะกร้า | จัดหมวด+สี |
| `budget-help` | ใช้เงินให้พอ | ซื้อของให้ครบโดยไม่เกินงบ | บวกลบ+วางแผน |
| `pet-remind` | เตือนเรื่องสัตว์ | ให้อาหารตามชนิดที่บอก / พาไปหาหมอ | ความรับผิดชอบ |

**โทน**: เป็น "ขอความช่วยเหลือ" ไม่ใช่ "สั่งให้ทำ" ทำไม่ได้ไม่มีใครโกรธ ไม่มีบ่น ไม่มีดุ จบด้วยขอบคุณ+กอด

## 29. ของแต่งตัวเพิ่ม + ปุ่มเลือกเป็น SVG (แก้ `buildCreatorRows`)

**ปัญหา**: แถว `type:'num'` 7 แถวแสดงเป็นปุ่มตัวเลข `1 2 3 4…` — ขัดหลักการโปรเจคที่ว่าเด็ก 5 ขวบต้องเข้าใจด้วยสายตาโดยไม่ต้องอ่านออก

**แก้**: ทุกปุ่มในแถวรูปทรงวาดเป็น **SVG จำลองของชิ้นนั้นจริง** ผ่านฟังก์ชันใหม่ `outfitIcon(rowKey, i)` สไตล์แบนพาสเทลขอบมนเข้าชุดกับ `H_ROW_ICONS` ที่วาดไว้แล้ว · ปุ่ม "ไม่ใส่" ยังเป็น `✖` · **ชิปสีคงเดิมทั้งหมด**

| แถว | วันนี้ | ใหม่ | ที่เพิ่ม |
|---|---|---|---|
| ทรงผม `H_HAIR_N` | 6 | 12 | ผมเปีย มวยผม ผมหยิก ผมสั้นแสก หางม้าสูง บ๊อบ |
| ดวงตา `H_EYE_N` | 8 | 12 | ตาง่วง ตาหัวใจ ตากลมโต ตาปิดยิ้ม |
| ลายเสื้อ `H_PATTERN_N` | 10 | 20 | ลายดาว หัวใจ จุด ทางเฉียง สัตว์ ตัวเลข |
| เครื่องหัว `H_HAT_N` | 9 | 18 | หูสัตว์ มงกุฎ หมวกกันน็อก โบว์ ไหมพรม พวงมาลัย |
| แว่นตา `H_GLASS_N` | 7 | 12 | กันแดดหัวใจ ดำน้ำ นักบิน วิทยาศาสตร์ |
| ของสะพาย `H_BAG_N` | 7 | 14 | เป้ลายสัตว์ กระเป๋าคาดอก ถังน้ำ ปีกผีเสื้อ เป้จรวด |
| ของถือ `H_HOLD_N` | 9 | 18 | ไม้กายสิทธิ์ ลูกโป่ง ตาข่ายจับแมลง ไอศกรีม หนังสือ กล้อง |
| **รองเท้าแบบ `H_SHOE_N` (แถวใหม่)** | — | 8 | ผ้าใบ บูท แตะ รองเท้าเต้น บูทกันฝน |

56 → **106 แบบ** (+50 ยังไม่นับสี) · ต้องวาดไอคอน SVG ~106 ชิ้น ⇒ กันเป็นเฟสของตัวเอง

## 30. ตึกแล็บ = แหล่งโจทย์ STEM

ดร.ฟ้า 🔬 (`npc-lab1`) + `npc-lab2` + `npc-lab3` + นักเรียน `npc-stu*` วันนี้พูดได้อย่างเดียว ให้เป็นจุดเดียวในเมืองที่แจกโจทย์วิทย์/คณิตเชิงทดลอง (ดร.ฟ้าให้ยากสุด 60–100 🪙 · นักเรียนหน้าตึกให้เวอร์ชันง่าย)

| id | โจทย์ | สถานะ |
|---|---|---|
| `sink-float` | จม/ลอย — หย่อนของลงน้ำ ทายก่อนแล้วดูผลจริง | ใหม่ |
| `magnet-test` | แม่เหล็กดูดอะไรได้บ้าง | ใหม่ |
| `plant-grow` | เรียง เมล็ด→ต้นกล้า→ดอก→ผล | ใหม่ |
| `states-matter` | แข็ง/เหลว/แก๊ส จัด 3 ช่อง | ใหม่ |
| `animal-habitat` | สัตว์อยู่ถิ่นไหน ทะเล/ป่า/ทะเลทราย/ขั้วโลก | ใหม่ |
| `measure-lab` | ตวงน้ำตามขีด / วัดความยาว | ใหม่ |
| `mix-color-lab` | ผสมสีในหลอดทดลอง | รื้อ engine ผสมสี |
| `circuit-lab` | ต่อวงจรให้ไฟติด | รื้อ engine วงจรไฟฟ้า |
| `balance-lab` | ตาชั่งสมดุล | รื้อ engine ตาชั่ง |
| `shape-build` | ต่อรูปทรงตามแบบ | รื้อ engine แท็งแกรม |

### 30.1 เกม coding ในแล็บ (เพิ่ม 2026-08-07)

แล็บไม่ได้มีแค่วิทย์/คณิต — เป็นจุดเดียวในเมืองที่แจกโจทย์ **เขียนโปรแกรม** ด้วย
ของดีคือ **engine มีครบอยู่แล้ว** ใน `js/games-code.js`: `startCodeGame()` · `robotLevelsFor(cat)` · ชุดด่าน `ROBOT_LEVELS` 12 ชุดครอบ ป.1–ป.6 · รองรับ `codeLoop` (วนซ้ำ) และ `codeCond` (เงื่อนไข) อยู่แล้ว ⇒ **เลือกชุดด่านตามระดับชั้นเด็กได้ทันที แทบไม่ต้องเขียน logic ใหม่**

| id | โจทย์ | สถานะ |
|---|---|---|
| `robot-lab` | พาหุ่นยนต์เดินตามคำสั่งไปถึงเป้า (เลือกชุดด่านตามชั้นเด็ก) | รื้อ engine `mode:'code'` |
| `code-loop-lab` | ใช้คำสั่ง "วนซ้ำ" ให้โปรแกรมสั้นที่สุด | รื้อ engine + `codeLoop:true` |
| `code-if-lab` | "ถ้าเจอกำแพงให้เลี้ยว" — โปรแกรมมีเงื่อนไข | รื้อ engine + `codeCond:true` |
| `code-order` | เรียงบล็อกคำสั่งที่สลับอยู่ให้ถูกลำดับ **ก่อน**กดรัน | ใหม่ |
| `code-debug` | ในชุดคำสั่งมีบรรทัดผิดอยู่ 1 บรรทัด หาให้เจอแล้วแก้ | ใหม่ |

**แบ่งหน้าที่ NPC ในแล็บ** — ดร.ฟ้า `npc-lab1` 🔬 วิทยาศาสตร์ · `npc-lab2` วัด/ตวง/คณิต · `npc-lab3` **หุ่นยนต์กับ coding** · นักเรียนหน้าตึก `npc-stu*` เวอร์ชันง่ายสำหรับเด็กเล็ก

**รวมโจทย์ในแล็บ: 15 แบบ** (STEM 10 + coding 5)

**รวม mechanic ทั้งระบบ**: 23 (ชุดแรก) + 8 (ครอบครัว) + 6 (STEM เขียนใหม่) + 2 (coding เขียนใหม่) + 2 (ดนตรี) = **41 mechanic** × 3 tier = 123 แม่แบบ ⇒ **~3,500 โจทย์**
(อีก 7 แบบในแล็บใช้ engine เดิมที่รื้อในเฟส 5 จึงไม่นับซ้ำ)

## 31. เฟสสุดท้าย — เครื่องดนตรี

หมวดเฟอร์นิเจอร์ใหม่ 🎵 แตะแล้วมีเสียงจริง ใช้ Web Audio ชุดเดิมจาก `js/shared/piano.js` **ไม่ต้องโหลดไฟล์เสียงเพิ่มเลย**

| ระดับ | เครื่อง | แตะแล้ว | ราคา |
|---|---|---|---|
| 1 เสียงเดียว | ฉิ่ง แทมบูริน กรับ ระฆังลม | ดังทันที เด็กเล็กสุดเล่นได้ | 400–600 🪙 |
| 2 เล่นเป็นชุด | กล่องดนตรี ระนาด ขลุ่ย | ทำนองสั้น + ตัวโน้ตลอย | 700–900 🪙 |
| 3 เปิดหน้าเต็ม | เปียโน คีย์บอร์ด กีตาร์ อูคูเลเล่ | หน้าเล่นแบบ "เปียโนของหนู" | 1,000–1,500 🪙 |

**ร้านมีอยู่แล้ว** — `shop-music` "ร้านเครื่องดนตรี" 🎸 (ล็อต 7×4 แทน `home-6` เดิม) + พี่โน้ต `npc-musicshop` พร้อมบท quest "ช่วยพี่ทายเสียงเครื่องดนตรีหน่อยไหม" สร้างไว้แล้วเมื่อ 2026-08-06 ⇒ เฟส 9 แค่เติมสินค้ากับ mechanic ไม่ต้องสร้างร้านใหม่

11 ชิ้น เป็นของแพงที่สุดในเกมโดยตั้งใจ · เควสต์ผูก: `play-along` (ฟังทำนอง 4–6 โน้ตแล้วเล่นตาม) · `find-sound` (ฟังเสียงทายเครื่อง) — ต้องมีเครื่องในบ้านก่อน แต่ไม่ล็อกทางตันเพราะยังมีอีก 37 mechanic

## 32. แผนเฟสฉบับใหม่ — 9 เฟส (แทนข้อ 21)

| เฟส | เนื้อหา | ทำไมลำดับนี้ |
|---|---|---|
| ~~**1**~~ ✅ | ~~เศรษฐกิจ + migration + **ชุดเฟอร์นิเจอร์เริ่มต้น 11 ชิ้น** + ร้านเฟอร์นิเจอร์/แฟชั่น~~ **เสร็จ 2026-08-07** (ดูข้อ 17.5 ว่าทำต่างจากแผนตรงไหน) | ต้องปิดของฟรีก่อน และของเก่าของเด็กต้องไม่หาย |
| **2** | quest engine + `quiz` ครบ 57 NPC + กระดาน 5/วัน + **ประตูเช็คความพร้อม** | หาเงินได้ทั้งเมืองทันที ระบบความยากถูกตั้งแต่วันแรก |
| **3** | สัตว์เลี้ยง: ร้าน · บ้านตามเงื่อนไข · อาหาร · ความหิว · ป่วย · หมอ | ก้อนใหญ่สุด ต้องรอเศรษฐกิจ |
| **4** | **พ่อ แม่ + เควสต์ครอบครัว 8 แบบ** | ผูกกับสัตว์เลี้ยง (`pet-remind`) จึงมาหลังเฟส 3 |
| **5** | mechanic กลุ่ม C — รื้อ engine เดิม 7 แบบ | คุ้มสุดต่อแรง |
| **6** | **แล็บ STEM 10 แบบ + coding 5 แบบ** | 7 ใน 15 ใช้ engine ที่รื้อไว้แล้วในเฟส 5 (รวม `mode:'code'` ที่มีครบอยู่แล้ว) |
| **7** | mechanic กลุ่ม B ที่เหลือ (10) + D จาก IDEA.md (4) | งานเขียนใหม่ล้วน ไม่รีบ |
| **8** | **เฟอร์นิเจอร์ +64 · ของแต่งตัว +50 · ปุ่ม SVG ~106 ไอคอน** | งานวาดล้วน ไม่แตะ logic ทำคู่ขนานได้ |
| **9** | **เครื่องดนตรี 11 ชิ้น + เควสต์ดนตรี 2 แบบ** | ปิดท้ายตามสั่ง ต้องมีเศรษฐกิจถึงหลักพันก่อน |

⚠️ **ขอบเขตโตขึ้น**: 6 → 9 เฟส · mechanic 23 → 41 · เฟอร์นิเจอร์ 116 → 180 · ของแต่งตัว 56 → 106 + ไอคอน SVG วาดมือ ~106 ชิ้น ⇒ **เฟส 8 คือก้อนที่บวมที่สุด** ถ้าอยากได้ระบบเล่นได้เร็วแนะนำแบ่งย่อยเป็นชุดละ 20 ไอคอน

## 33. สถานะการตัดสินใจ — ปิดครบแล้ว

- [x] ราคา / ค่าตอบแทน — ใช้ตัวเลขข้อ 17 + 19
- [x] จำนวนสัตว์เลี้ยง — 1 ตัว ซื้อซ้ำจ่ายครึ่งเดียว
- [x] ความหิว — ลดตามวันที่เข้าเล่น (−35/วันเล่น)
- [x] ของแถมฟรีตอนเริ่ม — ชุดมาตรฐาน **11 ชิ้นตามข้อ 26** วางในบ้านให้เลย (ปรับลดแล้ว 2026-08-07)
- [x] ชื่อ/หน้าตาพ่อแม่ — **มี default มาให้ แต่เด็กแก้ชื่อ+แต่งตัวใหม่เองได้ทั้งหมด** (ข้อ 28)
- [x] **เฟส 1 — เสร็จแล้ว 2026-08-07** (`js/house-shop.js` + จุดเชื่อมใน `js/house.js`/`js/house-map.js`/`index.html`/`css/style.css` + เทส `tests/house-shop.spec.js` 5 เทส)
- [ ] **เริ่มเฟส 2** (quest engine + `quiz` ครบ 57 NPC + กระดาน 5/วัน + ประตูเช็คความพร้อม) — รอสัญญาณจากผู้ใช้

## 34. สิ่งที่เฟส 1 ทิ้งไว้ให้เฟสถัดไป (เช็คลิสต์)

- [ ] **เฟส 2**: เควสต์ต้องจ่ายเหรียญผ่าน `window.OwlCoins.add()` เท่านั้น (ห้ามแตะ `progress.coins`)
- [ ] **เฟส 2-3**: ย้ายหมวดของนอกบ้านจากห้างเฟอร์นิเจอร์ไปร้านต้นไม้/ร้านของเล่นตามข้อ 17.4 (แก้ `SHOPS` ใน `js/house-shop.js` ที่เดียว)
- [ ] **เฟส 3**: ทำ migration ส่วนสัตว์เลี้ยงที่ยกไว้ (ข้อ 17.3) + ย้ายจุด seed `pet-house` ไปตอนซื้อ
- [ ] **เฟส 8**: ปุ่มเลือกชุดในร้านแฟชั่นยังเป็น "เลข + ชื่อไทย" อยู่ (`FIT_NAMES` ใน `js/house-shop.js`) — ตอนวาดไอคอน SVG 106 ชิ้นให้มาเปลี่ยนที่ `makeCard()` ด้วย

**แผนนี้อนุมัติแล้ว** — session ถัดไปที่ได้รับคำสั่งให้ทำต่อ ให้เริ่มที่เฟส 2 ตามข้อ 32 ได้เลย โดยยึดกติกาเหล็ก 7 ข้อในหัวเอกสาร

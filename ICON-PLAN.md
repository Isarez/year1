# 🎨 แผนเปลี่ยน emoji เป็นไอคอน SVG (ส่วนที่เหลือ)

> เขียน 2026-08-19 · ต่อจากกลุ่ม 1-2 ที่ทำเสร็จแล้ว (122 ไอคอนใน `js/house-icons.js`)
> **สถานะ 2026-08-19: ผู้ใช้อนุมัติให้ทำ top 300 แล้ว · เริ่มลงมือแล้ว**
> ✅ ทำไปแล้ว: ร้านสัตว์เลี้ยง (สัตว์ 12 + แท็บ 4) · หัวใจในหน้าแต่งตัว · **เฟส A เฟอร์นิเจอร์ครบ 193 ชิ้น**
> ✅ **เฟส A/B/C เสร็จ · D/E ทำตามขอบเขตที่ตัดสินไว้แล้ว** (2026-08-20)
>  - A เฟอร์นิเจอร์ 193 · B ป้ายร้าน 22 + ชาวบ้าน 68 + พ่อแม่/กระดาน
>  - C **แยกไฟล์แล้ว**: `js/shared/icons.js` (แกนกลาง + ไอคอน UI กลาง · โหลดทั้ง 2 หน้า)
>    · `js/house-icons.js` (แพ็กโหมดบ้าน · lazy) · `js/icons-quiz.js` (แพ็กคลังโจทย์ · หน้าหลัก)
>  - D/E **ตัวเรนเดอร์ `OwlIcons.text()` + แพ็กคลังโจทย์ 101 ไอคอน / map 118 emoji**
> 📌 ข้อ ① ตัดสินแล้ว: **วาด SVG เอง** (ผู้ใช้เลือก 2026-08-19)

## ทำไมต้องทำ

emoji วาดด้วยฟอนต์ของเครื่อง ⇒ Windows / Android / iOS / macOS ได้คนละรูป คนละขนาด
คนละน้ำหนักสี **และบางตัวไม่มี glyph เลย** (ขึ้นเป็นกล่องสี่เหลี่ยม) — เด็ก 5 ขวบดูรูปเป็นหลัก
ถ้ารูปไม่มา = เล่นไม่ได้ ไม่ใช่แค่ไม่สวย

---

## 📊 ของที่เหลือ (นับจริงจากซอร์ส 2026-08-19)

| กลุ่ม | ที่อยู่ | emoji ไม่ซ้ำ | โผล่ตรงไหน |
|---|---|---|---|
| **A** เฟอร์นิเจอร์ | `js/house-furniture.js` | ~190 | การ์ดในร้าน · แผงโหมดตกแต่ง · toast |
| **B** NPC + ป้ายร้าน | `js/house-map.js` | ~70 | ป้ายชื่อลอยเหนือหัว · การ์ดคุยกับ NPC |
| **C** UI + เกมหน้าหลัก | `js/app-ui.js`, `app-core.js`, `games-*.js`, `index.html` | ~150 | ปุ่ม · ป้ายสถานะ · หน้าสรุปผล |
| **D** โจทย์โหมดบ้าน | `js/house-quest-data.js` | ~490 | เนื้อโจทย์ในการ์ดเควสต์ |
| **E** โจทย์หน้าหลัก + คลัง engine | `js/data-cats-*.js`, `js/data-pools.js` | ~780 | เนื้อโจทย์ทุกเกม |

**คลังโจทย์ (D+E) รวมกัน = emoji ไม่ซ้ำ 912 ตัว ใช้จริง 10,661 ครั้ง**

### 🔑 การกระจายตัวแบบ "หางยาว" — ตัวเลขที่ตัดสินขอบเขตงาน

| วาดกี่ตัว | ครอบคลุมการใช้งานจริง |
|---|---|
| top 50 | 33.1% |
| top 100 | 49.1% |
| top 150 | 60.2% |
| **top 300** | **80.5%** |
| ครบ 912 | 100% |

⇒ **วาด 300 ตัวได้ 80% · อีก 612 ตัวที่เหลือได้เพิ่มแค่ 19.5%**

### 🔍 "หาง 612 ตัว" คือไอคอนของอะไร (แจกแจง 2026-08-19)

ครึ่งหนึ่งของหางโผล่ **ไม่เกิน 2 ครั้งทั้งเกม** (โผล่ครั้งเดียว 190 ตัว · 2 ครั้ง 115 ตัว)

| กลุ่ม | จำนวน | ใช้กี่ครั้ง | ตัวอย่าง |
|---|---|---|---|
| 🐾 สัตว์ | 64 | 209 | 🦓 🦘 🦜 🦥 🦭 🦔 🐫 🦞 |
| 🍜 อาหาร/ผลไม้ | 70 | 256 | 🥭 🫐 🥟 🥮 🍢 🫓 🧇 |
| 🚕 ยานพาหนะ/สถานที่ | 70 | 240 | 🚜 🛰️ 🏰 🗼 🕌 🚇 🎡 |
| ➡️ ลูกศร/สัญลักษณ์ | 44 | 129 | ⏭️ ⏪ 🔠 🅰️ 🔣 ✳️ |
| 🌋 ธรรมชาติ/อวกาศ/อากาศ | 24 | ~90 | 🪐 🌋 🌩️ 🌘 🍁 |
| 🎮 กีฬา/ของเล่น | 22 | ~75 | 🏸 ♟️ 🪅 🛹 🪂 |
| 🧰 ของใช้/เครื่องมือ | 21 | ~70 | 🧯 🪝 🗜️ ⛏️ 🪒 |
| 🕐 หน้าปัดนาฬิกา (ครึ่งชั่วโมง) | 20 | 73 | 🕜 🕝 🕞 🕟 |
| 📔 เครื่องเขียน/โรงเรียน | 19 | ~60 | 📗 🗂️ 🖇️ ✒️ 📇 |
| 👕 เสื้อผ้า/ของใช้ตัว | 25 | 65 | 👘 🥻 🩰 🥿 🪭 |
| 🧑 คน/อาชีพ | 19 | 68 | 🧑‍🎓 👨‍🍳 🧑‍🚒 🤰 |
| 🧪 วิทยาศาสตร์/สุขภาพ | 16 | ~55 | 🫁 🫀 🩺 💉 🦴 |
| 🎨 สี/รูปทรง | 15 | ~50 | 💛 🟤 🔶 🔻 🟰 |
| 🎷 ดนตรี | 11 | ~35 | 🪕 🪘 🎷 🪇 📯 |
| 😊 **หน้า/อารมณ์** | **34** | 88 | 😷 🤗 😨 🙅 — **ไม่ต้องทำ** (กติกาข้อ 5 อนุญาตให้เป็น emoji) |
| อื่นๆ ปะปน | ~148 | ~600 | ของโดดๆ ที่ไม่เข้ากลุ่มไหน |

### 💡 หางแบ่งได้เป็น 2 กองที่ "ราคาต่างกันมาก"

- 🟢 **กองถูก (~194 ตัว)** — สัตว์ 64 · อาหาร 70 · เสื้อผ้า 25 · นาฬิกา 20 · สี/รูปทรง 15
  พวกนี้อยู่ใน **ตระกูลที่ต้องวาดทรงร่วมอยู่แล้วใน top 300** ⇒ เพิ่มตัวที่ 65 ของตระกูลสัตว์
  = เติม 1 บรรทัด (ทรง + สี) เหมือนปลา 48 ชนิดที่ใช้ 23 ทรง **แทบไม่มีต้นทุนเพิ่ม**
- 🔴 **กองแพง (~246 ตัว)** — ยานพาหนะ/สถานที่ · ลูกศร/สัญลักษณ์ · เครื่องเขียน · วิทย์ ·
  ของใช้ · กีฬา · ดนตรี · คน/อาชีพ — **ของชิ้นเดียวโดดๆ ต้องวาดใหม่ทีละตัว**
  และเกินครึ่งโผล่ในเกมแค่ 1-2 ครั้ง
- ⚪ **ไม่ต้องทำ (34 ตัว)** — หน้า/อารมณ์

⇒ **ข้อเสนอที่ปรับแล้ว: ทำ top 300 + กองถูกทั้งหมด (~494 ตัว) ครอบคลุมราว 86-88%**
เหลือทิ้งไว้เป็น emoji เฉพาะ "ของโดดๆ ที่โผล่ 1-2 ครั้ง" ซึ่งเป็นกลุ่มที่แพงที่สุดและคุ้มน้อยที่สุด

---

## 🗺️ ลำดับเฟส (ทำไล่จากบนลงล่าง)

### เฟส A — เฟอร์นิเจอร์ ~190 ชิ้น 🥇 คุ้มที่สุด
- **ทำไมมาก่อน**: เป็น "ของที่เด็กต้องเลือกซื้อ/เลือกวาง" — ดูรูปไม่ออก = ซื้อผิดชิ้น
  และอยู่ในหน้าจอที่เด็กเปิดบ่อยที่สุด 2 หน้า (ร้าน + โหมดตกแต่ง)
- **วิธี**: ไม่วาด 190 รูป — จับกลุ่มเป็น **~35 ทรงร่วม** แล้วต่างกันที่สี/ลาย
  (เก้าอี้ · โต๊ะ · ตู้ · เตียง · โคมไฟ · ต้นไม้ · เครื่องเล่นสนาม · เครื่องดนตรี · ของใช้ในครัว …)
  กติกาเดียวกับปลา 48 ชนิดที่ใช้ 23 ทรงร่วม
- **ประเมิน**: 4-6 รอบงาน

### เฟส B — NPC + ป้ายร้าน ~70
- **วิธี**: หัว+ตัวทรงเดียวกันทั้งหมด ต่างกันที่ **หมวก/ของถือประจำอาชีพ + สีชุด** (~10 ทรงร่วม)
- **ประเมิน**: 1-2 รอบงาน

### เฟส C — UI + เกมหน้าหลัก ~150
- **วิธี**: วาดทีละตัว (ไม่มีทรงร่วมให้ใช้) แต่จำนวนไม่มากและซ้ำกับของที่วาดไว้แล้วบางส่วน
- ⚠ **หน้าครูใช้ `js/shared/*` ร่วมกับหน้าเด็ก** — ของเดิมต้องไม่เปลี่ยนพฤติกรรม
- **ประเมิน**: 3-4 รอบงาน

### เฟส D — โจทย์โหมดบ้าน ~490 (ทำเฉพาะที่ใช้บ่อย)
- ⚠ **ต้องแยก 2 ชนิดก่อน**: emoji ที่เป็น "ไอเทมในโจทย์" (แทนได้) กับ emoji ที่อยู่ **กลางประโยค**
  (แทนตรงๆ ไม่ได้ ต้องมีตัวเรนเดอร์ที่แปลงข้อความ → HTML)
- **ประเมิน**: 4-6 รอบงาน

### เฟส E — โจทย์หน้าหลัก ~780 (ทำเฉพาะ top 300 รวมกับเฟส D)
- ใหญ่และเสี่ยงที่สุด · แตะคลังโจทย์ 332 หมวดที่ผูกกับ progress ของผู้ใช้จริง
- **ประเมิน**: 6-10 รอบงาน

---

## 🏗️ งานโครงสร้างที่ต้องทำก่อนเริ่มเฟส C (สำคัญ)

ตอนนี้ `js/house-icons.js` **โหลดเฉพาะตอนเข้าโหมดบ้าน** และชื่อ global คือ `window.HouseIcons`
⇒ หน้าหลักเรียกใช้ไม่ได้เลย

1. **แยกเป็น core + แพ็ก**
   - `js/shared/icons.js` — core: `dk/lt/eye/G` + registry + `html/htmlOr/has/ids` (โหลดทั้ง 2 หน้า)
   - `js/icons-main.js` — UI หน้าหลัก (โหลดหน้าหลัก)
   - `js/icons-house.js` — ของเดิม (โหลด lazy ตอนเข้าบ้านเหมือนเดิม)
   - `js/icons-quiz.js` — ไอคอนในโจทย์ (โหลดตอนเริ่มเล่นเกม)
   - `window.HouseIcons` **ต้องยังใช้ได้เหมือนเดิม** (alias ไปที่ core) — ไฟล์โหมดบ้านเรียกอยู่หลายสิบจุด
2. **งบขนาดไฟล์**: ปัจจุบัน ~6 บรรทัด/ไอคอน · ถ้าถึง 1,200 ไอคอน ≈ 7,000 บรรทัด ≈ 250 KB
   ⇒ **ห้ามยัดไฟล์เดียว** ต้องแยกแพ็กและโหลดเท่าที่ใช้ (เครื่องเป้าหมายคือแท็บเล็ตเด็ก)

---

## 🔒 กติกาที่ต้องคงไว้ทุกเฟส (มาจากรอบที่ทำไปแล้ว)

1. **ไม่มีไอคอน = ถอยไปใช้ emoji เดิมเสมอ ห้ามพัง** (`htmlOr(id, emoji)`)
   ⇒ **ตาราง `e:`/`emoji:` ของไอเทมทุกตัวยังต้องอยู่ครบ ห้ามลบ**
2. **ต่างกันที่ "เงารวม" ไม่ใช่รายละเอียดเล็ก** — ที่ 26px รายละเอียดหายหมด
3. **ทรงร่วม + สีประจำตัว** ไม่ใช่วาดใหม่ทีละตัวให้คล้ายกันหมด
4. **ต้องถ่ายภาพหน้ารวมมาดูด้วยตาเสมอ** — เทสที่เช็คแต่สตริงจับ "รูปผิด" ไม่ได้
   (บทเรียนจริง: ปลา 19 ชนิดตาอยู่ข้างเดียวกับหาง เทสเขียวหมด)
5. **อารมณ์/ท่าทางในฟองคำพูดยังเป็น emoji ได้** (ผู้ใช้อนุญาต) — ที่ต้องเป็น SVG คือ
   ไอคอนของ "ของ/ปุ่ม/สถานะ" ที่เด็กต้องดูแล้วรู้ว่าคืออะไร
6. **ห้ามลบ/เปลี่ยน `cat.emoji` ของหมวดในหน้าหลัก** — ผูกกับสมุดสติกเกอร์และ progress ของผู้ใช้

## 🧪 เครื่องมือที่มีแล้ว (ใช้ต่อได้เลย)

- `tests/_shotIco2.spec.js` — ถ่ายแผ่นรวมไอคอนทั้งคลังพร้อมชื่อไทย (หน้าละ 24)
- `tests/_shotZoom.spec.js` — ซูมดูทีละตัวที่ 150px (`ICO_LIST=... ICO_OUT=...`)
- `tests/_shotFurn.spec.js` — เรนเดอร์โมเดล 3D ของตกแต่งเป็นแผ่นรวม (ใช้เทียบว่าไอคอนตรงกับของจริงไหม)
- เทส `IC8` — เรียกวาดทุก id จริง (จับ id ที่ลงทะเบียนไว้แต่วาดแล้วพัง)
- เทส `IC1/IC2` — ไอเทมทุกชิ้นต้องมีไอคอน · เป็น SVG จริง · ไม่ซ้ำกันเอง

---

## ❓ ต้องตัดสินใจก่อนเริ่ม (รอผู้ใช้)

### ① เฟอร์นิเจอร์ 190 ชิ้น — วาดมือ หรือ เรนเดอร์จากโมเดล 3D ที่มีอยู่แล้ว?

| | วาด SVG ทรงร่วม (แนะนำ) | เรนเดอร์ PNG จากโมเดล 3D |
|---|---|---|
| เวลา | 4-6 รอบงาน | ~1 รอบงาน |
| ความตรง | ต้องคุมเองว่าตรงกับโมเดล | **ตรงกับของจริง 100% เสมอ** |
| หน้าตา | เข้าชุดกับไอคอนแบนที่วาดไว้แล้ว | **คนละภาษาภาพกับไอคอนชุดเดิม** |
| ขนาดไฟล์ | ~1 KB/ตัว (โค้ด) | ~1-3 KB/ตัว (ไฟล์ภาพ 190 ไฟล์) |
| แก้โมเดลแล้ว | ต้องแก้ไอคอนตาม | ต้องเรนเดอร์ใหม่ (สคริปต์รันซ้ำได้) |

**คำแนะนำ: วาด SVG ทรงร่วม** — เพราะเหตุผลตั้งต้นของงานนี้คือ "ให้ทุกเครื่องเห็นรูปเดียวกัน"
ซึ่งทั้ง 2 ทางแก้ได้เท่ากัน แต่ความ **เข้าชุดกันของภาพ** เป็นสิ่งที่ทางที่ 2 เสียไปถาวร

### ✅ ② คลังโจทย์ (เฟส D+E) — ตัดสินแล้ว 2026-08-20

**ขอบเขตที่เลือก: แปลงเฉพาะจุดที่ "emoji คือของที่เด็กต้องดูแล้วรู้ว่าคืออะไร"**

| จุด | แปลงแล้ว | เหตุผล |
|---|---|---|
| รูปประกอบโจทย์ (`#q-emoji`) | ✅ | เด็กดูรูปนี้เพื่อเข้าใจโจทย์ |
| แถวการ์ดแพทเทิร์น | ✅ | เป็นตัวโจทย์โดยตรง |
| เงาปริศนา (`#shadow-prompt`) | ✅ | ทั้งเกมคือ "ดูเงาแล้วทาย" |
| ของในเกมนกฮูกสั่ง (`#ef-item`) | ✅ | เด็กต้องดูของแล้วตัดสินใจแตะ/ไม่แตะ |
| **ข้อความบนปุ่มตัวเลือก** | ❌ **ตั้งใจไม่แตะ** | ข้อความนั้นคือ **เฉลย** ที่ระบบใช้เทียบคำตอบ — เปลี่ยนเป็นรูปแล้วความถูกต้องของเกมจะไปผูกกับว่ามีไอคอนครบหรือไม่ |
| emoji ในประโยคอธิบาย/คำใบ้ | ❌ ตั้งใจไม่แตะ | เป็นของประดับ · glyph หายไม่ทำให้เล่นไม่ได้ |

**กลไกที่วางไว้ให้โตต่อได้เอง**: `OwlIcons.text(str)` แทน emoji ที่ "มีไอคอน" ให้อัตโนมัติ
และ **ปล่อยตัวที่ยังไม่มีไว้เป็น emoji เดิม** ⇒ เพิ่มไอคอนใหม่ = เติมตาราง `mapEmoji` อย่างเดียว
ไม่ต้องแตะโค้ดหน้าจออีก (ตอนนี้ครอบคลุม 118 emoji · เติมได้เรื่อยๆ)

⚠ `text()` **escape ข้อความทั้งหมดเสมอ** ⇒ ใส่ข้อความจากผู้ใช้ได้ปลอดภัย
⚠ ข้าม emoji ที่อยู่ในลำดับ ZWJ (👩‍🏫) และตัด variation selector (U+FE0F) ก่อนเทียบ

### (เก็บไว้อ้างอิง) ตัวเลือกที่เคยพิจารณา

| ทางเลือก | วาดกี่ตัว | ครอบคลุม | หมายเหตุ |
|---|---|---|---|
| top 300 เฉยๆ | 300 | 80.5% | ตัดหางทิ้งทั้งหมด |
| **top 300 + กองถูก (แนะนำ)** | **~494** | **~86-88%** | หางที่อยู่ในตระกูลทรงร่วมอยู่แล้ว (สัตว์/อาหาร/เสื้อผ้า/นาฬิกา/สี) เติมแทบไม่มีต้นทุน |
| ครบทุกตัว | 878 (ไม่นับหน้า/อารมณ์ 34) | 100% | ที่เพิ่มมาคือของโดดๆ ที่โผล่ 1-2 ครั้ง — แพงที่สุด คุ้มน้อยที่สุด |

---

## 📌 หมายเหตุ

- ตัวเลข emoji ในตารางนับจากซอร์สโดยตัดบรรทัดคอมเมนต์ออกแล้ว แต่ยังมีบางส่วนที่เป็น
  emoji ในข้อความ toast/คำอธิบาย (ไม่ใช่ไอคอนของไอเทม) ⇒ **ตัวเลขจริงที่ต้องวาดจะน้อยกว่านี้เล็กน้อย**
- แต่ละเฟสจบแล้วต้อง: ถ่ายแผ่นรวมมาดูด้วยตา → เพิ่มเทส → อัปเดต `CLAUDE.md`

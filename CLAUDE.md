# นกฮูกสนุกคิด — เตรียมสอบ ป.1

เว็บแอปสำหรับเตรียมสอบเข้า ป.1 ไม่มี build step (ไฟล์ static ล้วนๆ) deploy ผ่าน GitHub Pages จาก branch `main` ที่ https://isarez.github.io/year1/

ชื่อระบบ/แบรนด์: **Owlkids** — มีโดเมนชื่อ **https://owlkids.net**

## หลักการออกแบบหลัก (ยึดทุก feature/ทุกการตัดสินใจ)

- **Static single page template เสมอ** — ไม่มี build step, ไม่มี framework/bundler (React/Vue/webpack/vite ฯลฯ), ไม่มี backend/server-side code ทุก feature ใหม่ต้องทำงานได้ด้วยไฟล์ static ล้วนๆ (HTML/CSS/JS + asset) เปิดผ่าน GitHub Pages ได้ตรงๆ ถ้าจะใช้ library ภายนอก (เช่น Three.js) ต้อง self-host ไฟล์ไว้ใน repo เอง ห้ามพึ่ง CDN หรือ build tool ใดๆ
- **ธีม/ดีไซน์ทุกส่วนต้องออกแบบให้เหมาะกับเด็กอายุ 5 ขวบ** — สีสดใส ทรงมนน่ารัก ตัวอักษรใหญ่อ่านง่าย ไอคอน/ภาพต้องเข้าใจง่ายด้วยสายตาไม่ต้องอ่านออกเขียนได้คล่อง หลีกเลี่ยงองค์ประกอบที่ดูน่ากลัว/สมจริงเกินไป/ซับซ้อนเกินวัย ก่อนออกแบบ element ใหม่ให้อ้างอิงสไตล์ภาพ/ธีมที่มีอยู่แล้วในแอป (การ์ตูนแบน, สีพาสเทล/สดใส, ไอคอน SVG มน ๆ) ให้ไปในทิศทางเดียวกันเสมอ
- **localStorage คือฐานข้อมูลหลักของระบบทั้งหมด ไม่มี backend/server เก็บข้อมูลผู้ใช้** ทุก feature ใหม่ (progress, ตัวละคร, ของสะสม ฯลฯ) ต้องออกแบบให้เก็บ/อ่านผ่าน `localStorage` เป็นหลัก และคิดเผื่อ merge เข้ากับระบบ export/import ย้ายข้อมูลข้ามอุปกรณ์ที่มีอยู่แล้วด้วยเสมอ

## โครงสร้างไฟล์

- `index.html` — markup ล้วนๆ, อ้างอิง CSS/JS ผ่าน `<link>`/`<script src>`
- `css/style.css` — สไตล์ทั้งหมด
- `version` — ไฟล์ text ธรรมดา (ไม่มีนามสกุล) เก็บเลขเวอร์ชัน เช่น `v1.2.0` — **แก้เลขเวอร์ชันที่นี่เมื่อ release ใหม่** หน้าเว็บดึงค่านี้ผ่าน `fetch('version')` แล้วแสดงใน footer
- **ไฟล์ JS แยกตามหน้าที่ (แยกจาก `data.js`/`app.js` ก้อนเดียวเมื่อ 2026-07-30) — ยังเป็น classic script ธรรมดา ไม่มี module/build step ทุกไฟล์ใช้ global scope เดียวกัน**
  - ข้อมูลหมวด: `js/data-cats-prep.js`, `data-cats-p1.js` … `data-cats-p6.js` (ชั้นละไฟล์ ประกาศ `CATS_PREP`/`CATS_P1`…) แล้ว `js/data-cats.js` ต่อทั้งหมดเป็น `CATS` ตัวเดียว (**ลำดับในอาเรย์ = ลำดับการ์ดในหน้าหลัก ห้ามสลับ**) + `GRADES` + `CAT_REQUIRES`
  - `js/data-pools.js` — คลังข้อมูลของ engine ทั้งหมด (`AR_SENTENCES`, `LISTEN_WORDS`, `ROBOT_LEVELS`, `SORT_POOLS`, `DOTS_SHAPES` ฯลฯ)
  - `js/owl-messages.js` — `OWL_MSGS` ข้อความให้กำลังใจของนกฮูก
  - `js/app-core.js` — state, โปรไฟล์เด็ก, ระดับชั้น/อายุ, เสียง+เพลง, confetti, helper กลาง (`showOnlyView`, `beginSkillGame`, `finishP2Game`), render หน้าเลือกหมวด
  - `js/game-quiz.js` (ควิซ+หน้าสรุปผล) · `js/games-think.js` (จับคู่/ทายเงา/นกฮูกสั่ง/นักวิทย์) · `js/games-math.js` (เงิน เศษส่วน ตาชั่ง ปฏิทิน เส้นเวลา จัดหมวด โลก พิกัด แผนภูมิ พื้นที่ มุม นาฬิกา วงจรไฟฟ้า แท็งแกรม กระจก เรียงลำดับ) · `js/games-code.js` · `js/games-write.js` · `js/games-art.js` (ผสมสี/ดนตรี/เปียโน) · `js/games-listen.js` (ฟังคำ/เติมคำ) · `js/games-ar.js` (AR + handPlay + lazy-load โหมดบ้าน)
  - `js/app-ui.js` — ปุ่ม/toast/สมุดสติกเกอร์/modal/ย้ายข้อมูล/ธีม/พื้นหลัง **และบรรทัด INIT ท้ายไฟล์**
  - `js/shared/*.js` — **โค้ดกลางที่หน้าเด็กกับโหมดคุณครูใช้ร่วมกันจริงๆ (โหลดทั้ง 2 หน้า) แก้ที่นี่ที่เดียวมีผลทั้งคู่**: `audio.js` (เสียง/เพลง), `ui.js` (toast/ธีม/overlay/shuffle), `speech.js` (เลือกเสียงอ่าน), `piano.js` (คีย์เปียโน/โน้ต), `ar-core.js` (engine AR ลากการ์ด/โยงเส้น/มือการ์ตูน/กล้อง) — ฟังก์ชันที่ **ตั้งใจให้ต่างกัน** ระหว่าง 2 หน้า (เช่น `buildLevel`, `levelSuccess`, `updateArCursor`, `playMusicSequence`) ยังแยกอยู่ไฟล์ของใครของมัน ห้ามยุบรวม
  - โหมด **"บ้านของหนู" (3D)** — `js/house.js` (engine) + `js/house-map.js` (ผังเมือง/ข้อมูลแผนที่ ประกาศ `HOUSE_MAP()`) + `js/house-furniture.js` (คลังเฟอร์นิเจอร์ ประกาศ `HOUSE_FURNITURE()`) + `js/house-shop.js` (ร้านค้า/เศรษฐกิจ ประกาศ `HOUSE_SHOP()` — ตารางราคา, คลังสิทธิ์ `data.unlocked`, migration `econVer`, ชุดเริ่มต้น, หน้าร้าน · house.js เปิดออกมาที่ `window.HouseShop`) + `js/house-quests.js` (เควสต์ ประกาศ `HOUSE_QUESTS()` — กลไก `quiz`/`count`, สุ่มรายวันแบบ seeded, สูตรดาว→เหรียญ, กระดาน 5 ชุด/วัน, ประตูเช็คความพร้อม · **ไม่แตะ DOM/WebGL เลย** ป้าย "!" กับหน้าจอเล่นอยู่ใน house.js · เปิดออกมาที่ `window.HouseQuests`) + `js/house-family.js` (พ่อ-แม่ในบ้าน ประกาศ `HOUSE_FAMILY()` — ชื่อ/หน้าตา default + บทพูด · **ไม่แตะ DOM/WebGL** · เปิดที่ `window.HouseFamily`) + `js/house-pet-care.js` (การดูแลสัตว์เลี้ยง ประกาศ `HOUSE_PET_CARE()` — ความหิว/อาหาร/ป่วย/ค่ารักษา · **ไม่แตะ DOM/WebGL เลย** เหมือน house-quests.js · เปิดออกมาที่ `window.HousePetCare`) + `js/house-games.js` (สะพานไป engine หน้าหลัก) + `js/house-qbrowse.js` (หน้า "คลังคำถาม" — เครื่องมือเทส เปิดออกมาที่ `window.HouseQB`) + `js/house-devtools.js` (หน้า "ปรับค่าต่างๆ" — เครื่องมือเทส เปิดออกมาที่ `window.HouseDev`) + `js/vendor/three.min.js` — **ทั้งชุดไม่ได้อยู่ใน `index.html`** แต่ `js/games-ar.js` โหลดให้แบบ lazy ตามลำดับ three → map → furniture → shop → quests → pet-care → family → house → qbrowse → devtools ตอนกดเข้าบ้าน
    - 📚 **หน้า "คลังคำถาม" (`js/house-qbrowse.js`) — เครื่องมือเทสโจทย์ในโหมดบ้าน** เข้าจากปุ่ม `#house-qb-btn` ในเมนูเฟือง (`#house-ctrl-list`) · โจทย์ในเกมสุ่มทั้งหมด หน้านี้จึงกางคลังจริงเป็นตาราง **แยกตามระดับชั้น × กลไก** แล้วกดปุ่ม ▶ เล่นทีละข้อได้ (กลไก `quiz` = ทุกข้อในคลัง `CATS` ของชั้นนั้น แยกหมวด/วิชา/ชนิดโจทย์ img·pattern·emoji·ข้อความ · กลไก `count` = 16 ธีมของ + ตัวอย่างโจทย์ที่สุ่มได้ + รายชื่อ NPC ที่ใช้ธีมนั้น)
      - 🔒 **`QB_FEATURE_OFF` — หน้านี้เป็นเครื่องมือเทส ไม่ใช่ของให้เด็กเล่น ⇒ ก่อน merge เข้า `main`/deploy ทุกครั้งต้องปิดเสมอ** (ผู้ใช้สั่งไว้ 2026-08-08: เปิดไว้บน branch `feature/house-owl` เพื่อเทสคำถามตลอดทั้ง 9 เฟส แต่ deploy จริงต้องปิด) — สวิตช์อยู่ **จุดเดียว** คือ `const QB_ENABLED` ที่ต้นไฟล์ `js/house-qbrowse.js` ตั้งเป็น `false` แล้วจบ ไม่ต้องแตะ `index.html`/CSS (ปุ่มถูกซ่อนด้วย inline style จึงไม่ติดกับดัก specificity แบบ `.house-entry-btn[hidden]`) และชุดเทส `tests/house-qbrowse.spec.js` จะ `test.skip()` ตัวเองอัตโนมัติ เทสยังเขียวครบ — ค้นด้วย `grep -rn QB_FEATURE_OFF`
      - ⚠️ **เล่นในหน้านี้ต้องไม่แตะข้อมูลเด็กเด็ดขาด** — ไม่จ่ายเหรียญ ไม่กินโควตาเควสต์/กระดานวันนี้ ไม่บวกดาว ไม่บันทึกสถิติประตูความพร้อม (`spec.test:true` → `finishTestQuest()` ใน `js/house.js` ตัดทาง `awardCoins`/`QUESTS.finish` ทิ้ง) มีเทสคุมไว้ที่ `tests/house-qbrowse.spec.js`
      - ข้อมูลทั้งหมดมาจาก API อ่านอย่างเดียวใน `js/house-quests.js` (`catalogQuiz`/`catalogCats`/`catalogCount`/`countKinds`/`testRun`/`ownGrade`) และเล่นผ่าน `window.HouseQuestUI.playTest()` — **เพิ่ม mechanic ใหม่ในเฟส 5-7 ให้เติมที่ `MECHS` ใน house-quests.js แล้วเติมแท็บที่ `MECH_TABS` ใน house-qbrowse.js อีกที่เดียว**
    - **แผนแม่บทของโหมดนี้อยู่ที่ [`QUEST-DESIGN.md`](QUEST-DESIGN.md) (9 เฟส) — ต้องอ่านหัวข้อ "⭐ สรุปบริบท" ให้จบก่อนแตะโหมดบ้านทุกครั้ง** สถานะตอนนี้: **เฟส 1 (เศรษฐกิจ/ร้านค้า) เสร็จ 2026-08-07 · เฟส 2 (quest engine + กระดาน 5 ชุด/วัน + ประตูความพร้อม) เสร็จ 2026-08-08 · เฟส 3A (ร้านสัตว์เลี้ยง + ซื้อสัตว์/สีขน + บ้านสัตว์ตามเงื่อนไข) เสร็จ 2026-08-08 · เฟส 3B (อาหาร/ความหิว/ป่วย/หมอ) เสร็จ 2026-08-09 · เฟส 4A (พ่อ-แม่ในบ้าน + เควสต์ครอบครัว 1 ชุด/วัน) เสร็จ 2026-08-09 · เฟส 4B (มินิเกมครอบครัว 8 แบบ + ท่าประจำพ่อแม่) เสร็จ 2026-08-10 · เฟส 5 (mechanic กลุ่ม C — ยืม engine เกมหน้าหลักมาเล่นในการ์ดเควสต์) เสร็จ 2026-08-11 · **เฟส 6 (แล็บ STEM + coding) และ เฟส 7 (กลไกกลุ่ม B + D) เสร็จ 2026-08-12 ในรอบเดียวกัน** — เฟสถัดไปคือ 8 (งานวาดไอคอนล้วน)**
      - 🔬 **เฟส 6 + 7 — 20 กลไกใหม่ (เสร็จ 2026-08-12)** · รายละเอียดที่ทำต่างจากแผนอยู่ **ข้อ 41 ของ `QUEST-DESIGN.md` ต้องอ่านก่อนแตะกลไกพวกนี้**
        - **เฟส 6 (แล็บ)**: เขียนใหม่ 8 ตัว `sinkfloat` 🌊 `magnet` 🧲 `states` 🧊 `habitat` 🗺️ `plantgrow` 🌱 `measure` 📏 `codeorder` 🧾 `codedebug` 🐞 · ยืม engine อีก 5 ตัว `shapebuild`(แท็งแกรม) `circuit`(วงจรไฟฟ้า) `robot`/`codeloop`/`codecond`(หุ่นยนต์)
        - **เฟส 7 (กลุ่ม B)**: `payexact` 🧾 `changeback` 💵 `stockshelf` 🏪 `fishmath` 🎣 `recipeseq` 📋 `traffic` 🚦 `deliver` 📦 · **`escort` ถูกตัดทิ้งตามคำสั่งผู้ใช้** (ต้องเขียน NPC เดินตามเด็กทั้งชุด เสี่ยง dead end สูงสุด ได้ผลตอบแทนน้อยสุด)
        - **เฟส 7 (กลุ่ม D จาก `IDEA.md`)**: `spotdiff` 🔍 `flashcount` ⚡ `dressorder` 👗 `findhidden` 🗺️ — **ลบ 4 ไอเดียนี้ออกจาก `IDEA.md` แล้ว**
        - **เก็บตกเฟส 5**: `soundguess` 👂 (แผนบอกให้ยืมจาก `games-art.js` แต่หน้าหลัก**ไม่มีเกมทายเสียงจริง** มีแต่เกมเล่นตามทำนอง ⇒ เขียนใหม่โดยใช้ Web Audio ชุดเดิม)
        - 📦 **คลังตั้งต้นทุกกลไกต้องมี ≥ 40 รายการ** (ผู้ใช้สั่ง 2026-08-12) ของจริงได้ 40-46 ทุกตัว · มีเทสไล่นับที่ `tests/house-phase67.spec.js` **เพิ่มกลไกใหม่ต้องผ่านเกณฑ์นี้ด้วย**
        - 👥 **ใครแจกงานไหน — มี 2 ระบบที่ต่างกัน อย่าสับสน**: `LAB_MECHS`/`SPOT_MECHS` = **แทนที่พูลทั้งหมด** (ตึกแล็บ + ร้านดนตรี เป็น "จุดเดียวในเมือง" ที่แจกงาน STEM/coding/ทายเสียง ชาวบ้านทั่วเมืองสุ่มไม่ได้เลย · มีเทสคุม) · `BONUS_MECHS` = **เพิ่มเข้าพูลปกติ** (ร้านค้ายังได้ quiz/count/เกมยืม engine ตามปกติ **ห้ามเปลี่ยนเป็นแบบแทนที่** ไม่งั้นร้านสะดวกซื้อจะไม่มีวันแจกโจทย์คณิตทั่วไปอีกเลย)
        - ⚠️ **`pickCat()` ใน `js/house-games.js` ห้ามคืนหมวดที่ระดับชั้นสูงกว่าเด็กเด็ดขาด** — วงจรไฟฟ้ามีหมวดเดียวคือ ป.6 · แท็งแกรมมี ป.5-6 ของเดิม fallback เป็น `all` ⇒ **เด็ก ป.1 เคยได้โจทย์ ป.6 เข้าให้** ตอนนี้คืน `null` แล้ว `engineReady()` ถอยไป quiz ให้เอง · **หมวดหุ่นยนต์ที่ง่ายสุดคือของ ป.1 ⇒ เด็กระดับเตรียม ป.1 จะไม่ถูกแจกงาน coding เลย** (ถูกต้องแล้ว ถ้าอยากให้เล่นได้ต้องเพิ่มหมวดในคลัง `CATS` ก่อน ไม่ใช่ผ่อนกฎ)
        - ⚠️ **`rollBoard()` ต้องสุ่ม "คน" ก่อน "งาน" เสมอ** — งานแล็บผูกกับตัวคน สลับลำดับแล้วกระดานจะแจกงานแล็บให้ชาวบ้านมั่วไปหมด
        - 🐾 **`petfeed`/`shopping` ของเฟส 4B เปิดให้ NPC หยิบไปใช้ได้แล้ว** (ผู้ใช้สั่ง 2026-08-12 · ร้านสัตว์/ฟาร์ม ↔ petfeed · ร้านสะดวกซื้อ/ตลาด ↔ shopping) พร้อมเปลี่ยนคนสั่งจาก "คุณแม่" เป็นชื่อ NPC และเปลี่ยนคลังของตามธีมร้าน — **มินิเกมที่เหลือยังเป็นของเควสต์ครอบครัวล้วน** (มีเทสคุมที่ `tests/house-family-games.spec.js`)
        - 👗 **`dressorder` แต่งหุ่นในการ์ด ไม่ผูกกับของแต่งตัวที่เด็กซื้อจริง** (ผู้ใช้ตัดสิน 2026-08-12) — ตั้งแต่เฟส 1 ปิดของฟรี ถ้าสั่ง "ใส่หมวกฟาง" แล้วเด็กยังไม่ได้ซื้อ = dead end ทันที
        - ⏱️ **ตัดการจับเวลาออกจาก `traffic`/`flashcount`** — `IDEA.md` เขียนว่าให้กดทันเวลา/มีแถบเวลาหด แต่ขัดกติกาเหล็กข้อ 2 (ห้ามกดดัน/ลงโทษ) · `flashcount` ยังมี "โชว์แล้วซ่อน" เพราะนั่นคือตัวกลไกเอง ไม่ใช่การจับเวลาตอบ
        - 🖼️ **ทรงหน้าจอใหม่ 5 แบบ** (เพิ่มจาก 3 ทรงของเฟส 4B): `beaker` บีกเกอร์ SVG · `code` รายการคำสั่งมีเลขบรรทัด · `spot` 2 แถวเทียบกัน · `flash` โชว์แล้วซ่อน · `sound` ปุ่มฟังซ้ำ — ⚠️ `flash` ต้องจำว่าโชว์ไปแล้วด้วย `qFlashDone` ไม่งั้นตอบผิดแล้ววาดใหม่จะย้อนไปโชว์ของอีกรอบ = เฉลยให้ฟรี
        - 🚶 **งานเดินเพิ่ม 2 ปลายทาง**: `target:'npc'` (ส่งของ — จบเมื่อแตะตัวคนนั้น) · `target:'zone'` (หาของที่หาย — จบเมื่อเดินถึงย่าน ใช้ `questZonesAt()` ตัวเดิม) · ⚠️ ปลายทางของ `deliver` เลือกจาก `questableIds()` เท่านั้น และคำใบ้ต้องบอก **ชื่อคน + สถานที่** เสมอ ไม่งั้นเด็กเดินหาทั้งเมืองไม่เจอ
        - 🧪 **เทสอยู่ที่ `tests/house-phase67.spec.js` (10 เทส × 2 จอ)** — คุมคลัง ≥40 · ของซ้ำข้ามถัง · gen() ทุกกลไก × ทุกชั้น · ขอบเขตงานแล็บ · เกมชั้นสูงไม่หลุดไปชั้นเล็ก · การ์ดวาดได้จริง · ค่าตอบแทนไม่เปลี่ยน
        - 🧪 **บทเรียนจากการเขียนเทส**: mock `npcDefs: []` ทำให้ `deliver` ถอยไปใช้ `count` เงียบๆ ⇒ **เทสผ่านโดยไม่เคยรันโค้ดกลไกนั้นเลย** (ต้องส่งผัง NPC จริงผ่าน `__houseDbg.npcDefs()`) · เกมตะกร้าต้องเทสด้วยการ **ไล่หา subset ที่ผ่านได้จริง (brute force)** ไม่ใช่วางของทุกชิ้น — เป็นวิธีเดียวที่พิสูจน์ได้ว่าไม่มี dead end · `kind:'engine'` กับ `kind:'walk'` **ได้รับยกเว้นกติกา `MIN_Q` โดยตั้งใจ** อย่าเขียนเทสบังคับให้มี 5 ข้อ
      - 🎮 **เฟส 5 — กลไกกลุ่ม C (เสร็จ 2026-08-11)**: **ไม่ได้เขียนเกมใหม่เลยสักตัว** ยืม engine ของหน้าหลักมาทั้งดุ้นผ่าน "สัญญา Mount" (`js/owl-games.js` + `js/house-games.js`) ⇒ 8 กลไกใหม่ `mix` 🎨 · `memory` 🎴 · `balance` ⚖️ · `clockset` 🕐 · `shadow` 🫥 · `sortcat2` 🗂️ · `orderg` 🔢 · `melody` 🎹 (คีย์ใน `MECHS` กับชื่อ engine ไม่เหมือนกันบางตัว เช่น `clockset` → engine `clock` เพราะชื่อ `clock` ถูกมินิเกมนาฬิกาของเฟส 4B จองไปแล้ว)
        - **NPC/กระดานสุ่มได้ทั้ง 3 กลุ่มแล้ว** (`rollWorkMech()` ใน `js/house-quests.js`): quiz 45% · count 17% · ที่เหลือสุ่มจากกลุ่ม engine — ต่างจากเฟส 4B ที่ NPC/กระดานได้แค่ quiz/count · **มินิเกมครอบครัวของ 4B ยังเป็นของเควสต์ครอบครัวล้วนเหมือนเดิม** (มีเทสคุม)
        - ⚠️ **1 เควสต์ = 1 รอบเกม (10 ด่านของ engine เดิม) ไม่ใช่ 5-10 ข้อแบบกลไกอื่น** ⇒ ดาวมาจาก "จำนวนที่พลาด" ที่ engine คืนมา ไม่ใช่ `run.wrong` · ค่าตอบแทนยังเท่ากลไกอื่นเป๊ะ (`coinsFor()` ไม่รู้จักจำนวนข้ออยู่แล้ว)
        - ⚠️ **`engineReady()` เช็คว่าเกมนั้นลงทะเบียนกับ `OwlGames` + อยู่ใน `HouseGames.ALLOW` จริงไหม** ก่อนแจกงาน — ถ้าไม่พร้อมจะถอยไป quiz ให้เอง ⇒ **เพิ่มเกมเข้า `ALLOW` แล้วมันจะโผล่ในเควสต์เองทันที ไม่ต้องแก้ที่อื่น** (แต่ต้องลองเล่นจริงในการ์ดใบเล็กก่อนเสมอ)
      - 🐾 **สัตว์เลี้ยงต้องซื้อแล้ว ไม่แจกฟรีอีกต่อไป** (`ECON_VER = 4`) — ราคา 250/450/800/1,500 🪙 ตามกลุ่ม + สีขน 100 🪙 (`PET_PRICE`/`PET_COLOR_PRICE` ใน `js/house-shop.js` · **ห้ามลดราคาโดยไม่ถามผู้ใช้** สัตว์คือ money sink ก้อนใหญ่สุดของเกม · มีเทสคุมตัวเลขไว้) · สิทธิ์เก็บใน `data.unlocked` เป็น `pet:<ชนิด>` / `pet:<ชนิด>:<สี>` · **ซื้อครั้งเดียวเป็นของถาวร ปล่อยคืนแล้วรับกลับมาเลี้ยงใหม่ฟรี** · เลี้ยงได้ทีละ 1 ตัวเหมือนเดิม
      - 📐 **การ์ดเล่นเควสต์ (`.house-qz`) ขยายใหญ่ขึ้นทุกหมวดแล้ว 2026-08-10 ตามคำสั่งผู้ใช้** — จอแคบ `min(640px,96vw)`/สูงสุด 90vh · จอกว้าง `min(760px,78vw)`/88vh (เดิม 520/560px · ⚠ ตัวเลข vw ของกฎจอกว้างมีผลกับแท็บเล็ตแนวตั้งด้วย 820px × 70vw ได้แค่ 574px จึงต้องเป็น 78vw) พร้อมขยายตัวอักษร/อิโมจิ/ปุ่มตัวเลือกตามสัดส่วน **ห้ามย่อกลับโดยไม่ถาม** · ยังเป็น **การ์ดลอย ไม่เต็มจอ** ตามข้อ 10 ของ `QUEST-DESIGN.md` เด็กต้องเห็นเมืองข้างหลังอยู่
      - 🧪 **ชุดเทสต้องรอ `__houseDbg.ready()` ก่อนสั่งอะไรในโหมดบ้านเสมอ ห้ามรอแค่ `mode() === 'world'`** — `hMode` มีค่าเริ่มต้นเป็น `'world'` ตั้งแต่ `js/house.js` โหลดเสร็จ ทั้งที่ `startHouseGame()` ยังไม่ทำงาน ⇒ เทสที่รอแค่ค่านั้นจะเปิดการ์ด/แผงก่อนบ้านพร้อม แล้ว `startHouseGame()` ที่ตามมาทีหลังเรียก `closeQuestPanel()` ปิดทิ้งเงียบๆ **เทสแดงแบบสุ่มๆ หาสาเหตุยากมาก** (เจอ 2026-08-10)
      - 🖥️ **แถบบนของโหมดบ้าน (ผู้ใช้สั่งปรับ 2026-08-09)**: chip ชื่อเด็กต้อง **สูงเท่าป้ายเหรียญเป๊ะ** (42px · 38px จอแคบ — ตัวเลขผูกกันใน `css/style.css` แก้ที่ไหนต้องแก้อีกที่) และแสดง **ชื่อ + อายุในวงเล็บ** ("มะลิ (7 ขวบ)" · ป้ายเหนือหัวตัวละครยังเป็นชื่อเปล่า) · **chip ชื่อเด็กไม่ใช่ปุ่มอีกต่อไป** (เดิมกดแล้วเด้งออกไปหน้าเลือกเด็ก เด็กเผลอกดโดนบ่อย) ⇒ เป็น `<div class="child-chip child-chip-static">` **ไม่ใช่ `<button>`** จะได้ไม่มี hover/โฟกัส/เคอร์เซอร์มือหลอกว่ากดได้
        - ⚠️ **แถบบน (`.quiz-top`) ในโหมดบ้านต้อง `pointer-events:none` ที่ตัวแถว แล้วเปิดเฉพาะลูกที่กดได้** — แถวนี้กว้างเต็มจอและโปร่งใส ถ้าเปิดทั้งแถวช่องว่างระหว่างปุ่มจะกินคลิกจนเด็ก **แตะพื้นตรงระดับแถบบนแล้วตัวละครไม่เดิน** (ผู้ใช้แจ้ง 2026-08-09) · ตัวดันช่องว่าง `.quiz-top-info` ต้องปิดด้วย · **เพิ่มปุ่มใหม่ในแถวนี้ไม่ต้องทำอะไรเพิ่ม** (`> *` ครอบให้แล้ว) แต่ถ้าเพิ่ม wrapper ชั้นในต้องเช็คว่ายังกดได้ · มีเทสวัดด้วย `elementFromPoint` จริงที่ `tests/house-family.spec.js`
        - 📐 **ระยะห่างของ UI ในโหมดบ้านต้องแน่นเสมอ — ทุกชิ้นลอยทับโลก 3D ที่เด็กกำลังเล่นอยู่** (ผู้ใช้สั่ง 2026-08-09): แถวชื่อเด็กกับแถบสัตว์เลี้ยงต้อง **สูงเท่ากัน (42px · 38px จอแคบ) ชิดขอบซ้ายตรงกัน (x เท่ากัน) และห่างกันแค่ 6px** — `.house-overlay-ui .quiz-top{margin-bottom:0}` ทับค่า 18px ของหน้าควิซไว้โดยตั้งใจ · ⚠️ **ก่อนบอกว่าจัดระยะเสร็จ ให้วัด `getBoundingClientRect()` จริงในเบราว์เซอร์ทั้งจอกว้างและ 430px** อย่าเดาจาก CSS (รอบนี้เดาผิดหลายรอบกว่าจะพอดี)
        - 🖱️ **ห้ามเขียน `cursor:default` / `cursor:auto` ทับที่ไหนก็ตาม** — แอปนี้ตั้งเคอร์เซอร์เป็น **รูปนกฮูก SVG** ไว้ที่ `body` (หัวข้อ CUSTOM OWL CURSOR ใน `css/style.css`) การใส่ `default` = ทับด้วยลูกศรระบบธรรมดา เคอร์เซอร์ประจำแอปหายทันที ⇒ ถ้าจะเอา "มือชี้" ออกจากของที่กดไม่ได้ ให้ใช้ **`cursor:inherit`** (เจอมาแล้ว 2026-08-09 กับ `.child-chip-static`)
        - อายุแยกเป็น `#house-child-age` ต่างหากและ `flex-shrink:0` **ห้ามยุบรวมกับชื่อ** — จอแคบแถวบนของเต็มจนต้องตัดข้อความ ถ้ารวมก้อนเดียวอายุจะหายทั้งดุ้นเหลือ "มะ…" · คำว่า "ขวบ" อยู่ใน `.hc-age-unit` ซึ่งถูกซ่อนที่ ≤480px เหลือ "(7)" เพื่อคืนที่ให้ชื่อ · **ปุ่ม ← โผล่แค่ 3 หน้าเท่านั้น: แต่งตัว (`hMode==='creator'`) · สัตว์เลี้ยง (`hMode==='pet'`) · แต่งบ้าน (`editMode`)** — ที่เหลือซ่อนหมด **รวมตอนเปิดร้าน/การ์ดเควสต์/กระดาน/หน้าเทส** เพราะกล่องพวกนั้นมีปุ่มปิดของตัวเองอยู่ในกล่องแล้วทุกอัน (ออกจากร้าน/ปิด/ปิดกระดาน) มีปุ่มซ้อนอีกปุ่มมุมจอเด็กสับสน (ผู้ใช้สั่ง 2026-08-09 · มีเทสคุมครบทุกหน้าที่ `tests/house-pets.spec.js`) · **ห้ามลบ logic ปิดแผงใน handler ของปุ่มนี้** ถึงจะเรียกผ่านปุ่มไม่ได้แล้วก็ตาม (ชุดเทสยิง `dispatchEvent` เข้ามาใช้อยู่) ⇒ ทางออกจากโหมดบ้านเหลือทางเดียวคือปุ่ม 🚪 **"ออกจากบ้าน" (`#house-exit-btn`) ในเมนูเฟือง** · ⚠️ **ห้ามลบ element `#house-back`** (listener + ชุดเทสยิง `dispatchEvent` ใส่ id นี้อยู่) และต้องมี `.back-btn[hidden]{display:none}` ใน CSS ไม่งั้นซ่อนไม่ลง (กับดัก specificity เดียวกับ `.house-entry-btn[hidden]`)
      - 📋 **แถบสรุปเควสต์วันนี้ `#house-quest-bar` (เพิ่ม 2026-08-09 ตามคำสั่งผู้ใช้)**: บอกว่าวันนี้เหลือกี่ชุด/เสร็จกี่ชุด (NPC 8 + กระดาน 5 + ครอบครัว 1 = **14 ชุด/วัน**) กดแล้วเปิดหน้ารายการ `#house-qsum` มี **แถบดาวของวันนี้อยู่บนสุด** (ได้กี่ดาว/เต็มเท่าไหร่ + หลอดความคืบหน้า — คนละตัวกับดาวสะสมทั้งชีวิตที่กระดานเควสต์) · แต่ละแถวโชว์ดาว 3 ดวงของชุดนั้น **วาดเป็น SVG และเปลี่ยนสีตามจำนวนดาวที่ทำได้** (1 ดาว = ทองแดง `#D9A066` · 2 ดาว = ส้ม `#F0A14B` · 3 ดาว = ทอง `#FFC73A` · ยังไม่ได้ = จาง `#E7DCC8`) **ไม่มีตัวเลขกำกับ** (ผู้ใช้สั่ง 2026-08-09 — ใช้สีบอกแทน เด็กเหลือบเห็นก็รู้) · ⚠️ **ห้ามเปลี่ยนกลับไปใช้ emoji ⭐/☆** เพราะเปลี่ยนสีไม่ได้ และบางเครื่องไม่มี glyph ☆ · แยก 2 กลุ่ม **"❗ ยังไม่ได้ทำ" มาก่อนเสมอ แล้วต่อด้วย "✅ ทำเสร็จแล้ว"** (ขีดฆ่า+จางลง ไม่ซ่อนทิ้ง เด็กจะได้เห็นความคืบหน้าของตัวเอง) บอกว่าเป็นงานของใคร **ร้าน/สถานที่ไหน** · แถบอยู่ **แถวที่ 3 ใต้แถบสัตว์เลี้ยง** · **ไม่มีข้อความกำกับ เหลือแค่ไอคอน SVG (กระดานเช็คลิสต์) + ตัวเลข ❗/✅** (ผู้ใช้สั่ง 2026-08-09) ⇒ ไอคอนต้องสื่อความหมายเองให้ได้ ห้ามเปลี่ยนเป็น emoji · ข้อมูลมาจาก `QUESTS.daySummary()` (คืนข้อมูลดิบ ฝั่งหน้าจอแปลชื่อเอง) · HUD ในโหมดบ้านมี 3 แถว: แถบบน → แถบสัตว์เลี้ยง → แถบเควสต์ — **แถวล่าง 2 แถวใช้คลาส `.house-hud-row` ตัวเดียวกัน ระยะห่างเท่ากันหมด (`margin-top:10px`)** · เคยมี `.house-hud-row2` ให้แถบเควสต์ชิดขึ้น 7px ผู้ใช้สั่งเอาออก 2026-08-12 **ห้ามใส่กลับ** (มีเทสคุม)
        - ⚠️ **หาชื่อสถานที่ของ NPC ห้ามดูแค่ `def.lot`** — NPC ส่วนใหญ่ (แม่ค้ารถเข็น/นักวิทย์/ชาวนา) ไม่มีฟิลด์นี้ จะขึ้นว่า "ในหมู่บ้าน" เกือบทั้งหน้า ⇒ ลำดับที่ใช้: `def.lot` → `lotAt(def.x, def.z)` ระยะ 0 แล้ว 3 ช่อง → `zoneNameAt()`
        - ⚠️ **`zoneNameAt()` ห้ามเดากล่องพิกัดย่านเอง** ให้ใช้ค่าที่ผังประกาศไว้จริง (`MARKET`, `FARM_PLOTS`) — รอบแรกเดาเองแล้วแม่ค้าตลาดขึ้นว่า "ริมหาด" เพราะตลาดจริงอยู่ `x8-19/z56-62` ไม่ใช่ที่เดาไว้ (กติกาเดียวกับที่ห้ามเดาพิกัดตอนแก้ผังเมือง)
        - 🖱️ **กล่อง/popup ทุกใบในโหมดบ้าน แตะพื้นที่ด้านนอกแล้วปิด** (ผู้ใช้สั่ง 2026-08-09) — ทำที่ `handleTap()` **จุดเดียว** เพราะกล่องเป็นการ์ดลอยไม่ได้คลุมทั้งจอ คลิกนอกกล่องจึงตกมาถึง canvas เสมอ (ไม่ต้องทำ backdrop ให้แต่ละกล่อง และไม่บังโลก 3D) · ⚠️ **ทุกสาขาต้อง `return`** ไม่งั้นเด็กปิดกล่องแล้วตัวละครเดินตามไปด้วยในคลิกเดียว · เพิ่มกล่องใหม่ต้องมาต่อแถวที่นี่ด้วย
        - ⚠️ **`.house-hud-row` เป็น `pointer-events:none`** (สืบจาก `.house-overlay-ui`) ⇒ ของที่เพิ่มในแถวนี้ต้องได้ `pointer-events:auto` เอง — ตั้งไว้ที่ `> *` แล้ว เพิ่มแถบใหม่ทีหลังจึงไม่ต้องแก้อีก (เจอตอนเพิ่มแถบสรุปแล้วกดไม่ได้)
      - 💬 **เคอร์เซอร์ "ฟองคำพูด" ตอน hover ชาวบ้าน (เพิ่ม 2026-08-09 ตามคำสั่งผู้ใช้)**: วางเมาส์บนตัว NPC แล้วเคอร์เซอร์เปลี่ยนเป็นฟองคำพูด บอกว่า "คนนี้คุยได้" — ออกแบบให้เป็นชุดเดียวกับเคอร์เซอร์นกฮูก 2 ตัวเดิม (พาเลตต์เดียวกันเป๊ะ · ทำขอบด้วยรูปทรงทึบซ้อนกันแทน stroke บางๆ ให้คมตอนย่อเหลือ 36px · hotspot = ปลายหางฟอง `8 32`) เขียนเป็น SVG อ่านออกใน `css/style.css` ที่ `#house-canvas.house-talk-hover` ไม่ใช่ base64 · ตัวจับ hover อยู่ใน `js/house.js` (`pointermove` บน canvas) — **raycast ใส่เฉพาะตัว NPC ไม่ใช่ทั้งฉาก + throttle 90ms** ไม่งั้นกินเฟรมเรตตอนลากเมาส์ · ⚠️ **ต้องมี trailing edge ด้วย** ไม่งั้นเลื่อนเมาส์เร็วๆ ไปหยุดบนตัวคนพอดีแล้วเคอร์เซอร์ไม่เปลี่ยน (throttle ทิ้ง event สุดท้ายแล้วไม่มี event ตามมาอีก) · จอสัมผัสข้าม (ไม่มี hover)
        - 🧪 **เทสที่ต้องการ "ที่ว่างบนจอ" (ไม่มีตัวที่คุยได้) ห้าม hardcode พิกัดเด็ดขาด** — desktop กับ tablet มุมกล้องต่างกัน จุดที่ว่างบนจอหนึ่งอาจมีคนยืนอยู่พอดีบนอีกจอ (เทสแดงแบบนี้มาแล้ว 2026-08-10 ทั้ง `house-pets` และ `house-family`) ⇒ ใช้ `window.__houseTalkAt(x, y)` (เช็คเรขาคณิตล้วน ไม่ผ่าน throttle) ไล่หาจุดว่างเอาผ่าน helper `emptySpot(page)` ในไฟล์เทส · **หลังวาร์ปไปที่ใหม่ต้องหาจุดว่างใหม่ทุกครั้ง**
        - 🧪 **เขียนเทสที่ต้องเล็ง NPC ให้ใช้ `__houseDbg.npcOnScreen()`** — มันเลือก **คนที่ยืนประจำที่** ให้ก่อน เพราะชาวบ้านที่เดินไปมาขยับทุกเฟรม วัดพิกัดเสร็จแล้วเลื่อนเมาส์ไปเขาเดินหนีไปแล้ว · และหน้าบ้านเด็ก**ไม่มีชาวบ้านอยู่ในจอเลยสักคน** (NPC เข้าเขตบ้านไม่ได้) ต้อง `__houseDbg.tp()` ไปที่ชุมชนก่อน
      - 💬 **คุย NPC ที่มีงาน → การ์ดเควสต์เด้งใน 280ms** (เดิม 900ms ผู้ใช้แจ้งว่าอืดจนนึกว่าเกมค้าง) · **ห้ามเพิ่มกลับให้ยาวกว่านี้** · และระหว่างการ์ดเควสต์/การ์ดคุณหมอเปิดอยู่ NPC เจ้าของงานถูกตรึงให้ **ยืนหันหน้าหาเด็ก ไม่เดินหนี** (`qzNpcId` + จุดตรึงใน `updateNpcs()` · เคลียร์ที่ `closeQuestPanel()` · มีเทสคุมไว้)
      - 🧹 **เฟส 4B — มินิเกมครอบครัว (เสร็จ 2026-08-10 · ครบ 8 เกม)**: มินิเกม 8 แบบตามตารางข้อ 28 ของ `QUEST-DESIGN.md` · **โผล่เฉพาะเควสต์ครอบครัววันละ 1 ชุดเท่านั้น** (`FAM_MECHS` ใน `js/house-quests.js` — NPC/กระดานยังสุ่มได้แค่ `quiz`/`count` เหมือนเดิม มีเทสคุมไว้)
        - **12 เกม** (`FAM_MECHS` — สุ่มวันละ 1 ชุด): 🧹 เก็บของเข้าที่ (`tidy`) · 🧺 แยกผ้าซัก (`laundry`) · 🍳 ช่วยทำอาหาร (`cook`) · 🕰️ กิจวัตรของหนู (`routine`) · 🐾 เตือนเรื่องสัตว์ (`petfeed`) · 💰 ใช้เงินให้พอ (`budget`) · 🛒 จำของที่แม่สั่ง (`shopping`) · ⏰ ตื่นให้ตรงเวลา (`clock`) · 🍽️ กินข้าวพร้อมหน้า (`dinner`) · 🏪 ไปซื้อของให้แม่ (`market`) · 📚 เรียงลำดับจากคลังหน้าหลัก (`orderlearn`) · 🗂️ จัดหมวดของจากคลังหน้าหลัก (`sortcat`) — บวก `quiz`/`count` ที่มีมาตั้งแต่เฟส 4A
        - **แบ่งเป็น 3 ทรงหน้าจอเท่านั้น** (เพิ่มเกมใหม่ให้เลือกทรงที่มีก่อน): ① `kind:'sort'` ถังหลายใบ (tidy/laundry/petfeed) ② `kind:'sort'` + `layout:'slots'` ช่องเรียงลำดับ 1..N ช่องละชิ้น (cook/routine) ③ `kind:'sort'` + `basket:true` ตะกร้าใบเดียว + **ปุ่มยืนยันของตัวเอง** (budget/shopping — เกมพวกนี้ไม่ได้ใช้ของทุกชิ้น จึงไม่มีจังหวะ “ถาดว่าง” ให้ตรวจอัตโนมัติ) · ส่วน `clock` เป็นโจทย์ 4 ตัวเลือกธรรมดาที่มีรูปหน้าปัดประกอบ (`it.clock`) ใช้ทางตอบเดิมทั้งหมด
        - ⚠️ **เกม `petfeed` ต้องเลือกสัตว์ที่กินคนละชนิดกันเท่านั้น** — แมวกับเพนกวินกินปลาเหมือนกัน ถ้าอยู่กระดานเดียวกันเด็กวางถูกก็ยังถูกนับว่าผิด · **ตารางอาหารตัวจริงอยู่ที่ `FOOD` ใน `js/house-pet-care.js`** ส่งเข้ามาทาง `kit.petFoods()` (ไฟล์เควสต์เก็บแค่ชื่อ/หน้าสัตว์ที่ `PET_FACE` · มีเทสคุมว่าสัตว์ทุกตัวในตารางอาหารต้องมีชื่อที่นี่)
        - ⚠️ **เกม `budget` ตั้งใจให้มีคำตอบถูกได้หลายชุด** (ครบจำนวน + ไม่เกินงบ = ผ่าน) เด็กได้คิดเอง ไม่ใช่ทายว่าผู้ใหญ่คิดชุดไหน · งบคำนวณจาก “ราคาถูกสุด N ชิ้น + เผื่อเล็กน้อย” ⇒ **มีคำตอบที่เป็นไปได้เสมอ** (มีเทสคุม) · ราคาแสดงเป็น **เหรียญ CSS `.hs-coin`** ห้ามใช้ `฿` หรือ emoji 🪙
        - ⚠️ **เกม `clock` ใช้คำบอกเวลาชุดเดียวกับเกมนาฬิกาวิเศษในหน้าหลัก** (`h โมง` / `h โมงครึ่ง` / `h โมง m นาที` — ดู `clockTimeText()` ใน `js/games-math.js`) และ **เข็มสั้นต้องวาดทีหลัง = อยู่หน้าเข็มยาว** กติกาเดียวกันทั้งแอป
        - 🚶 **เควสต์ "เดินไปทำนอกบ้าน" 2 ชุด (`dinner` / `market`) — งานชิ้นเดียวจบ ไม่เข้ากติกาขั้นต่ำ 5 ข้อโดยตั้งใจ** (ผู้ใช้อนุมัติ 2026-08-10 · กติกานั้นใช้กับเกมที่เปิด popup ให้ตอบคำถามเท่านั้น)
          - `dinner` = รับงาน → **การ์ดปิดเองให้เด็กเดินได้** → ไปนั่งโต๊ะ/เก้าอี้ **ในบ้าน** = จบงาน (จับที่ `startSit()`) · **ต้องมีโต๊ะหรือเก้าอี้ในบ้านก่อนถึงจะแจกงานนี้** (`hasIndoorSeat()` ส่งเข้าเอนจินทาง `kit` — ไม่งั้นเด็กรับงานแล้วทำไม่ได้ = dead end)
          - `market` = จำรายการที่บ้าน → เดินไปตลาด → **พอเข้าเขตตลาดกระดานซื้อของเด้งขึ้นเอง** (`walkQuestTileCheck()` เช็คทุกเฟรมตอนมีงานค้าง — ⚠️ เช็คแค่ตอนก้าวถึงช่องใหม่ไม่พอ เด็กอาจถูกวาร์ปหรือยืนอยู่ในตลาดตั้งแต่แรก)
          - ⚠️ **ต้องคว้า `qRun` เก็บไว้ที่ `walkQuest` ก่อนเรียก `closeQuestPanel()`** เพราะฟังก์ชันนั้นล้าง `qRun` ทิ้งเสมอ · ออกจากโหมดบ้าน = ทิ้งงานเดินที่ค้าง (ยังไม่นับว่าทำเสร็จ กลับมาคุยรับใหม่ได้)
        - 👨‍👩‍👧 **ท่าประจำของพ่อแม่ทำแล้ว** (`PARENT_POSE` ใน `js/house.js`) — ทำครัวคนสองมือ · อ่านหนังสือยกมือค้างเกือบนิ่ง · ถูพื้นถูเร็ว · เก็บที่นอน/จัดของสลับมือ **ท่าต้องไม่ซ้ำกันทุกกิจกรรม** (มีเทสคุม)
        - 🖐️ **ท่าเล่น = ลากของไปวางในถัง (drag & drop)** (ผู้ใช้สั่ง 2026-08-10) · ยังมีทางสำรอง "แตะเลือก → แตะถัง" ไว้ด้วย (ขยับไม่ถึง 6px = นับเป็นแตะ) เด็กที่ลากไม่ไหวต้องมีทางไปต่อเสมอ · แตะ/ลากของที่อยู่ในถังแล้ว = เอากลับลงถาด · **วางครบทุกชิ้น = ตรวจให้เอง ไม่มีปุ่ม "ส่งคำตอบ"** · **ขนาดกล่องของ 74×74px (ในถัง 58px · จอแคบ 62/50px)** ผู้ใช้สั่งขยาย 2026-08-10 ห้ามย่อกลับโดยไม่ถาม
          - ⚠️ **ห้ามใช้ HTML5 drag-and-drop (`draggable`/`dragstart`) เด็ดขาด** — ไม่ทำงานบนจอสัมผัสซึ่งคือเครื่องหลักที่เด็กเล่น ⇒ เขียนเองด้วย pointer event · ตัวที่กำลังลากต้อง **`pointer-events:none`** ไม่งั้น `elementFromPoint()` เจอแต่ตัวมันเอง เลยไม่รู้ว่าปล่อยลงถังไหน · ของที่ลากได้ต้อง **`touch-action:none`** ไม่งั้นนิ้วไปเลื่อนหน้าจอแทน
          - ⚠️ **เทสกระดานนี้ห้ามใช้ `el.click()`** — มันฟัง pointerdown/move/up ไม่ได้ฟัง click กดแล้วเงียบไม่มีอะไรเกิดขึ้น ⇒ ใช้ helper `DRAG_FN` ใน `tests/house-family-games.spec.js` ยิง PointerEvent จริง
        - ⚠️ **ของทุกชิ้นต้องมีถังที่ถูกต้องแค่ถังเดียวอย่างชัดเจน** — ห้ามใส่ของกำกวม (📖 อยู่ห้องนอนหรือห้องนั่งเล่น? ⏰ กับ 🕰️ ต่างกันยังไง?) เด็กตอบตามเหตุผลตัวเองแล้วยัง "ผิด" = ลงโทษเด็กกลายๆ ผิดกติกาเหล็กข้อ 2 ⇒ **ถังเป็น "หมวดของ" (ครัว/ห้องน้ำ/ของเล่น/ของใช้เรียน) ไม่ใช่ห้องในบ้าน** · มีเทสคุมว่าของชิ้นเดียวห้ามอยู่ 2 ถังในเกมเดียวกัน
        - ⚠️ **อิโมจิที่ใช้ต้องดูออกตอนย่อเหลือ 30px** — 🪀 (โยโย่) กลายเป็นลูกบอลเขียวเฉยๆ · 🍳 ที่ 23px อ่านเป็นแว่นขยาย (เจอจากการดูภาพจริง 2026-08-10 ⇒ ไอคอนถังขยับเป็น 30px) **ก่อนเพิ่มของใหม่ให้ถ่ายภาพจริงมาดูก่อนเสมอ อย่าเชื่อว่าอิโมจิสื่อความหมายเองได้**
        - 🔢 **จำนวนโจทย์สุ่ม 5-10 ข้อต่อเควสต์ ไม่จำเป็นต้องเท่ากันทุกเควสต์** (ผู้ใช้สั่ง 2026-08-10) — สุ่มที่ `buildRun()` ด้วย seed ของเควสต์นั้น ⇒ เปิดใหม่ได้ชุดเดิมเสมอ · ระดับชั้นมีผลกับ **ความยากของแต่ละข้อ** ไม่ใช่จำนวนข้อ · ขั้นต่ำคือ `MIN_Q = 5` (มินิเกมใหม่ทุกตัวต้องอ้างค่านี้ ห้ามตั้งเลขต่ำกว่าเอง)
        - 💰 **จำนวนข้อไม่มีผลกับเงินเด็ดขาด** — `coinsFor()` คิดจากประเภทเควสต์ × ดาว เท่านั้น เควสต์ 5 ข้อกับ 10 ข้อได้เท่ากันเป๊ะ (มีเทสคุม) ⇒ ปรับจำนวนข้อได้อิสระโดยไม่ทำเงินเฟ้อ
        - 📈 **ความยากไล่ระดับภายในเควสต์เดียว** (ผู้ใช้สั่ง 2026-08-10): ข้อแรกง่ายสุด ข้อท้ายเต็มระดับชั้น · `stepDiff(diff, i, n)` ใน `js/house-quests.js` เป็นตัวคิดสัดส่วน `p` ให้ทุกกลไกใช้ร่วมกัน (นับของ = จำนวน/ชนิดของ · เกมลาก = จำนวนชิ้น · เรียงลำดับ = จำนวนขั้น) · **กลไก `quiz` ได้ฟรีจากการเรียงตามลำดับที่โจทย์อยู่ในคลัง `CATS`** (คลังเขียนไล่ง่าย→ยากอยู่แล้ว) · ⚠️ **เทสการไล่ระดับต้องวัดเป็นค่าเฉลี่ยครึ่งแรก vs ครึ่งหลัง** ห้ามเทียบข้อต่อข้อ เพราะเนื้อโจทย์ยังสุ่มอยู่
        - ♻️ **ดึงคลังโจทย์จากเกมหน้าหลักมาใช้ซ้ำ** (ผู้ใช้สั่ง 2026-08-10): 📚 `orderlearn` ใช้ `ORDER_SETS` (เรียงลำดับตามหลักสูตร แยกแท็ก ป.4/ป.5/ป.6) · 🗂️ `sortcat` ใช้ `EF_CATEGORIES` (หมวดของจากเกม "นกฮูกสั่ง") · ทั้งคู่อ่าน global จาก `js/data-pools.js` ที่โหลดมาก่อนเสมอ **ถ้าไม่มีต้องถอยไปใช้คลังของโหมดบ้านเอง ห้ามพัง** · ระดับชั้นที่ไม่มีคลังจะไม่ถูกแจกเกมนั้น (`famMechOk`)
        - 🚫 **ห้ามมีโจทย์ซ้ำภายในเควสต์เดียว** — กลไกที่สุ่มสร้างโจทย์เองมีโอกาสออกซ้ำ ⇒ `uniqueRun()` ตัดซ้ำด้วย `itemSig()` แล้วสุ่มเพิ่มจนครบ · **ถ้าคลังเล็กจนหาไม่ครบจริง ยอมให้เควสต์สั้นลง ดีกว่าให้เด็กเจอโจทย์เดิมซ้ำ** (เกณฑ์ดาวคิดจาก `run.items.length` จึงยืดตามเอง) · มีเทสไล่ทุกกลไก × ทุกชั้น
        - 🧩 **เพิ่มมินิเกมใหม่ต้องแตะ 4 ที่**: `MECHS` + (ถ้าใช้ payload แบบใหม่) `verify()` ใน `js/house-quests.js` → เติมใน `FAM_MECHS` → เพิ่มสาขาใน `renderQuestStep()` ของ `js/house.js` (ตัวที่ไม่มี `it.kind` = โจทย์ตอบคำถามแบบเดิม) → เพิ่มแท็บใน `MECH_TABS` ของ `js/house-qbrowse.js` · **ส่งคำตอบผ่าน `QUESTS.submit(run, payload)`** (ตัวใหม่ที่รองรับทุกกลไก · `answer(run, i)` เดิมยังใช้ได้กับโจทย์ 4 ตัวเลือก)
        - เทสอยู่ที่ `tests/house-family-games.spec.js` (28 เทส × 2 จอ) · ⚠️ **เทสที่วนเล่นหลายข้อ ห้ามเช็ค “ยังมีข้อถัดไปไหม” จากการมี `.hqz-sort` บนจอ** — หน้า “จำรายการของ” ไม่มี element นั้น จะถูกเข้าใจผิดว่าเล่นจบแล้ว ⇒ ดูจาก `__houseDbg.qRun()` แทน
      - 👨‍👩‍👧 **เฟส 4A — พ่อ-แม่ในบ้าน (`js/house-family.js` · เสร็จ 2026-08-09)**: ยืนอยู่ในบ้านชั้นใน (พ่อ=ห้องนั่งเล่น · แม่=ครัว) ตัวสูงกว่าเด็ก 1.25 เท่า · **มี default ครบทั้งชื่อ (`คุณพ่อ`/`คุณแม่`) และหน้าตา (`H_DEFAULT_PARENT_DAD/MOM` ใน `js/house-map.js`) เด็กเปิดบ้านครั้งแรกเจอเลย ไม่มีหน้าจอบังคับสร้าง** · **ไม่มี `data.parents` = ใช้ default** ⇒ เด็กเก่าไม่พัง ไม่ต้อง migrate · **พ่อแม่เดินไปมาในบ้านและสุ่มทำกิจกรรม "ตามอุปกรณ์ที่เด็กวางไว้จริง" เท่านั้น** (อ่านจาก `data.decor.in` → `PARENT_ACTS` แมปตามหมวดเฟอร์นิเจอร์: ครัว🍳 · โต๊ะ🍽️ · ที่นั่ง📖 · เตียง🛏️ · ห้องน้ำ🧼 · ของแต่ง🪴) **บ้านไม่มีของชนิดนั้น = ไม่มีกิจกรรมนั้น** ไม่แกล้งทำลอยๆ ให้เด็กงง · เด็กเดินเข้าใกล้ = หยุดหันหน้าคุยด้วย ไม่เดินหนี · **แตะตัว = คุยเพื่อทำเควสต์เท่านั้น ไม่มีการ์ดแต่งตัวเด้งมาบัง** (ผู้ใช้สั่ง 2026-08-09) · แต่งตัวผ่านปุ่ม 👥 `#house-parent-btn` บนแถบบนที่ **โผล่เฉพาะตอนอยู่ในบ้าน** ปุ่มเดียวเข้าหน้าแต่งตัว แล้วเลือกพ่อ/แม่จาก **แท็บ `#house-creator-tabs`** ข้างใน (สลับแท็บ = บันทึกของคนเดิมก่อนอัตโนมัติ) · **แถวเลือกเพศถูกปิดตอนแต่งตัวให้พ่อแม่** เพราะเพศถูกกำหนดจากแท็บแล้ว · **แตะพ่อ/แม่ = เด็กเดินไปหาก่อนแล้วค่อยคุย** (เหมือนแตะชาวบ้านในเมือง — `walkToParent()` → `finishArrive()` `type:'parent'`) ระหว่างนั้นพ่อแม่ยืนรอ ไม่เดินหนี · ⚠️ `tileBesideTarget()` เดิม hardcode `OUT_*` ไว้ แก้ให้รู้ฉากแล้ว (ไม่งั้นคำนวณผิดกริดตอนอยู่ในบ้าน) · **ป้ายงาน "!" / "✓" ลอยเหนือหัวพ่อแม่** ใช้ texture/geometry ชุดเดียวกับชาวบ้านในเมือง (`npcMarkMat`/`NPC_MARK_GEO`) เด็กจะได้อ่านสัญลักษณ์เดียวกันทั้งเกม — ขึ้นเฉพาะคนที่วันนี้เป็นคนขอ อีกคนไม่มีป้าย · เคอร์เซอร์ฟองคำพูดใช้กับพ่อแม่ในบ้านด้วย
        - ⚠️ **`buildCharacter()` เก็บ rig ไว้ที่ `g.userData` ตรงๆ (`{rig, legs, arms, head}`) ไม่ใช่ `userData.anim`** (อันนั้นเป็นของเฟอร์นิเจอร์/สัตว์) — อ่านผิดแล้ว **ท่าเดินไม่ทำงานทั้งชุด ตัวเลื่อนไปเฉยๆ เหมือนลอย** โดยไม่มี error ให้จับ (เจอ 2026-08-09)
        - ⚠️ **ออกจากหน้าแต่งตัวพ่อ/แม่ ต้องคืน `creatorCfg` เป็นของเด็กก่อนเสมอ** — `exitCreatorToWorld()` เรียก `rebuildChar(creatorCfg)` ถ้า `creatorCfg` ยังเป็นหน้าตาพ่อแม่ **ตัวเด็กจะกลายเป็นพ่อ/แม่ทันที** (โดนทั้งตอนบันทึกและยกเลิก · บั๊กจริงที่ผู้ใช้เจอ 2026-08-09) และ **ห้ามตั้ง `creatorWho='child'` ก่อนเรียก `exitCreatorToWorld()`** ไม่งั้นตัวกันในนั้นไม่ทำงาน · มีเทสคุมทั้ง 2 ทางที่ `tests/house-family.spec.js` · ⚠️ **`creatorWho` ต้องถูกตั้งทุกครั้งที่เปิดหน้าแต่งตัว** ไม่งั้นกดบันทึกแล้วเขียนทับตัวละครผิดคน (มีเทสคุมว่าห้ามแตะ `data.char` ของเด็ก) · **เควสต์ครอบครัววันละ 1 ชุด โควตาแยกจาก NPC/กระดานเด็ดขาด · 14-39 🪙 (`FAM_BASE.family` ใน `js/house-quests.js` — ผู้ใช้เลือกตัวเลขนี้เอง ห้ามปรับโดยไม่ถาม)** สุ่มคงที่ต่อวันว่าพ่อหรือแม่เป็นคนขอ อีกคนบอกใบ้ · **โทนต้องเป็น "ขอความช่วยเหลือ" ปฏิเสธได้เสมอ ไม่มีบ่นไม่มีดุ** · รายละเอียดที่ทำต่างจากแผนอยู่ข้อ 38 ของ `QUEST-DESIGN.md`
      - 🍖 **เฟส 3B — อาหาร/ความหิว/ป่วย/หมอ (`js/house-pet-care.js` · เสร็จ 2026-08-09)**: อาหาร **8 ชนิดครอบคลุมสัตว์ครบ 12 ตัว** ขายแท็บ 🍖 ในร้านสัตว์เลี้ยง (45-150 🪙/ถุง · **1 ถุง = 5 มื้อ · 1 มื้อ = อิ่ม +50**) เก็บใน `data.petFood` เป็น "จำนวนมื้อ" · ความอิ่ม 0-100 **ลดตาม "วันที่เข้าเล่น" (−35/วันเล่น) ไม่ใช่เวลาจริง** ⇒ หายไป 3 เดือนกลับมาก็ลดแค่วันเดียว · โชว์หัวใจเหนือหัวเมื่อ < 50 · **ป่วยต่อเมื่อปล่อยหิวสนิท 2 วันเล่นติดกัน** แล้วนอนในบ้านสัตว์ออกมาไม่ได้ หายเองไม่ได้ ต้องคุยหมอ/พยาบาลจ่าย **120 🪙** · **เงินไม่พอ = ทำงานช่วยคุณหมอแทน** (เล่นเควสต์จบ 1 ชุดแล้วหายป่วย — ห้ามมี dead end) · ให้อาหารผิดชนิดสัตว์ส่ายหัว **ไม่เสียของ ไม่เสียเงิน** · **แถบสถานะ `#house-pet-bar` ใต้ชื่อเด็ก** (ชื่อสัตว์ · ไอคอนอาหารที่น้องกิน · หลอดความอิ่มเปลี่ยนสีเขียว→ส้ม→แดงอ่อน · ปุ่มให้อาหาร) โผล่เฉพาะตอนมีสัตว์ · ในร้านสัตว์ **แตะเลือกสัตว์ตัวไหน สีขนของตัวนั้นถึงโผล่ต่อท้าย** (section เดียวเสมอ ไม่กางทุกตัวที่มี — ผู้ใช้สั่ง 2026-08-09) · **สัตว์ที่ยังไม่มีก็ดู/พรีวิวสีได้** (ผู้ใช้สั่งเพิ่ม 2026-08-09 กลับข้อตัดสินใจเดิมของเฟส 3A) แต่ **ยังซื้อสีไม่ได้จนกว่าจะรับมาเลี้ยง** — `buyPetColor()` กันไว้ + หัวข้อ section บอกเหตุผล · การ์ดอาหารต้อง **บอกเสมอว่าเป็นของสัตว์ตัวไหน** (นี่คือตัวสอนหลักของเฟสนี้) · **ให้อาหาร = กดปุ่มในแถบครั้งเดียวป้อนเลย ไม่มี popup ให้เลือกชนิด** (ผู้ใช้สั่งเอาออก 2026-08-09 — สัตว์แต่ละตัวกินได้ชนิดเดียวอยู่แล้ว การ์ดจึงมีของใบเดียว = คลิกเปล่าเพิ่มมาขั้นนึงเฉยๆ · บทเรียน "สัตว์ตัวไหนกินอะไร" ไปสอนที่การ์ดอาหารในร้านแทน) · **อิ่มเต็มแล้วกด = น้องบอกว่าอิ่มอยู่ ไม่หักอาหาร** · แถบสถานะโชว์ **ไอคอนอาหาร + จำนวนมื้อคงเหลือ (×N) ต่อท้ายหลอด** หมดแล้วจางลง · **ป้อนอาหารมีอนิเมชันในฉาก ~2.6 วิ** (`startFeedAnim`/`updateFeedAnim` ใน `js/house.js`): เด็กหันหาน้อง+ยื่นชาม → น้องวิ่งเข้ามา → ก้มกินหัวโยกๆ + ✨ → กระโดดดีใจ ❤️ · ระหว่างนี้ `updatePet()` กับ `syncPetSick()` **ต้องหยุดคุมน้อง** (เช็ค `feedAnim`) และตอนจบต้อง sync `hPet.tile` ไม่งั้นก้าวต่อไปน้องจะวาร์ปกลับช่องเดิม · ระยะ **เด็ก → ชาม (.78) → น้อง (1.38)** ต้องห่างพอให้เห็นครบ 3 ชิ้น (เคยตั้งชิดกว่านี้แล้วตัวน้องบังชามมิด) · ⚠️ engine clamp `dt` ที่ 50ms/เฟรม (`js/house.js`) ⇒ **ตอนเฟรมตก "เวลาในเกม" เดินช้ากว่านาฬิกาจริงหลายเท่า** เทสที่รออนิเมชันจบต้องเผื่อ timeout เยอะ (ใช้ 25 วิ) · **ห้ามแก้ตัวเลขชุดนี้โดยไม่ถามผู้ใช้** (มีเทสคุมที่ `tests/house-pet-care.spec.js` 13 เทส) · รายละเอียดที่ทำต่างจากแผนอยู่ข้อ 37 ของ `QUEST-DESIGN.md`
        - ⚠️ **`PETCARE.*` ทุกตัวอ่าน localStorage + JSON.parse ⇒ ห้ามเรียกในลูปวาดภาพทุกเฟรม** — ผลถูก cache ไว้ที่ `petCareHud` ใน `js/house.js` (คำนวณใหม่วินาทีละครั้ง) ถ้าจะเพิ่มอะไรที่ต้องรู้สถานะสัตว์ทุกเฟรม ให้อ่านจาก `petCareHud` ไม่ใช่เรียก `PETCARE` ตรงๆ
        - 🎁 **รับเลี้ยงสัตว์ปุ๊บได้อาหารถุงแรกของชนิดนั้นฟรีทันที** (และเด็กที่เลี้ยงอยู่ก่อนเฟส 3B ได้ย้อนหลัง) — เด็กใหม่มี 0 เหรียญ ถ้าน้องหิวแล้วยังซื้ออาหารไม่ได้คือทางตันตั้งแต่วันแรก **ห้ามลบตรรกะนี้** (อยู่ใน `sync()`/`onAdopt()` ของ `js/house-pet-care.js`)
        - **ปล่อยสัตว์คืนแล้วอาหารที่ซื้อไว้ต้องไม่หาย** (`onRelease()` ล้างแค่ `data.care` ไม่แตะ `data.petFood`)
        - ⚠️ **ห้ามใส่ CSS `animation: … infinite` ที่ขยับตัวปุ่ม/แถบที่กดได้** — ปุ่มที่เต้นไม่หยุดเด็กกดพลาดง่าย และ Playwright รอ element นิ่งก่อนคลิกจนหมดเวลา (เจอกับ `.house-pet-bar` เมื่อ 2026-08-09 เทสเกือบแดง ใช้เวลา 25 วิ) ⇒ ย้าย animation ไปที่ชิ้นที่กดไม่ได้แทน (ตอนนี้อยู่ที่ `.hpb-meter` เป็น box-shadow ไม่ใช่ transform)
        - ⚠️ **emoji `🍽️` ไม่มีในฟอนต์บางเครื่อง ขึ้นเป็นวงกลมเทาทึบ** (กับดักเดียวกับ `🪙`) ⇒ ไอคอนชามในปุ่มให้อาหารวาดเป็น SVG · ป้ายข้อความอยู่ใน `#hpb-feed-label` **ห้ามเขียน `textContent` ทับทั้งปุ่ม** ไม่งั้นไอคอนหาย
        - ⚠️ **ห้ามใส่ `word-break:break-word` กับข้อความไทยในการ์ด** — เบราว์เซอร์ตัดคำไทยกลางคำ ("หมาน้อย" → "หมา/น้อย") ⇒ ฝั่ง JS คั่นบรรทัดด้วย `\n` เองแล้วใช้ `white-space:pre-line` (ดู `.hs-sub`)
      - ⚠️ **เด็กที่เลี้ยงสัตว์อยู่ก่อนเฟส 3A ต้องได้ชนิด+สีนั้นฟรีถาวร** — `migrate()` ใน `js/house-shop.js` ข้อ 4 ทำให้แล้ว **ห้ามลบบล็อกนั้น** แม้จะดูเหมือนโค้ดตาย (ผิดกติกาเหล็กข้อ 3 ทันที)
      - 🏙️ **ผังย่านห้างรื้อใหม่ 2026-08-08/09** (ข้อ 36 ของ `QUEST-DESIGN.md`): แฟชั่นมอลล์ย้ายไปต่อแถวเดียวกับเฟอร์นิเจอร์มอลล์ (x0-4/z44-52 · `face:'x'` หันหน้าทางเดียวกัน) แทนที่ทุ่งทานตะวันที่เอาออก · กระท่อมช่างไม้ z33-35 · **ลานทางเดินใหม่ x5-10/z44-53** มีโบโซ่+รถเข็นลูกโป่ง และรถเข็นมายากล+พี่มายากลตั้งกลางลาน (กระท่อมช่างไม้ถอยเป็น z32-34 แล้วเมื่อ 2026-08-09) · **เปิดร้านใหม่ 2 ร้าน**: 🌷 ร้านต้นไม้ (x22-25/z47-50 หน้าโรงเรียน · `buildGardenShop`) ขาย `garden`/`seatout`/`decorout` · 🎠 ร้านของเล่น (x6-10/z39-43 · `buildToyShop`) ขาย `play` ⇒ **ห้างเฟอร์นิเจอร์เหลือของในบ้านล้วน** · **ทุกหมวดต้องมีร้านขายเสมอ ห้ามมีหมวดตกหล่น** (มีเทสคุมไว้)
      - 🗺️ **ก่อนขยับล็อต/แก้ผังเมืองทุกครั้ง ให้ดัมพ์กริดเดินได้จริงจาก engine มาดูก่อน** — `window.__houseDbg.grid()` คืน `outGrid` (index `grid[z][x]` · 0=เดินได้ 1=น้ำ 2=สะพาน) เอามาวาด ASCII เทียบกับ `VILLAGE_LOTS` แล้วค่อยตัดสินใจ ห้ามเดาพิกัดเอง (รอบ 2026-08-08 เจอว่าคำสั่ง "ขยับ 5 ช่อง" จะไปทับลานแคมป์พอดี)
      - ⚠️ **`VILLAGE2_ROADS` ใน `js/house-map.js` ถูก `.map(s2Rect)` = บวก `EPAD2` (10) บนแกน z** ⇒ เขียนพิกัดถนน/ลานต้อง **ลบ 10 ก่อนเสมอ** (ลานจริง z43-53 → เขียน `z0:33, z1:43`) ส่วน `VILLAGE_LOTS` ท่อน `.concat([...])` ท้ายไฟล์เป็นพิกัดจริงตรงๆ ไม่ต้องลบ
      - 🏡 **กรอบบริเวณบ้านเด็ก (`HOME_ZONE`) ย่อลง 2 รอบเมื่อ 2026-08-09**: x12-27/z26-45 → x13-27/z27-41 → **จริง x13-25 / z27-38** — วาง/ย้ายของตกแต่งนอกบ้านได้แค่ในกรอบนี้ · ⚠️ **แก้กรอบนี้เมื่อไหร่ ต้องเช็คว่าของที่ `seedWorldDecor()` แจกเด็กใหม่ยังอยู่ในกรอบครบทุกชิ้น** (มีตัวกรอง `inHomeZone` ที่ `TREES.filter(...)` แล้ว) ไม่งั้นเด็กจะเจอต้นไม้ที่ย้าย/ลบไม่ได้
        - 🚧 **รั้วล้อม "ทั้งกรอบ" แล้ว ไม่ใช่แค่สนามรอบบ้าน** ⇒ `YARD` = `HOME_ZONE` เป๊ะๆ · ช่องรั้วเปิด 2 ทางอยู่ที่ `GATE_TILES` (x25/z35-36 ตรงแนวสะพานเหนือ · x19-20/z38 ตรงแนวทางเดินดินไปสะพานใต้) — `GATE_TILE` (เอกพจน์) ตัวเก่ากลายเป็นแค่ "ปลายทางเดินหินหน้าประตูบ้าน" ไม่ใช่ช่องรั้วแล้ว
        - 💾 **รั้วเป็น decor ที่ seed ครั้งเดียวต่อเด็ก** ⇒ ย้าย/ย่อกรอบเมื่อไหร่ เด็กที่เล่นอยู่แล้วจะค้างรั้วแนวเก่า ต้องปั๊ม `MAP_V` แล้วเขียน migration (ตอนนี้ `MAP_V = 4` ทิ้งรั้วเก่า + วางรั้วใหม่ + ดึงของที่ตกนอกกรอบกลับเข้าใน ดู `migrateHouseMap` + `fenceSeedRecs()` ใน `js/house.js`)
        - 🚫 **ชาวบ้านห้ามเดินเข้าบริเวณบ้านเด็ก** — กันไว้ 4 จุดใน `js/house.js` (กรอบเดินเล่น `n.tiles` · ช่องปลายทาง `npcWalkTile` · เส้นทาง `findPath(..., npcNoHome)` · การถูกดัน `npcNudge`) ทุกจุดเช็คจาก `inHomeZone` อย่างเดียว จึงเลื่อนตามกรอบเองอัตโนมัติ
      - 🪴 **`POT_SPOTS` ใน `js/house-map.js`** = กระถางต้นไม้ตั้งพื้นรายช่อง (`buildPotPlant()`) สำหรับตกแต่งซอกข้างร้าน/ริมทางที่ต้นไม้ป่างอกไม่ได้ — เติมช่องใหม่ได้ที่ลิสต์เดียว (ต่อครบทั้ง 3 จุดแล้ว: บล็อกทางเดิน · กัน `wildPlantable` · วาดแบบ merge)
      - ⚠️ **`torus()` ใน `js/house.js` คืนวงที่ "ตั้งฉาก"** (TorusGeometry อยู่บนระนาบ XY) ⇒ **ห่วงที่ต้องวางแบนต้องใส่ `rotation.x = Math.PI/2` เองเสมอ** — เคยลืมมาแล้ว 2 ชิ้นในคลังเฟอร์นิเจอร์ (แทรมโพลีน/บ่อน้ำ ขอบตั้งขึ้นเหมือนล้อรถ แก้แล้ว 2026-08-09) ที่ยังตั้งฉากโดยตั้งใจคือ นาฬิกาแขวน + ซุ้มดอกไม้
      - ⚠️ **`tapStaticScene()` ใน `js/house.js` — ลำดับการเช็คมีผลจริง**: ต้องเช็ค "ช่องนี้เป็นที่นั่ง" (`seatMap`) **ก่อน** บล็อก "แตะโดนถนน/ลาน → เดินไปเฉยๆ" เสมอ เพราะม้านั่งในเมืองเกือบทุกตัวตั้งบนลาน/ริมถนน ถ้าสลับลำดับ **ม้านั่งในเมืองจะนั่งไม่ได้ทั้งเมืองแบบเงียบๆ** (ม้านั่งบนพื้นหญ้ายังนั่งได้ จึงจับได้ยาก — เจอ+แก้ 2026-08-09)
      - ⚡ **เฟรมเรตในโหมดบ้าน = จำนวน draw call ไม่ใช่จำนวนสามเหลี่ยม** · วิธีวัด: `renderer.info.render.calls` (เก็บ median ~60 เฟรม ค่าเฟรมเดียวแกว่งมาก)
      - 🐞 **บั๊กใหญ่ที่แก้แล้ว 2026-08-09 (ข้อ 36.3 ของ `QUEST-DESIGN.md`)**: `hairShell()`/`addHatHair()` ทิ้งค่าชั่วคราว `_hairTop`/`_hairW` ไว้บน `head.userData` แล้ว `mergeDecorGroup()` **ยกเลิกการรวมทั้งก้อนทันทีที่เจอ userData บนลูก** ⇒ **NPC ทุกคนที่มีผมไม่เคยถูก merge เลย** (27-42 draw call/คน) แก้ด้วย `clearBuildScratch(core)` ใน `buildVillager()` — ตลาด **551 → 113 calls** · `geometries` ทั้งฉาก 1,279 → 372 · **เร็วขึ้นทั้งแผนที่**
        - 📌 **เพิ่มค่าชั่วคราวลง `userData` ของชิ้นส่วนตัวละครเมื่อไหร่ ต้องมาล้างที่ `clearBuildScratch()` ด้วยเสมอ** ไม่งั้นบั๊กนี้กลับมาเงียบๆ (ไม่มีอะไรพัง แค่ช้าลง จับได้ยากมาก)
      - ตลาดลดจาก 4 คัน/แถว เหลือ **3 คัน/แถว (9 คัน 9 แม่ค้า)** ตามคำสั่งผู้ใช้ 2026-08-09 **ห้ามเพิ่มกลับโดยไม่ถามผู้ใช้**
      - ⚠️ **ห้ามรัน `npx playwright test` ขณะกำลังแก้ไฟล์ในโปรเจค** — dev server เสิร์ฟไฟล์สด ไฟล์ที่ syntax พังชั่วขณะจะทำให้เทสแดงยกแผงทั้งที่โค้ดไม่ผิด (เจอ 7 เทสแดงแบบนี้ 2026-08-09)
      - 🧭 **ทิศประจำแผนที่ (ยึดตามนี้ทั้งเกม)**: เหนือ = −x · ใต้ = +x · **ตะวันออก = −z** · ตะวันตก = +z (ดู `COMPASS_DIRS` ใน `js/house.js`) — อย่าเดาว่า +x คือตะวันออก
      - 🏠 **ไม่มีสัตว์ = ไม่มีบ้านสัตว์** (`syncPetHouse()` ใน `js/house.js`) — เลิก seed ให้ทุกคนแล้ว บ้านโผล่ตอนรับเลี้ยงตัวแรก · ปล่อยคืนแล้วหายไปด้วย · ในโหมดตกแต่งย้ายที่ได้แต่ลบเองไม่ได้ · **แต่บ้านสัตว์ที่เด็กเก่ามีอยู่แล้วห้ามไปลบ** · ⚠️ **ค่าตอบแทนเควสต์จูนลง ~3 เท่าแล้ว (2026-08-08) กันเงินเฟ้อ — เด็กได้วันละ ~130-310 🪙 เทียบกับของทั้งเกม ~15,800 🪙 (≈ 80 วัน) ห้ามปรับขึ้นกลับโดยไม่ถามผู้ใช้ก่อน** (`FAM_BASE`/`BOARD_BONUS`/`DAY_CAP` ใน `js/house-quests.js` · มีเทสคุมช่วงไว้) เฟสถัดไป = เฟส 3 (สัตว์เลี้ยงครบวงจร) · ✅ วงจร "หาเงิน → ใช้เงิน" ปิดครบแล้ว เปิดให้เด็กเล่นจริงได้ (แต่ยังต้องถามผู้ใช้ก่อน merge ทุกครั้ง) · **เงินในโหมดบ้านจ่ายผ่าน `awardCoins()` ใน `js/house.js` จุดเดียวเท่านั้น** (ห้ามเรียก `OwlCoins.add()` จากที่อื่นในโหมดบ้าน) · เควสต์เดินสำรวจของเก่า (`QUEST_POOL` + `questEvent()` + `QUEST_ENABLED=false`) ยังอยู่ครบแต่ปิดไว้ รอเฟส 7 มารื้อใช้ (หรือตอนว่างถ้าเด็กคนนี้มีบ้านแล้ว) พร้อมเปิดม่านโหลด `#house-loading` ให้ตั้งแต่เริ่มดาวน์โหลด (~1.2 MB) แล้ว `house.js` เดินหลอดต่อจาก 22%
    - ⚠️ **สถานะต่างกันระหว่าง branch (ระวังตอน merge)**: บน `main` (ที่ deploy จริง ตั้งแต่ v2.0.0 / 2026-08-02) โหมดนี้ **ปิดทางเข้าไว้ เด็กเข้าไม่ได้** เพราะยังพัฒนาไม่เสร็จ — แต่บน branch `feature/house-owl` **เปิดไว้เพื่อพัฒนาต่อ** ⇒ **ก่อน merge เข้า `main` ครั้งถัดไป ต้องถามผู้ใช้ทุกครั้งว่าจะเปิดให้เด็กเล่นจริงหรือปิดไว้เหมือนเดิม** ห้ามปล่อยให้เปิดขึ้น production โดยไม่ได้ถาม
      - 📌 **มีสวิตช์ 3 ตัวคนละเรื่องกัน อย่าสับสน**: `HOUSE_FEATURE_OFF` = ทางเข้าโหมดบ้านทั้งโหมด (เปิด/ปิดต้องถามผู้ใช้) · `QB_ENABLED` ใน `js/house-qbrowse.js` = ปุ่ม 📚 คลังคำถาม · `DEV_ENABLED` ใน `js/house-devtools.js` = ปุ่ม 🛠️ ปรับค่าต่างๆ — **2 ตัวหลังเป็นเครื่องมือเทส ต้องตั้งเป็น `false` ทุกครั้งที่ deploy เสมอ ไม่ต้องถาม** ตอน merge ต้องจัดการครบทั้ง 3
      - 🛠️ **หน้า "ปรับค่าต่างๆ" (`js/house-devtools.js` · `window.HouseDev`) — เครื่องมือเทสระบบ เพิ่ม 2026-08-09 ตามคำสั่งผู้ใช้**: 3 แท็บ = ความหิวสัตว์ (ตั้งความอิ่ม 0-100 · ทำให้ป่วย/รักษา · เติม-ล้างคลังอาหาร) · เงิน (เพิ่ม/ตั้งยอดผ่าน `OwlCoins` เท่านั้น) · ปลดล็อกของ (`SHOP.devUnlockAll/devLockAll` — **ปลดล็อกโดยไม่ตัดเงิน** เพราะเป็นของโกงสำหรับเทส ไม่ใช่การซื้อ) · **ทุกอย่างแก้ผ่าน API สาธารณะเท่านั้น ห้ามเขียน localStorage ตรงๆ** ไม่งั้น migration/cache ภายในจะเพี้ยนแบบที่เกมจริงไม่มีทางเจอ · เทสอยู่ที่ `tests/house-devtools.spec.js` (skip ตัวเองอัตโนมัติเมื่อ `DEV_ENABLED=false`)
      - ⭐ **เมนูใน `#house-ctrl-list` ที่เป็นเครื่องมือเทสต้องมี `<span class="hc-test-star">*</span>` (ดาวแดง) ต่อท้ายชื่อเสมอ** (ผู้ใช้สั่ง 2026-08-09) — เพิ่มเมนูเทสใหม่เมื่อไหร่ต้องใส่ด้วยทุกครั้ง มีเทสคุมไว้ว่าเมนูเทสต้องมีดาวและเมนูของเด็กต้องไม่มี
    - วิธีปิด (ที่ใช้บน `main`) ทำไว้ 3 จุด ค้นด้วย `grep -rn HOUSE_FEATURE_OFF`:
      1. `index.html` — ปุ่ม `#house-entry-btn` + `.house-entry-desc` ใส่ `hidden` (**ห้ามลบ element** เพราะ `js/house.js` ผูก listener กับ id นี้ตรงๆ และชุดเทสยิง `dispatchEvent('click')` เข้ามาทดสอบ engine อยู่ — เทสจึงยังเขียวครบ)
      2. `css/style.css` — `.house-entry-btn[hidden]{display:none}` **จำเป็นจริง ห้ามลบ** เพราะ `display:inline-flex` เดิมมี specificity สูงกว่า `[hidden]{display:none}` ของเบราว์เซอร์ ใส่แค่ attribute `hidden` อย่างเดียวปุ่มจะยังโผล่อยู่
      3. `js/games-ar.js` — `preloadHouseIfOwned()` `return;` ทันที ไม่ดาวน์โหลดชุด 3D เบื้องหลัง และเพื่อนซี้ตัวจิ๋วหน้าหลักจะไม่ขึ้นตามไปด้วย (`houseBuddyRefresh` อยู่ใน `house.js` ที่ไม่ถูกโหลด)
      - เปิดกลับ: ทำย้อน 3 จุดข้างบน แล้วเทสว่าปุ่มโผล่จริงในเบราว์เซอร์
    - 📌 **`QUEST-DESIGN.md` = แผนแม่บทของโหมดบ้าน (อนุมัติแล้ว 2026-08-07 · ทำถึงเฟส 2 แล้ว)** — ⚠️ **หัวข้อ 17.5 กับ 32.5 คือ "สิ่งที่ทำจริงต่างจากแผน" ของเฟส 1/2 ต้องอ่านคู่กับแผนเสมอ** — ระบบเควสต์/เศรษฐกิจเหรียญ/ร้านค้า/สัตว์เลี้ยง/พ่อแม่ในบ้าน/STEM/เครื่องดนตรี รวม 9 เฟส · **ก่อนแตะโค้ดโหมดบ้านทุกครั้งต้องอ่านหัวข้อ "⭐ สรุปบริบท — อ่านตรงนี้ก่อนเสมอ" ที่หัวไฟล์ให้จบก่อน** (มีกติกาเหล็ก 7 ข้อ + ข้อที่ผู้ใช้ล็อกแล้วห้ามเปลี่ยนเอง + ลำดับเฟส) ส่วนที่เหลือของไฟล์เปิดอ่านเฉพาะตอนทำเฟสนั้นๆ
- 🔌 **`js/owl-games.js` — "สัญญา Mount" ให้ engine เกมของหน้าหลักไปเล่นที่อื่นได้ (ทำ 2026-08-11 ก่อนเริ่มเฟส 5)**
  - **ปัญหาที่แก้**: engine ทุกตัวใน `js/games-*.js` ผูกกับ view ของตัวเองแบบตายตัว (`showOnlyView(mixView)` แล้ว `$('mix-hint')`) ⇒ โหมดบ้านเอาไปใช้ไม่ได้ ต้องเขียนกลไกซ้ำ พอถึงเฟส 5-7 จะมีของซ้ำกันราว 20 ชุด
  - **วิธีที่ใช้**: **ย้ายทั้งกล่อง view ไปแปะในโฮสต์ใหม่** (id ลูกๆ ยังไม่ซ้ำในเอกสาร `$('mix-hint')` จึงยังหาเจอ) ⇒ **ไม่ต้องแก้โค้ด engine แม้แต่บรรทัดเดียว** · `unmount()` คืน view กลับที่เดิมด้วย placeholder (ลำดับ section มีผลกับ CSS บางตัว)
  - **ตะเข็บส่งผลลัพธ์มีจุดเดียว**: `finishP2Game()` ใน `js/games-math.js` (engine ทุกตัววิ่งผ่าน 25 จุด) — แทรก `OwlGames.handleFinish()` บรรทัดแรก ถ้ากำลัง mount อยู่ให้ส่งผลกลับโฮสต์แทนเด้งหน้าสรุปหน้าหลัก
  - ลงทะเบียนแล้ว **23 เกม** (ท้ายไฟล์ `games-art/think/math/code/write.js`) · ⚠️ **`stop()` ต้องล้าง state ให้หมด** ไม่งั้นเกมเดิมค้างทำงานหลังปิดการ์ด · **ห้ามลงทะเบียน ar-view** (กินทั้งจอ + มีวงจรกล้องของตัวเอง)
  - CSS `.og-embedded` ยุบ view ให้พอดีกล่อง ⚠️ **ห้ามตั้ง position/overflow ทับมากกว่านี้** — engine บางตัววัดตำแหน่งจาก `getBoundingClientRect()` จริง (ลากการ์ด/ลากเข็ม) layout เปลี่ยนมากจะเพี้ยน
- 🏠 **`js/house-games.js` — สะพานระหว่างการ์ดเควสต์กับ engine หน้าหลัก** · `house.js` ห่อด้วย IIFE ไฟล์อื่นแตะภายในไม่ได้ ⇒ คุยผ่าน `window.HouseQuestUI` **ประตูเดียว** (`openCard`/`closeCard`/`makeBtn`/`award`) **เพิ่มประตูใหม่เท่าที่จำเป็นจริงๆ** ไม่งั้นการแยกไฟล์ไร้ความหมาย · โค้ดใหม่ของเฟส 5-7 ให้มาอยู่ไฟล์นี้ ไม่ใช่ยัดใน `house.js` (12,900 บรรทัดแล้ว) · `ALLOW` คุมว่าเกมไหนเหมาะกับการ์ดใบเล็ก **เพิ่มทีละตัวหลังลองเล่นจริงเท่านั้น**
- 🚪 **`js/app-landing.js` — หน้าเลือกทางหลังเลือกเด็ก** (🏠 เข้าเมือง / 📚 ทำโจทย์) · แก้ปัญหาชุด 3D 1.2 MB ที่เด็กซึ่งแค่อยากทำโจทย์ไม่ต้องโหลด · `enterHome()` ถูกแยกเป็น `enterHome()` (ถามหน้าเลือกก่อน) กับ `enterHomeReal()` (ของเดิม) · ⚠️ **โผล่ครั้งเดียวต่อ "รอบการเล่นของเด็กหนึ่งคน"** กลับจากเกมต้องไม่เจอซ้ำ (ตัวคุมคือ `chosen`) **แต่กลับไปหน้าเลือกเด็กเมื่อไหร่ต้องล้าง `chosen` เสมอ** — `renderChildSelect()` ใน `js/app-core.js` เรียก `OwlLanding.reset()` ให้ (ถ้าลืม เด็กจะเลือกคนใหม่แล้วถูกโยนเข้าหน้าหมวดเลย ไม่มีทางเข้าเมือง · บั๊กจริงที่ผู้ใช้เจอ 2026-08-11) · มีปุ่ม **`#landing-back` "← เปลี่ยนคนเล่น"** กลับไปหน้าเลือกเด็กได้จากหน้านี้ (ทำให้เล็กกว่าการ์ด 2 ใบชัดเจน เด็กจะได้ไม่เข้าใจว่าเป็นทางเลือกที่ 3) · **ไอคอนการ์ดเป็น SVG ทั้งคู่** (`assets/icons/my-house.svg` / `assets/icons/landing-quiz.svg`) **ห้ามเปลี่ยนกลับเป็น emoji** (ผู้ใช้สั่ง 2026-08-11) · **โหมดบ้านปิดอยู่ (`HOUSE_FEATURE_OFF`) = ข้ามหน้านี้ไปเลย** ดูจากปุ่ม `#house-entry-btn` ว่า hidden ไหม ไม่ใช่ค่าคงที่
- 📷 **MediaPipe Hands ที่ self-host ไว้ (`js/vendor/mediapipe/`) — ต้องมีไฟล์ครบ ไม่งั้น "เปิดกล้องได้แต่ไม่เจอมือ" แบบเงียบๆ**
  - 🐞 **บั๊กใหญ่ที่แก้แล้ว 2026-08-11**: ตอน self-host เมื่อ 2026-07-30 **ลืมดาวน์โหลด `hands.binarypb`** (ตัวกราฟของโมเดล) ⇒ wasm โหลดผ่าน กล้องติด หลอดไฟกล้องขึ้น แต่พอเริ่มประมวลผลจะ `Check failed: tempConfig.ParseFromString(binary_graph)` แล้ว abort **⇒ เกม AR ทุกเกม + โหมดมือ ตรวจจับมือไม่ได้เลยตลอด v2.0.0 ที่อยู่บน production** (บนหน้าจอไม่มี error ให้เห็น มีแต่ 404 ใน network tab)
  - ⚠️ **ห้ามแก้ปัญหาแบบนี้ด้วยการกลับไปใช้ CDN** — โปรเจคนี้ต้อง self-host ทุกไลบรารี · `teacher/teacher-ar.js` เคยหลุดใช้ CDN อยู่ 3 จุด ย้ายมาใช้ `../js/vendor/mediapipe/` แล้ว 2026-08-11
  - 🧪 **เทสคุมไว้ 3 ตัว**: `tests/mediapipe-assets.spec.js` (ไฟล์ครบ + ห้ามมี CDN ในโค้ด) · `tests/handplay-camera.spec.js` (**เปิดกล้องปลอมของ chromium จริง** แล้วเช็คว่ากราฟคายผลลัพธ์ออกมา — ตอนไฟล์หายค่านี้เป็น 0 เสมอ) ⚠️ ไฟล์หลังต้องแยกไฟล์เพราะ `launchOptions` ตั้งใน `test.describe()` ไม่ได้
  - ⚠️ ตอนนี้ทุกที่ใช้ `modelComplexity:0` (โมเดล lite) **ถ้าเปลี่ยนเป็น 1 ต้องดาวน์โหลด `hand_landmark_full.tflite` มาเพิ่มด้วย** ไม่งั้นเจอบั๊กเดิมซ้ำเป๊ะ
- 🖐️ **โหมดมือ (เล่นด้วยมือหน้ากล้อง) ใช้ได้ในโหมดบ้านแล้ว** — ใช้ `handPlay` เดิมใน `js/games-ar.js` ทั้งดุ้น ไม่ได้เขียนใหม่ (เปิดกล้องแต่ **ไม่โชว์ภาพกล้องเป็นพื้นหลัง** วาดแค่มือการ์ตูน · ปลายนิ้วชี้ = เคอร์เซอร์ · จีบนิ้ว = คลิก)
  - ⚠️ **`hpVisibleView()` ต้องรวม `body > section` ด้วย** — `#house-view` กับ `#ar-view` อยู่**นอก** `<main>` (เรื่อง stacking context) ถ้าเลือกแค่ `main > section` โหมดบ้านจะติดปุ่มกล้องไม่ได้เลย
  - 👆 **ในโหมดบ้าน "ชี้ค้าง 1.5 วินาที" = ตอบ ไม่ต้องจีบนิ้ว** (ผู้ใช้สั่ง 2026-08-12 · `HP_DWELL_MS` + `setHandDwellMode()` ใน `js/games-ar.js`) — ระหว่างนับ ปุ่มมี **แถบเขียวไล่จากซ้ายไปขวาด้วยความเร็วคงที่ (linear)** (`.hp-dwell` ใน `css/style.css` · `clip-path` คุมด้วยตัวแปร `--hp-dwell-p` 0→1) ขยับนิ้วออก = เริ่มนับใหม่
    - ⚠️ **เส้นเล็ง/แถบนับถอยหลังต้องวาด "ในกรอบของชิ้นนั้น" เสมอ** (`outline-offset` ติดลบ · pseudo `inset:0`) — ของเดิมวาดล้นออกไป 3-7px แล้วโผล่พ้นขอบการ์ดคำถามออกไปในโลก 3D (ผู้ใช้แจ้ง 2026-08-12)
    - ⚠️ **ของบางชิ้นฟัง `pointerdown` ไม่ได้ฟัง `click`** (คีย์เปียโน) ⇒ ยิง `.click()` แล้วเงียบสนิท · ลิสต์อยู่ที่ `HP_POINTER_SEL` ใน `js/games-ar.js` และยิงผ่าน `hpFire()` **เพิ่มของใหม่ให้เติมที่ลิสต์เดียวนี้** (กับดักเดียวกับถังของเกมลากวางที่ต้องใช้ `data-hp-click`)
    - ⚠️ **ใช้เฉพาะโหมดบ้านเท่านั้น** — เกม ป.3 กับ `ar-match` ในหน้าหลักยังเป็นจีบนิ้วเหมือนเดิม (ล็อกไว้ในข้อห้ามด้านล่าง) · `mountHandPlayHouse()` เปิด · `mountHandPlay()` ปิด · **จีบนิ้วยังใช้ได้อยู่ ไม่ได้เอาออก** และ **ต้องบอกเด็กด้วยว่าทำได้ทั้ง 2 ทาง** (ข้อความตอนเปิดกล้อง · ผู้ใช้สั่ง 2026-08-12) — มีเทสคุมว่าจีบนิ้วต้องตอบทันทีแม้อยู่ในโหมดชี้ค้าง · มีเทสคุมทั้ง 2 ฝั่งที่ `tests/pre-phase5.spec.js`
  - 🎹 **การ์ดเควสต์กว้างขึ้นเมื่อ mount เกมของหน้าหลัก** (`body.og-mounted .house-creator.house-qz{width:min(1040px,92vw)}`) เพราะ engine พวกนี้ออกแบบมาสำหรับเต็มหน้าจอ — เปียโน 15 คีย์ต้องการ ~980px แต่การ์ดปกติให้แค่ ~708px เด็กต้องเลื่อนหาคีย์ (ผู้ใช้แจ้ง 2026-08-12) · คีย์ยืดพอดีกล่องด้วยกฎ `.og-embedded #music-piano` ชุดเดียวกับ modal "เปียโนของหนู" **ห้ามปล่อยให้ต้องเลื่อนดูอีก** มีเทสวัดจริงที่ `tests/house-phase5.spec.js`
  - ปุ่มกล้องติดตอน `qzShow()` (เปิดการ์ดเควสต์) · ถอดตอน `closeQuestPanel()` · ถังของเกมลาก-วางไม่ใช่ `<button>` ต้องมี `data-hp-click` · **เดินในโลก 3D ด้วยมือไม่ได้** (ใช้ pointer event ไม่ใช่ click) ตั้งใจให้เป็นแบบนั้น
    - ⚠️ **ห้ามสร้าง `new Hands()` ใหม่ทุกครั้งที่เปิดกล้อง** — ใช้ `getHandsSolution()` (ตัวเดียวทั้งแอป ใช้ซ้ำตลอด) · ตัวเก่าที่ถูกทิ้งด้วย `= null` เฉยๆ ไม่ได้คืนระบบไฟล์ในหน่วยความจำของ wasm ⇒ ตัวใหม่อ่านไฟล์โมเดลไม่เจอแล้ว abort ทั้งกราฟ (`LocalFileContentsCalculator failed: Failed to read file …`) **อาการคือปิดการ์ดคำถามแล้วเปิดกล้องใหม่ มือไม่ขึ้น "บางที"** (รอบ 1 ดี · รอบ 2 พัง · รอบ 3 ดี — บั๊กจริงที่ผู้ใช้เจอ 2026-08-11 · มีเทสวนเปิด-ปิด 3 รอบคุมไว้ที่ `tests/handplay-camera.spec.js`)
    - ⚠️ **`mountHandPlayHouse()` ต้อง idempotent — ห้ามขึ้นต้นด้วย `unmountHandPlay()` แบบ `mountHandPlay()` ของหน้าหลัก** เพราะ `qzShow()` ในโหมดบ้านถูกเรียก **8 จุด** (รับงาน → เริ่มเล่น → ทุกครั้งที่เปลี่ยนข้อ → หน้าสรุป) ⇒ เด็กเปิดกล้องแล้ว **มือหายทันทีที่ตอบข้อแรก** โดยไม่มี error ให้จับ (บั๊กจริงที่ผู้ใช้เจอ 2026-08-11 · ตอนนี้เช็ค `hpBtn.parentNode === top` แล้วออกก่อน · มีเทสคุมที่ `tests/pre-phase5.spec.js` โดยดูว่า `#hp-layer` ยังไม่ถูกซ่อน)
  - ⚡ **ห้ามแก้ "โลก 3D กระตุกตอนเปิดกล้อง" ด้วยการหรี่ลูปวาด** — ลองมาแล้วทั้งแบบเว้นตามเวลา (20fps) และเว้นเฟรม (30fps) ผู้ใช้ยังเจอกระตุกอยู่ (2026-08-12) เพราะ **ตัวการจริงคือ inference ของ MediaPipe ที่รันบน main thread เฟรมละ ~10-20 ms แล้วบล็อก rAF ของโลก** ⇒ แก้ที่ต้นทาง: `onFrame` ของกล้องใน `startHandPlay()` **ส่งเข้าโมเดลเว้นเฟรม** (งานหนักลดครึ่ง · เคอร์เซอร์มือ ~15 ครั้ง/วินาที เหลือเฟือสำหรับกลไก "ชี้ค้าง") · `window.HouseFrameHint` ยังอยู่เป็นจุดต่อแต่ **ส่ง `false` เสมอ**
  - เทสอยู่ที่ `tests/pre-phase5.spec.js` (7 เทส × 2 จอ)
- ลำดับการโหลด script สำคัญ (ห้ามสลับ): `data-cats-*.js` → `data-cats.js` → `data-pools.js` → `owl-messages.js` → `app-core.js` → `game-*.js`/`games-*.js` → `app-ui.js` (ท้ายสุดเสมอ เพราะ INIT เรียกใช้ฟังก์ชันจากทุกไฟล์)
- ไม่มี build step ก็จริง แต่การเปิดไฟล์ตรงๆ ผ่าน `file://` ยังใช้ได้ปกติ (ข้อมูลเป็น `.js` ไม่ใช่ `.json` ที่ต้อง `fetch` ผ่าน server เท่านั้น)
- `IDEA.md` — เก็บไอเดียเกม/หมวดใหม่ที่ยังไม่ได้ทำ แต่ละไอเดียเก็บเป็น **หัวข้อเกม** + **หมวดหมู่** + **รายละเอียดเกม** เสมอ (รายละเอียดเติมทีหลังได้ถ้ายังไม่ได้คิด) **อ่านไฟล์นี้เฉพาะตอนจะเริ่มทำเกมใหม่เท่านั้น** (ไม่ต้องอ่านทุกครั้งที่ทำงานอื่น) และเมื่อนำไอเดียไหนไปทำเป็นเกมจริงแล้ว **ต้องลบไอเดียนั้นออกจากไฟล์ทันที**
- `teacher/` — **โหมดคุณครู** (เข้าผ่าน URL `owlkids.net/teacher` เท่านั้น ไม่มีปุ่มจากหน้าหลัก): `teacher/index.html` + `teacher/teacher.js` + `teacher/teacher.css` — หน้าแยกที่ reuse `css/style.css` และธีม/พื้นหลังเดียวกับหน้าหลัก แต่ **JS แยกของตัวเองทั้งหมด ห้ามโหลดไฟล์ `js/app-*.js`/`js/games-*.js` ในหน้านี้** (ผูกกับ DOM ของ index.html แน่นมาก) logo เกมสำหรับครูเลือกอยู่ที่ `assets/icons/teacher/` (12 แบบ) ข้อมูลครูเก็บ localStorage key `owlkids_teacher_profile` / `owlkids_teacher_games` (แยกจากข้อมูลเด็กหน้าหลัก) — ธีมกลางวัน/กลางคืนใช้ key `p1quiz_theme` ร่วมกับหน้าหลักโดยตั้งใจ ให้สลับธีมแล้วตรงกันทั้งสองหน้า

## กฎการทำงานในโปรเจคนี้

- **ตอบผู้ใช้เป็นภาษาไทยเสมอ** ไม่ว่าจะถามอะไร (โค้ด/ชื่อตัวแปรเป็นภาษาอังกฤษได้ตามปกติ)
- **ห้าม `git push` เอง** ต้องถามผู้ใช้ก่อนทุกครั้ง แม้จะ commit ไปแล้วก็ตาม เมื่อได้รับคำสั่ง "push" หรือ "deploy" ให้ทำตามขั้นตอนนี้เสมอ:
  1. **สรุปสิ่งที่เปลี่ยนแปลงจาก version ก่อนหน้า** — ไฟล์ไหนเปลี่ยน, เพิ่ม/แก้/ลบอะไร
  2. **สรุป task ที่ต้องทำก่อน push** — เช่น อัปเดต `version`, สร้าง GitHub Release, อัปเดต `README.md`, อัปเดต `LOG.md` changelog (ระบุว่าแต่ละอย่างยังต้องทำหรือทำไปแล้ว)
  3. ถามว่า **"confirm push ไหม?"** — รอผู้ใช้ตอบ **"cf"** เท่านั้น จึงจะ push ได้ ห้าม push ก่อนได้รับ "cf" เด็ดขาด
- **ถ้าจะทำ feature ใหม่ ต้องสร้าง branch `feature/<ชื่อ>` แยกจาก `main` ก่อนเริ่มเขียนโค้ดเสมอ** พัฒนา+เทสทั้งหมดบน branch นั้น ห้าม commit ตรงเข้า `main` ระหว่างพัฒนา feature ใหม่ เมื่อทำและเทสเสร็จแล้วให้ **ถามผู้ใช้ก่อนทุกครั้ง** ว่าจะ merge เข้า `main` ไหม (รอคำยืนยันก่อน ห้าม merge เองโดยไม่ถาม) — งานเล็กน้อย (แก้บั๊กเล็ก, ปรับข้อความ/สี, chore ทั่วไป) ไม่ต้องสร้าง branch แยกก็ได้ ใช้ดุลยพินิจว่า "เป็น feature ใหม่" หรือไม่ ถ้าไม่แน่ใจให้ถามผู้ใช้ก่อน
- **ถ้าทำเกมใหม่ (หมวดใหม่ใน `CATS`) หรือนำไอเดียจาก `IDEA.md` มาพัฒนาเป็นเกมจริง ต้องสร้าง branch `feature/<ชื่อ>` แยกจาก `main` ก่อนเริ่มเขียนโค้ดเสมอ ไม่ต้องใช้ดุลยพินิจ ไม่มีข้อยกเว้น** พัฒนา+เทสทั้งหมดบน branch นั้นจนเสร็จ (รวม syntax check, ทดสอบเล่นเกมจริง) แล้ว **ถามผู้ใช้ก่อนทุกครั้ง** ว่าจะ merge เข้า `main` ไหม รอคำยืนยันก่อนเสมอ ห้าม merge เข้า `main` เอง
- **Visitor badge ใน footer (`.visitor-badge` img src) — มี 2 จุด**: ใช้บริการ `hitscounter.dev` (เปลี่ยนจาก `visitor-badge.laobi.icu` ที่ปิดบริการไปแล้ว เมื่อ 2026-07-14) counter key คือ query param `url=` (URL-encoded) — หน้าหลัก `index.html` ใช้ `url=https%3A%2F%2Fowlkids.net` (prod) / `url=https%3A%2F%2Fowlkids.net%2Ftest` (dev), หน้าโหมดคุณครู `teacher/index.html` ใช้ `url=https%3A%2F%2Fowlkids.net%2Fteacher` (prod) / `url=https%3A%2F%2Fowlkids.net%2Fteacher-test` (dev) — param อื่น (`label=visitors`, `icon=eye`, `color=`) คงเดิมทั้งสองโหมด ระหว่างพัฒนา/ทดสอบใช้ค่า dev เสมอ (กันนับยอด visitor จริงเพี้ยนจากการเทส) แต่ก่อน push/deploy จริงทุกครั้งต้อง **แก้ param `url=` เป็นค่า prod ทั้ง 2 ไฟล์** ก่อน commit แล้วพอ push สำเร็จเสร็จสิ้นแล้ว ให้ **แก้กลับเป็นค่า dev ทันทีทั้ง 2 ไฟล์** (แก้ไฟล์ตรงๆ เลย **ไม่ต้อง commit หรือ push**) เพื่อกลับสู่โหมดพัฒนาปกติ — ทำทุกครั้งที่ push ไม่ต้องถามก่อน
- **ตรวจสอบก่อนบอกว่าทำเสร็จ**: รัน JS syntax check กับไฟล์ JS ทุกไฟล์ (เรียงตามลำดับโหลดจริง) ก่อนส่งมอบงานทุกครั้ง แล้วรันชุดเทส `npx playwright test` ด้วยถ้าแตะโค้ด engine/ข้อมูลหมวด เช่น
  ```bash
  node -e "
  const fs = require('fs');
  const files = ['js/data-cats-prep.js','js/data-cats-p1.js','js/data-cats-p2.js','js/data-cats-p3.js','js/data-cats-p4.js','js/data-cats-p5.js','js/data-cats-p6.js','js/data-cats.js','js/data-pools.js','js/owl-messages.js','js/app-core.js','js/game-quiz.js','js/games-think.js','js/games-math.js','js/games-code.js','js/games-write.js','js/games-art.js','js/games-listen.js','js/games-ar.js','js/app-ui.js','js/house-shop.js','js/house-quests.js','js/house-pet-care.js','js/house-family.js','js/house.js','js/house-qbrowse.js','js/house-devtools.js'];
  const combined = files.map(f=>fs.readFileSync(f,'utf8')).join('\n');
  try { new Function(combined); console.log('OK JS syntax'); } catch(e){ console.error('ERROR:', e.message); }
  "
  ```
- **ชุดเทส Playwright — ติดตั้งไว้นอกโปรเจค (ห้าม `npm install` ในโฟลเดอร์โปรเจค)**:
  1. **ก่อนรัน `npx playwright test` ครั้งแรกของแต่ละเครื่อง/แต่ละ session ให้รัน `bash tools/setup-playwright.sh` เสมอ** สคริปต์นี้เช็คให้เองว่าเครื่องนี้มี Playwright แล้วหรือยัง ถ้ามีครบแล้วจะจบทันทีไม่ทำอะไร (รันซ้ำได้ปลอดภัย) ถ้ายังไม่มีจะติดตั้งให้เอง แล้วจดสถานะไว้ที่ `~/.local/share/playwright-shared/.installed` — **เครื่องที่มีไฟล์นี้แล้วคือติดตั้งเรียบร้อยแล้ว ไม่ต้องติดตั้งซ้ำ**
  2. ตัว Playwright ติดตั้งกลางไว้ที่ `~/.local/share/playwright-shared/` (นอก repo) ส่วน `node_modules` ในโปรเจคเป็นแค่ **symlink** ชี้ไปที่นั่น (จึงไม่มีไฟล์ dependency จริงอยู่ในโปรเจค) — เบราว์เซอร์ chromium อยู่ที่ `~/Library/Caches/ms-playwright` ตามปกติของ Playwright
  3. `package.json` ในโปรเจคเก็บไว้เพื่อบอกเวอร์ชันที่ต้องการ (สคริปต์อ่านค่า `devDependencies['@playwright/test']` ไปติดตั้ง) — **อย่ารัน `npm install` ที่ root โปรเจค** เพราะจะเขียนทับ symlink แล้วเอา `node_modules` ตัวจริงกลับเข้ามาในโปรเจคอีก ถ้าจะอัปเวอร์ชันให้แก้ `package.json` แล้วรัน `bash tools/setup-playwright.sh`
  4. `.gitignore` ใช้ `node_modules` (ไม่มี `/` ปิดท้าย) โดยตั้งใจ — ถ้าใส่ `/` จะไม่ ignore symlink
- **หลัง push/deploy สำเร็จทุกครั้ง**:
  1. สรุปสิ่งที่เปลี่ยนแปลงสั้นๆ ให้ผู้ใช้ทราบ
  2. เขียนสรุปนั้นลงไฟล์ `LOG.md` ด้วย โดยเพิ่มเป็นรายการใหม่ **ไว้บนสุด** ของไฟล์ (รูปแบบ: `- YYYY-MM-DD: สรุปสั้นๆ`) แล้วอัปเดตหัวข้อ "ประวัติการเปลี่ยนแปลง (changelog)" ใน `CLAUDE.md` ให้เป็นรายการล่าสุด 5 รายการเสมอ (ตัดรายการเก่าสุดออกถ้าเกิน 5) และถ้าเปลี่ยนแปลงพฤติกรรม/สถานะสำคัญของแอป ให้อัปเดตหัวข้อ "สถานะปัจจุบันของแอป" ใน `CLAUDE.md` ให้ตรงด้วย
  3. ก่อน push/deploy ทุกครั้ง ให้ตรวจและอัปเดต `README.md` ให้ตรงกับสถานะล่าสุดของแอปด้วย (เช่น ฟีเจอร์ใหม่, หมวดหมู่ใหม่) แล้ว commit `README.md` ไปพร้อมกับ `CLAUDE.md`, `LOG.md` และไฟล์โค้ดที่เปลี่ยนใน push เดียวกัน
  4. **ทุกครั้งที่ release version ใหม่ ต้องเขียนทับไฟล์ `changelog` ที่ root ด้วยเสมอ** (ไฟล์ text ธรรมดาไม่มีนามสกุลเหมือน `version`, ใช้แสดงในหน้า "รายละเอียดการอัพเดท" ของแอปให้ผู้ปกครองอ่าน) — **เก็บแค่ version ล่าสุด version เดียว เขียนทับของเดิมทุกครั้ง ไม่สะสมประวัติ** เขียนเป็นภาษาอ่านง่ายสำหรับผู้ปกครอง **ห้ามใส่ศัพท์ technical** (ชื่อไฟล์/ฟังก์ชัน/CSS class ฯลฯ) รูปแบบ: บรรทัดแรกคือเลข version, บรรทัดขึ้นต้น `## ` คือหัวข้อหมวด (เช่น เกมใหม่, ปรับปรุงหน้าตา, แก้ไขข้อผิดพลาด), บรรทัดขึ้นต้น `- ` คือรายการย่อยในหมวดนั้น (ดู parser `parseChangelog()` ใน `js/app-ui.js`) แล้ว commit ไปพร้อมกับไฟล์อื่นๆ ใน push เดียวกัน
  5. **อัปเดต memory ถาวรของ Claude (directory memory ประจำโปรเจค) ทุกครั้งหลัง deploy เสร็จ** — ทบทวนว่ารอบงานนี้มีอะไรที่ควรจำข้ามเซสชันแล้วยังไม่ได้บันทึก เช่น บทเรียน/feedback จากผู้ใช้ระหว่างทำงานรอบนี้, ปัญหา deploy ที่เจอ+วิธีแก้ (เช่น Pages build ค้างต้องสั่ง rebuild), งานที่ยังค้างต่อเนื่อง — **ไม่ต้องบันทึกสิ่งที่ repo บันทึกไว้แล้ว** (โค้ด, LOG.md, CLAUDE.md, git history) ถ้าไม่มีอะไรใหม่จริงๆ ก็ข้ามได้ แต่ต้องทบทวนเสมอ
- **หลัง `git push` ทุกครั้ง ต้อง track สถานะ deploy ของ GitHub Pages แล้วสรุปสั้นๆ ให้ผู้ใช้ทราบ**:
  1. รอสักครู่แล้วเช็คด้วย `gh run list --limit 3` (workflow "pages build and deployment") และ/หรือ `gh api repos/Isarez/year1/pages` เพื่อดู `status`/`https_certificate`
  2. ถ้ายังไม่เสร็จ ให้เช็คซ้ำเป็นระยะ (เช่นทุก 60-180 วินาทีด้วย ScheduleWakeup) จนกว่าจะจบ — งานอื่นในเทิร์นนั้นทำต่อได้ระหว่างรอ
  3. เมื่อ deploy จบ ให้สรุปสั้นๆ 1-2 บรรทัดว่า **สำเร็จหรือไม่**:
     - สำเร็จ: ยืนยันด้วยการเช็ค `curl -I` ที่ URL จริงว่าได้ 200 และ `last-modified`/`etag` อัปเดตแล้ว (workflow run บางครั้งค้างสถานะ "queued" ทั้งที่ deploy จริงเสร็จแล้ว ให้ยึด `curl` เป็นหลักถ้าขัดแย้งกัน)
     - ไม่สำเร็จ: ต้องบอก **error message** ที่เจอจาก `gh run view <id> --log-failed` ให้ผู้ใช้ทราบด้วยเสมอ ไม่ใช่แค่บอกว่า "fail" เฉยๆ
  4. **แจ้งสรุปผล deploy (ข้อ 3) ใน Discord channel `1524252244965589082` ช่องนี้ช่องเดียวเท่านั้น** (ใช้ chat_id นี้กับ Discord reply tool) ไม่ว่าจะสั่ง push มาจากช่องทางไหนก็ตาม (Discord ช่องอื่น หรือ session ปกติ) — **ห้ามแจ้งผล deploy ซ้ำใน channel ที่สั่งงานมา** ให้แจ้งเฉพาะช่อง deploy log นี้ช่องเดียว
- **ถ้าเพิ่มเกม/หมวดใหม่ หรือแก้ไขอะไรที่มีนัยสำคัญ (breaking change, ปรับพฤติกรรมหลักของแอป, โครงสร้างไฟล์ใหม่ ฯลฯ) ต้อง `gh release create` ก่อน push/deploy เสมอ**:
  1. ตั้ง tag แบบ semver (เช่น `v1.1.0` ปรับ minor เมื่อเพิ่มฟีเจอร์/เกมใหม่, `v1.0.1` ปรับ patch เมื่อแก้บั๊ก/ปรับเล็กน้อย) — เช็ค tag ล่าสุดด้วย `git tag --list` หรือ `gh release list` ก่อนตั้งเวอร์ชันถัดไป
  2. เขียน release notes สรุปการเปลี่ยนแปลงเป็นภาษาไทย (ใช้เนื้อหาเดียวกับที่จะเขียนลง changelog ใน `LOG.md`)
  3. **push commit ให้เสร็จก่อน แล้วค่อยรัน** `gh release create <tag> --title "<tag>: <สรุปสั้นๆ>" --notes "<รายละเอียด>"` — **ห้ามสร้าง release ก่อน push เด็ดขาด** เพราะ GitHub จะผูก tag กับ HEAD เก่าบน origin/main ณ ตอนนั้น (commit ใหม่ยังไม่ถูก push ขึ้นไป) ทำให้ tag ชี้ commit ผิด version — เคยพลาดมาแล้วกับ v1.19.0/v1.20.0 ต้องไล่ลบ tag + สร้างใหม่ด้วย `--target <full SHA>` ทีหลัง (แก้เมื่อ 2026-07-19)
  4. **แก้ `?v=` ของทุก `<script src>`/`<link href>` ใน `index.html` และ `teacher/index.html` ให้เป็นเลข version ใหม่ด้วยเสมอ** (cache-busting — ถ้าลืม ผู้ใช้เดิมจะยังได้ไฟล์เก่าจาก cache ทั้งที่ deploy ใหม่แล้ว) แก้ทีเดียวทุกไฟล์ได้ด้วย `sed -i '' 's/?v=<เก่า>/?v=<ใหม่>/g' index.html teacher/index.html`
     - ⚠️ **แล้วต้องแก้ `window.APP_ASSET_VER` ที่ต้นไฟล์ `js/app-core.js` ด้วยมืออีกจุดเสมอ** — เป็น cache-buster ของไฟล์ที่โหลดแบบ lazy (mediapipe hands/camera_utils, three.min.js, house*.js) ซึ่ง**อยู่นอก `index.html` คำสั่ง `sed` ข้างบนจับไม่ถึง** เคยลืมมาแล้วตอน v2.0.0 (ค้างที่ `?v=1.20.0`) รอบนั้นไม่กระทบเพราะไฟล์เหล่านั้นเพิ่งถูกเพิ่มใหม่ยังไม่มีใคร cache แต่ถ้าลืมรอบที่ไฟล์พวกนี้เปลี่ยน ผู้ใช้เดิมจะได้ของเก่า — ตรวจด้วย `grep -rn APP_ASSET_VER js/app-core.js`
  5. **แก้ไฟล์ `version` ที่ root ให้ตรงกับ tag ใหม่เสมอ** (เช่น `v1.3.0`) — ทำพร้อมกับ commit อื่นใน push เดียวกัน ห้ามลืม
  6. งานเล็กน้อย (ปรับ CSS ระยะห่าง, แก้ข้อความ, แก้สีเฉพาะจุด) ไม่ต้องสร้าง release ทุกครั้ง — ใช้ดุลยพินิจว่า "มีนัยสำคัญ" หรือไม่ ถ้าไม่แน่ใจให้ถามผู้ใช้ก่อน
- **icon และ emoji ของเกม/หมวดใหม่ต้องไม่ซ้ำกับที่มีอยู่แล้ว**: ก่อนเพิ่มเกม/หมวดใหม่ ให้ grep หรืออ่าน `CATS` ใน `js/data-cats-*.js` เพื่อดู emoji ที่ใช้อยู่ทั้งหมด แล้วเลือก emoji ที่ยังไม่มีใครใช้ — ทั้ง `cat.emoji` (icon บนการ์ดและสติกเกอร์ในสมุด) และ icon ที่แสดงใน UI อื่นๆ ห้ามซ้ำกันเด็ดขาด เพราะ emoji เดียวกันในสมุดสติกเกอร์จะทำให้แยกหมวดไม่ออก
- **ป้าย "NEW ✨" บนหน้าเลือกหมวด (`cat.isNew:true` ใน `js/data-cats-*.js`)**:
  1. **ถ้าเพิ่มเกม/หมวดใหม่ ต้องติด `isNew:true` ให้เสมอ** ไม่ต้องถามก่อน (ทำอัตโนมัติทุกครั้งที่เพิ่มหมวดใหม่)
  2. **ห้ามลบ `isNew:true` ของหมวดเดิม/เวอร์ชันก่อนหน้าโดยไม่ถามก่อนเด็ดขาด** — ก่อนลบต้อง **list รายชื่อหมวดที่จะลบ badge ออกมาให้ผู้ใช้ดูก่อนเสมอ** แล้วรอผู้ใช้ยืนยันก่อนแก้ไฟล์ ห้ามลบเองแม้จะดูสมเหตุสมผลว่า "ไม่ใหม่แล้ว" ก็ตาม
- **การ์ดหมวด (`.cat-card`) ในหน้าหลักทุก section (`#cat-grid`, `#cat-grid-interactive`, `#cat-grid-skill`, `#cat-grid-listen` และ section ใหม่ในอนาคต) ต้องมีขนาดเท่ากันเสมอ** — ห้ามปรับ padding/font-size/min-height ของการ์ดเฉพาะหมวดใดหมวดหนึ่งจนขนาดต่างจากการ์ดอื่น หรือปล่อยให้เนื้อหาในการ์ด (ชื่อยาว, meta ยาว) ดันขนาดการ์ดให้เพี้ยนไปจากการ์ดหมวดอื่น ถ้าจะเพิ่มหมวด/เกมใหม่ ให้ใช้ class `.cat-card` เดิมเสมอ ไม่สร้างสไตล์การ์ดแยกเฉพาะหมวด

## สถานะปัจจุบันของแอป (สำคัญ อย่าย้อนกลับโดยไม่ถาม)

- **ธีมกลางวัน/กลางคืน**: ปุ่ม `#theme-toggle` ใน header สลับ `body.night-mode`, จำค่าไว้ที่ `localStorage['p1quiz_theme']`. กลางคืน: ท้องฟ้าเปลี่ยนเป็นสีกรมท่า, พระอาทิตย์ (`.bg-sun`) fade ออกและพระจันทร์ (`.bg-moon`) fade เข้า, เมฆเปลี่ยนสี, ลูกโป่งลอยเปลี่ยนเป็นดาว. ตัวอักษรที่ไม่ได้อยู่บนการ์ดขาว/ครีม (header title, hero, footer, section title อย่าง `.cat-section-title`) ต้องมี override สี `body.night-mode` เพราะสีเริ่มต้น (`--ink`/`--ink-soft` น้ำตาลเข้ม) อ่านไม่ออกบนพื้นหลังกลางคืน
  - ข้อควรระวัง CSS: `@keyframes` ที่ animate `opacity` จะ override static opacity rule เสมอ ไม่ว่าจะ specificity อะไร — ถ้าจะซ่อน/โชว์ element ตามธีม ต้อง scope animation ไว้ใน selector ของธีมนั้นๆ ด้วย ไม่ใช่ใส่ไว้ที่ base rule
  - Grid ที่มีเนื้อหาไม่ยอมหด (เช่น emoji ใหญ่) ต้องใช้ `grid-template-columns:repeat(N,minmax(0,1fr))` ไม่ใช่ `repeat(N,1fr)` เฉยๆ ไม่งั้นจะ overflow ออกนอก panel บนจอมือถือแคบ (`#emoji-picker` เจอปัญหานี้มาแล้ว)
- **เพลงพื้นหลัง**: procedural Web Audio (ไม่ใช่ไฟล์เสียง) มี **5 เพลง** (`MUSIC_TRACKS` ใน `js/app-core.js` แต่ละเพลงมี bpm ของตัวเอง) เล่นจบเพลงแล้วพัก 2 จังหวะต่อเพลงถัดไปวนเป็น playlist, default เป็น **เปิด** สำหรับผู้ใช้ใหม่ (`localStorage['p1quiz_music'] !== 'off'`) เสียงเบา (`0.025`) ห้ามปรับเสียงดังขึ้นโดยไม่ถาม — โน้ตเพลงใช้ oscillator หลักเป็น sine (โทนเดียวกับ `playCorrect`) ห้ามเปลี่ยนกลับเป็น triangle/square โดยไม่ถาม, เสียงคลิกปุ่ม (`playClick`) เป็น sine E5+E6 และมีแฟนแฟร์ `playCongrats()` ดังพร้อมพลุตอนจบเกมทุกโหมด
- **หมวดหมู่ (`CATS`)**: math, thai (ภาษาไทย 1), thai2 (ล็อกจนกว่าจะทำ thai1 จบ), english, behavior, animals, days, iq1-iq4 (ล็อกตามลำดับผ่าน `CAT_REQUIRES`), pattern (เติมแพทเทิร์น 🔮)
- 💰 **ค่าตอบแทนเควสต์ (จูนล่าสุด 2026-08-09 — ห้ามปรับขึ้นกลับโดยไม่ถามผู้ใช้)** — **ทุกระดับชั้นได้เท่ากันหมด**:

  | ประเภท | 1 ดาว | 2 ดาว | 3 ดาว | ท้าทาย (3 ดาว ×1.5) |
  |---|---|---|---|---|
  | เควสต์ NPC (`FAM_BASE.A` = 6) | 6 | 8 | 10 | 14 |
  | กระดาน (`board` = 8) | 8 | 10 | 13 | 19 |
  | ครอบครัว (`family` = 10) | 10 | 13 | 16 | 24 |

  ตัวคูณ: `STAR_MUL = [0, 1.0, 1.3, 1.6]` · `CHAL_MUL = 1.5` · **ตัวคูณระดับชั้น = 1.0 ทุกชั้น** (ผู้ใช้สั่ง 2026-08-09 · เดิม ป.1 ×1.0 → ป.6 ×1.4 ทำให้พี่ได้มากกว่าน้องทั้งที่ทำงานเท่ากัน) — พารามิเตอร์ `tier` ใน `coinsFor()` ยังรับไว้แต่ไม่มีผลกับเหรียญแล้ว · **มีเทสคุมว่าทุกชั้นต้องได้เท่ากันเป๊ะ** · รายได้สูงสุด/วัน = **206 🪙** (รวมโบนัสดาว 35 + โบนัสกระดาน 10)
- ⭐ **โบนัสดาวรายวัน (เพิ่ม 2026-08-09)**: ได้ดาวถึง **ครึ่งหนึ่ง**ของดาวเต็มวันนี้ → กดรับ **15 🪙** · ได้ **ครบเต็ม** → กดรับอีก **20 🪙** · **ต้องกดรับเองที่ "หมุด" บนหลอดดาวในหน้ารายการเควสต์** (ไม่จ่ายอัตโนมัติ เด็กจะได้รู้ตัวว่าได้อะไรมา) — หมุดวางตามสัดส่วนดาวที่ต้องได้ (~50% / 100%) **บอกล่วงหน้าว่าทำถึงตรงไหนได้เงินเพิ่ม** โดยแสดง **รูปเหรียญ + จำนวนเงิน** และ **จำนวนดาวที่ต้องได้ใต้เหรียญ** (เหรียญวาดด้วย CSS `.hs-coin` **ห้ามใช้ emoji 🪙** — บางเครื่องไม่มี glyph) · เทา = ยังไม่ถึง · ทองเรืองแสง = กดรับได้ · เขียว ✓ = รับแล้ว · ปุ่มเควสต์บน HUD ขึ้น 🎁 + วงแหวนทองเด้งเมื่อมีของรอรับ · ยังอยู่ใต้ `DAY_CAP` เหมือนเควสต์ปกติ · ⚠️ **ต่างจากโบนัส "ทำครบ 5 ชุด" ที่ถูกเอาออกไป** — อันนี้วัดที่ **คุณภาพ (ดาว)** ไม่ใช่จำนวนชุด และครึ่งทางก็ได้แล้ว จึงไม่กดดันให้ทำครบ
- 💰 **โบนัสกระดาน "ทำครบ 5 ชุด" = 10 🪙** (`BOARD_BONUS` · กดรับที่ปุ่ม `#hq-claim` บนกระดาน) — เคยเป็น 35 แล้วถูกเอาออกทั้งก้อนเมื่อ 2026-08-09 จากนั้นผู้ใช้สั่งเอากลับมาที่ **10** ในวันเดียวกัน · ก้อนเล็กลงมากจึงไม่กดดันให้ "ต้องเก็บให้ครบ" เหมือนเดิม · **ห้ามปรับขึ้นโดยไม่ถามผู้ใช้**
- 📝 **จำนวนข้อต่อ 1 เควสต์ = 5-10 ข้อ ไล่ตามชั้น** (`difficulty().qN` ใน `js/house-quests.js` · ผู้ใช้สั่งเพิ่มจาก 3-5 เมื่อ 2026-08-09): เตรียม ป.1/ป.1 = 5 · ป.2 = 6 · ป.3 = 7 · ป.4 = 8 · ป.5 = 9 · ป.6 = 10 · ⚠️ **ค่าตอบแทนคิดต่อเควสต์เหมือนเดิม** (เหรียญต่อข้อจึงลดลงครึ่งหนึ่ง — ตั้งใจ ยืดเวลาเล่นโดยไม่ทำเงินเฟ้อ) · ⚠️ **เกณฑ์ดาวต้องยืดตามจำนวนข้อเสมอ** (`starsOf` คิดเป็นสัดส่วน ~2/3 ของจำนวนข้อ) ไม่งั้นเควสต์ยาวจะเข้มขึ้นเงียบๆ จนเด็กได้ 1 ดาวตลอด — ผิดกติกาเหล็กข้อ 2
- **รายละเอียดราย engine/เกม ย้ายไปเป็น skill แล้ว (โหลดเฉพาะตอนใช้ ไม่กินคอนเท็กซ์ทุก session)** — เรียกด้วย Skill tool เมื่อจะแก้เกมกลุ่มนั้น:
  - `game-engines-skill` — เกมฝึกทักษะ (โดมิโน/ทายเงา/ผสมสี/ดนตรี/เปียโนของหนู/นาฬิกาวิเศษ/เติมแพทเทิร์น)
  - `game-engines-ar` — เกม AR ที่ใช้กล้อง + โหมดเล่นด้วยมือ (handPlay) + มือการ์ตูน
  - `listen-write-games` — เกมฟังคำศัพท์ / ฟังประโยคเติมคำ / ลากเส้นต่อจุด + กติกาคลังคำ
  - `grade-levels` — ระบบระดับชั้น ป.1-ป.4 + mechanic ใหม่ของ ป.4 + กติกาหลักสูตร
  - `teacher-mode` — โหมดคุณครู (`teacher/`)
- **ข้อห้ามสำคัญ (สรุปจาก skill ด้านบน — ห้ามย้อนหรือเปลี่ยนโดยไม่ถามผู้ใช้ก่อนทุกข้อ)**:
  - ห้ามใส่สายรุ้งกลับในพื้นหลังกลางวัน / ห้ามเปลี่ยนหน้าหม้อผสมสีกลับเป็น emoji / ห้ามเปลี่ยน oscillator เพลงพื้นหลังกลับเป็น triangle-square / ห้ามเพิ่มเสียงเพลงพื้นหลังให้ดังขึ้น
  - ห้ามเปลี่ยน mechanic ของเกมลากเส้นต่อจุดจาก "ลาก" กลับเป็น "แตะ" และห้ามใส่ highlight บอกใบ้จุดถัดไป
  - ห้ามเปลี่ยนท่ามือของ `ar-match` (แตะจุดแล้วลาก / จีบนิ้ว = ยกเลิก) กลับเป็น pinch-to-grab
  - ห้ามใส่ "o" นำหน้าตัวอักษรไทยที่ยืนเดี่ยวไม่ได้ในเกมฟังคำไทย และห้ามใส่ประโยคภาษาไทยกลับในเกมฟังประโยคเติมคำ (เป็นอังกฤษล้วนตามคำขอผู้ใช้)
  - ห้ามลบ id เดิมของหมวดที่มีอยู่แล้ว (เช่น `ar-match`, `ar-math`, `cloze-th1/2`) เพราะผูก progress/สติกเกอร์ของผู้ใช้
  - ห้ามเปลี่ยน mechanic ของเกมนาฬิกา (เข็มสั้นอยู่หน้า / จับได้เฉพาะตัวเข็ม / ลากแบบ relative)
  - ห้ามใช้ `desktopOnly:true` กับเกมที่ไม่ได้ตั้งใจล็อกมือถือโดยไม่ถามก่อน
  - ห้ามใช้ `#ar-camera-msg` แบบการ์ดค้างจอสำหรับกรณีมือถือ (ใช้ `showToast()` เท่านั้น)
- **"days" category**: ค่าตำแหน่งคำตอบถูก shuffle แบบ one-time manual fix ไปแล้ว (ของเดิมตอบ index 0 หมด) — อย่า shuffle ซ้ำอีก
- **พื้นหลังกลางวัน**: ท้องฟ้าสีฟ้า gradient, มีพระอาทิตย์หมุนช้าๆ, เมฆลอย, ลูกโป่งลอย, ประกายดาวระยิบระยับ — เคยมีสายรุ้งแล้วเอาออกตามคำขอผู้ใช้ **ห้ามใส่กลับโดยไม่ถาม**
- **นกฮูกมาสคอต**: มุมขวาล่าง คลิกแล้วสุ่มข้อความให้กำลังใจจาก `OWL_MSGS.cheer` (คนละปุ่มกับสมุดสติกเกอร์ `#sticker-tally-btn`)
- **Favicon/ไอคอน**: `assets/favicon.svg` เป็น source of truth ถ้าแก้ต้อง regenerate PNG ทุกไซส์ด้วย `rsvg-convert -w <size> -h <size> assets/favicon.svg -o assets/<name>.png`
- **ข้อมูลผู้ใช้ทั้งหมดเก็บใน `localStorage` เท่านั้น ไม่มี backend** — footer มีข้อความยืนยันเรื่องนี้ ต้องคงความจริงนี้ไว้ถ้าจะเพิ่มฟีเจอร์ sync/backend ในอนาคต
- **Theme transition**: ท้องฟ้ากลางวัน/กลางคืนใช้ 2 เลเยอร์ `.bg-sky-day`/`.bg-sky-night` ซ้อนกันแล้ว crossfade ด้วย `opacity 2s` (ไม่ใช้ `transition:background` บน gradient เพราะเบราว์เซอร์ tween gradient ตรงๆ ไม่ได้) พระอาทิตย์/พระจันทร์ใช้ wrapper div แยกจาก SVG เดิมเพื่อทำ transform translateY แยกจาก animation หมุน/pulse เดิม (element เดียวมี transform 2 ระบบพร้อมกันไม่ได้)
- **ปุ่มวิธีติดตั้งแอป**: `#install-toggle` เป็นปุ่ม text ("วิธีติดตั้งแอปบน iPad/แท็บเล็ต") ลอยมุมล่างซ้าย (`.install-link-btn`) ไม่ใช่ icon ใน header อีกต่อไป กดแล้วเปิด `#install-modal` (คำแนะนำ iPad/iPhone Safari + Android Chrome) ซ่อนอัตโนมัติตอนอยู่ใน AR view (`body.ar-open .install-link-btn{display:none}`)
- **ปุ่มรายละเอียดการอัพเดท**: `#changelog-open-btn` อยู่ใต้ปุ่มนำเข้าข้อมูล (`#child-import-btn`) ในหน้าเลือกโปรไฟล์เด็ก (`.changelog-open-btn{margin-top:22px}` เว้นระยะเพิ่มเพราะคนละกลุ่มฟังก์ชัน) กดแล้วเปิด `#changelog-modal` (pattern เดียวกับ `#install-modal`) โหลดเนื้อหาจากไฟล์ `changelog` ที่ root ผ่าน `fetch('changelog')` แล้ว parse ด้วย `parseChangelog()` ใน `js/app-ui.js` — ไฟล์ `changelog` เก็บแค่ **version ล่าสุด version เดียว** (เขียนทับทุก release ไม่สะสมประวัติ), modal มีปุ่มปิด 2 จุด (✕ มุมบนขวา `.modal-x-btn` + ปุ่มปิดล่าง) ดูกติกาอัปเดตไฟล์นี้ทุก release ในหัวข้อ push/deploy ด้านบน
- **ปุ่มเต็มหน้าจอ**: มี 2 ปุ่มที่ผูก logic เดียวกัน — `#fullscreen-toggle` ใน header (หน้าปกติ) และ `#ar-fullscreen-toggle` ใน `.quiz-top` ของ AR view (มุมขวาบน ข้าง `#ar-level-counter`) ทั้งคู่ผูกกับ `fsBtns` array และ `refreshFsBtn()` เดียวกัน ถ้าเพิ่มปุ่มเต็มหน้าจอใหม่ให้เพิ่ม id เข้า array นี้แทนการเขียน handler แยก
## ประวัติการเปลี่ยนแปลง (changelog)

Changelog ทั้งหมดย้ายไปอยู่ที่ [LOG.md](LOG.md) แล้ว เพื่อไม่ให้ไฟล์นี้ใหญ่เกินไป — เก็บไว้ที่นี่แค่ 5 รายการล่าสุด ส่วนรายการเก่ากว่านี้ดูที่ `LOG.md`

- 2026-08-02: **v2.0.0 — merge `feature/house-owl` (136 commit) เข้า `main`** งานตั้งแต่ 2026-07-29 ค้างบน branch ไม่เคยขึ้น production ขึ้นพร้อมกันรอบเดียว: **ระดับชั้น ป.1–ป.6 ครบ** (หมวด 40 → 332), **แยกไฟล์ทั้งระบบ** (`data.js`+`app.js` → `data-cats-*`/`data-pools`/`app-core`/`app-ui`/`game-quiz`/`games-*`/`shared/*`), เกมใหม่จำนวนมาก, lazy-load AR/3D, ชุดเทส Playwright 36 เทส + `tools/setup-playwright.sh`; **โหมด "บ้านของหนู" 3D ปิดทางเข้าไว้ (`HOUSE_FEATURE_OFF`)** โค้ด/เทสอยู่ครบแต่ซ่อนปุ่ม + ไม่ preload ชุด 3D; ระบบ **เงินนกฮูก 🪙** (`window.OwlCoins`) วางไว้แล้วแต่ยังไม่มีที่หา/ใช้เงิน
- 2026-07-19: teacher v1.19.0 — **โหมดครู: เพิ่ม mechanic สร้างโจทย์เอง 3 แบบ** จับคู่ความจำ 🎴 (`match`, `teacher/teacher-match.js`, view `#memory-view`), ผสมสี 🎨 (`mix`, `teacher/teacher-mix.js`, `#mix-view`), ดนตรี 🎹 (`music`, `teacher/teacher-music.js`, `#music-view` — ครูกดเปียโนแต่งทำนองเอง limit 30 โน้ต/ทำนอง) — engine port จาก `js/app.js` คง id/class/CSS เดิม, sync จุดซ่อน view ใน `startTeacherAR` เพิ่ม memory/mix/music — teacher-only ไม่แตะ version ฝั่งเด็ก
- 2026-07-18: v1.20.0 — **เพิ่มเกมใหม่ "นาฬิกาวิเศษ 1/2/3/4" 🕐🕰️⏳⏰ (section เกมฝึกทักษะ)** (`skill-clock1..4`, `mode:'clock'` `clockMode:1-4`, view `#clock-view`, ล็อกลำดับ, mechanic ลากหมุนเข็มครั้งแรกของแอป): เกม 1 ชั่วโมงตรง/โมงครึ่ง, เกม 2 นาที (snap 1 + ปุ่ม ±1 นาที), เกม 3 "อีก N ชม. เป็นกี่โมง?", เกม 4 "อีก N ชม. M นาที" ทดชั่วโมงอัตโนมัติ — หน้าปัด SVG หน้ายิ้มกะพริบตา, เข็มสั้นอยู่หน้า+จับได้เฉพาะเข็ม+ลาก relative, กรอบสุ่ม 5 แบบ, อ่านโจทย์เสียงไทย, icon `clock-1..4.svg` (สมุดสติกเกอร์ 40 หมวด); `IDEA.md` +7 ไอเดียใหม่ (เสนอหมวด "เกมศิลปะ")
- 2026-07-18: v1.19.0 — **เพิ่มเกมใหม่ "เติมแพทเทิร์น" 🔮 (section คำถาม-คำตอบ)** (id `pattern`, `poolPick:10` สุ่ม 10 จากคลัง 30 ข้อ 3 tier เรียงง่าย→ยาก 4/3/3 ผ่าน `pickQuizQuestions()` ใหม่ใน app.js, `q.pattern` render แถวการ์ด emoji + ช่อง ? เส้นประเต้นใน `renderQuestion()`, ตัวเลือก emoji ใหญ่ย่อฟอนต์ด้วย `graphemeLen()`, icon `pattern.svg`, ป้าย NEW); **เปียโนของหนู: ฟังเพลงอัปเกรด** — ตัวโน้ตวิ่งติ๊ก ✓ + คีย์ flash ตามจังหวะ (`playMusicSequence` รับ opts `{onNote,onStop}`), ปุ่มเปลี่ยนเป็น "⏹ หยุดเพลง" ระหว่างเล่น, จบ/หยุด/เปลี่ยนเพลง/ปิด modal reset อัตโนมัติ + แก้บั๊ก flash ชี้ `#music-piano` เสมอทำให้คีย์ใน modal ไม่กระพริบ
- 2026-07-17: v1.18.0 — **เพิ่มเกมใหม่ "ลากเส้นต่อจุด 1/2" (section ใหม่ "เกมฝึกเขียน")** (`write-dots1` ✏️ 5-7 จุด / `write-dots2` 🖍️ 10-13 จุด ล็อกลำดับ, `type:'write'` `mode:'dots'`, view `#dots-view`): ลากเส้นต่อจุดตามตัวเลข (mechanic ลากเท่านั้น เส้นประตามนิ้ว ไม่มี highlight ใบ้) ครบแล้วเฉลยรูปจริง, คลัง `DOTS_SHAPES` อย่างละ 30 รูปสุ่ม 10 ต่อรอบ, `body.dots-open` ย่อนกฮูกกันบังจุดบนมือถือ, icon `dots-1/2.svg`+`section-write.svg`; **เกมดนตรี 2 เหลือ 7 ด่าน** + เพิ่ม `beats` 10 เพลงให้ฟังเพลงใน "เปียโนของหนู" ถูกจังหวะจริง; ลบป้าย NEW จากผสมสี 1/2 + เกมดนตรี 1/2/3

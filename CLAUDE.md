# นกฮูกสนุกคิด — เตรียมสอบ ป.1

เว็บแอปสำหรับเตรียมสอบเข้า ป.1 ไม่มี build step (ไฟล์ static ล้วนๆ) deploy ผ่าน GitHub Pages จาก branch `main` ที่ https://isarez.github.io/year1/

ชื่อระบบ/แบรนด์: **Owlkids** — มีโดเมนชื่อ **https://owlkids.net**

## หลักการออกแบบหลัก (ยึดทุก feature/ทุกการตัดสินใจ)

- **Static single page template เสมอ** — ไม่มี build step, ไม่มี framework/bundler (React/Vue/webpack/vite ฯลฯ), ไม่มี backend/server-side code ทุก feature ใหม่ต้องทำงานได้ด้วยไฟล์ static ล้วนๆ (HTML/CSS/JS + asset) เปิดผ่าน GitHub Pages ได้ตรงๆ ถ้าจะใช้ library ภายนอก (เช่น Three.js) ต้อง self-host ไฟล์ไว้ใน repo เอง ห้ามพึ่ง CDN หรือ build tool ใดๆ
- **ธีม/ดีไซน์ทุกส่วนต้องออกแบบให้เหมาะกับเด็กอายุ 5 ขวบ** — สีสดใส ทรงมนน่ารัก ตัวอักษรใหญ่อ่านง่าย ไอคอน/ภาพต้องเข้าใจง่ายด้วยสายตาไม่ต้องอ่านออกเขียนได้คล่อง หลีกเลี่ยงองค์ประกอบที่ดูน่ากลัว/สมจริงเกินไป/ซับซ้อนเกินวัย ก่อนออกแบบ element ใหม่ให้อ้างอิงสไตล์ภาพ/ธีมที่มีอยู่แล้วในแอป (การ์ตูนแบน, สีพาสเทล/สดใส, ไอคอน SVG มน ๆ) ให้ไปในทิศทางเดียวกันเสมอ
- **localStorage คือฐานข้อมูลหลักของระบบทั้งหมด ไม่มี backend/server เก็บข้อมูลผู้ใช้** ทุก feature ใหม่ (progress, ตัวละคร, ของสะสม ฯลฯ) ต้องออกแบบให้เก็บ/อ่านผ่าน `localStorage` เป็นหลัก และคิดเผื่อ merge เข้ากับระบบ export/import ย้ายข้อมูลข้ามอุปกรณ์ที่มีอยู่แล้วด้วยเสมอ

## โครงสร้างไฟล์

- `index.html` — markup ล้วนๆ, อ้างอิง CSS/JS ผ่าน `<link>`/`<script src>`
- `css/style.css` — สไตล์ทั้งหมด
- `version` — ไฟล์ text ธรรมดา (ไม่มีนามสกุล) เก็บเลขเวอร์ชัน เช่น `v1.2.0` — **แก้เลขเวอร์ชันที่นี่เมื่อ release ใหม่** หน้าเว็บดึงค่านี้ผ่าน `fetch('version')` แล้วแสดงใน footer
- **ไฟล์ JS แยกตามหน้าที่ (แยกจาก `data.js`/`app.js` ก้อนเดียวเมื่อ 2026-07-30) — ยังเป็น classic script ธรรมดา ไม่มี module/build step ทุกไฟล์ใช้ global scope เดียวกัน**
  - ข้อมูลหมวด: `js/data-cats-prep.js`, `data-cats-p1.js` … `data-cats-p6.js` (ชั้นละไฟล์ ประกาศ `CATS_PREP`/`CATS_P1`…) แล้ว `js/data-cats.js` ต่อทั้งหมดเป็น `CATS` ตัวเดียว (**ลำดับในอาเรย์ = ลำดับการ์ดในหน้าหลัก ห้ามสลับ**) + `GRADES` + `CAT_REQUIRES`
  - `js/data-pools.js` — คลังข้อมูลของ engine ทั้งหมด (`AR_SENTENCES`, `LISTEN_WORDS`, `ROBOT_LEVELS`, `SORT_POOLS`, `DOTS_SHAPES` ฯลฯ)
  - `js/owl-messages.js` — `OWL_MSGS` ข้อความให้กำลังใจของนกฮูก
  - `js/app-core.js` — state, โปรไฟล์เด็ก, ระดับชั้น/อายุ, เสียง+เพลง, confetti, helper กลาง (`showOnlyView`, `beginSkillGame`, `finishP2Game`), render หน้าเลือกหมวด
  - `js/game-quiz.js` (ควิซ+หน้าสรุปผล) · `js/games-think.js` (จับคู่/ทายเงา/นกฮูกสั่ง/นักวิทย์) · `js/games-math.js` (เงิน เศษส่วน ตาชั่ง ปฏิทิน เส้นเวลา จัดหมวด โลก พิกัด แผนภูมิ พื้นที่ มุม นาฬิกา วงจรไฟฟ้า แท็งแกรม กระจก เรียงลำดับ) · `js/games-code.js` · `js/games-write.js` · `js/games-art.js` (ผสมสี/ดนตรี/เปียโน) · `js/games-listen.js` (ฟังคำ/เติมคำ) · `js/games-ar.js` (AR + handPlay + lazy-load โหมดบ้าน)
  - `js/app-ui.js` — ปุ่ม/toast/สมุดสติกเกอร์/modal/ย้ายข้อมูล/ธีม/พื้นหลัง **และบรรทัด INIT ท้ายไฟล์**
  - `js/shared/*.js` — **โค้ดกลางที่หน้าเด็กกับโหมดคุณครูใช้ร่วมกันจริงๆ (โหลดทั้ง 2 หน้า) แก้ที่นี่ที่เดียวมีผลทั้งคู่**: `audio.js` (เสียง/เพลง), `ui.js` (toast/ธีม/overlay/shuffle), `speech.js` (เลือกเสียงอ่าน), `piano.js` (คีย์เปียโน/โน้ต), `ar-core.js` (engine AR ลากการ์ด/โยงเส้น/มือการ์ตูน/กล้อง) — ฟังก์ชันที่ **ตั้งใจให้ต่างกัน** ระหว่าง 2 หน้า (เช่น `buildLevel`, `levelSuccess`, `updateArCursor`, `playMusicSequence`) ยังแยกอยู่ไฟล์ของใครของมัน ห้ามยุบรวม
  - โหมด **"บ้านของหนู" (3D)** — เมืองจำลองที่เด็กมีบ้าน/ตัวละคร/สัตว์เลี้ยง/พ่อแม่ ทำเควสต์หาเหรียญมาซื้อของแต่งบ้าน
    **แผนแม่บท 9 เฟสทำครบแล้ว 2026-08-12** (เศรษฐกิจ · เควสต์ 40+ กลไก · สัตว์เลี้ยง · ครอบครัว · แล็บ STEM/coding · ของ 190 ชิ้น · เครื่องดนตรี)
    - ไฟล์: `js/house.js` (ระบบเกม) + `house-map.js` (ผังเมือง) + `house-models.js` (โมเดลตึก/ร้าน) + `house-avatar.js` (ตัวละคร) + `house-furniture.js` (ของ 190 ชิ้น) + `house-shop.js` (ร้าน/เศรษฐกิจ) + `house-quest-data.js` (คลังโจทย์) + `house-quests.js` (engine เควสต์) + `house-pet-care.js` + `house-pet-toys.js` (ของเล่นสัตว์เลี้ยง) + `house-family.js` + `house-games.js` + `house-tutor-steps.js`/`house-tutor.js` (ระบบสอนเล่น เฟส 15) + `house-qbrowse.js`/`house-devtools.js` (เครื่องมือเทส) — **ทั้งชุดโหลดแบบ lazy จาก `js/games-ar.js` ตอนกดเข้าบ้าน ลำดับสำคัญ ห้ามสลับ**
    - 📖 **รายละเอียดทั้งหมดอยู่ใน skill `house-mode`** (โครงสร้างไฟล์ · ระบบเควสต์ · เศรษฐกิจ · กติกาที่ห้ามย้อนทุกข้อ) — เรียกด้วย Skill tool ก่อนแตะโค้ดโหมดบ้านทุกครั้ง
    - 📌 **แผนแม่บทคือ [`QUEST-DESIGN.md`](QUEST-DESIGN.md)** — ต้องอ่านหัวข้อ "⭐ สรุปบริบท" ให้จบก่อนลงมือ + ข้อ 40-43 (สิ่งที่เฟส 5-9 ทำต่างจากแผน)
    - ✅ **เฟส 10 + 11 เสร็จแล้ว 2026-08-13** (ลำดับล่าสุด **แทรกเฟส 14 เพิ่ม 2026-08-14**: 10 = จังหวะเวลา · 11 = มินิเกมกลุ่ม A · 12 = กลุ่ม C · 13 = กลุ่ม B+D · **14 = เพลงพื้นหลังธีมฟาร์ม** · 15 = ฤดูกาล)
      - **เฟส 10 — จังหวะเวลา**: `starsOf()` พลาดฟรี 1 ข้อ · `TIER_MUL` 1.00-1.15 (คูณโบนัสด้วย) · ทุกกลไกมี `shape`+`secPerQ` · `walkQCount()` · แยกงานหลัก 6 / งานรอง 8 (ตัวนับ ❗ นับเฉพาะงานหลัก)
      - **เฟส 11 — มินิเกมกลุ่ม A 5 เกมใน `js/house-play.js`** (ไฟล์ใหม่ · คุยผ่านประตูใหม่ `window.HouseWorld`): 🙈 ซ่อนแอบ · 🍃 เก็บของประจำวัน · 🎣 ตกปลา · 📷 ช่างภาพ · 🌱 แปลงผัก — **ทางเข้าเดียวคือปุ่ม 🎈 `#house-play-btn` + แผง `#house-playpanel`** (ห้ามแตกเป็น 5 ทางเข้า) · โผล่เฉพาะฉากนอกบ้าน
      - **ระลอก 2 เปิดตาราง `qN` ใหม่แล้ว**: การ์ด 9-12 · ลาก 6-8 ตามชั้น (`Q_CARD`/`Q_DRAG`) — **ทับมติ 2026-08-10 ที่ให้สุ่ม 5-10** · ⚠️ **แก้ตาราง `qN` เมื่อไหร่ต้องคำนวณ `TIER_MUL` ใหม่ทุกครั้ง**
      - 📷 **ถ่ายรูปในเกมไม่ต้องเปิด `preserveDrawingBuffer`** — `window.__housePaint()` วาด 1 เฟรมแล้ว `toDataURL()` ในจังหวะเดียวกัน ⇒ ได้ภาพครบโดยไม่แลกเฟรมเรต **ห้ามเปลี่ยนกลับไปเปิด flag นั้น**
      - 🧪 เทส: `tests/house-phase10.spec.js` (8×2) · `tests/house-phase11.spec.js` (11×2) · รายละเอียดที่ต่างจากแผนอยู่**ข้อ 47 ของ `QUEST-DESIGN.md`**
      - 🗺️ **HUD แถวบนจัดใหม่ 2026-08-14**: เข็มทิศเป็น**คอลัมน์แรกหน้าชื่อเด็ก สูงเท่า 2 แถว** (94px · จอแคบ 86px · **ทึบสนิท ห้ามใส่ opacity กลับ**) · แถว 2 = แถบสัตว์เลี้ยง · แถว 3 = แถบเควสต์อยู่ใต้เข็มทิศ · **ปุ่มสัตว์เลี้ยง/แต่งตัว/แต่งบ้าน/แต่งตัวพ่อแม่ ย้ายไปข้างปุ่มกล้องมุมขวาบน และโผล่เฉพาะตอนอยู่ที่บ้าน** (`refreshHomeBtns()` — ในตัวบ้านหรือใน `inHomeZone`) · ⏸ **แถบเกมซ่อนแอบมีปุ่ม "พักก่อน" ในตัว** (2026-08-17 · ตัวแถบเป็น `pointer-events:none` ปุ่มต้องเปิดกลับเอง) · 🎣 **ตกปลาอยู่ = เดินไม่ได้** (กันที่ `handleTap()` + `walkTo()`) ยกเว้นแตะทุ่นเพื่อดึงเบ็ด (มีท่า `pull` ด้วย) · 🐾 **แถบสัตว์เลี้ยงโชว์เสมอ** ยังไม่มีสัตว์ = สถานะกุญแจล็อก + ปุ่ม "ไปรับเลี้ยง"
      - 🔒 **กติกาที่ล็อกเพิ่ม 2026-08-14 (รายละเอียดครบอยู่ใน skill `house-mode`)**: 🪵 ท่าน้ำเป็น "พื้นไม้ปูบนน้ำ" — ช่องท่าต้องเป็นน้ำจริง (`isWaterDeckTile`) · **ตัววาดพื้นห้ามปูบล็อกหญ้าทับ** · วาดแผ่นไม้ทีละช่อง **ห้ามสร้างแถบยาวแล้วหมุน** · 🎣 **จุดตกปลาห่างจากฝั่ง 1 ช่อง** (มีน้ำคั่น 1 ช่องระหว่างที่ยืนกับทุ่น · พิกัดผู้ใช้กำหนดเอง) · 🕺 ทุก action ต้องมีท่าทางเด็ก (`HouseWorld.pose()`) — **`rig` หมุนรอบฝ่าเท้า เอนเกิน .26 rad เห็นเป็นล้มคว่ำ** · 🛒 เพดานจำนวนของอยู่ที่ `FURN_MAX` ใน `js/house-shop.js` ที่เดียว (แปลงผัก 8 · ตะกร้าขายของ 1 · บ้านสัตว์ 1) และของที่ไม่มีขายใช้ธง `noShop:true`
    - ✅ **เฟส 12 + 12.1 เสร็จแล้ว 2026-08-15** — 12 = กิจกรรมกับเพื่อนตัวน้อย (ลูบหัว/อาบน้ำ/เล่นบอล/สอนท่า + ความสุข + ปลอกคอ + รอยเปื้อน · **ข้อ 49**) · **12.1 = ของเล่นสัตว์เลี้ยง 7 ชิ้น (ข้อ 50-51)**
      - 🧸 **ของเล่น 8 ชิ้น** (🎾 ลูกบอลแถมฟรี + 🪢 เชือกดึง 120 · 🪶 ไม้ล่อขนนก 150 · 🥏 จานร่อน 200 · 🧸 ตุ๊กตาผ้า 240 · 🫧 ฟองสบู่ 290 · 🛟 ห่วงกระโดด 340 · 🛝 สไลเดอร์ 400) — **ทุกชิ้นมีท่าเล่นของตัวเองทั้งฝั่งน้องและท่าเด็ก** อยู่ในไฟล์ใหม่ **`js/house-pet-toys.js`** (แพทเทิร์น `HOUSE_PET_TOYS(kit)` · โหลดก่อน `house.js`) · `house.js` มีแค่ act kind `'toy'` ที่ dispatch ต่อ
      - 🔒 **ราคารวม 1,740 🪙 ≈ 8 วัน (~6.5% ของของทั้งเกม) — ห้ามปรับขึ้นโดยไม่ถามผู้ใช้** ของเล่นเป็น money sink ที่ไม่คืนเป็นเงิน แพงกว่านี้จะแย่งงบของแต่งบ้าน · **แต้มความสุข +6..+10 อยู่ที่ `PET_TOYS` ใน `js/house-shop.js` ที่เดียว** ไฟล์ท่าเล่นไม่รู้จักตัวเลขนี้เลย
      - ⚠️ **`id` ของสไลเดอร์ต้องเป็น `pet-slide`** — คลังเฟอร์นิเจอร์มี `slide` อยู่ก่อนแล้ว ชนกันแล้วเทส "ของเล่นห้ามปนกับของตกแต่ง" แดงทันที · **ของเล่นไม่ผ่านโหมดตกแต่งเลย** ไม่ต้องหาที่วาง ไม่กินเพดาน `FURN_MAX`
      - 🐞 **กับดักที่เจอซ้ำรอบ 2**: `window.__houseDbg` มีคีย์ `charPose` อยู่แล้ว เติมชื่อซ้ำ ตัวหลังชนะ hook ใหม่เงียบหายโดยไม่มี error (กับดักเดียวกับ `action` ของเปียโนเฟส 9) ⇒ **เพิ่ม hook ใหม่ต้อง `grep` ชื่อก่อนเสมอ** · 🧪 **เครื่องเทสวาดได้ ~3 fps จริง** ท่า 4.5 วิกินนาฬิกาจริง ~30 วิ ⇒ ต้องใช้ `petActSeek()` กรอไปใกล้จบ **ห้ามนอนรอ**
    - ✅ **เฟส 13 เสร็จแล้ว 2026-08-15 — 5 เกม (ข้อ 52 · ปรับจากแผนเดิม 6 เกม)**: 🍰 ร้านขนม (ลาก) · 🫥 ของหายไปไหน (**แตะของตรงๆ**) · 🕵️ ทายว่าใคร (**ทรงเดิน** — โชว์เงาแล้วเดินไปหาคนนั้นในเมือง) · 🎨 ระบายสีตามเลข (**รางวัลคือกรอบรูปแขวนจริง**) · 🎺 วงดนตรีข้างถนน (**โลก 3D ไม่มีโจทย์/คะแนน/เหรียญ**) — **ตัด 🦉 นกฮูกสั่งตามลำดับทิ้ง** เพราะซ้ำกับ `melody`/`playalong`/`owlsay` · เทส `tests/house-phase13.spec.js` 11×2
      - 🔓 **เปิด engine หน้าหลักที่ไม่เคยถูกใช้อีก 8 ตัว 2026-08-15**: 💰 money · 🍕 fraction · 📅 calendar · ⏳ timeline · 📊 chart · 🌍 world · 🪞 mirror · 🦉 ef — **ผูกกับ NPC ตามอาชีพผ่าน `BONUS_MECHS` ไม่โยนเข้าพูลรวม** · 🦉 **ต้องปิดตัวจับเวลา (`EF_NO_TIMER`) ตอนเล่นในบ้าน** ขัดกติกาเหล็กข้อ 2 · 🚧 **`coord`/`area`/`dots`/`science` ยังเปิดไม่ได้** (เนื้อหาตกใต้ขอบการ์ดบนจอเตี้ย · `science` เป็น view เต็มจอเหมือน `ar-view`) **มีเทสกันเผลอเปิดกลับ**
      - ⚠️ **DOM rect วัด "ถูกตัด" ไม่เจอ** — ต้องวัดเทียบตัวครอบที่ `overflow` ไม่ใช่ `visible` **และถ่ายภาพจริงมาดูด้วยตาเสมอ** (บั๊ก `#ef-rule` ที่กติกาเกมหายไปทั้งบรรทัด จับได้จากภาพเท่านั้น)
    - 🔧 **ชุดแก้ 2026-08-16 (ผู้ใช้แจ้ง 6 ข้อรวด · รายละเอียดครบอยู่ข้อ 53 ของ `QUEST-DESIGN.md`)**
      - 🐞 **"16 เกมยืม engine กดเล่นจากคลังโจทย์ไม่ได้" — ตัวเกมไม่ได้พัง** ต้นเหตุคือ engine พวกนั้นมีหมวดเฉพาะบางระดับชั้น (วงจรไฟ = ป.6 · แท็งแกรม = ป.5-6 · ตาชั่ง/แผนภูมิ = ป.3+) ในเกมจริง `engineReady()` กรองไว้ **แต่ `testRun()` ของหน้าคลังโจทย์ข้ามด่านนั้น** ⇒ `pickCat()` คืน null แล้ว `startEngineGame()` เรียก `finishEngineRound(0)` = เด้งหน้าสรุปทันทีแบบเงียบๆ ⇒ แก้ที่ `engineGrades()` ใน `js/house-qbrowse.js` (บอกว่าเล่นได้ชั้นไหน + ปุ่มกระโดดไปชั้นนั้น) **ห้ามแก้ด้วยการผ่อนกฎ `pickCat` ให้คืนหมวดข้ามชั้น**
      - 📚 **แถบกลไกในคลังโจทย์แบ่งเป็น 7 กลุ่มแล้ว** (`MECH_GROUPS`) — 57 ตัวในแถบเดียวเลื่อนหาไม่เจอ · **เพิ่มกลไกใหม่ต้องเติมชื่อลงกลุ่มด้วย** (ตกหล่นไปกอง "อื่นๆ" มีเทสจับ) · กลไกที่ต้องเดินไปทำตามตำแหน่งติดป้าย **🚶** อัตโนมัติจากธง `walk`
      - 🫥 **"ของหายไปไหน" ใส่ตัวลวงแล้ว (ทับกติกาเดิมที่ห้ามมี)** — เด็กเล็ก 1 · ชั้นโต 2 · ตัวลวงต้องมาจากกองเดียวกันและ **ห้ามซ้ำของบนโต๊ะ** · **เทสเดิมถูกกลับด้าน ไม่ได้ลบทิ้ง**
      - 🎺 **เครื่องดนตรี 11 ชิ้นมีเสียงของตัวเองจริงแล้ว — แก้ 2 รอบ** (`js/shared/piano.js` · สังเคราะห์คลื่นเองใน JS แล้วยัดลง `AudioBuffer` · ไม่โหลดไฟล์เสียงเพิ่ม · หน้าครูไม่กระทบ) — **รอบแรกผู้ใช้ตีกลับ**: ทำ voice แยกครบและเทสเขียว แต่ฟังจริงแล้ว "ทุกอันยังเป็นเสียงเปียโน ต่างแค่ทำนอง" เพราะใช้วิธีสังเคราะห์เดียวกันหมด (sine + โอเวอร์โทน) ⇒ 🔑 **"ตัวเลขต่างกัน" ≠ "เสียงต่างกัน" — เครื่องดนตรีต่างกันที่ *วิธีเกิดเสียง*** · รอบสองใช้แบบจำลองจริงคนละวิธี: `pluck` **Karplus-Strong** (กีตาร์/อูคูเลเล่) · `metal` โอเวอร์โทนไม่ลงตัวจางคนละอัตรา (ฉิ่ง/ระฆังลม/กล่องดนตรี/เปียโน) · `wood` (กรับ) · `shake` (แทมบูริน) · `bar` (ระนาด) · `wind` เสียงลมจริง (ขลุ่ย) · `fm` (คีย์บอร์ด) · **แต่ละเครื่องอยู่คนละช่วงเสียงตามจริง** (`oct`) · 🔒 **ห้ามยุบกลับเป็นตารางโอเวอร์โทนวิธีเดียว** · **ต้องเป็นเสียงเดียวกันทั้งตอนแตะที่บ้านและตอนเล่นเกมทาย** · **โน้ตแรกต้องดัง synchronous** · 🧪 **เทสต้องวัดจากคลื่นจริง ไม่ใช่เช็คว่าชื่อ voice ต่างกัน** (เทสรอบแรกเขียวทั้งที่เสียงเหมือนกัน = สาเหตุที่บั๊กหลุดถึงผู้ใช้)
      - 🎣 **กลไก "ทำจริงแล้วเอาไปส่ง" ตัวแรก: `fishcatch`** — รับงาน → การ์ดปิด → ตกปลาจริงด้วยระบบเฟส 11 ให้ครบ (2/3/4 ตัวตามชั้น) → เดินกลับไปแตะคนที่สั่งงาน · **ไม่มีโจทย์ในการ์ดเลย** · ปลาที่ตกได้ยังเข้าถังเด็กตามปกติ · 🚪 **ประตูเดียวคือ `HouseWorld.questCaught(kind)`** — กลไกใหม่ให้ส่ง kind ใหม่ **ห้ามเปิดประตูใหม่รายเกม** · ⚠️ **ห้ามผูกกับ `questEvent()`** (คนละระบบ + มีตัวกันนับซ้ำรายชิ้น ⇒ ตกปลาชนิดเดิม 2 ตัวนับได้ตัวเดียว = เควสต์ไม่มีวันจบ) · **ไอเดียกลไกอื่นที่ทำแบบเดียวกันได้อยู่ข้อ 53.6 ยังไม่ทำ รอ "cf"**
    - ✅ **เฟส 14 เสร็จแล้ว 2026-08-16 — เพลงพื้นหลังธีมฟาร์ม (ข้อ 54.6)**: 4 เพลง 3 ชั้น (ทำนอง+เบส+คอร์ด) sine ล้วน ใน `js/house-music.js` (**ไฟล์เก็บแต่โน้ต ไม่มีตัวเลขความดังเลย**) — เช้าที่ฟาร์ม (C major · 92) · เดินเล่นในทุ่ง (F major · 84) · สายลมกับต้นไม้ (A minor · 76) · กลับบ้านตอนเย็น (C major · 88) ยาว 63-69 วิ · ขยาย `js/shared/audio.js` ด้วย `setMusicPlaylist()` (**idempotent**) + `musicSchedulerLayered()` + `MUSIC_LAYER_MIX` (**ตัวเลขความดังรายชั้นอยู่ที่นี่ที่เดียว**) — **ทุกอย่าง optional ล้วน หน้าครูไม่กระทบ** · ตะเข็บ 2 จุด `enterHouseGame()`/`stopHouseGame()` ⚠️ **ห้ามย้ายไปผูก `switchScene()`** · 🎶 **เมนู "ฟังเพลงธีม" ในเมนูเฟือง = เครื่องมือเทส สวิตช์ `MUSIC_PANEL_ENABLED` ใน `js/house-music-ui.js` ต้องเป็น `false` ตอน deploy** (ตัวที่ 4 ต่อจาก HOUSE_FEATURE_OFF/QB_ENABLED/DEV_ENABLED · ไม่ต้องถาม)
    - 🔧 **รอบแก้ระลอก 2 · 2026-08-16 (ข้อ 54 ของ `QUEST-DESIGN.md`)**
      - 🔇 **ปิด `findsound` ไม่ให้ถูกแจกเป็นเควสต์จริง** (ธง `testOnly:true` · `mechOk()` ตัดทิ้ง) เพราะเสียงยังไม่เหมือนเครื่องจริงพอให้เด็กแยกออก = โจทย์เดาสุ่ม · **ยังเล่นทดสอบในคลังโจทย์ได้** (`testRun` ไม่ผ่าน `mechOk`) · **ห้ามเปิดกลับจนกว่าผู้ใช้จะฟังแล้วยืนยัน** · 🐞 **ช่องโหว่ที่เจอ: `bonusMechsFor()` ไม่เคยกรองด้วย `mechOk` เลย** กลไกที่ปิดไว้หลุดออกทางนั้นได้ — แก้แล้ว
      - 🎣 **เควสต์ตกปลาจริงปรับใหม่**: **สุ่มบ่อ/ทะเล** (คำใบ้บอกแหล่งน้ำเสมอ — ปลาคนละชุดกันสนิท) · **ระบุชนิดปลา** · **ห้ามสั่ง `rare 3` และ `junk`** (โผล่ 10% เควสต์จบยาก) · เด็กเล็ก 1 ชนิด · ป.5-6 = 2-3 ชนิด **รวมไม่เกิน 4 ตัว** · **`rare 2` สั่งได้ไม่เกิน 1 ชนิด/รอบ และเฉพาะชั้นโต** · แจกโดยลุงตกปลา/ลุงชาวประมง
      - 🐟 **คลังปลา 21 → 48 ชนิด** (น้ำจืด 24 · ทะเล 24) + ธง **`junk:true`** สำหรับของที่ไม่ใช่ปลา (ตกได้ในโหมดอิสระ แต่ห้ามถูกสั่งในเควสต์)
      - 🗑️ **ถอด `fishmath` (ตกปลาคิดเลข) ออก** · 🔒 **กติกาใหม่: เกมไหนทำเป็น Action จริงในโลกแล้ว ให้ถอดเวอร์ชันการ์ดออกทันที**
      - 🌍 **งาน Action จริงชุดที่ 2 — 3 กลไก**: 🍃 `collectgive` (เก็บของ 2-4 ชิ้น) · 💧 `watergarden` (รดน้ำ 1-3 แปลง) · 📷 `photogive` (ถ่ายรูป 1-2 รูป) — สร้างจากโรงงานกลาง **`actionMech()` เพิ่มตัวใหม่ให้เรียกโรงงานนี้ ห้ามเขียนใหม่** · 🔒 **ทุกตัวต้องเช็ค `worldStock()` ก่อนแจกเสมอ** (อยู่ที่ `js/house-play.js` จุดเดียว) ไม่งั้นเด็กเก็บของครบไปแล้วเพิ่งได้งาน = **ตันทั้งวัน** · 🚪 **`HouseWorld.questCaught(kind, id)` ประตูเดียว** — ใบสั่ง 1 แถว = `{k, id, n}` · **แถวที่ `id` ว่าง = อะไรก็ได้ในงานชนิดนั้น**
    - 🌍 **ชุด A + B — เปลี่ยนการ์ดเป็น Action จริง (2026-08-16 · ข้อ 55 ของ `QUEST-DESIGN.md`)** รูปแบบที่ผู้ใช้กำหนด: **รับคำสั่ง → ไปทำจริงในโลก → กลับมาส่งงาน**
      - **ชุด A (ที่บ้าน/กับน้อง)**: 🕰️ `routine` แตะของในบ้านตามลำดับ · 🐾 `petcare` ลูบ/เล่นกับน้องจริง · 🐕 `pettrick` สอนท่าจริง · 🎹 `playalong` กลับบ้านไปแตะเครื่องดนตรีจริง — ⚠️ **`petcare` สั่งได้เฉพาะ "ลูบหัว/เล่นด้วย"** (ให้อาหาร/อาบน้ำมีเงื่อนไขของตัวเอง สั่งแล้วอาจทำไม่ได้ = ค้างทั้งวัน)
      - **ชุด B (ร้านสะดวกซื้อ · งาน 2 ขา)**: 🛒 `shopping` หยิบของจากชั้นแล้วจ่ายเงินจริง · 💵 `changeback` ซื้อของแล้วคิดเงินทอน · 🏪 `stockshelf` จัดของขึ้นชั้น — `target:'mart'` + `walkQuest.leg` (1 = ไปร้าน · 2 = กลับไปส่ง) · 🔒 **ห้ามจบเควสต์ที่ร้าน ต้องเดินกลับไปส่งเสมอ** · **เงินไม่พอต้องไม่ตัน**
      - 💸 **เควสต์ที่ต้องใช้เงินซื้อของ ต้องคืนเงินตอนจบ** (ผู้ใช้สั่ง) — `questSpend()` จดยอด แล้ว `finishQuest()` จ่าย **ค่าตอบแทนปกติ + เงินที่จ่ายไป** · ⚠️ **นับเฉพาะตอนมีเควสต์ธง `buy` ค้างอยู่** ไม่งั้นซื้อของแต่งบ้านเล่นๆ ก็ได้เงินคืน = ช่องโกงเงินไม่จำกัด
      - 🕰️ **`routine` ต้องอิงของที่เด็กวางไว้จริง** (`routineCats()` อ่าน `data.decor.in`) — **ไม่มีของครบ 2 หมวด = ไม่ถูกแจก** (ผู้ใช้สั่ง: "หากเด็กไม่มีจะเล่นไม่ผ่าน")
      - ⚠️ **ห้ามใส่กลไกที่ต้องเดินกลับไปส่งที่ตัว NPC ลงใน `FAM_MECHS`** — พ่อแม่รับงานคืนไม่ได้ (ทางส่งอยู่ที่ `talkToNpc` เท่านั้น · `tapParent` คนละทาง)
      - 🐞 **`whois` ตกหล่นจาก `WALK_MECHS_SHAPE` มาตั้งแต่เฟส 13** ⇒ ถูกนับเป็นการ์ด คำนวณจำนวนข้อ/งบเวลาผิดมาตลอด — แก้แล้ว · **ตัวไหนมี `walk:true` ต้องอยู่ในลิสต์นั้นเสมอ**
      - 🎶 **แก้หน้าฟังเพลง**: `stopMusic()` เดิมหรี่แค่ระดับเสียงรวม **แต่โน้ตที่นัดล่วงหน้า ~1 วิยังดังต่อ** ⇒ 2 เพลงทับกัน · เพิ่ม `stopMusic(true)` ที่เก็บ node แล้วสั่งหยุดจริง (`musicNodes`) · ปุ่มหยุด/ปิดเดิมสลับไป playlist หน้าหลักก่อนค่อยสลับกลับ ⇒ ได้ยินเพลงหน้าทำโจทย์แวบหนึ่ง แก้แล้ว
      - 📊 **Action จริง 9 → 16 ตัว จาก 65 กลไก (25%)** · 🔒 **เหลือเพดานอีก 4 ตัว** (ล็อกไว้ ~20 ในข้อ 54.8) เกินนี้เด็กเดินมากกว่าคิด และงบเวลา 240 วิ/เควสต์พัง
    - ✅ **เฟส 15 เสร็จแล้ว 2026-08-17 — ระบบสอนเล่น (Tutorial) · ข้อ 56 ของ `QUEST-DESIGN.md` (ข้อ 56.9 = สิ่งที่ทำต่างจากสเปก)**
      - ไฟล์ใหม่ 2 ตัว: **`js/house-tutor.js` (engine) + `js/house-tutor-steps.js` (บทเรียน)** — โหลด lazy ต่อจาก `house-devtools.js` (**บทเรียนต้องมาก่อน engine**) · engine คุยผ่านประตูเดียว `window.HouseTutor` · `js/house.js` แตะแค่ 5 จุด (start/stop/tick/onScene + ปลุกบทสัตว์เลี้ยงตอน `PETCARE.onAdopt`)
      - **5 บท**: ① รู้จักเมือง + เควสต์แรก + ซื้อเฟอร์นิเจอร์ + ตกแต่งบ้าน ② ปลูกผัก ③ ตกปลา + ตะกร้า ④ ร้านสัตว์เลี้ยง ⑤ **ดูแลสัตว์เลี้ยง (`event:true` เด้งเองตอนได้สัตว์ตัวแรก ห้ามเริ่มเอง)** · **ทุกขั้นมีบรรทัด `note` คำอธิบายประกอบ** (ผู้ใช้สั่ง) มีเทสไล่ตรวจว่าครบทุกขั้น
      - **คำสั่ง 6 ชนิด** `say`/`goto`/`tapUI`/`tapWorld`/`await`/`grant` · **ขั้นที่เป็น action ไม่มีปุ่มกดข้ามเลย** ขั้นพูดล้วนมีปุ่ม "เข้าใจแล้ว" ที่โผล่ช้ากว่าข้อความ 1.2 วิ · ปุ่ม **"พาไปเลย"** เดินจริงห้ามวาร์ป
      - 🔒 **กติกาที่ล็อก**: ม่านมืด **`pointer-events:none` ห้ามบล็อกการกด/การเดิน** · **ห้ามใช้ `box-shadow:0 0 0 9999px` ทำม่านมืด** (เงาไม่ทาบทับ canvas 3D — ใช้แผ่นสี่เหลี่ยม 4 แผ่นแทน · เทส 15M คุมไว้) · **จับเวลาด้วย `Date.now()` ห้ามสะสมจาก `dt`** · **เงินก้นถุง 20 แจกครั้งเดียวคุมด้วยธง `tut.gotStart` ห้ามดูจาก `done`** (ไม่งั้นกดเรียนใหม่ = ได้เงินฟรีทุกรอบ) · **ปุ่มข้าม/เรียนใหม่อยู่ในเมนูเฟืองเท่านั้น เด็กข้ามเองไม่ได้**
      - 🧪 **เทสชุดเดิมทุกไฟล์ต้อง seed `tut:{skip:true}`** ไม่งั้นฟองนกฮูกเด้งทับจุดที่เทสสั่งแตะ · เทสใหม่ `tests/house-phase15.spec.js` (13×2)
      - ❌ **ระบบฤดูกาลถูกยกเลิกแล้ว** (ผู้ใช้ตัดสิน 2026-08-16: **กินทรัพยากรเครื่องมากเกินไป** — เครื่องเป้าหมายคือแท็บเล็ตเด็ก) · ข้อ 46 เก็บไว้เป็นบันทึกการประเมินเท่านั้น
      - 📖 **รายละเอียดสเปกเต็ม + ข้อที่ทำต่างจากสเปกอยู่ที่ข้อ 56 ของ `QUEST-DESIGN.md`** (56.9 = ที่ทำต่าง)
    - 📌 **บันทึกเก่า (เก็บไว้อ้างอิง)**
      - **เฟส 10 — มินิเกมเบาสมองในโลก 3D 14 เกม (ลำดับ A→C→B→D)** · ภาพรวมข้อ 44 · รายละเอียดรายเกมอยู่ใน `IDEA.md` หัวข้อ "🏠 ไอเดียมินิเกมของโหมดบ้านของหนู" (ต้องรอ **"cf"** ก่อนลงมือตามกติกาของไฟล์นั้น) — เหตุผล: กลไก 49 ตัวที่มีอยู่เป็น "การ์ดลอยให้ตอบโจทย์" เกือบหมด เด็กจะเบื่อ
      - **เฟส 11 — จังหวะเวลา & งบเวลาเควสต์ (อนุมัติแล้ว)** · ข้อ 45 · เป้าเวลาเล่น **20-60 นาที/วัน** · จำนวนข้อใหม่ **การ์ด 9-12 · ลาก 6-8 · ทรงเดิน 5-10 ตามระยะที่วัดด้วย `findPath()` จริง (ห้ามใช้เส้นตรง)** ทับมติ 5-10 ข้อของ 2026-08-09 · `starsOf()` ให้ **พลาดฟรี 1 ข้อเสมอไม่ว่ากี่ข้อ** · แบ่ง **งานหลัก 6 ชุด (กระดาน 5 + ครอบครัว 1) / งานรอง 8 ชุด (NPC)** ตัวนับ ❗ นับเฉพาะงานหลัก
      - 💰 **ตัวคูณระดับชั้นกลับมาแล้ว (ข้อ 45.8 · อนุมัติ 2026-08-12)** — `1.00` (เตรียม ป.1/ป.1) · `1.05` (ป.2-3) · `1.10` (ป.4) · `1.15` (ป.5-6) ⇒ เงิน/วัน **206 → 237** · **ตัวเลขนี้ถอดจากอัตราส่วนเวลาเล่นจริง ไม่ใช่ตั้งลอยๆ** เพราะเฟส 11 ทำให้ ป.6 ทำ 12 ข้อ/ชุดแต่ ป.1 ทำ 9 ข้อ ⇒ **ไม่ขัดมติ 2026-08-09** ที่ห้ามใส่ตัวคูณกลับ (มตินั้นอิงเงื่อนไข "ทำงานเท่ากัน" ซึ่งไม่จริงแล้ว) · ⚠️ ต้องคูณ `STAR_BONUS`/`BOARD_BONUS` ด้วย ไม่งั้นปิดช่องว่างไม่สนิท · ⚠️ **เทสเดิมที่บังคับว่า "ทุกชั้นได้เท่ากันเป๊ะ" จะแดง ต้องเปลี่ยนเกณฑ์ ห้ามลบทิ้ง**
      - 🔒 **กติกาที่ล็อกใหม่ 2026-08-12**: **จำนวนชุดที่จ่ายเงินต้องเท่ากันทุกระดับชั้น (14 ชุด/วัน) — ปรับเวลาด้วย "จำนวนข้อต่อชุด" เท่านั้น ห้ามปรับด้วย "จำนวนชุดต่อวัน"** · **ห้ามล็อกจำนวนงานตามระดับชั้น** (ต่างกันได้แค่คำแนะนำบนหน้าจอ) · **ตัวคูณห้ามเกิน 1.20 และต้องถอดจากเวลาที่วัดจริงเสมอ — แก้ตาราง `qN` เมื่อไหร่ต้องคำนวณตัวคูณใหม่ทุกครั้ง** · `coinsFor()` **รู้จักได้แค่ `tier` ห้ามรู้จักจำนวนข้อจริงของเควสต์** (ไม่งั้นกลไกที่มีข้อเยอะจ่ายมากกว่าในชั้นเดียวกัน เด็กจะเลือกเล่นแต่ตัวที่คุ้ม)
      - ⚖️ **นิยาม "ยุติธรรม" ของโหมดบ้านเปลี่ยนแล้ว: จาก "🪙 ต่อวันเท่ากัน" เป็น "🪙 ต่อนาทีเท่ากัน (±5%) + เวลาที่ต้องลงเพื่อซื้อของครบทั้งเกมเท่ากัน (~95 ชม. ทุกชั้น)"** · `DAY_CAP = 400` ยังคุมอยู่ (คอมเมนต์ "ป.6 ~340" ค้างจากยุคเก่า แก้ข้อความได้ ห้ามแตะตัวเลข)
      - 🗺️ **ลำดับเฟสใหม่ (จัดใหม่ 2026-08-13 · ทับลำดับเดิมของ 2026-08-12) — ทำไล่จากบนลงล่าง**
        1. **เฟส 10 — จังหวะเวลา ระลอกโครงสร้าง** (ข้อ 45 **ยกเว้นตาราง `qN` ข้อ 45.4**): `starsOf` พลาดฟรี 1 ข้อ · ตัวคูณระดับชั้น · แยกงานหลัก/รอง · วาง `secPerQ` + สูตรทรงเดิน — **แตะแค่ `house-quests.js` ไม่แตะ 3D · เด็กได้ประโยชน์ทันทีโดยไม่มีใครถูกเพิ่มงาน**
        2. **เฟส 11 — มินิเกมกลุ่ม A 5 เกม** (เล่นในเมือง 3D) **แล้วปิดท้ายด้วยการเปิดตาราง `qN` ใหม่** (ข้อ 45.4) — เริ่มที่ **A1 ซ่อนแอบ** เพราะใช้ของเดิมเกือบ 100% และเป็นตัวพิสูจน์สูตรทรงเดินก่อนลงทุนอีก 13 เกม
        3. **เฟส 12 — มินิเกมกลุ่ม C 3 เกม** (สัตว์เลี้ยง) · 4. **เฟส 13 — กลุ่ม B + D 6 เกม** (การ์ดเบาสมอง/ผ่อนคลาย) · 5. **เฟส 14 — เพลงพื้นหลังธีมฟาร์ม** (แทรกเข้ามา 2026-08-14) · 6. **เฟส 15 — ~~ระบบฤดูกาล~~ ❌ ยกเลิก → 🎓 ระบบสอนเล่น (tutorial) ข้อ 56 ✅ เสร็จ 2026-08-17**
        - ⚠️ **ห้ามเปิดตาราง `qN` ใหม่ตั้งแต่เฟส 10** — เพิ่มข้อเด็กเล็ก 5 → 9 = งานเพิ่ม 80% ถ้าเปิดตอนยังไม่มีเกมใหม่มาสลับ เด็กจะเจอ "โจทย์แบบเดิมเป๊ะ แต่เยอะขึ้นเกือบเท่าตัว" = **แย่ลง ตรงข้ามกับเจตนาของแผน**
        - 📌 **เลขข้อในเอกสาร (44/45/46/48) ไม่ขยับตามเลขเฟส** — ข้อ 44 = คลังมินิเกม (เฟส 11-13) · ข้อ 45 = จังหวะเวลา (เฟส 10 + ท้ายเฟส 11) · ข้อ 46 = ฤดูกาล (**เฟส 15**) · ข้อ 48 = เพลงธีมฟาร์ม (เฟส 14)
      - 💤 **เฟส 15 (ท้ายสุด · เดิมเป็นเฟส 14) — ระบบฤดูกาล (ตั้งไว้ 2026-08-13 · ยังไม่ตัดสินใจทำ)** — ข้อ 46 ของ `QUEST-DESIGN.md` เป็น**การประเมินขนาดงานเท่านั้น ยังไม่ใช่สเปก ⚠️ ต้องกลับมาคุยกับผู้ใช้ก่อนลงมือทุกครั้ง** (ยังมีคำถามค้าง 3 ข้อ) · ประเมินไว้ **13-20 วัน** (ลดเหลือ 4-6 วันได้ถ้าทำแค่แสง+สีธรรมชาติ) · 🔑 **ตัวกำหนดขนาดงานคือ "hex ไม่ใช่ semantic"** — มีสีฮาร์ดโค้ด **2,709 จุด · 513 สีไม่ซ้ำ ไม่มี palette กลาง** เขียวหญ้ากับเสื้อเด็กใช้ค่าเดียวกัน ⇒ remap ที่ `toonMat()` ตรงๆ จะทำให้ของเด็กเปลี่ยนสีเอง · ✅ ของที่ช่วยได้: `matCache` (วัสดุแชร์ทั้งฉาก แก้สีแล้วกระจายทันทีแม้ merge ไปแล้ว) + `lightLerp` (ระบบไล่แสง smoothstep ที่ day/night ใช้อยู่)
    - ⚠️ **สถานะต่างกันระหว่าง branch (ระวังตอน merge)**: บน `main` โหมดนี้ **ปิดทางเข้าไว้ เด็กเข้าไม่ได้** (`HOUSE_FEATURE_OFF` · ค้นด้วย `grep -rn HOUSE_FEATURE_OFF`) แต่บน `feature/house-owl` **เปิดไว้เพื่อพัฒนา** ⇒ **ก่อน merge เข้า `main` ต้องถามผู้ใช้ทุกครั้ง**ว่าจะเปิดให้เด็กเล่นจริงหรือปิดไว้เหมือนเดิม
    - 🔌🧩 **ต่อวงจรไฟฟ้า + ต่อรูปทรง (แท็งแกรม) เปิดให้เล่นตั้งแต่ ป.1 แล้ว (2026-08-17)** — เดิมมีเฉพาะ ป.5-6 ⇒ เด็กชั้นเล็กเล่นไม่ได้ และโหมดบ้านไม่เคยแจกเป็นเควสต์ · เพิ่มหมวด `p1..p5-circuit` / `p1..p4-tangram` พร้อมเพดานความยาก `circuitMax` (ขนาดกระดาน) และ `tangramMax` (จำนวนชิ้น) · **แก้ด้วยการเพิ่มหมวดของแต่ละชั้น ไม่ใช่ผ่อนกฎ `pickCat` ให้ข้ามชั้น** (ข้อนั้นยังห้ามเหมือนเดิม) · ⚠️ เทส `house-phase67` ที่เคยบังคับว่า "ป.1 ต้องเล่นไม่ได้" **ถูกกลับด้าน ไม่ได้ลบทิ้ง**
- 🪙 **เหรียญในเกมมีแค่ 4 แบบ: 1 · 2 · 5 · 10 (ผู้ใช้สั่ง 2026-08-17)** — ตัด 20/50/100 ออกจาก `moneyLevelConfig()` ทุกระดับ และ**ลดช่วงราคาลงตาม** (ทอน 300 ด้วยเหรียญ 10 = หยิบ 30 เหรียญ เด็กทำไม่ไหว) · 💵 "เงินที่ลูกค้าจ่ายมา" ยังเป็นหลักสิบ/ร้อยได้เพราะโชว์เป็นตัวเลขในฟองคำพูด ไม่ได้วาดเป็นเหรียญ
- ✋ **ท่า "แบมือ = แตะ · กำมือ = ไม่แตะ" ใช้เฉพาะเกมนกฮูกสั่ง (`ef`)** — สวิตช์ `setHandOpenMode()` ใน `js/games-ar.js` เปิดจาก `houseTune()` ใน `js/house-games.js` · **ห้ามเปิดให้เกมอื่น** เกมที่ต้องเล็งแม่น (ลากเส้น/คีย์เปียโน) เด็กจะเผลอแบมือแล้วกดโดนของที่ไม่ได้ตั้งใจ · จีบนิ้ว/ชี้ค้างยังใช้ได้เหมือนเดิมทุกเกม
- 💇 **ไอคอนทรงผมในหน้าแต่งตัวแยกตามเพศ (แก้ 2026-08-17)** — `addHair()` วาด**ทรงคนละชุดกันสนิท**ระหว่างชาย/หญิงที่ index เดียวกัน แต่เดิมไอคอนมีตารางเดียว ⇒ เลือกเด็กผู้หญิงแล้วรูปตัวอย่างไม่เปลี่ยน · ตอนนี้มี `OI_HAIR_SHAPES_G` และ `outfitIcon(rowKey, i, girl)` · **เปลี่ยนเพศต้อง `buildCreatorRows()` ใหม่ทั้งแถว** · ⚠️ **เพิ่มทรงใหม่ต้องต่อท้ายพร้อมกันทั้ง `js/house-avatar.js` และตารางไอคอนใน `js/house-map.js`** · เทส `tests/house-hair-icons.spec.js`
    - 🗂️ **ปุ่ม "จัดการข้อมูล" เข้าถึงได้ 3 ทาง (ผู้ใช้สั่ง 2026-08-17)**: แถบบนหน้าเลือกเด็ก (`#clear-btn`) · หน้าเลือกโหมด (`#landing-data`) · เมนูเฟืองในโหมดบ้าน (`#house-data-btn`) — **ทั้ง 3 ทางเปิด `#clear-modal` ตัวเดียวกัน ห้ามทำกล่องใหม่ซ้ำ** (ระบบย้ายข้อมูล/รีเซ็ต/ลบ ต้องมีจุดเดียว) · กล่องเป็น z-index 200 อยู่เหนือ `#house-view` (70) จึงเปิดทับเมืองได้เลย และปุ่มรีเซ็ต/ลบจบด้วย `location.reload()` อยู่แล้ว ⇒ ไม่ต้องสั่งออกจากเมืองก่อน · เทส `tests/house-data-menu.spec.js`
    - ⚠️ **มีสวิตช์ 4 ตัวคนละเรื่องกัน**: `HOUSE_FEATURE_OFF` = ทางเข้าโหมดบ้าน (ต้องถามผู้ใช้) · `QB_ENABLED` ใน `js/house-qbrowse.js` = ปุ่ม 📚 คลังคำถาม · `DEV_ENABLED` ใน `js/house-devtools.js` = ปุ่ม 🛠️ ปรับค่าต่างๆ · `MUSIC_PANEL_ENABLED` ใน `js/house-music-ui.js` = ปุ่ม 🎶 ฟังเพลงธีม — **3 ตัวหลังเป็นเครื่องมือเทส ต้องตั้งเป็น `false` ทุกครั้งที่ deploy ไม่ต้องถาม**
    - 🧪 **ชุดเทสต้องรอ `__houseDbg.ready()` ก่อนสั่งอะไรเสมอ ห้ามรอแค่ `mode() === 'world'`** — `hMode` เป็น `'world'` ตั้งแต่ไฟล์โหลดเสร็จ ทั้งที่บ้านยังไม่พร้อม ⇒ เทสแดงแบบสุ่มหาสาเหตุยากมาก
    - ⚠️ **ห้ามรัน `npx playwright test` ขณะกำลังแก้ไฟล์ในโปรเจค** — dev server เสิร์ฟไฟล์สด ไฟล์ที่ syntax พังชั่วขณะทำให้เทสแดงยกแผงทั้งที่โค้ดไม่ผิด
- 🔌 **`js/owl-games.js` — "สัญญา Mount" ให้ engine เกมของหน้าหลักไปเล่นที่อื่นได้ (ทำ 2026-08-11 ก่อนเริ่มเฟส 5)**
  - **ปัญหาที่แก้**: engine ทุกตัวใน `js/games-*.js` ผูกกับ view ของตัวเองแบบตายตัว (`showOnlyView(mixView)` แล้ว `$('mix-hint')`) ⇒ โหมดบ้านเอาไปใช้ไม่ได้ ต้องเขียนกลไกซ้ำ พอถึงเฟส 5-7 จะมีของซ้ำกันราว 20 ชุด
  - **วิธีที่ใช้**: **ย้ายทั้งกล่อง view ไปแปะในโฮสต์ใหม่** (id ลูกๆ ยังไม่ซ้ำในเอกสาร `$('mix-hint')` จึงยังหาเจอ) ⇒ **ไม่ต้องแก้โค้ด engine แม้แต่บรรทัดเดียว** · `unmount()` คืน view กลับที่เดิมด้วย placeholder (ลำดับ section มีผลกับ CSS บางตัว)
  - **ตะเข็บส่งผลลัพธ์มีจุดเดียว**: `finishP2Game()` ใน `js/games-math.js` (engine ทุกตัววิ่งผ่าน 25 จุด) — แทรก `OwlGames.handleFinish()` บรรทัดแรก ถ้ากำลัง mount อยู่ให้ส่งผลกลับโฮสต์แทนเด้งหน้าสรุปหน้าหลัก
  - ลงทะเบียนแล้ว **23 เกม** (ท้ายไฟล์ `games-art/think/math/code/write.js`) · ⚠️ **`stop()` ต้องล้าง state ให้หมด** ไม่งั้นเกมเดิมค้างทำงานหลังปิดการ์ด · **ห้ามลงทะเบียน ar-view** (กินทั้งจอ + มีวงจรกล้องของตัวเอง)
  - CSS `.og-embedded` ยุบ view ให้พอดีกล่อง ⚠️ **ห้ามตั้ง position/overflow ทับมากกว่านี้** — engine บางตัววัดตำแหน่งจาก `getBoundingClientRect()` จริง (ลากการ์ด/ลากเข็ม) layout เปลี่ยนมากจะเพี้ยน
- 🏠 **`js/house-games.js` — สะพานระหว่างการ์ดเควสต์กับ engine หน้าหลัก** · `house.js` ห่อด้วย IIFE ไฟล์อื่นแตะภายในไม่ได้ ⇒ คุยผ่าน `window.HouseQuestUI` **ประตูเดียว** (`openCard`/`closeCard`/`makeBtn`/`award`) **เพิ่มประตูใหม่เท่าที่จำเป็นจริงๆ** ไม่งั้นการแยกไฟล์ไร้ความหมาย · โค้ดใหม่ของเฟส 5-7 ให้มาอยู่ไฟล์นี้ ไม่ใช่ยัดใน `house.js` (12,900 บรรทัดแล้ว) · `ALLOW` คุมว่าเกมไหนเหมาะกับการ์ดใบเล็ก **เพิ่มทีละตัวหลังลองเล่นจริงเท่านั้น**
- 🚪 **`js/app-landing.js` — หน้าเลือกทางหลังเลือกเด็ก** (🏠 เข้าเมือง / 📚 ทำโจทย์) · แก้ปัญหาชุด 3D 1.2 MB ที่เด็กซึ่งแค่อยากทำโจทย์ไม่ต้องโหลด · `enterHome()` ถูกแยกเป็น `enterHome()` (ถามหน้าเลือกก่อน) กับ `enterHomeReal()` (ของเดิม) · ⚠️ **โผล่ครั้งเดียวต่อ "รอบการเล่นของเด็กหนึ่งคน"** กลับจากเกมต้องไม่เจอซ้ำ (ตัวคุมคือ `chosen`) **แต่กลับไปหน้าเลือกเด็กเมื่อไหร่ต้องล้าง `chosen` เสมอ** — `renderChildSelect()` ใน `js/app-core.js` เรียก `OwlLanding.reset()` ให้ (ถ้าลืม เด็กจะเลือกคนใหม่แล้วถูกโยนเข้าหน้าหมวดเลย ไม่มีทางเข้าเมือง · บั๊กจริงที่ผู้ใช้เจอ 2026-08-11) · มีปุ่ม **`#landing-back` "← เปลี่ยนคนเล่น"** กลับไปหน้าเลือกเด็กได้จากหน้านี้ (ทำให้เล็กกว่าการ์ด 2 ใบชัดเจน เด็กจะได้ไม่เข้าใจว่าเป็นทางเลือกที่ 3) · **ไอคอนการ์ดเป็น SVG ทั้งคู่** (`assets/icons/my-house.svg` / `assets/icons/landing-quiz.svg`) **ห้ามเปลี่ยนกลับเป็น emoji** (ผู้ใช้สั่ง 2026-08-11) · **โหมดบ้านปิดอยู่ (`HOUSE_FEATURE_OFF`) = ข้ามหน้านี้ไปเลย** ดูจากปุ่ม `#house-entry-btn` ว่า hidden ไหม ไม่ใช่ค่าคงที่
- 📷 **MediaPipe Hands ที่ self-host ไว้ (`js/vendor/mediapipe/`) — ต้องมีไฟล์ครบ ไม่งั้น "เปิดกล้องได้แต่ไม่เจอมือ" แบบเงียบๆ**
  - 🐞 **บั๊กใหญ่ที่แก้แล้ว 2026-08-11**: ตอน self-host เมื่อ 2026-07-30 **ลืมดาวน์โหลด `hands.binarypb`** (ตัวกราฟของโมเดล) ⇒ wasm โหลดผ่าน กล้องติด หลอดไฟกล้องขึ้น แต่พอเริ่มประมวลผลจะ `Check failed: tempConfig.ParseFromString(binary_graph)` แล้ว abort **⇒ เกม AR ทุกเกม + โหมดมือ ตรวจจับมือไม่ได้เลยตลอด v2.0.0 ที่อยู่บน production** (บนหน้าจอไม่มี error ให้เห็น มีแต่ 404 ใน network tab)
  - ⚠️ **ห้ามแก้ปัญหาแบบนี้ด้วยการกลับไปใช้ CDN** — โปรเจคนี้ต้อง self-host ทุกไลบรารี · `teacher/teacher-ar.js` เคยหลุดใช้ CDN อยู่ 3 จุด ย้ายมาใช้ `../js/vendor/mediapipe/` แล้ว 2026-08-11
  - 🧪 **เทสคุมไว้ 3 ตัว**: `tests/mediapipe-assets.spec.js` (ไฟล์ครบ + ห้ามมี CDN ในโค้ด) · `tests/handplay-camera.spec.js` (**เปิดกล้องปลอมของ chromium จริง** แล้วเช็คว่ากราฟคายผลลัพธ์ออกมา — ตอนไฟล์หายค่านี้เป็น 0 เสมอ) ⚠️ ไฟล์หลังต้องแยกไฟล์เพราะ `launchOptions` ตั้งใน `test.describe()` ไม่ได้
  - ⚠️ ตอนนี้ทุกที่ใช้ `modelComplexity:0` (โมเดล lite) **ถ้าเปลี่ยนเป็น 1 ต้องดาวน์โหลด `hand_landmark_full.tflite` มาเพิ่มด้วย** ไม่งั้นเจอบั๊กเดิมซ้ำเป๊ะ
- 🖐️ **โหมดมือ (เล่นด้วยมือหน้ากล้อง) ใช้ได้ในโหมดบ้านแล้ว** — ใช้ `handPlay` เดิมใน `js/games-ar.js` ทั้งดุ้น ไม่ได้เขียนใหม่ (เปิดกล้องแต่ **ไม่โชว์ภาพกล้องเป็นพื้นหลัง** วาดแค่มือการ์ตูน · ปลายนิ้วชี้ = เคอร์เซอร์ · จีบนิ้ว = คลิก)
  - ⚠️ **`hpVisibleView()` ต้องรวม `body > section` ด้วย** — `#house-view` กับ `#ar-view` อยู่**นอก** `<main>` (เรื่อง stacking context) ถ้าเลือกแค่ `main > section` โหมดบ้านจะติดปุ่มกล้องไม่ได้เลย
  - 👆 **ในโหมดบ้าน "ชี้ค้าง 1.5 วินาที" = ตอบ ไม่ต้องจีบนิ้ว** (ผู้ใช้สั่ง 2026-08-12 · `HP_DWELL_MS` + `setHandDwellMode()` ใน `js/games-ar.js`) — ระหว่างนับ ปุ่มมี **แถบเขียวไล่จากซ้ายไปขวาด้วยความเร็วคงที่ (linear)** (`.hp-dwell` ใน `css/style.css` · `clip-path` คุมด้วยตัวแปร `--hp-dwell-p` 0→1) ขยับนิ้วออก = เริ่มนับใหม่
    - ⚠️ **เส้นเล็ง/แถบนับถอยหลังต้องวาด "ในกรอบของชิ้นนั้น" เสมอ** (`outline-offset` ติดลบ · pseudo `inset:0`) — ของเดิมวาดล้นออกไป 3-7px แล้วโผล่พ้นขอบการ์ดคำถามออกไปในโลก 3D (ผู้ใช้แจ้ง 2026-08-12)
    - ⚠️ **ของบางชิ้นฟัง `pointerdown` ไม่ได้ฟัง `click`** (คีย์เปียโน) ⇒ ยิง `.click()` แล้วเงียบสนิท · ลิสต์อยู่ที่ `HP_POINTER_SEL` ใน `js/games-ar.js` และยิงผ่าน `hpFire()` **เพิ่มของใหม่ให้เติมที่ลิสต์เดียวนี้** (กับดักเดียวกับถังของเกมลากวางที่ต้องใช้ `data-hp-click`)
    - ⚠️ **ใช้เฉพาะโหมดบ้านเท่านั้น** — เกม ป.3 กับ `ar-match` ในหน้าหลักยังเป็นจีบนิ้วเหมือนเดิม (ล็อกไว้ในข้อห้ามด้านล่าง) · `mountHandPlayHouse()` เปิด · `mountHandPlay()` ปิด · **จีบนิ้วยังใช้ได้อยู่ ไม่ได้เอาออก** และ **ต้องบอกเด็กด้วยว่าทำได้ทั้ง 2 ทาง** (ข้อความตอนเปิดกล้อง · ผู้ใช้สั่ง 2026-08-12) — มีเทสคุมว่าจีบนิ้วต้องตอบทันทีแม้อยู่ในโหมดชี้ค้าง · มีเทสคุมทั้ง 2 ฝั่งที่ `tests/pre-phase5.spec.js`
  - 🎹 **การ์ดเควสต์กว้างขึ้นเมื่อ mount เกมของหน้าหลัก** (`body.og-mounted .house-creator.house-qz{width:min(1040px,92vw)}`) เพราะ engine พวกนี้ออกแบบมาสำหรับเต็มหน้าจอ — เปียโน 15 คีย์ต้องการ ~980px แต่การ์ดปกติให้แค่ ~708px เด็กต้องเลื่อนหาคีย์ (ผู้ใช้แจ้ง 2026-08-12) · คีย์ยืดพอดีกล่องด้วยกฎ `.og-embedded #music-piano` ชุดเดียวกับ modal "เปียโนของหนู" **ห้ามปล่อยให้ต้องเลื่อนดูอีก** มีเทสวัดจริงที่ `tests/house-phase5.spec.js`
  - ปุ่มกล้องติดตอน `qzShow()` (เปิดการ์ดเควสต์) · ถอดตอน `closeQuestPanel()` · ถังของเกมลาก-วางไม่ใช่ `<button>` ต้องมี `data-hp-click` · **เดินในโลก 3D ด้วยมือไม่ได้** (ใช้ pointer event ไม่ใช่ click) ตั้งใจให้เป็นแบบนั้น
    - ⚠️ **ห้ามสร้าง `new Hands()` ใหม่ทุกครั้งที่เปิดกล้อง** — ใช้ `getHandsSolution()` (ตัวเดียวทั้งแอป ใช้ซ้ำตลอด) · ตัวเก่าที่ถูกทิ้งด้วย `= null` เฉยๆ ไม่ได้คืนระบบไฟล์ในหน่วยความจำของ wasm ⇒ ตัวใหม่อ่านไฟล์โมเดลไม่เจอแล้ว abort ทั้งกราฟ (`LocalFileContentsCalculator failed: Failed to read file …`) **อาการคือปิดการ์ดคำถามแล้วเปิดกล้องใหม่ มือไม่ขึ้น "บางที"** (รอบ 1 ดี · รอบ 2 พัง · รอบ 3 ดี — บั๊กจริงที่ผู้ใช้เจอ 2026-08-11 · มีเทสวนเปิด-ปิด 3 รอบคุมไว้ที่ `tests/handplay-camera.spec.js`)
    - ⚠️ **`mountHandPlayHouse()` ต้อง idempotent — ห้ามขึ้นต้นด้วย `unmountHandPlay()` แบบ `mountHandPlay()` ของหน้าหลัก** เพราะ `qzShow()` ในโหมดบ้านถูกเรียก **8 จุด** (รับงาน → เริ่มเล่น → ทุกครั้งที่เปลี่ยนข้อ → หน้าสรุป) ⇒ เด็กเปิดกล้องแล้ว **มือหายทันทีที่ตอบข้อแรก** โดยไม่มี error ให้จับ (บั๊กจริงที่ผู้ใช้เจอ 2026-08-11 · ตอนนี้เช็ค `hpBtn.parentNode === top` แล้วออกก่อน · มีเทสคุมที่ `tests/pre-phase5.spec.js` โดยดูว่า `#hp-layer` ยังไม่ถูกซ่อน)
  - ⚡ **ห้ามแก้ "โลก 3D กระตุกตอนเปิดกล้อง" ด้วยการหรี่ลูปวาด** — ลองมาแล้วทั้งแบบเว้นตามเวลา (20fps) และเว้นเฟรม (30fps) ผู้ใช้ยังเจอกระตุกอยู่ (2026-08-12) เพราะ **ตัวการจริงคือ inference ของ MediaPipe ที่รันบน main thread เฟรมละ ~10-20 ms แล้วบล็อก rAF ของโลก** ⇒ แก้ที่ต้นทาง: `onFrame` ของกล้องใน `startHandPlay()` **ส่งเข้าโมเดลเว้นเฟรม** (งานหนักลดครึ่ง · เคอร์เซอร์มือ ~15 ครั้ง/วินาที เหลือเฟือสำหรับกลไก "ชี้ค้าง") · `window.HouseFrameHint` ยังอยู่เป็นจุดต่อแต่ **ส่ง `false` เสมอ**
  - เทสอยู่ที่ `tests/pre-phase5.spec.js` (7 เทส × 2 จอ)
- ลำดับการโหลด script สำคัญ (ห้ามสลับ): `data-cats-*.js` → `data-cats.js` → `data-pools.js` → `owl-messages.js` → `app-core.js` → `game-*.js`/`games-*.js` → `app-ui.js` (ท้ายสุดเสมอ เพราะ INIT เรียกใช้ฟังก์ชันจากทุกไฟล์)
- ไม่มี build step ก็จริง แต่การเปิดไฟล์ตรงๆ ผ่าน `file://` ยังใช้ได้ปกติ (ข้อมูลเป็น `.js` ไม่ใช่ `.json` ที่ต้อง `fetch` ผ่าน server เท่านั้น)
- `IDEA.md` — เก็บไอเดียเกม/หมวดใหม่ที่ยังไม่ได้ทำ แต่ละไอเดียเก็บเป็น **หัวข้อเกม** + **หมวดหมู่** + **รายละเอียดเกม** เสมอ (รายละเอียดเติมทีหลังได้ถ้ายังไม่ได้คิด) **อ่านไฟล์นี้เฉพาะตอนจะเริ่มทำเกมใหม่เท่านั้น** (ไม่ต้องอ่านทุกครั้งที่ทำงานอื่น) และเมื่อนำไอเดียไหนไปทำเป็นเกมจริงแล้ว **ต้องลบไอเดียนั้นออกจากไฟล์ทันที**
- `teacher/` — **โหมดคุณครู** (เข้าผ่าน URL `owlkids.net/teacher` เท่านั้น ไม่มีปุ่มจากหน้าหลัก): `teacher/index.html` + `teacher/teacher.js` + `teacher/teacher.css` — หน้าแยกที่ reuse `css/style.css` และธีม/พื้นหลังเดียวกับหน้าหลัก แต่ **JS แยกของตัวเองทั้งหมด ห้ามโหลดไฟล์ `js/app-*.js`/`js/games-*.js` ในหน้านี้** (ผูกกับ DOM ของ index.html แน่นมาก) logo เกมสำหรับครูเลือกอยู่ที่ `assets/icons/teacher/` (12 แบบ) ข้อมูลครูเก็บ localStorage key `owlkids_teacher_profile` / `owlkids_teacher_games` (แยกจากข้อมูลเด็กหน้าหลัก) — ธีมกลางวัน/กลางคืนใช้ key `p1quiz_theme` ร่วมกับหน้าหลักโดยตั้งใจ ให้สลับธีมแล้วตรงกันทั้งสองหน้า

## กฎการทำงานในโปรเจคนี้

- **ตอบผู้ใช้เป็นภาษาไทยเสมอ** ไม่ว่าจะถามอะไร (โค้ด/ชื่อตัวแปรเป็นภาษาอังกฤษได้ตามปกติ)
- **ห้าม `git push` เอง** ต้องถามผู้ใช้ก่อนทุกครั้ง แม้จะ commit ไปแล้วก็ตาม เมื่อได้รับคำสั่ง "push" หรือ "deploy" ให้ทำตามขั้นตอนนี้เสมอ:
  1. **สรุปสิ่งที่เปลี่ยนแปลงจาก version ก่อนหน้า** — ไฟล์ไหนเปลี่ยน, เพิ่ม/แก้/ลบอะไร
  2. **สรุป task ที่ต้องทำก่อน push** — เช่น อัปเดต `version`, สร้าง GitHub Release, อัปเดต `README.md`, อัปเดต `LOG.md` changelog (ระบุว่าแต่ละอย่างยังต้องทำหรือทำไปแล้ว)
  3. ถามว่า **"confirm push ไหม?"** — รอผู้ใช้ตอบ **"cf"** เท่านั้น จึงจะ push ได้ ห้าม push ก่อนได้รับ "cf" เด็ดขาด
- **ถ้าจะทำ feature ใหม่ ต้องสร้าง branch `feature/<ชื่อ>` แยกจาก `main` ก่อนเริ่มเขียนโค้ดเสมอ** พัฒนา+เทสทั้งหมดบน branch นั้น ห้าม commit ตรงเข้า `main` ระหว่างพัฒนา feature ใหม่ เมื่อทำและเทสเสร็จแล้วให้ **ถามผู้ใช้ก่อนทุกครั้ง** ว่าจะ merge เข้า `main` ไหม (รอคำยืนยันก่อน ห้าม merge เองโดยไม่ถาม) — งานเล็กน้อย (แก้บั๊กเล็ก, ปรับข้อความ/สี, chore ทั่วไป) ไม่ต้องสร้าง branch แยกก็ได้ ใช้ดุลยพินิจว่า "เป็น feature ใหม่" หรือไม่ ถ้าไม่แน่ใจให้ถามผู้ใช้ก่อน
- **ถ้าทำเกมใหม่ (หมวดใหม่ใน `CATS`) หรือนำไอเดียจาก `IDEA.md` มาพัฒนาเป็นเกมจริง ต้องสร้าง branch `feature/<ชื่อ>` แยกจาก `main` ก่อนเริ่มเขียนโค้ดเสมอ ไม่ต้องใช้ดุลยพินิจ ไม่มีข้อยกเว้น** พัฒนา+เทสทั้งหมดบน branch นั้นจนเสร็จ (รวม syntax check, ทดสอบเล่นเกมจริง) แล้ว **ถามผู้ใช้ก่อนทุกครั้ง** ว่าจะ merge เข้า `main` ไหม รอคำยืนยันก่อนเสมอ ห้าม merge เข้า `main` เอง
- **Visitor badge ใน footer (`.visitor-badge` img src) — มี 2 จุด**: ใช้บริการ `hitscounter.dev` (เปลี่ยนจาก `visitor-badge.laobi.icu` ที่ปิดบริการไปแล้ว เมื่อ 2026-07-14) counter key คือ query param `url=` (URL-encoded) — หน้าหลัก `index.html` ใช้ `url=https%3A%2F%2Fowlkids.net` (prod) / `url=https%3A%2F%2Fowlkids.net%2Ftest` (dev), หน้าโหมดคุณครู `teacher/index.html` ใช้ `url=https%3A%2F%2Fowlkids.net%2Fteacher` (prod) / `url=https%3A%2F%2Fowlkids.net%2Fteacher-test` (dev) — param อื่น (`label=visitors`, `icon=eye`, `color=`) คงเดิมทั้งสองโหมด ระหว่างพัฒนา/ทดสอบใช้ค่า dev เสมอ (กันนับยอด visitor จริงเพี้ยนจากการเทส) แต่ก่อน push/deploy จริงทุกครั้งต้อง **แก้ param `url=` เป็นค่า prod ทั้ง 2 ไฟล์** ก่อน commit แล้วพอ push สำเร็จเสร็จสิ้นแล้ว ให้ **แก้กลับเป็นค่า dev ทันทีทั้ง 2 ไฟล์** (แก้ไฟล์ตรงๆ เลย **ไม่ต้อง commit หรือ push**) เพื่อกลับสู่โหมดพัฒนาปกติ — ทำทุกครั้งที่ push ไม่ต้องถามก่อน
- **ตรวจสอบก่อนบอกว่าทำเสร็จ**: รัน JS syntax check กับไฟล์ JS ทุกไฟล์ (เรียงตามลำดับโหลดจริง) ก่อนส่งมอบงานทุกครั้ง แล้วรันชุดเทส `npx playwright test` ด้วยถ้าแตะโค้ด engine/ข้อมูลหมวด เช่น
  ```bash
  node -e "
  const fs = require('fs');
  const files = ['js/data-cats-prep.js','js/data-cats-p1.js','js/data-cats-p2.js','js/data-cats-p3.js','js/data-cats-p4.js','js/data-cats-p5.js','js/data-cats-p6.js','js/data-cats.js','js/data-pools.js','js/owl-messages.js','js/app-core.js','js/game-quiz.js','js/games-think.js','js/games-math.js','js/games-code.js','js/games-write.js','js/games-art.js','js/games-listen.js','js/games-ar.js','js/app-ui.js','js/house-shop.js','js/house-quests.js','js/house-pet-care.js','js/house-family.js','js/house.js','js/house-play.js','js/house-qbrowse.js','js/house-devtools.js'];
  const combined = files.map(f=>fs.readFileSync(f,'utf8')).join('\n');
  try { new Function(combined); console.log('OK JS syntax'); } catch(e){ console.error('ERROR:', e.message); }
  "
  ```
- **ชุดเทส Playwright — ติดตั้งไว้นอกโปรเจค (ห้าม `npm install` ในโฟลเดอร์โปรเจค)**:
  1. **ก่อนรัน `npx playwright test` ครั้งแรกของแต่ละเครื่อง/แต่ละ session ให้รัน `bash tools/setup-playwright.sh` เสมอ** สคริปต์นี้เช็คให้เองว่าเครื่องนี้มี Playwright แล้วหรือยัง ถ้ามีครบแล้วจะจบทันทีไม่ทำอะไร (รันซ้ำได้ปลอดภัย) ถ้ายังไม่มีจะติดตั้งให้เอง แล้วจดสถานะไว้ที่ `~/.local/share/playwright-shared/.installed` — **เครื่องที่มีไฟล์นี้แล้วคือติดตั้งเรียบร้อยแล้ว ไม่ต้องติดตั้งซ้ำ**
  2. ตัว Playwright ติดตั้งกลางไว้ที่ `~/.local/share/playwright-shared/` (นอก repo) ส่วน `node_modules` ในโปรเจคเป็นแค่ **symlink** ชี้ไปที่นั่น (จึงไม่มีไฟล์ dependency จริงอยู่ในโปรเจค) — เบราว์เซอร์ chromium อยู่ที่ `~/Library/Caches/ms-playwright` ตามปกติของ Playwright
  3. `package.json` ในโปรเจคเก็บไว้เพื่อบอกเวอร์ชันที่ต้องการ (สคริปต์อ่านค่า `devDependencies['@playwright/test']` ไปติดตั้ง) — **อย่ารัน `npm install` ที่ root โปรเจค** เพราะจะเขียนทับ symlink แล้วเอา `node_modules` ตัวจริงกลับเข้ามาในโปรเจคอีก ถ้าจะอัปเวอร์ชันให้แก้ `package.json` แล้วรัน `bash tools/setup-playwright.sh`
  4. `.gitignore` ใช้ `node_modules` (ไม่มี `/` ปิดท้าย) โดยตั้งใจ — ถ้าใส่ `/` จะไม่ ignore symlink
- **หลัง push/deploy สำเร็จทุกครั้ง**:
  1. สรุปสิ่งที่เปลี่ยนแปลงสั้นๆ ให้ผู้ใช้ทราบ
  2. เขียนสรุปนั้นลงไฟล์ `LOG.md` ด้วย โดยเพิ่มเป็นรายการใหม่ **ไว้บนสุด** ของไฟล์ (รูปแบบ: `- YYYY-MM-DD: สรุปสั้นๆ`) แล้วอัปเดตหัวข้อ "ประวัติการเปลี่ยนแปลง (changelog)" ใน `CLAUDE.md` ให้เป็นรายการล่าสุด 5 รายการเสมอ (ตัดรายการเก่าสุดออกถ้าเกิน 5) และถ้าเปลี่ยนแปลงพฤติกรรม/สถานะสำคัญของแอป ให้อัปเดตหัวข้อ "สถานะปัจจุบันของแอป" ใน `CLAUDE.md` ให้ตรงด้วย
  3. ก่อน push/deploy ทุกครั้ง ให้ตรวจและอัปเดต `README.md` ให้ตรงกับสถานะล่าสุดของแอปด้วย (เช่น ฟีเจอร์ใหม่, หมวดหมู่ใหม่) แล้ว commit `README.md` ไปพร้อมกับ `CLAUDE.md`, `LOG.md` และไฟล์โค้ดที่เปลี่ยนใน push เดียวกัน
  4. **ทุกครั้งที่ release version ใหม่ ต้องเขียนทับไฟล์ `changelog` ที่ root ด้วยเสมอ** (ไฟล์ text ธรรมดาไม่มีนามสกุลเหมือน `version`, ใช้แสดงในหน้า "รายละเอียดการอัพเดท" ของแอปให้ผู้ปกครองอ่าน) — **เก็บแค่ version ล่าสุด version เดียว เขียนทับของเดิมทุกครั้ง ไม่สะสมประวัติ** เขียนเป็นภาษาอ่านง่ายสำหรับผู้ปกครอง **ห้ามใส่ศัพท์ technical** (ชื่อไฟล์/ฟังก์ชัน/CSS class ฯลฯ) รูปแบบ: บรรทัดแรกคือเลข version, บรรทัดขึ้นต้น `## ` คือหัวข้อหมวด (เช่น เกมใหม่, ปรับปรุงหน้าตา, แก้ไขข้อผิดพลาด), บรรทัดขึ้นต้น `- ` คือรายการย่อยในหมวดนั้น (ดู parser `parseChangelog()` ใน `js/app-ui.js`) แล้ว commit ไปพร้อมกับไฟล์อื่นๆ ใน push เดียวกัน
  5. **อัปเดต memory ถาวรของ Claude (directory memory ประจำโปรเจค) ทุกครั้งหลัง deploy เสร็จ** — ทบทวนว่ารอบงานนี้มีอะไรที่ควรจำข้ามเซสชันแล้วยังไม่ได้บันทึก เช่น บทเรียน/feedback จากผู้ใช้ระหว่างทำงานรอบนี้, ปัญหา deploy ที่เจอ+วิธีแก้ (เช่น Pages build ค้างต้องสั่ง rebuild), งานที่ยังค้างต่อเนื่อง — **ไม่ต้องบันทึกสิ่งที่ repo บันทึกไว้แล้ว** (โค้ด, LOG.md, CLAUDE.md, git history) ถ้าไม่มีอะไรใหม่จริงๆ ก็ข้ามได้ แต่ต้องทบทวนเสมอ
- **หลัง `git push` ทุกครั้ง ต้อง track สถานะ deploy ของ GitHub Pages แล้วสรุปสั้นๆ ให้ผู้ใช้ทราบ**:
  1. รอสักครู่แล้วเช็คด้วย `gh run list --limit 3` (workflow "pages build and deployment") และ/หรือ `gh api repos/Isarez/year1/pages` เพื่อดู `status`/`https_certificate`
  2. ถ้ายังไม่เสร็จ ให้เช็คซ้ำเป็นระยะ (เช่นทุก 60-180 วินาทีด้วย ScheduleWakeup) จนกว่าจะจบ — งานอื่นในเทิร์นนั้นทำต่อได้ระหว่างรอ
  3. เมื่อ deploy จบ ให้สรุปสั้นๆ 1-2 บรรทัดว่า **สำเร็จหรือไม่**:
     - สำเร็จ: ยืนยันด้วยการเช็ค `curl -I` ที่ URL จริงว่าได้ 200 และ `last-modified`/`etag` อัปเดตแล้ว (workflow run บางครั้งค้างสถานะ "queued" ทั้งที่ deploy จริงเสร็จแล้ว ให้ยึด `curl` เป็นหลักถ้าขัดแย้งกัน)
     - ไม่สำเร็จ: ต้องบอก **error message** ที่เจอจาก `gh run view <id> --log-failed` ให้ผู้ใช้ทราบด้วยเสมอ ไม่ใช่แค่บอกว่า "fail" เฉยๆ
  4. **แจ้งสรุปผล deploy (ข้อ 3) ใน Discord channel `1524252244965589082` ช่องนี้ช่องเดียวเท่านั้น** (ใช้ chat_id นี้กับ Discord reply tool) ไม่ว่าจะสั่ง push มาจากช่องทางไหนก็ตาม (Discord ช่องอื่น หรือ session ปกติ) — **ห้ามแจ้งผล deploy ซ้ำใน channel ที่สั่งงานมา** ให้แจ้งเฉพาะช่อง deploy log นี้ช่องเดียว
- **ถ้าเพิ่มเกม/หมวดใหม่ หรือแก้ไขอะไรที่มีนัยสำคัญ (breaking change, ปรับพฤติกรรมหลักของแอป, โครงสร้างไฟล์ใหม่ ฯลฯ) ต้อง `gh release create` ก่อน push/deploy เสมอ**:
  1. ตั้ง tag แบบ semver (เช่น `v1.1.0` ปรับ minor เมื่อเพิ่มฟีเจอร์/เกมใหม่, `v1.0.1` ปรับ patch เมื่อแก้บั๊ก/ปรับเล็กน้อย) — เช็ค tag ล่าสุดด้วย `git tag --list` หรือ `gh release list` ก่อนตั้งเวอร์ชันถัดไป
  2. เขียน release notes สรุปการเปลี่ยนแปลงเป็นภาษาไทย (ใช้เนื้อหาเดียวกับที่จะเขียนลง changelog ใน `LOG.md`)
  3. **push commit ให้เสร็จก่อน แล้วค่อยรัน** `gh release create <tag> --title "<tag>: <สรุปสั้นๆ>" --notes "<รายละเอียด>"` — **ห้ามสร้าง release ก่อน push เด็ดขาด** เพราะ GitHub จะผูก tag กับ HEAD เก่าบน origin/main ณ ตอนนั้น (commit ใหม่ยังไม่ถูก push ขึ้นไป) ทำให้ tag ชี้ commit ผิด version — เคยพลาดมาแล้วกับ v1.19.0/v1.20.0 ต้องไล่ลบ tag + สร้างใหม่ด้วย `--target <full SHA>` ทีหลัง (แก้เมื่อ 2026-07-19)
  4. **แก้ `?v=` ของทุก `<script src>`/`<link href>` ใน `index.html` และ `teacher/index.html` ให้เป็นเลข version ใหม่ด้วยเสมอ** (cache-busting — ถ้าลืม ผู้ใช้เดิมจะยังได้ไฟล์เก่าจาก cache ทั้งที่ deploy ใหม่แล้ว) แก้ทีเดียวทุกไฟล์ได้ด้วย `sed -i '' 's/?v=<เก่า>/?v=<ใหม่>/g' index.html teacher/index.html`
     - ⚠️ **แล้วต้องแก้ `window.APP_ASSET_VER` ที่ต้นไฟล์ `js/app-core.js` ด้วยมืออีกจุดเสมอ** — เป็น cache-buster ของไฟล์ที่โหลดแบบ lazy (mediapipe hands/camera_utils, three.min.js, house*.js) ซึ่ง**อยู่นอก `index.html` คำสั่ง `sed` ข้างบนจับไม่ถึง** เคยลืมมาแล้วตอน v2.0.0 (ค้างที่ `?v=1.20.0`) รอบนั้นไม่กระทบเพราะไฟล์เหล่านั้นเพิ่งถูกเพิ่มใหม่ยังไม่มีใคร cache แต่ถ้าลืมรอบที่ไฟล์พวกนี้เปลี่ยน ผู้ใช้เดิมจะได้ของเก่า — ตรวจด้วย `grep -rn APP_ASSET_VER js/app-core.js`
  5. **แก้ไฟล์ `version` ที่ root ให้ตรงกับ tag ใหม่เสมอ** (เช่น `v1.3.0`) — ทำพร้อมกับ commit อื่นใน push เดียวกัน ห้ามลืม
  6. งานเล็กน้อย (ปรับ CSS ระยะห่าง, แก้ข้อความ, แก้สีเฉพาะจุด) ไม่ต้องสร้าง release ทุกครั้ง — ใช้ดุลยพินิจว่า "มีนัยสำคัญ" หรือไม่ ถ้าไม่แน่ใจให้ถามผู้ใช้ก่อน
- **icon และ emoji ของเกม/หมวดใหม่ต้องไม่ซ้ำกับที่มีอยู่แล้ว**: ก่อนเพิ่มเกม/หมวดใหม่ ให้ grep หรืออ่าน `CATS` ใน `js/data-cats-*.js` เพื่อดู emoji ที่ใช้อยู่ทั้งหมด แล้วเลือก emoji ที่ยังไม่มีใครใช้ — ทั้ง `cat.emoji` (icon บนการ์ดและสติกเกอร์ในสมุด) และ icon ที่แสดงใน UI อื่นๆ ห้ามซ้ำกันเด็ดขาด เพราะ emoji เดียวกันในสมุดสติกเกอร์จะทำให้แยกหมวดไม่ออก
- **ป้าย "NEW ✨" บนหน้าเลือกหมวด (`cat.isNew:true` ใน `js/data-cats-*.js`)**:
  1. **ถ้าเพิ่มเกม/หมวดใหม่ ต้องติด `isNew:true` ให้เสมอ** ไม่ต้องถามก่อน (ทำอัตโนมัติทุกครั้งที่เพิ่มหมวดใหม่)
  2. **ห้ามลบ `isNew:true` ของหมวดเดิม/เวอร์ชันก่อนหน้าโดยไม่ถามก่อนเด็ดขาด** — ก่อนลบต้อง **list รายชื่อหมวดที่จะลบ badge ออกมาให้ผู้ใช้ดูก่อนเสมอ** แล้วรอผู้ใช้ยืนยันก่อนแก้ไฟล์ ห้ามลบเองแม้จะดูสมเหตุสมผลว่า "ไม่ใหม่แล้ว" ก็ตาม
- **การ์ดหมวด (`.cat-card`) ในหน้าหลักทุก section (`#cat-grid`, `#cat-grid-interactive`, `#cat-grid-skill`, `#cat-grid-listen` และ section ใหม่ในอนาคต) ต้องมีขนาดเท่ากันเสมอ** — ห้ามปรับ padding/font-size/min-height ของการ์ดเฉพาะหมวดใดหมวดหนึ่งจนขนาดต่างจากการ์ดอื่น หรือปล่อยให้เนื้อหาในการ์ด (ชื่อยาว, meta ยาว) ดันขนาดการ์ดให้เพี้ยนไปจากการ์ดหมวดอื่น ถ้าจะเพิ่มหมวด/เกมใหม่ ให้ใช้ class `.cat-card` เดิมเสมอ ไม่สร้างสไตล์การ์ดแยกเฉพาะหมวด

## สถานะปัจจุบันของแอป (สำคัญ อย่าย้อนกลับโดยไม่ถาม)

- **ธีมกลางวัน/กลางคืน**: ปุ่ม `#theme-toggle` ใน header สลับ `body.night-mode`, จำค่าไว้ที่ `localStorage['p1quiz_theme']`. กลางคืน: ท้องฟ้าเปลี่ยนเป็นสีกรมท่า, พระอาทิตย์ (`.bg-sun`) fade ออกและพระจันทร์ (`.bg-moon`) fade เข้า, เมฆเปลี่ยนสี, ลูกโป่งลอยเปลี่ยนเป็นดาว. ตัวอักษรที่ไม่ได้อยู่บนการ์ดขาว/ครีม (header title, hero, footer, section title อย่าง `.cat-section-title`) ต้องมี override สี `body.night-mode` เพราะสีเริ่มต้น (`--ink`/`--ink-soft` น้ำตาลเข้ม) อ่านไม่ออกบนพื้นหลังกลางคืน
  - ข้อควรระวัง CSS: `@keyframes` ที่ animate `opacity` จะ override static opacity rule เสมอ ไม่ว่าจะ specificity อะไร — ถ้าจะซ่อน/โชว์ element ตามธีม ต้อง scope animation ไว้ใน selector ของธีมนั้นๆ ด้วย ไม่ใช่ใส่ไว้ที่ base rule
  - Grid ที่มีเนื้อหาไม่ยอมหด (เช่น emoji ใหญ่) ต้องใช้ `grid-template-columns:repeat(N,minmax(0,1fr))` ไม่ใช่ `repeat(N,1fr)` เฉยๆ ไม่งั้นจะ overflow ออกนอก panel บนจอมือถือแคบ (`#emoji-picker` เจอปัญหานี้มาแล้ว)
- **เพลงพื้นหลัง**: procedural Web Audio (ไม่ใช่ไฟล์เสียง) มี **5 เพลง** (`MUSIC_TRACKS` ใน `js/app-core.js` แต่ละเพลงมี bpm ของตัวเอง) เล่นจบเพลงแล้วพัก 2 จังหวะต่อเพลงถัดไปวนเป็น playlist, default เป็น **เปิด** สำหรับผู้ใช้ใหม่ (`localStorage['p1quiz_music'] !== 'off'`) เสียงเบา (`0.025`) ห้ามปรับเสียงดังขึ้นโดยไม่ถาม — โน้ตเพลงใช้ oscillator หลักเป็น sine (โทนเดียวกับ `playCorrect`) ห้ามเปลี่ยนกลับเป็น triangle/square โดยไม่ถาม, เสียงคลิกปุ่ม (`playClick`) เป็น sine E5+E6 และมีแฟนแฟร์ `playCongrats()` ดังพร้อมพลุตอนจบเกมทุกโหมด
- **หมวดหมู่ (`CATS`)**: math, thai (ภาษาไทย 1), thai2 (ล็อกจนกว่าจะทำ thai1 จบ), english, behavior, animals, days, iq1-iq4 (ล็อกตามลำดับผ่าน `CAT_REQUIRES`), pattern (เติมแพทเทิร์น 🔮)
- 💰 **ค่าตอบแทนเควสต์ (จูนล่าสุด 2026-08-09 — ห้ามปรับขึ้นกลับโดยไม่ถามผู้ใช้)** — **ทุกระดับชั้นได้เท่ากันหมด**:

  | ประเภท | 1 ดาว | 2 ดาว | 3 ดาว | ท้าทาย (3 ดาว ×1.5) |
  |---|---|---|---|---|
  | เควสต์ NPC (`FAM_BASE.A` = 6) | 6 | 8 | 10 | 14 |
  | กระดาน (`board` = 8) | 8 | 10 | 13 | 19 |
  | ครอบครัว (`family` = 10) | 10 | 13 | 16 | 24 |

  ตัวคูณ: `STAR_MUL = [0, 1.0, 1.3, 1.6]` · `CHAL_MUL = 1.5` · **ตัวคูณระดับชั้น = 1.0 ทุกชั้น** (ผู้ใช้สั่ง 2026-08-09 · เดิม ป.1 ×1.0 → ป.6 ×1.4 ทำให้พี่ได้มากกว่าน้องทั้งที่ทำงานเท่ากัน) — พารามิเตอร์ `tier` ใน `coinsFor()` ยังรับไว้แต่ไม่มีผลกับเหรียญแล้ว · **มีเทสคุมว่าทุกชั้นต้องได้เท่ากันเป๊ะ** · รายได้สูงสุด/วัน = **206 🪙** (รวมโบนัสดาว 35 + โบนัสกระดาน 10)
- ⭐ **โบนัสดาวรายวัน (เพิ่ม 2026-08-09)**: ได้ดาวถึง **ครึ่งหนึ่ง**ของดาวเต็มวันนี้ → กดรับ **15 🪙** · ได้ **ครบเต็ม** → กดรับอีก **20 🪙** · **ต้องกดรับเองที่ "หมุด" บนหลอดดาวในหน้ารายการเควสต์** (ไม่จ่ายอัตโนมัติ เด็กจะได้รู้ตัวว่าได้อะไรมา) — หมุดวางตามสัดส่วนดาวที่ต้องได้ (~50% / 100%) **บอกล่วงหน้าว่าทำถึงตรงไหนได้เงินเพิ่ม** โดยแสดง **รูปเหรียญ + จำนวนเงิน** และ **จำนวนดาวที่ต้องได้ใต้เหรียญ** (เหรียญวาดด้วย CSS `.hs-coin` **ห้ามใช้ emoji 🪙** — บางเครื่องไม่มี glyph) · เทา = ยังไม่ถึง · ทองเรืองแสง = กดรับได้ · เขียว ✓ = รับแล้ว · ปุ่มเควสต์บน HUD ขึ้น 🎁 + วงแหวนทองเด้งเมื่อมีของรอรับ · ยังอยู่ใต้ `DAY_CAP` เหมือนเควสต์ปกติ · ⚠️ **ต่างจากโบนัส "ทำครบ 5 ชุด" ที่ถูกเอาออกไป** — อันนี้วัดที่ **คุณภาพ (ดาว)** ไม่ใช่จำนวนชุด และครึ่งทางก็ได้แล้ว จึงไม่กดดันให้ทำครบ
- 💰 **โบนัสกระดาน "ทำครบ 5 ชุด" = 10 🪙** (`BOARD_BONUS` · กดรับที่ปุ่ม `#hq-claim` บนกระดาน) — เคยเป็น 35 แล้วถูกเอาออกทั้งก้อนเมื่อ 2026-08-09 จากนั้นผู้ใช้สั่งเอากลับมาที่ **10** ในวันเดียวกัน · ก้อนเล็กลงมากจึงไม่กดดันให้ "ต้องเก็บให้ครบ" เหมือนเดิม · **ห้ามปรับขึ้นโดยไม่ถามผู้ใช้**
- 📝 **จำนวนข้อต่อ 1 เควสต์ = 5-10 ข้อ ไล่ตามชั้น** (`difficulty().qN` ใน `js/house-quests.js` · ผู้ใช้สั่งเพิ่มจาก 3-5 เมื่อ 2026-08-09): เตรียม ป.1/ป.1 = 5 · ป.2 = 6 · ป.3 = 7 · ป.4 = 8 · ป.5 = 9 · ป.6 = 10 · ⚠️ **ค่าตอบแทนคิดต่อเควสต์เหมือนเดิม** (เหรียญต่อข้อจึงลดลงครึ่งหนึ่ง — ตั้งใจ ยืดเวลาเล่นโดยไม่ทำเงินเฟ้อ) · ⚠️ **เกณฑ์ดาวต้องยืดตามจำนวนข้อเสมอ** (`starsOf` คิดเป็นสัดส่วน ~2/3 ของจำนวนข้อ) ไม่งั้นเควสต์ยาวจะเข้มขึ้นเงียบๆ จนเด็กได้ 1 ดาวตลอด — ผิดกติกาเหล็กข้อ 2
- **รายละเอียดราย engine/เกม ย้ายไปเป็น skill แล้ว (โหลดเฉพาะตอนใช้ ไม่กินคอนเท็กซ์ทุก session)** — เรียกด้วย Skill tool เมื่อจะแก้เกมกลุ่มนั้น:
  - `game-engines-skill` — เกมฝึกทักษะ (โดมิโน/ทายเงา/ผสมสี/ดนตรี/เปียโนของหนู/นาฬิกาวิเศษ/เติมแพทเทิร์น)
  - `game-engines-ar` — เกม AR ที่ใช้กล้อง + โหมดเล่นด้วยมือ (handPlay) + มือการ์ตูน
  - `listen-write-games` — เกมฟังคำศัพท์ / ฟังประโยคเติมคำ / ลากเส้นต่อจุด + กติกาคลังคำ
  - `grade-levels` — ระบบระดับชั้น ป.1-ป.4 + mechanic ใหม่ของ ป.4 + กติกาหลักสูตร
  - `teacher-mode` — โหมดคุณครู (`teacher/`)
  - `house-mode` — โหมด "บ้านของหนู" (3D) ครบทั้ง 9 เฟส: โครงสร้างไฟล์ · ระบบเควสต์ 40+ กลไก · เศรษฐกิจ/ร้านค้า · สัตว์เลี้ยง · ครอบครัว · แล็บ STEM/coding · เครื่องดนตรี · กติกาที่ห้ามย้อน
- **ข้อห้ามสำคัญ (สรุปจาก skill ด้านบน — ห้ามย้อนหรือเปลี่ยนโดยไม่ถามผู้ใช้ก่อนทุกข้อ)**:
  - ห้ามใส่สายรุ้งกลับในพื้นหลังกลางวัน / ห้ามเปลี่ยนหน้าหม้อผสมสีกลับเป็น emoji / ห้ามเปลี่ยน oscillator เพลงพื้นหลังกลับเป็น triangle-square / ห้ามเพิ่มเสียงเพลงพื้นหลังให้ดังขึ้น
  - ห้ามเปลี่ยน mechanic ของเกมลากเส้นต่อจุดจาก "ลาก" กลับเป็น "แตะ" และห้ามใส่ highlight บอกใบ้จุดถัดไป
  - ห้ามเปลี่ยนท่ามือของ `ar-match` (แตะจุดแล้วลาก / จีบนิ้ว = ยกเลิก) กลับเป็น pinch-to-grab
  - ห้ามใส่ "o" นำหน้าตัวอักษรไทยที่ยืนเดี่ยวไม่ได้ในเกมฟังคำไทย และห้ามใส่ประโยคภาษาไทยกลับในเกมฟังประโยคเติมคำ (เป็นอังกฤษล้วนตามคำขอผู้ใช้)
  - ห้ามลบ id เดิมของหมวดที่มีอยู่แล้ว (เช่น `ar-match`, `ar-math`, `cloze-th1/2`) เพราะผูก progress/สติกเกอร์ของผู้ใช้
  - ห้ามเปลี่ยน mechanic ของเกมนาฬิกา (เข็มสั้นอยู่หน้า / จับได้เฉพาะตัวเข็ม / ลากแบบ relative)
  - ห้ามใช้ `desktopOnly:true` กับเกมที่ไม่ได้ตั้งใจล็อกมือถือโดยไม่ถามก่อน
  - ห้ามใช้ `#ar-camera-msg` แบบการ์ดค้างจอสำหรับกรณีมือถือ (ใช้ `showToast()` เท่านั้น)
- **"days" category**: ค่าตำแหน่งคำตอบถูก shuffle แบบ one-time manual fix ไปแล้ว (ของเดิมตอบ index 0 หมด) — อย่า shuffle ซ้ำอีก
- **พื้นหลังกลางวัน**: ท้องฟ้าสีฟ้า gradient, มีพระอาทิตย์หมุนช้าๆ, เมฆลอย, ลูกโป่งลอย, ประกายดาวระยิบระยับ — เคยมีสายรุ้งแล้วเอาออกตามคำขอผู้ใช้ **ห้ามใส่กลับโดยไม่ถาม**
- **นกฮูกมาสคอต**: มุมขวาล่าง คลิกแล้วสุ่มข้อความให้กำลังใจจาก `OWL_MSGS.cheer` (คนละปุ่มกับสมุดสติกเกอร์ `#sticker-tally-btn`)
- **Favicon/ไอคอน**: `assets/favicon.svg` เป็น source of truth ถ้าแก้ต้อง regenerate PNG ทุกไซส์ด้วย `rsvg-convert -w <size> -h <size> assets/favicon.svg -o assets/<name>.png`
- **ข้อมูลผู้ใช้ทั้งหมดเก็บใน `localStorage` เท่านั้น ไม่มี backend** — footer มีข้อความยืนยันเรื่องนี้ ต้องคงความจริงนี้ไว้ถ้าจะเพิ่มฟีเจอร์ sync/backend ในอนาคต
- **Theme transition**: ท้องฟ้ากลางวัน/กลางคืนใช้ 2 เลเยอร์ `.bg-sky-day`/`.bg-sky-night` ซ้อนกันแล้ว crossfade ด้วย `opacity 2s` (ไม่ใช้ `transition:background` บน gradient เพราะเบราว์เซอร์ tween gradient ตรงๆ ไม่ได้) พระอาทิตย์/พระจันทร์ใช้ wrapper div แยกจาก SVG เดิมเพื่อทำ transform translateY แยกจาก animation หมุน/pulse เดิม (element เดียวมี transform 2 ระบบพร้อมกันไม่ได้)
- **ปุ่มวิธีติดตั้งแอป**: `#install-toggle` เป็นปุ่ม text ("วิธีติดตั้งแอปบน iPad/แท็บเล็ต") ลอยมุมล่างซ้าย (`.install-link-btn`) ไม่ใช่ icon ใน header อีกต่อไป กดแล้วเปิด `#install-modal` (คำแนะนำ iPad/iPhone Safari + Android Chrome) ซ่อนอัตโนมัติตอนอยู่ใน AR view (`body.ar-open .install-link-btn{display:none}`)
- **ปุ่มรายละเอียดการอัพเดท**: `#changelog-open-btn` อยู่ใต้ปุ่มนำเข้าข้อมูล (`#child-import-btn`) ในหน้าเลือกโปรไฟล์เด็ก (`.changelog-open-btn{margin-top:22px}` เว้นระยะเพิ่มเพราะคนละกลุ่มฟังก์ชัน) กดแล้วเปิด `#changelog-modal` (pattern เดียวกับ `#install-modal`) โหลดเนื้อหาจากไฟล์ `changelog` ที่ root ผ่าน `fetch('changelog')` แล้ว parse ด้วย `parseChangelog()` ใน `js/app-ui.js` — ไฟล์ `changelog` เก็บแค่ **version ล่าสุด version เดียว** (เขียนทับทุก release ไม่สะสมประวัติ), modal มีปุ่มปิด 2 จุด (✕ มุมบนขวา `.modal-x-btn` + ปุ่มปิดล่าง) ดูกติกาอัปเดตไฟล์นี้ทุก release ในหัวข้อ push/deploy ด้านบน
- **ปุ่มเต็มหน้าจอ**: มี 2 ปุ่มที่ผูก logic เดียวกัน — `#fullscreen-toggle` ใน header (หน้าปกติ) และ `#ar-fullscreen-toggle` ใน `.quiz-top` ของ AR view (มุมขวาบน ข้าง `#ar-level-counter`) ทั้งคู่ผูกกับ `fsBtns` array และ `refreshFsBtn()` เดียวกัน ถ้าเพิ่มปุ่มเต็มหน้าจอใหม่ให้เพิ่ม id เข้า array นี้แทนการเขียน handler แยก
## ประวัติการเปลี่ยนแปลง (changelog)

Changelog ทั้งหมดย้ายไปอยู่ที่ [LOG.md](LOG.md) แล้ว เพื่อไม่ให้ไฟล์นี้ใหญ่เกินไป — เก็บไว้ที่นี่แค่ 5 รายการล่าสุด ส่วนรายการเก่ากว่านี้ดูที่ `LOG.md`

- 2026-08-02: **v2.0.0 — merge `feature/house-owl` (136 commit) เข้า `main`** งานตั้งแต่ 2026-07-29 ค้างบน branch ไม่เคยขึ้น production ขึ้นพร้อมกันรอบเดียว: **ระดับชั้น ป.1–ป.6 ครบ** (หมวด 40 → 332), **แยกไฟล์ทั้งระบบ** (`data.js`+`app.js` → `data-cats-*`/`data-pools`/`app-core`/`app-ui`/`game-quiz`/`games-*`/`shared/*`), เกมใหม่จำนวนมาก, lazy-load AR/3D, ชุดเทส Playwright 36 เทส + `tools/setup-playwright.sh`; **โหมด "บ้านของหนู" 3D ปิดทางเข้าไว้ (`HOUSE_FEATURE_OFF`)** โค้ด/เทสอยู่ครบแต่ซ่อนปุ่ม + ไม่ preload ชุด 3D; ระบบ **เงินนกฮูก 🪙** (`window.OwlCoins`) วางไว้แล้วแต่ยังไม่มีที่หา/ใช้เงิน
- 2026-07-19: teacher v1.19.0 — **โหมดครู: เพิ่ม mechanic สร้างโจทย์เอง 3 แบบ** จับคู่ความจำ 🎴 (`match`, `teacher/teacher-match.js`, view `#memory-view`), ผสมสี 🎨 (`mix`, `teacher/teacher-mix.js`, `#mix-view`), ดนตรี 🎹 (`music`, `teacher/teacher-music.js`, `#music-view` — ครูกดเปียโนแต่งทำนองเอง limit 30 โน้ต/ทำนอง) — engine port จาก `js/app.js` คง id/class/CSS เดิม, sync จุดซ่อน view ใน `startTeacherAR` เพิ่ม memory/mix/music — teacher-only ไม่แตะ version ฝั่งเด็ก
- 2026-07-18: v1.20.0 — **เพิ่มเกมใหม่ "นาฬิกาวิเศษ 1/2/3/4" 🕐🕰️⏳⏰ (section เกมฝึกทักษะ)** (`skill-clock1..4`, `mode:'clock'` `clockMode:1-4`, view `#clock-view`, ล็อกลำดับ, mechanic ลากหมุนเข็มครั้งแรกของแอป): เกม 1 ชั่วโมงตรง/โมงครึ่ง, เกม 2 นาที (snap 1 + ปุ่ม ±1 นาที), เกม 3 "อีก N ชม. เป็นกี่โมง?", เกม 4 "อีก N ชม. M นาที" ทดชั่วโมงอัตโนมัติ — หน้าปัด SVG หน้ายิ้มกะพริบตา, เข็มสั้นอยู่หน้า+จับได้เฉพาะเข็ม+ลาก relative, กรอบสุ่ม 5 แบบ, อ่านโจทย์เสียงไทย, icon `clock-1..4.svg` (สมุดสติกเกอร์ 40 หมวด); `IDEA.md` +7 ไอเดียใหม่ (เสนอหมวด "เกมศิลปะ")
- 2026-07-18: v1.19.0 — **เพิ่มเกมใหม่ "เติมแพทเทิร์น" 🔮 (section คำถาม-คำตอบ)** (id `pattern`, `poolPick:10` สุ่ม 10 จากคลัง 30 ข้อ 3 tier เรียงง่าย→ยาก 4/3/3 ผ่าน `pickQuizQuestions()` ใหม่ใน app.js, `q.pattern` render แถวการ์ด emoji + ช่อง ? เส้นประเต้นใน `renderQuestion()`, ตัวเลือก emoji ใหญ่ย่อฟอนต์ด้วย `graphemeLen()`, icon `pattern.svg`, ป้าย NEW); **เปียโนของหนู: ฟังเพลงอัปเกรด** — ตัวโน้ตวิ่งติ๊ก ✓ + คีย์ flash ตามจังหวะ (`playMusicSequence` รับ opts `{onNote,onStop}`), ปุ่มเปลี่ยนเป็น "⏹ หยุดเพลง" ระหว่างเล่น, จบ/หยุด/เปลี่ยนเพลง/ปิด modal reset อัตโนมัติ + แก้บั๊ก flash ชี้ `#music-piano` เสมอทำให้คีย์ใน modal ไม่กระพริบ
- 2026-07-17: v1.18.0 — **เพิ่มเกมใหม่ "ลากเส้นต่อจุด 1/2" (section ใหม่ "เกมฝึกเขียน")** (`write-dots1` ✏️ 5-7 จุด / `write-dots2` 🖍️ 10-13 จุด ล็อกลำดับ, `type:'write'` `mode:'dots'`, view `#dots-view`): ลากเส้นต่อจุดตามตัวเลข (mechanic ลากเท่านั้น เส้นประตามนิ้ว ไม่มี highlight ใบ้) ครบแล้วเฉลยรูปจริง, คลัง `DOTS_SHAPES` อย่างละ 30 รูปสุ่ม 10 ต่อรอบ, `body.dots-open` ย่อนกฮูกกันบังจุดบนมือถือ, icon `dots-1/2.svg`+`section-write.svg`; **เกมดนตรี 2 เหลือ 7 ด่าน** + เพิ่ม `beats` 10 เพลงให้ฟังเพลงใน "เปียโนของหนู" ถูกจังหวะจริง; ลบป้าย NEW จากผสมสี 1/2 + เกมดนตรี 1/2/3

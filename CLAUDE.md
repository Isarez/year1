# นกฮูกสนุกคิด — เตรียมสอบ ป.1

เว็บแอปสำหรับเตรียมสอบเข้า ป.1 ไม่มี build step (ไฟล์ static ล้วนๆ) deploy ผ่าน GitHub Pages จาก branch `main` ที่ https://isarez.github.io/year1/

## โครงสร้างไฟล์

- `index.html` — markup ล้วนๆ, อ้างอิง CSS/JS ผ่าน `<link>`/`<script src>`
- `css/style.css` — สไตล์ทั้งหมด
- `js/data.js` — ชุดข้อมูลคำถาม/ข้อความหลัก: `CATS` (คำถามทุกหมวด), `AR_SENTENCES` (ประโยคเกม AR), `CAT_REQUIRES` (กติกาล็อกหมวด)
- `js/owl-messages.js` — `OWL_MSGS` ข้อความให้กำลังใจของนกฮูก
- `js/app.js` — logic ทั้งหมดของแอป (state, render, quiz flow, AR game, ธีม, เสียง ฯลฯ) โหลดหลังสุดเพราะอ้างอิงตัวแปรจาก data.js/owl-messages.js
- ลำดับการโหลด script สำคัญ: `data.js` → `owl-messages.js` → `app.js` (ห้ามสลับ เพราะ app.js ใช้ตัวแปร global จากสองไฟล์แรก)
- ไม่มี build step ก็จริง แต่การเปิดไฟล์ตรงๆ ผ่าน `file://` ยังใช้ได้ปกติ (ข้อมูลเป็น `.js` ไม่ใช่ `.json` ที่ต้อง `fetch` ผ่าน server เท่านั้น)

## กฎการทำงานในโปรเจคนี้

- **ตอบผู้ใช้เป็นภาษาไทยเสมอ** ไม่ว่าจะถามอะไร (โค้ด/ชื่อตัวแปรเป็นภาษาอังกฤษได้ตามปกติ)
- **ห้าม `git push` เอง** ต้องถามผู้ใช้ก่อนทุกครั้ง แม้จะ commit ไปแล้วก็ตาม
- **ตรวจสอบก่อนบอกว่าทำเสร็จ**: รัน JS syntax check กับไฟล์ JS ทั้ง 3 ไฟล์ (เรียงตามลำดับโหลดจริง) ก่อนส่งมอบงานทุกครั้ง เช่น
  ```bash
  node -e "
  const fs = require('fs');
  const files = ['js/data.js','js/owl-messages.js','js/app.js'];
  const combined = files.map(f=>fs.readFileSync(f,'utf8')).join('\n');
  try { new Function(combined); console.log('OK JS syntax'); } catch(e){ console.error('ERROR:', e.message); }
  "
  ```
- **หลัง push/deploy สำเร็จทุกครั้ง**:
  1. สรุปสิ่งที่เปลี่ยนแปลงสั้นๆ ให้ผู้ใช้ทราบ
  2. เขียนสรุปนั้นลงไฟล์นี้ (`CLAUDE.md`) ด้วย โดยเพิ่มเป็นรายการใหม่ในหัวข้อ "ประวัติการเปลี่ยนแปลง (changelog)" ด้านล่าง (รูปแบบ: `- YYYY-MM-DD: สรุปสั้นๆ`) และถ้าเปลี่ยนแปลงพฤติกรรม/สถานะสำคัญของแอป ให้อัปเดตหัวข้อ "สถานะปัจจุบันของแอป" ให้ตรงด้วย
  3. ก่อน push/deploy ทุกครั้ง ให้ตรวจและอัปเดต `README.md` ให้ตรงกับสถานะล่าสุดของแอปด้วย (เช่น ฟีเจอร์ใหม่, หมวดหมู่ใหม่) แล้ว commit `README.md` ไปพร้อมกับ `CLAUDE.md` และไฟล์โค้ดที่เปลี่ยนใน push เดียวกัน
- **หลัง `git push` ทุกครั้ง ต้อง track สถานะ deploy ของ GitHub Pages แล้วสรุปสั้นๆ ให้ผู้ใช้ทราบ**:
  1. รอสักครู่แล้วเช็คด้วย `gh run list --limit 3` (workflow "pages build and deployment") และ/หรือ `gh api repos/Isarez/year1/pages` เพื่อดู `status`/`https_certificate`
  2. ถ้ายังไม่เสร็จ ให้เช็คซ้ำเป็นระยะ (เช่นทุก 60-180 วินาทีด้วย ScheduleWakeup) จนกว่าจะจบ — งานอื่นในเทิร์นนั้นทำต่อได้ระหว่างรอ
  3. เมื่อ deploy จบ ให้สรุปสั้นๆ 1-2 บรรทัดว่า **สำเร็จหรือไม่**:
     - สำเร็จ: ยืนยันด้วยการเช็ค `curl -I` ที่ URL จริงว่าได้ 200 และ `last-modified`/`etag` อัปเดตแล้ว (workflow run บางครั้งค้างสถานะ "queued" ทั้งที่ deploy จริงเสร็จแล้ว ให้ยึด `curl` เป็นหลักถ้าขัดแย้งกัน)
     - ไม่สำเร็จ: ต้องบอก **error message** ที่เจอจาก `gh run view <id> --log-failed` ให้ผู้ใช้ทราบด้วยเสมอ ไม่ใช่แค่บอกว่า "fail" เฉยๆ
- **ถ้าเพิ่มเกม/หมวดใหม่ หรือแก้ไขอะไรที่มีนัยสำคัญ (breaking change, ปรับพฤติกรรมหลักของแอป, โครงสร้างไฟล์ใหม่ ฯลฯ) ต้อง `gh release create` ก่อน push/deploy เสมอ**:
  1. ตั้ง tag แบบ semver (เช่น `v1.1.0` ปรับ minor เมื่อเพิ่มฟีเจอร์/เกมใหม่, `v1.0.1` ปรับ patch เมื่อแก้บั๊ก/ปรับเล็กน้อย) — เช็ค tag ล่าสุดด้วย `git tag --list` หรือ `gh release list` ก่อนตั้งเวอร์ชันถัดไป
  2. เขียน release notes สรุปการเปลี่ยนแปลงเป็นภาษาไทย (ใช้เนื้อหาเดียวกับที่จะเขียนลง changelog ใน `CLAUDE.md`)
  3. รัน `gh release create <tag> --title "<tag>: <สรุปสั้นๆ>" --notes "<รายละเอียด>"` แล้วค่อย push commit ตามปกติ (release อ้างอิง commit ปัจจุบันบน `main` ณ ตอนสร้าง)
  4. งานเล็กน้อย (ปรับ CSS ระยะห่าง, แก้ข้อความ, แก้สีเฉพาะจุด) ไม่ต้องสร้าง release ทุกครั้ง — ใช้ดุลยพินิจว่า "มีนัยสำคัญ" หรือไม่ ถ้าไม่แน่ใจให้ถามผู้ใช้ก่อน

## สถานะปัจจุบันของแอป (สำคัญ อย่าย้อนกลับโดยไม่ถาม)

- **ธีมกลางวัน/กลางคืน**: ปุ่ม `#theme-toggle` ใน header สลับ `body.night-mode`, จำค่าไว้ที่ `localStorage['p1quiz_theme']`. กลางคืน: ท้องฟ้าเปลี่ยนเป็นสีกรมท่า, พระอาทิตย์ (`.bg-sun`) fade ออกและพระจันทร์ (`.bg-moon`) fade เข้า, เมฆเปลี่ยนสี, ลูกโป่งลอยเปลี่ยนเป็นดาว. ตัวอักษรที่ไม่ได้อยู่บนการ์ดขาว/ครีม (header title, hero, footer, section title อย่าง `.cat-section-title`) ต้องมี override สี `body.night-mode` เพราะสีเริ่มต้น (`--ink`/`--ink-soft` น้ำตาลเข้ม) อ่านไม่ออกบนพื้นหลังกลางคืน
  - ข้อควรระวัง CSS: `@keyframes` ที่ animate `opacity` จะ override static opacity rule เสมอ ไม่ว่าจะ specificity อะไร — ถ้าจะซ่อน/โชว์ element ตามธีม ต้อง scope animation ไว้ใน selector ของธีมนั้นๆ ด้วย ไม่ใช่ใส่ไว้ที่ base rule
  - Grid ที่มีเนื้อหาไม่ยอมหด (เช่น emoji ใหญ่) ต้องใช้ `grid-template-columns:repeat(N,minmax(0,1fr))` ไม่ใช่ `repeat(N,1fr)` เฉยๆ ไม่งั้นจะ overflow ออกนอก panel บนจอมือถือแคบ (`#emoji-picker` เจอปัญหานี้มาแล้ว)
- **เพลงพื้นหลัง**: procedural Web Audio (ไม่ใช่ไฟล์เสียง) default เป็น **เปิด** สำหรับผู้ใช้ใหม่ (`localStorage['p1quiz_music'] !== 'off'`) เสียงเบา (`0.025`) ห้ามปรับเสียงดังขึ้นโดยไม่ถาม
- **หมวดหมู่ (`CATS`)**: math, thai (ภาษาไทย 1), thai2 (ล็อกจนกว่าจะทำ thai1 จบ), english, behavior, animals, days, iq1-iq4 (ล็อกตามลำดับผ่าน `CAT_REQUIRES`)
- **"days" category**: ค่าตำแหน่งคำตอบถูก shuffle แบบ one-time manual fix ไปแล้ว (ของเดิมตอบ index 0 หมด) — อย่า shuffle ซ้ำอีก
- **พื้นหลังกลางวัน**: ท้องฟ้าสีฟ้า gradient, มีพระอาทิตย์หมุนช้าๆ, เมฆลอย, ลูกโป่งลอย, ประกายดาวระยิบระยับ — เคยมีสายรุ้งแล้วเอาออกตามคำขอผู้ใช้ **ห้ามใส่กลับโดยไม่ถาม**
- **นกฮูกมาสคอต**: มุมขวาล่าง คลิกแล้วสุ่มข้อความให้กำลังใจจาก `OWL_MSGS.cheer` (คนละปุ่มกับสมุดสติกเกอร์ `#sticker-tally-btn`)
- **Favicon/ไอคอน**: `assets/favicon.svg` เป็น source of truth ถ้าแก้ต้อง regenerate PNG ทุกไซส์ด้วย `rsvg-convert -w <size> -h <size> assets/favicon.svg -o assets/<name>.png`
- **ข้อมูลผู้ใช้ทั้งหมดเก็บใน `localStorage` เท่านั้น ไม่มี backend** — footer มีข้อความยืนยันเรื่องนี้ ต้องคงความจริงนี้ไว้ถ้าจะเพิ่มฟีเจอร์ sync/backend ในอนาคต
- **มินิเกม AR (จับคู่นิ้วมือ)**: หมวด `ar-thai`/`ar-eng` (ต่อประโยค ลากคำใส่ช่องตามลำดับ), `ar-math` (คิดเลข บวก/ลบ 1-2 หลัก ลากการ์ดคำตอบที่ถูกใส่ช่องเดียว) และ `ar-match`/`ar-match-en` (โยงเส้น ลากจากจุดสัญลักษณ์ซ้ายไปยังจุดคำตอบขวาให้ตรงคู่กัน) ทั้ง 5 หมวดมี `isNew:true` ทำให้ขึ้นป้าย "NEW ✨" บนการ์ดหน้าเลือกหมวด (ลบออกเมื่อผู้ใช้เห็นว่าเกมไม่ใหม่แล้วโดยถามก่อน). หน้าเลือกหมวดแบ่งเป็น 2 section: "คำถาม-คำตอบ" (`#cat-grid`) กับ "การโต้ตอบ" (`#cat-grid-interactive`, มีแต่หมวด type `ar`)
  - `ar-match` (id เดิม, `lang:'th'`) และ `ar-match-en` (id ใหม่, `lang:'en'`) ใช้ข้อมูลคู่จับคู่ร่วมกันจาก `AR_MATCH_ITEMS` ใน `js/data.js` ซึ่งเป็น object แยกภาษา `{th:[...], en:[...]}` (emoji ตรงกันทุกตำแหน่ง คนละแค่คำแปล) — `buildMatchLevel(cat)` เลือก pool ด้วย `AR_MATCH_ITEMS[cat.lang || 'th']` ห้ามลบ id `ar-match` เดิมทิ้งเพราะจะทำให้ progress/สติกเกอร์ผู้ใช้เดิมที่ทำหมวดนี้ไปแล้วหาย ให้เพิ่ม id ใหม่แทนเสมอเวลาจะเพิ่มภาษา
  - `ar-match` ใช้ตัวแปร state แยก (`arDragLineFrom`) และ element ต่างหาก (`#ar-match-wrap`, `#ar-match-left`, `#ar-match-right`, `#ar-match-svg`) จาก 2 โหมดแรกเพราะกลไกคือ "ลากเส้นจากจุด A ไป B" ไม่ใช่ "ลากการ์ดไปวางช่อง" — เส้นที่ลากแล้ว/กำลังลากวาดด้วย SVG `<line>` ผ่าน `getBoundingClientRect()` ของจุด (coordinate ระบบเดียวกับ `.ar-cursor`/`.ar-card` คือ page/client coords ตรงๆ ไม่ผ่าน viewBox scaling)
  - **ท่ามือของ `ar-match` ต่างจาก `ar-thai`/`ar-eng`/`ar-math`**: 2 โหมดแรกใช้ "จีบนิ้วเพื่อหยิบ, คลายจีบเพื่อวาง" (pinch-to-grab) แต่ `ar-match` ใช้ "แตะจุดวงกลมแล้วลากได้ทันที ไม่ต้องจีบ" (เริ่มลากเมื่อปลายนิ้วชี้แตะจุดที่ยังไม่จับคู่ โดยไม่ pinch) และ "จีบนิ้วเพื่อยกเลิกการลาก" (pinch = cancel, ไม่ใช่ drop) — การปล่อย/จับคู่สำเร็จเกิดอัตโนมัติเมื่อปลายนิ้วชี้เข้าใกล้จุดฝั่งตรงข้ามที่ถูกต้อง (ดู `updateArCursor` branch `cat.mode==='match'`) ห้ามเปลี่ยนพฤติกรรมนี้กลับเป็น pinch-to-grab โดยไม่ถาม
  - ข้อควรระวัง CSS: `.ar-slot-row`/`.ar-match-wrap` ต้องมี `[hidden]{display:none;}` กำกับเสมอ เพราะ element ที่มี `display:flex` ประกาศไว้ในคลาสอยู่แล้ว จะไม่ถูกซ่อนด้วย HTML attribute `hidden` เฉยๆ (author CSS ชนะ UA `[hidden]` stylesheet เสมอไม่ว่า specificity จะเท่ากันแค่ไหน)
  - **มือถือ (`window.innerWidth < 768`, ดู `isMobileViewport()` ใน `js/app.js`) ไม่ใช้กล้อง AR เลย**: `initHandTracking()` เช็คก่อนบรรทัดแรกว่าเป็นมือถือหรือไม่ ถ้าใช่จะไม่เรียก `getUserMedia`/MediaPipe เลย (ไม่มี prompt ขอสิทธิ์กล้อง) ข้ามไปโหมดลากด้วยนิ้ว/เมาส์ทันที และแจ้งเตือนด้วย `showToast()` (ครั้งเดียว หายไปเอง) — **ห้ามเปลี่ยนกลับไปใช้ `#ar-camera-msg` แบบการ์ดค้างจอสำหรับกรณีมือถือ** เพราะเคยชนซ้อนทับกับ `.ar-hint-chip` อ่านไม่ออก (การ์ด `#ar-camera-msg` แบบเดิมเก็บไว้เฉพาะกรณีกล้องพังบนเดสก์ท็อป/แท็บเล็ตเท่านั้น ผ่าน `catch(err)`)
  - หน้าเลือกหมวด section "การโต้ตอบ (AR)" มีข้อความแนะนำ (`.cat-section-note`) บอกให้เล่นบนแท็บเล็ต/โน้ตบุ๊ก/คอมพิวเตอร์เพื่อใช้กล้องได้เต็มรูปแบบ และเตือนว่ามือถือจะไม่ใช้กล้อง
  - เมื่อ `body.ar-mobile-nocam` (ตั้งใน `startARGame` ตอนเช็คว่าเป็นมือถือ) พื้นหลัง AR view จะโปร่งใส (`#ar-view`/`.ar-bg-layer{background:transparent}`) ให้เห็นท้องฟ้ากลางวัน/กลางคืนของธีมจริงแทนพื้นหลังกรมท่าคงที่เดิม พร้อมซ่อน `.app-header`/`footer` ไปด้วย (สอง element นี้อยู่นอก view sections เป็น persistent chrome เดิมถูกบัง opacity โดย `#ar-view` ทึบแสง พอพื้นหลังโปร่งใสจะเห็นทะลุเข้ามาถ้าไม่ซ่อน) ส่วนขนาดตัวหนังสือ/การ์ดของเกม AR ทั้งหมดมี media query `@media (max-width:480px)` ลดขนาดลงให้เหมาะกับจอมือถือ
- **Theme transition**: ท้องฟ้ากลางวัน/กลางคืนใช้ 2 เลเยอร์ `.bg-sky-day`/`.bg-sky-night` ซ้อนกันแล้ว crossfade ด้วย `opacity 2s` (ไม่ใช้ `transition:background` บน gradient เพราะเบราว์เซอร์ tween gradient ตรงๆ ไม่ได้) พระอาทิตย์/พระจันทร์ใช้ wrapper div แยกจาก SVG เดิมเพื่อทำ transform translateY แยกจาก animation หมุน/pulse เดิม (element เดียวมี transform 2 ระบบพร้อมกันไม่ได้)
- **ปุ่มวิธีติดตั้งแอป**: `#install-toggle` เป็นปุ่ม text ("วิธีติดตั้งแอปบน iPad/แท็บเล็ต") ลอยมุมล่างซ้าย (`.install-link-btn`) ไม่ใช่ icon ใน header อีกต่อไป กดแล้วเปิด `#install-modal` (คำแนะนำ iPad/iPhone Safari + Android Chrome) ซ่อนอัตโนมัติตอนอยู่ใน AR view (`body.ar-open .install-link-btn{display:none}`)
- **ปุ่มเต็มหน้าจอ**: มี 2 ปุ่มที่ผูก logic เดียวกัน — `#fullscreen-toggle` ใน header (หน้าปกติ) และ `#ar-fullscreen-toggle` ใน `.quiz-top` ของ AR view (มุมขวาบน ข้าง `#ar-level-counter`) ทั้งคู่ผูกกับ `fsBtns` array และ `refreshFsBtn()` เดียวกัน ถ้าเพิ่มปุ่มเต็มหน้าจอใหม่ให้เพิ่ม id เข้า array นี้แทนการเขียน handler แยก

## ประวัติการเปลี่ยนแปลง (changelog)

- 2026-07-02: เพิ่มมินิเกม AR 3 หมวด (ต่อประโยคไทย/Eng, คิดเลข) พร้อมแก้บั๊กจับ/วางการ์ด, ปรับ layout ช่องวางขึ้นด้านบน, style glassmorphism, แยกหน้าเลือกหมวดเป็น 2 section, เพิ่ม animation พระอาทิตย์ตก-พระจันทร์ขึ้นและ crossfade สีท้องฟ้าตอนสลับธีม, เพิ่มป้าย NEW บนการ์ดหมวดใหม่
- 2026-07-02: เพิ่มปุ่ม "วิธีติดตั้งแอปบน iPad/แท็บเล็ต" (ย้ายจาก icon ใน header มาเป็นปุ่ม text ลอยมุมล่างซ้าย) และเพิ่มปุ่มเต็มหน้าจอในโหมด AR (มุมขวาบน)
- 2026-07-02: แยกไฟล์เดี่ยว `index.html` ออกเป็น `index.html` (markup) + `css/style.css` + `js/data.js` + `js/owl-messages.js` + `js/app.js` เพื่อให้ดูแล/แก้ไขง่ายขึ้น พฤติกรรมแอปเหมือนเดิมทุกอย่าง ไม่มี build step เพิ่ม
- 2026-07-02: เพิ่มความหลากหลายของประโยคในเกม AR ต่อประโยค — ขยาย `AR_SENTENCES` (ไทย/อังกฤษ × 3/4/5 คำ) จากเดิม 6 ประโยคต่อชุด เป็น 20 ประโยคต่อชุด (รวม 120 ประโยค) เพื่อลดโอกาสเจอประโยคซ้ำเวลาเล่นซ้ำ
- 2026-07-02: แก้สีข้อความหัวข้อ section หน้าเลือกหมวด (`.cat-section-title` เช่น "คำถาม-คำตอบ", "การโต้ตอบ") ในโหมดกลางคืน — เดิมใช้สี `--ink` น้ำตาลเข้มซึ่งไม่มี override สำหรับกลางคืน ทำให้อ่านไม่ออกบนพื้นหลังกรมท่า เพิ่ม `body.night-mode .cat-section-title{color:#FFF7E0}` ให้เข้าชุดกับสีหัวข้ออื่นๆ
- 2026-07-02: เพิ่มความหลากหลายของก้อนเมฆลอยพื้นหลัง — เดิม `spawnCloud()` ใน `js/app.js` ใช้ SVG รูปทรงเดียวซ้ำทุกก้อน เปลี่ยนเป็นสุ่มเลือกจาก `CLOUD_SHAPES` (4 รูปทรง จำนวน/ขนาด/ตำแหน่ง ellipse ต่างกัน) ทุกครั้งที่ spawn
- 2026-07-02: เพิ่มมินิเกม AR ใหม่ `ar-match` ("โยงเส้น") — ลากเส้นจากจุดสัญลักษณ์ (emoji) ด้านซ้ายไปยังจุดคำตอบ (ข้อความ) ด้านขวาให้ตรงคู่กัน 10 ด่าน ด่านละ 3-5 คู่ (สเกลตามด่านเหมือนเกมต่อประโยค) ข้อมูลคู่จับคู่ 30 คู่อยู่ที่ `AR_MATCH_ITEMS` ใน `js/data.js` (คนละ state กับการลากการ์ดของอีก 2 โหมด)
- 2026-07-02: ปรับ UX เกม `ar-match` — เปลี่ยนท่ามือจาก "จีบนิ้วเพื่อหยิบ" เป็น "แตะจุดวงกลมแล้วลากได้ทันที" และ "จีบนิ้วเพื่อยกเลิกการลาก" แทน (ดูรายละเอียดใน "สถานะปัจจุบันของแอป" ด้านบน), ขยาย emoji/คำตอบให้ใหญ่ขึ้นและเว้นระยะห่างแนวตั้งระหว่างแต่ละคู่มากขึ้น, ย้ายจุดวงกลมของฝั่งคำตอบมาไว้ด้านหน้า (ใกล้ศูนย์กลาง) แทนที่จะอยู่ริมขอบจอ, และลดระยะห่างแนวนอนระหว่างจุดซ้าย-ขวาให้ใกล้กันขึ้น (จาก `justify-content:space-between` เต็มจอ เป็น `justify-content:center; gap` ที่กำหนดตายตัว)
- 2026-07-02: เพิ่มระยะห่างเกม `ar-match` ตามคำขอ — ระยะห่างแนวนอนระหว่างจุดคำถาม (ซ้าย) กับจุดคำตอบ (ขวา) จาก `gap:min(22vw,220px)` เป็น `gap:clamp(24px,8vw,220px)` (ปรับสูตรใหม่กันจอแคบ overflow ด้วย ลด padding ข้างจาก 6vw เหลือ 4vw) และเพิ่มระยะห่างแนวตั้งระหว่างแต่ละคู่ (`.ar-match-col` gap) จาก 30px เป็น 46px — ทดสอบแล้วว่าคำยาวสุดในคลัง (`พระอาทิตย์` ฯลฯ) ไม่ overflow แม้บนจอมือถือแคบสุด (375px)
- 2026-07-02: ปรับระยะห่างเกม `ar-match` เพิ่มอีกรอบตามคำขอ — `.ar-match-row` gap เป็น `clamp(24px,100vw,400px)` (จอกว้างกว่า 400px จะได้ gap เต็ม 400px), `.ar-match-col` gap เป็น 60px. พบว่าค่า `100vw` ใน clamp ทำให้จอแคบกว่า 400px (เช่นมือถือ 375px) ได้ gap เกือบเต็มจอ ดันคอลัมน์ซ้ายหลุดออกนอกจอ — แก้โดยเพิ่ม `@media (max-width:480px){ .ar-match-row{ gap:clamp(24px,8vw,220px); } }` ครอบไว้ เพื่อให้จอกว้าง (เดสก์ท็อป/แท็บเล็ต) ได้ gap 400px เต็มตามที่ขอ แต่จอแคบยังเล่นได้ไม่ overflow
- 2026-07-02: เพิ่ม "(AR)" ต่อท้ายหัวข้อ section "การโต้ตอบ" พร้อมข้อความแนะนำให้เล่นบนแท็บเล็ต/โน้ตบุ๊ก/คอมพิวเตอร์เพื่อใช้กล้องได้เต็มรูปแบบ (มือถือจะไม่ใช้กล้อง). ทำให้เกม AR ทั้ง 4 หมวดข้ามการขอสิทธิ์กล้องไปเลยเมื่อเล่นบนมือถือ (`isMobileViewport()`, จอกว้าง <768px) ใช้นิ้วลากได้ทันทีแบบเดียวกับตอนกล้องพัง, ปรับพื้นหลัง AR view ให้โปร่งใสตอนอยู่บนมือถือเพื่อให้เห็นท้องฟ้ากลางวัน/กลางคืนตามธีมจริงแทนพื้นหลังกรมท่าคงที่, ลดขนาดตัวหนังสือ/การ์ดของเกม AR ทั้งหมดผ่าน media query ให้เหมาะกับจอมือถือ — ระหว่างทางเจอบั๊ก: ข้อความแจ้งเตือนกล้อง (`#ar-camera-msg-text`) ค้างข้ามขนาดจอถ้าสลับจากมือถือไปเดสก์ท็อปในเซสชันเดียว แก้โดยให้ทั้ง branch มือถือและ branch กล้องพังตั้งค่าข้อความใหม่ทุกครั้ง
- 2026-07-02: แก้บั๊ก UI มือถือของ AR ที่พื้นหลังโปร่งใส — เพราะ `.app-header`/`footer` เป็น persistent chrome นอก view sections เดิมถูกบัง opacity ด้วย `#ar-view` ทึบแสง พอพื้นหลังโปร่งใส (จากงานรอบก่อน) เลยเห็นทะลุเข้ามาซ้อนทับเนื้อหาเกม แก้โดยซ่อนสอง element นี้ด้วย `body.ar-mobile-nocam .app-header, footer{display:none}`. เปลี่ยนกล่องแจ้งเตือน "มือถือไม่ใช้กล้อง" จากการ์ดลอยค้างจอ (ซึ่งซ้อนทับ `.ar-hint-chip` อ่านไม่ออก) เป็น `showToast()` ครั้งเดียวแทน
- 2026-07-02: ปรับข้อความ `.ar-hint-chip` ของเกม AR ทั้ง 3 หมวด (`ar-thai`/`ar-eng`, `ar-math`, `ar-match`) ให้เหมาะกับขนาดหน้าจอ — บนมือถือ (`isMobileViewport()`) เปลี่ยนจาก "✋ จีบนิ้วหยิบ..." เป็น "👆 แตะ...แล้วลาก..." (มือถือไม่มีกล้อง ไม่มีท่าจีบนิ้วให้ทำ) และตัดวงเล็บ "(จีบนิ้วถ้าอยากยกเลิก)" ออกจากหมวด `ar-match` บนมือถือด้วยเหตุผลเดียวกัน ส่วนเดสก์ท็อป/แท็บเล็ตยังคงข้อความเดิม (จีบนิ้ว) ไว้. และลด `.ar-match-row` gap จาก 400px เหลือ 250px ตามคำขอ
- 2026-07-03: เพิ่ม `@media (min-width:1001px){ .ar-match-row{ gap:400px; } }` ให้เกม `ar-match` — จอกว้างกว่า 1000px (เดสก์ท็อป/แท็บเล็ตแนวนอนจอใหญ่) โจทย์ (ซ้าย) กับคำตอบ (ขวา) จะห่างกันมากขึ้น (400px) ส่วนจอ ≤1000px ยังใช้ gap 250px ตามเดิม ทดสอบแล้วไม่ overflow แม้จอกว้าง 1920px กับ 5 คู่ (ด่านมากสุด)
- 2026-07-03: ขยาย `AR_MATCH_ITEMS` จาก 30 คู่เป็น 59 คู่ — เพิ่มหมวดผลไม้ (ส้ม/องุ่น/สตรอว์เบอร์รี/แครอท), สัตว์ฟาร์ม (ไก่/หมู/วัว/กระต่าย), แมลง (ผีเสื้อ/ผึ้ง), อวัยวะ (มือ/ตา/หู/จมูก/ปาก), รูปทรงเรขาคณิต (สามเหลี่ยม/สี่เหลี่ยม/วงกลม), ของใช้/สิ่งของ (ตุ๊กตาหมี/หิมะ/คลื่น/ขนมปัง/นม/ถุงเท้า/เสื้อ/เค้ก/ประตู/หน้าต่าง/เตียง) เพื่อให้โจทย์มีความหลากหลายมากขึ้นและลดโอกาสเจอคู่ซ้ำในแต่ละด่าน
- 2026-07-03: เพิ่มเกมโยงเส้นภาษาอังกฤษ `ar-match-en` (แยกจาก `ar-match` เดิมซึ่งเปลี่ยนชื่อเป็น "โยงเส้น (ไทย)" เพื่อให้คู่กับชื่อใหม่ "โยงเส้น (Eng)") — restructure `AR_MATCH_ITEMS` ใน `js/data.js` จาก array เดียวเป็น `{th:[...], en:[...]}` (59 คู่ emoji เดิม แปลเป็นคำอังกฤษคู่กัน), `buildMatchLevel()` ใน `js/app.js` เลือก pool ตาม `cat.lang`, hint/mistake/success message ของโหมด match แตกกิ่งภาษาแบบเดียวกับเกมต่อประโยคไทย/Eng. เก็บ id `ar-match` เดิมไว้ (ไม่เปลี่ยนเป็น `ar-match-th`) เพื่อไม่ให้ progress/สติกเกอร์ผู้ใช้เดิมหาย
